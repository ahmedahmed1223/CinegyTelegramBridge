# خطة تطوير CinegyTelegramBridge

آخر تحديث: 2026-09-09 — الإصدار 8.16.0

## مبادئ النطاق

- البوت مخصص للمحادثات الخاصة مع مستخدمين مصرح لهم، ولا يدعم المجموعات أو القنوات.
- سلامة البث على الهواء تتقدم على سهولة إضافة الميزات.
- كل إصلاح سلوكي يبدأ باختبار انحدار، وكل إصدار يمر عبر `Run-Checks.ps1` واختبار دخان تشغيلي.
- لا تُحفظ الأسرار أو ملفات التشغيل في Git.

## الإصدار 4.0.1 — تثبيت بوابة الإصدار

- [x] تشغيل اختبار دخان `onair.json` فعليًا ضمن اكتشاف Pester.
- [x] منع إعادة حفظ `onair.json` عندما لا يتغير `ActiveId`.
- [x] قبول تحديثات محادثات Telegram الخاصة فقط ورفض المجموعات والقنوات.
- [x] إضافة اختبارات انحدار للسلوك السابق، بما فيها تنظيف سجل القالب بعد EXIT.
- [x] إزالة تحذيرات `PSScriptAnalyzer` أو توثيق الاستثناءات المقصودة بدقة.
- [x] تحديث رقم الإصدار وREADME وCHANGELOG.
- [x] تشغيل الفحوص الآلية الكاملة (141 اختبارًا ناجحًا، بلا نتائج Analyzer).
- [x] تنفيذ اختبار دخان حي آمن للإصدار 4.2.44: Telegram API متصل، Cinegy متصل، واكتشاف القالب الخارجي `ticker` على الطبقة 8 دون تغيير الهواء.
- [ ] تنفيذ دورة SHOW/HIDE على طبقة Cinegy تجريبية؛ متوقف تشغيليًا لأن `TemplateTestLayer=0` ولا يجوز استخدام طبقة إنتاج بدلًا منها.

## الإصدار 4.1 — الاعتمادية والمراقبة

- [x] اعتماد رقم طبقة Cinegy كهوية الربط الأساسية، مع إبقاء اسم القالب وصفًا تشغيليًا.
- [x] إضافة فحص يدوي يقارن طبقات `templates.json` بحالة Cinegy الحية و`onair.json`.
- [x] اكتشاف المشاهد التي بدأها برنامج Cinegy وإتاحتها للإخفاء أو الخروج.
- [x] عرض وقت آخر مزامنة ناجحة في شاشة الحالة.
- [x] تسجيل سبب إضافة وإزالة كل سجل في `bridge.log` فقط.
- [x] إبقاء `onair.json` حالة حية للقوالب الموجودة على الهواء، وليس سجلًا تاريخيًا.
- [x] تحسين التنبيهات المتدرجة عند انقطاع Cinegy وTelegram وعودتهما.
- [x] فحص كل طبقات القوالب عند الإقلاع واكتشاف المشاهد الخارجية قبل استقبال الأوامر.
- [x] تمييز مشاهد Cinegy Air الخارجية أو المجدولة عن مشاهد Bot بمصدر واضح.
- [x] استخراج اسم ملف قالب `.cintitle` للمشهد الخارجي من وصف العنصر النشط بدل عرض اسم حدث الطبقة العام، مع إبقاء الطبقة هوية الربط.
- [x] إعادة فحص طبقة Cinegy مباشرة بعد الإخفاء أو الخروج قبل تحديث الحالة المحلية.
- [x] اختبار إعادة التشغيل أثناء وجود قالب على الهواء أو حدث مجدول ومنع إعادة الحدث.
- [x] عزل جميع ملفات تشغيل Pester عن `logs/onair.json` وبقية ملفات الخدمة الحية.
- [x] إضافة إدارة سحب وتعطيل صلاحيات المستخدمين مع سجل تدقيق.
- [x] تقييد ACL على ملفات الإعدادات والنسخ الاحتياطية.

## الإصدار 4.2 — قابلية الصيانة

- [x] إضافة مفضلة يدوية مستقلة لكل مستخدم مع خيار موحد محجوز ومعطل افتراضيًا.
- [x] إضافة Alias تشغيلي دائم لأسماء المستخدمين وإظهاره في الحالة والتدقيق.

- [x] فصل الإعدادات والتخزين في وحدات مستقلة.
- [x] فصل Telegram API والصلاحيات والتدفقات التفاعلية.
- [x] فصل مزامنة Cinegy والمراقبة.
- [x] فصل الجدولة والبث الحي وffmpeg.
- [x] تقليل الحالة العامة `$script:` تدريجيًا دون إعادة كتابة شاملة.
- [x] نقل وحدة واحدة في كل دفعة مع تشغيل الاختبارات بعد كل نقل.

## الإصدار 5.0 — التشغيل المؤسسي

> خطة التنفيذ التفصيلية للإصدار 5.0 (التفكيك، الإزاحات، سلامة الهواء) في
> [docs/PLAN-5.0.md](docs/PLAN-5.0.md). البنود أدناه هي النطاق المؤسسي المنفَّذ.

- [x] حماية الأسرار اختياريًا باستخدام DPAPI مع إبقاء السلوك الحالي افتراضيًا.
- [x] سجل تدقيق دائم ومنظم بصيغة JSONL مع معرف عملية.
- [x] حُسم نطاق الصلاحيات بدورين فقط: مشغّل ومشرف؛ الصلاحيات المؤسسية الإضافية غير مطلوبة لهذا البوت المخصص للمستخدمين.
- [x] وضع صيانة يمنع التحكم العادي ويحافظ على الإخفاء الطارئ للمشرف.
- [x] حزمة إصدار منقحة مع SHA-256 وآلية ترقية ورجوع موثقة، ودعم توقيع Authenticode عند توفير شهادة.
- [ ] إنتاج النسخة الموقعة فعليًا؛ لا توجد حاليًا شهادة Code Signing صالحة في مخزن المستخدم.
- [x] CI على Windows يشغّل Parser وPSScriptAnalyzer وPester لكل تغيير.

## بوابة النشر

1. `Run-Checks.ps1` يخرج بالرمز `0`، بلا اختبارات فاشلة.
2. عدد الاختبارات يتضمن مجموعة اختبار الدخان، ولا يعتمد على اكتشاف اسم ملف خاطئ.
3. لا توجد أسرار أو ملفات تشغيل جديدة ضمن `git diff` أو الملفات المتعقبة.
4. ينجح اختبار خاص فعلي: بدء، حالة، عرض تجريبي، إخفاء، ثم فحص الاستعادة.
5. تُحفظ نسخة الإصدار السابق قبل استبدال ملفات الخدمة.

## سجل البنود المقترحة الكامل

هذا هو سجل الـbacklog المعتمد بعد استبعاد مجموعات Telegram. علامة `[ ]`
تعني أن البند موثق ولم يُنفذ بعد، ولا تعني إدخاله في الإصدار الحالي.

### الأمان وإدارة المستخدمين

- [x] رفض المجموعات والقنوات وقبول المحادثات الخاصة فقط، مع الاختبارات والتوثيق.
- [x] إبقاء `RequireUserLevelAuth` مفعّلًا افتراضيًا.
- [x] شاشة المستخدمين المصرح لهم مع السحب والتعطيل المؤقت والتأكيد.
- [x] تسجيل تاريخ الإضافة، ومن أضاف المستخدم، وآخر نشاط، ومنح وسحب الصلاحيات.
- [x] Alias تشغيلي لكل مستخدم بدل الاكتفاء بالرقم.
- [x] إتاحة تعديل أو حذف Alias مباشرة من شاشة إدارة المستخدمين.
- [x] اعتماد دورين فقط: مشغّل ومشرف، مع منع حذف أو تعطيل آخر مشرف. دور «عرض فقط» خارج النطاق بقرار تشغيلي.
- [x] Windows ACL للإعدادات والنسخ، ونقل اختياري للأسرار إلى DPAPI.
- [x] تنقيح معرفات المستخدمين والأسرار من حزم التشخيص العامة.

### الاعتمادية والمراقبة

- [x] اعتماد الطبقة كهوية والاسم كوصف، وتسجيل تغييرات الهواء في `bridge.log`.
- [x] مقارنة الحالة الحية مع Cinegy عند الإقلاع وعند طلب المستخدم وعرض آخر مزامنة.
- [x] عمر الحالة وتصنيف متصل أو متأخر أو غير متاح أو غير معروف.
- [x] عدّاد فشل وعتبة تنبيه وتنبيه استعادة لـCinegy وTelegram بلا تكرار.
- [x] إعادة محاولة الجدولة بتأخير أُسّي تدريجي وحد أقصى، مع تعطيلها افتراضيًا.
- [x] اختبار إعادة التشغيل مع قالب حي وحدث مجدول ومنع إعادة التنفيذ.
- [x] اختبار الانقطاع والاستعادة وتلف ملفات الحالة والاسترجاع من النسخ الاحتياطية.

### سلامة التشغيل على الهواء

- [x] وضع صيانة يمنع `SHOW` و`HIDE` و`EXIT` والتحديث والجدولة مؤقتًا، مع إبقاء الإخفاء الطارئ للمشرف.
- [x] إخفاء طارئ مع تأكيد مزدوج.
- [x] عرض القالب الحالي ومصدره أو مستخدمه قبل استبداله، مع تحذير عند تقادم حالة الطبقة.
- [x] منع الطبقات المحجوزة والقوالب المعطلة مؤقتًا من `SHOW` مع إبقاء `HIDE/EXIT` متاحًا.
- [x] تحذير استبدال مشهد مستخدم آخر.
- [x] مهلة تلقائية للقوالب الحساسة.
- [x] منع `SHOW` عند عدم يقين Cinegy مع فحص مباشر للطبقة قبل التنفيذ.
- [x] معرف ومدة ونتيجة لكل عملية تحكم في الهواء داخل `bridge.log`.
- [x] تراجع آمن قصير العمر إلى آخر حالة يديرها البوت عندما تؤكد حالة Cinegy ذلك.

### تجربة المستخدم

- [x] تبسيط تصفح القوالب وإضافة بحث وتصنيف ومعاينة قبل الاختيار.
- [x] عرض المستخدم والوقت المنقضي للقالب الموجود على الهواء.
- [x] عرض بيانات المشهد الخارجي ومصدر مشاهد المستخدمين وAlias داخل الحالة، وتنظيم الحالة الكاملة في أقسام تشغيلية واضحة.
- [x] صفحة آخر العمليات لكل مستخدم ورسائل إجرائية وزر إعادة المحاولة.
- [x] مفضلة يدوية مستقلة قابلة للتعديل لكل مستخدم.
- [x] تقدم إدخال الحقول ومعاينة المسودة قبل التأكيد.
- [x] اختصار نصوص الأزرار الطويلة مركزيًا بحد قابل للضبط مع دعم العربية والإيموجي دون تغيير أوامر الأزرار.
- [x] نسخ حدث سابق وتعديل وقته وإظهار المنطقة الزمنية في مراجعته.

### القوالب والطبقات

- [x] كشف تعارض أرقام الطبقات وفحص المسارات وإظهار القوالب غير الصالحة في الصحة، مع اعتبار اشتراك عدة قوالب في طبقة واحدة مسموحًا وفق نموذج التشغيل.
- [x] اختبار قالب على طبقة تجربة مستقلة، ونسخة احتياطية ومقارنة قبل تعديل التعريفات.
- [x] تعطيل قالب مؤقتًا من الإعدادات ومنعه مركزيًا من `SHOW`.
- [x] فئات اختيارية وإظهار آخر وقت استخدام للقالب.
- [x] استيراد وتصدير تعريفات القوالب مع تحقق ومقارنة ونسخة احتياطية وتأكيد.

### الجدولة

- [x] كشف تعارض الطبقات ضمن نافذة زمنية قابلة للضبط والتحذير قبل اعتماد الجدول.
- [x] سبب الفشل وسياسة إعادة محاولة مجدولة قابلة للضبط ومعطلة افتراضيًا.
- [x] سجل تنفيذ جدولة مستقل بصيغة JSONL دون محتوى الحقول التحريرية.
- [x] منع إعادة تنفيذ الحدث بعد إعادة تشغيل الجسر.
- [x] تعليق الجدولة، إشعار مسبق، نسخ وتعديل الحدث، وتاريخ انتهاء للتكرار.

### التشخيص والسجلات

- [x] إصدار التطبيق ووقت التشغيل وحالة الخدمات ضمن التشخيص.
- [x] وقت البناء ومدة التشغيل والمعالج والذاكرة ومساحة القرص وأحجام الملفات وعدّادات النجاح والفشل والحظر.
- [x] JSONL منظم وCorrelation ID وفصل سجل التشغيل عن سجل التدقيق الأمني.
- [x] حزمة تشخيص منقحة وفحص مساحة القرص وتنبيه نمو السجلات والنسخ.

### إعادة الهيكلة والاختبارات والإصدارات

- [x] فصل الإعدادات والتخزين وTelegram والصلاحيات والتدفقات وCinegy والمراقبة والجدولة والبث.
- [x] تقليل `$script:` وتعريف نماذج واضحة ونقل وحدة واحدة في كل إصدار فرعي.
- [x] جعل Analyzer واختبارات الدخان جزءًا من بوابة الإصدار.
- [x] اختبارات Telegram API والمهلات وHTTP وفشل القرص وJSON التالف.
- [x] اختبارات العربية والمحارف الخاصة وحدود الحقول والرسائل.
- [x] CI على Windows/PowerShell 7 وتقرير اختبار محفوظ وحزمة بلا أسرار أو سجلات.
- [x] checksum للحزمة وتوثيق الترقية والرجوع إلى الإصدار السابق.

## 7.86 – 8.6 — الشاشات والسجلّات والإشعارات والإدخال

عشرون إصدارًا في يومين، والاختبارات من 1439 إلى 1511. الملخّص هنا لأن سجل
التغييرات طويل، والوكيل التالي يحتاج الخريطة لا الرحلة.

- [x] كل الشاشات صارت جداول ثرية مع نصٍّ احتياطي، والمدد تُقرأ بالأشهر والأسابيع.
- [x] **قياس الحمولة ذاتيًّا** وتحذيرٌ عند 70% — بعد أن كلّفنا تقريرٌ واحد جداول
      كل الشاشات جلسةً كاملة (7.87).
- [x] **حدّ أربعين صفًّا** لكل جدول في مكان واحد، بعد أن وُجدت ثلاثة جداول بلا حدّ.
- [x] سجل عمليات دائم بنوافذ 24/48/72 ساعة و7 أيام وتنقّل يومًا بيوم.
- [x] تاريخ طلبات الوصول، والموافقة بضغطتين، وإبلاغ كل المشرفين بها.
- [x] **إصلاح مسار الترقية**: كان يمنح الصلاحية دون الإشعارات، فصنع مشرفًا صامتًا.
- [x] إشعار العرض والرفع لكل قالب، بجهةٍ يختارها المشرف، ولا يقاطع من يكتب،
      ويحذّر من يجهّز القالب نفسه.
- [x] إدخال الإعدادات بالضغط: 90 رقمًا بأزرار، وشبكة للساعات، وساعة لنافذة
      الصيانة، ومنتقيات للقوائم، ومدًى للأرقام الحسّاسة.
- [x] الاختبارات لا تكلّم الشبكة، والمانجر يعرض ما يجري في سطر واحد.

## المفتوح الآن — بالترتيب

1. ~~**تشغيل يوم كامل ثم القياس**~~ — **قِيس عند 8.16**. ثلاثة أسابيع من
   `bridge.log` (17 أغسطس – 9 سبتمبر) أعطت الجواب:
   - **تحذير حمولة واحد في المدة كلها**، ولشاشة واحدة: `📖 دليل الجسر` عند
     **182%** من الحدّ للمشرف و**115%** للمشغّل. أي أن كل شاشة أخرى — الحالة
     والتقارير وسجل العمليات والطبقات — بقيت دون 70% مع بيانات محطة حقيقية.
     فحدّ 12000 ليس ضيّقًا؛ الدليل وحده كان يتجاوزه، وقد صار مسقوفًا.
   - **صفر احتجاز في فترة الهدوء**: الفترة معطّلة في هذه المحطة، فرقم «خمس
     رسائل محجوزة» ما يزال بلا بيانات ولا يمكن تثبيته من هنا.
   - **557 عملية هواء** مرّت بقاعدة النصف دقيقة بلا شكوى مسجّلة.
   والباقي المفتوح من هذا البند: أرقام فترة الهدوء، وتحتاج محطة تُفعّلها.
2. **حارسٌ في البوّابة للجداول**: اختبارٌ يرفض أي `Get-*Blocks` جديد لا يمرّ
   بـ`Select-RichTableRows`. النمط تكرّر أربع مرات؛ ما تكرّر أربعًا يستحقّ قاعدة
   لا يقظة — كما فعل حارس التصفيح.
   > **ما يزال مفتوحًا عند 8.15، ومعه ما وجدناه**: من ست وعشرين دالة
   > `Get-*Blocks` تمرّ **ستٌّ** فقط بـ`Select-RichTableRows`. فالحارس ليس
   > إضافةً سريعة: يحتاج تصنيف الستّ والعشرين أولًا — أيُّها يبني من مجموعة
   > تنمو مع بيانات المحطة وأيُّها محتواه ثابت — وإلا وُلد بقائمة استثناءات
   > تُفرغه من معناه. حارسٌ يُستثنى منه عشرون ليس حارسًا.
3. **المدير**: البحث بالوقت **وصل في 8.7** — الحقل يقول ذلك بنفسه الآن
   («تصفية بالنص أو بالوقت (مثل 07:2)»). الباقي المفتوح: لوحةُ حالةٍ أغنى فوق
   السجل، وقراءةٌ غير متزامنة لملفَّي النبضة والسجل (مؤجَّلة بقصد: السجلّات
   محليّة، والعلاج إعادة هيكلة أكبر من قيمتها هنا).
4. ~~**174 إعدادًا خطرٌ بذاته**~~ — **متجاوَز عند 8.15**: الإعدادات موزّعة على
   تسعة أبواب، كل باب مُصفَّح، ومعها بحثٌ بالاسم أو بالوصف، و«المعدّل فقط».
   الشاشة لم تعد جدارًا.
5. **حدٌّ معروف**: الطلبات المرفوضة قبل 7.99 غير مسجّلة — الشاشة تقول ذلك،
   ولا سبيل لاستعادتها.
