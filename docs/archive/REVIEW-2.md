# تقييم ونقد وخطة تحسين — CinegyTelegramBridge

مراجعة نقدية للحالة الحالية بعد جولتَي التطوير، **بما في ذلك نقد عملي أنا**.
مبنية على قراءة الكود و`bridge.log` الفعلي وفحوصات آلية جديدة.

---

## أولًا: تقييم صريح

### ما هو جيد فعلًا

| الجانب | التقييم |
|---|---|
| نموذج الصلاحيات | سليم الآن — فحص على مستوى المستخدم، وفصل واضح بين المشغّل والمشرف |
| التصميم غير المتزامن للقطات | صحيح ومُحكم — الحلقة لا تتجمّد، والمهلة تتقلّص ذكيًا عند وجود عمل معلّق |
| مراقبة البث | تفرّق بين "أوقفه المستخدم" و"مات وحده"، وهو التمييز الصحيح |
| دمج `Save-Config` من القرص | يحلّ مشكلة حقيقية وقعت فعلًا |
| التوثيق | شامل ومربوط بالأسباب لا بالوصف فقط |

### ما هو ضعيف — والمسؤولية عليّ

**١. لا يوجد أي اختبار آلي. إطلاقًا.**

هذه أهم نقطة في التقرير كله. طوال العمل اعتمدتُ على:
- فحوصات نصية (توازن أقواس، تطابق أسماء)
- نقل الخوارزميات إلى Python لاختبارها

وهذا **لا يختبر PowerShell**. النتيجة الملموسة: ثلاثة أعطال وصلت إليك أنت
لا إليّ:

| العطل | كان سيُكتشف بـ |
|---|---|
| `System.Object[]` في كل رسالة | تشغيل واحد |
| `The property 'Count' cannot be found` | تشغيل واحد |
| فشل ⏭ تخطي | تشغيل واحد |

ثلاثتها من **عائلة واحدة**: دلالات PowerShell عند حدود الدوال (إرجاع
المصفوفات، الربط بالمعاملات). لا يلتقطها التحليل الساكن، ويلتقطها أي تشغيل.

**السبب الجذري:** `pwsh` غير متاح في بيئتي ومحاولات تثبيته محجوبة شبكيًا.
لكن هذا تفسير لا عذر — كان عليّ أن أصرّح بحدّة أكبر أن الكود **غير مُختبَر
تشغيليًا**، وأن أطلب منك تشغيل اختبار دخان بعد كل دفعة قبل الاعتماد عليها
على الهواء.

**٢. تضخّم في المزايا (feature creep).**

طلبتَ "نفّذ كل المقترحات" ونفّذتُها. لكن دوري كان أن أعترض على بعضها:

| الميزة | نقد |
|---|---|
| النبضة اليومية | معطّلة افتراضيًا، وقد لا تُستخدم أبدًا |
| المفضّلة | مع قالبين فقط لا معنى لها |
| النصوص الجاهزة | مفيدة نظريًا، وقد حذفتَها أنت من `templates.json` — مؤشر |
| العرض المؤقّت | مفيد، لكن هل استُخدم؟ |
| ٢٦ إعدادًا | كل إعداد = مسار كود إضافي = احتمال عطل |

**المبدأ الذي خالفتُه:** في نظام بث حيّ، كل سطر كود غير مستخدم هو مخاطرة
بلا عائد. الأفضل كان تسليم النواة الصلبة أولًا، ثم إضافة الباقي عند طلبه.

**٣. ملف واحد بـ ٢٣٧٣ سطرًا و٩٨ دالة.**

يعمل، لكنه صعب التنقّل والاختبار والمراجعة. الحالة مقبولة لسكربت، غير
مقبولة لأداة تتحكم بالهواء.

**٤. لم يُشغَّل `PSScriptAnalyzer` ولا مرة.**

الأداة القياسية لتحليل PowerShell — كانت سترصد الأفعال غير المعتمدة
والمتغيرات غير المستخدمة وأنماطًا خطرة.

---

## ثانيًا: مشاكل قائمة — مرتّبة بالخطورة

### 🔴 خطيرة (تمسّ الهواء أو الأمان)

#### ١. إعادة تنفيذ أوامر قديمة عند إعادة التشغيل

```powershell
$offset = 0          # عند كل إقلاع
```

تليجرام يحتفظ بالتحديثات **غير المؤكَّدة حتى ٢٤ ساعة**. فإن انهار الجسر (أو
أعدتَ تشغيله) وكان هناك ضغط زر لم يُعالَج، فسيُنفَّذ **فور الإقلاع**.

**السيناريو:** مشغّل يضغط "عاجل"، الجسر يسقط قبل المعالجة، تعيد تشغيله بعد
ساعة أثناء نشرة أخرى ⇒ **يظهر العاجل على الهواء بلا سبب واضح**.

**الحل:** عند الإقلاع، اسحب التحديثات وتجاهل ما هو أقدم من ٦٠ ثانية
(`update.message.date`)، أو احفظ آخر `offset` في ملف. الأول أبسط وأأمن.

#### ٢. تسريب مفتاح البث في الرسائل والسجل

أضفتُ في الجولة الأخيرة عرض آخر سطر من stderr لـ ffmpeg:

```powershell
$detail = Get-LastErrorLine -Path (Join-Path $logDir "relay-stderr.log")
... Send-TelegramMessage ... "سبب ffmpeg: $detail"
```

ffmpeg **يطبع رابط الإخراج في رسائل الخطأ عادةً**، ورابط الإخراج هو
`rtmp://…/s/<مفتاح البث>`. فالمفتاح قد يُرسل إلى محادثة تليجرام ويُكتب في
`bridge.log`.

**نقد ذاتي:** أضفتُ هذا كتحسين تشخيصي دون التفكير في محتوى ما أعرضه. خطأ
مباشر مني.

**الحل:** دالة تنقيح (redaction) تستبدل أي `rtmp://...` وأي سلسلة تشبه مفتاحًا
بـ `rtmp://***` قبل العرض أو التسجيل.

#### ٣. زر الطوارئ 🚨 قد يتجمّد ١٠ ثوانٍ لكل طبقة

```powershell
foreach ($l in $layers) { Invoke-HideLayer ... }   # HTTP متزامن، TimeoutSec = 10
```

إن كان Air Pro معلّقًا أو الشبكة متعثّرة، فزر **الطوارئ تحديدًا** — الذي
يُضغط في أسوأ اللحظات — يحجب البوت بالكامل ١٠ ثوانٍ × عدد الطبقات.

وهذا ينطبق على كل استدعاءات Air (`SHOW`/`HIDE`/`EXIT`/postbox) وليس على
إخفاء الكل وحده.

**نقد ذاتي:** عالجتُ الحجب في مسار ffmpeg وتركتُ مسار Air Pro — وهو المسار
**الأهم** لأنه في قلب كل عملية.

**الحل:** خفض `TimeoutSec` إلى ٣ ثوانٍ لأوامر الجرافيك (شبكة محلية، لا مبرر
لعشر)، وتنفيذ إخفاء الكل بالتوازي، أو نقله إلى نمط المهام غير المتزامنة.

#### ٤. كتابة `config.json` غير ذرّية

```powershell
$target | ConvertTo-Json | Set-Content -Path $ConfigPath
```

انقطاع كهرباء أو انهيار أثناء الكتابة ⇒ **ملف مبتور** ⇒ الجسر لا يقلع أصلًا
(`ConvertFrom-Json` يفشل) ⇒ فقدان التوكن وقائمة المصرّح لهم.

**الحل:** الكتابة إلى ملف مؤقت ثم `Move-Item -Force`، مع نسخة احتياطية
`config.bak`.

### 🟠 متوسطة

#### ٥. لا حدّ لطول النص الذاهب إلى الهواء

مشغّل يلصق ٣٠٠٠ حرف ⇒ يذهب كما هو إلى المشهد ⇒ تشوّه الجرافيك على الهواء.
لا يوجد أي تحقق.

**الحل:** `MaxFieldLength` (مثلًا ٢٠٠ حرفًا) مع رسالة رفض واضحة، وتحذير عند
الاقتراب من الحد.

#### ٦. إعداد أمني قابل للتعطيل من الشات

`RequireUserLevelAuth` موجود في شاشة ⚙️ الإعدادات. ضغطة واحدة (أو خطأ) تُضعف
نموذج الصلاحيات كله بلا تأكيد ولا تنبيه.

**الحل:** فصل الإعدادات الأمنية في قسم محمي يتطلب تأكيدًا صريحًا، أو
إخراجها من الشات نهائيًا (ملف فقط).

#### ٧. حالة "على الهواء" تُفقد عند إعادة التشغيل

`$script:OnAir` في الذاكرة فقط. بعد إعادة تشغيل، الجرافيك ما زال على الهواء
لكن البوت "لا يعرف" — فيختفي صف 🔴 وزر الإخفاء السريع.

**الحل:** حفظه في `logs/onair.json` واستعادته عند الإقلاع (مع اعتباره
تقديريًا).

#### ٨. `templates.json` معطوب ⇒ إغراق السجل

مسار فشل التحليل لا يُخزّن في الذاكرة المؤقتة، فيُعاد التحليل ويُسجَّل
التحذير **عند كل استدعاء** — أي عشرات المرات لكل ضغطة زر.

**الحل:** تخزين نتيجة الفشل مؤقتًا أيضًا حسب `LastWriteTime`.

#### ٩. كتابة قرص متزامنة على مسار الهواء

`Add-UsageCount` تكتب `usage.json` عند **كل إظهار**. صغيرة، لكنها I/O متزامن
في أكثر المسارات حساسية.

**الحل:** كتابة مؤجّلة (كل ٦٠ ثانية أو عند الإغلاق).

### 🟡 صغيرة

- لا يوجد `Send-TelegramPhoto` retry (بينما الرسائل النصية لها).
- النبضة تعتمد الساعة المحلية دون اعتبار للتوقيت الصيفي.
- الرسائل عربية والكود إنجليزي — متّسق داخليًا لكن التوثيق مختلط.
- لا يوجد مؤشر إصدار (`$BridgeVersion`) يظهر في `ℹ️ الحالة` — يصعّب معرفة
  أي نسخة تعمل فعلًا.

---

## ثالثًا: خطة التحسين

### المرحلة صفر — الاختبار (قبل أي شيء آخر)

هذه المرحلة **أهم من كل ما بعدها مجتمعًا**، ولا يمكنني تنفيذها وحدي.

1. **اختبار دخان يدوي** بعد كل تغيير، وقبل أي اعتماد على الهواء:
   `/بدء` ← قالب بحقول ← ⏭ تخطي ← 🚨 إخفاء الكل ← 📸 صورة ← ⚙️ إعداد ←
   📜 السجل.
2. **`PSScriptAnalyzer`** — سطر واحد، يكشف فئات كاملة من المشاكل:
   ```powershell
   Install-Module PSScriptAnalyzer -Scope CurrentUser
   Invoke-ScriptAnalyzer -Path .\TelegramBridge.ps1 -Severity Warning,Error
   ```
3. **اختبارات Pester** للدوال الخالصة (بلا شبكة): `Split-TelegramText`,
   `ConvertTo-ProcessArgumentLine`, `Get-JsonProp`, تحليل القوالب،
   `Get-FieldPromptText`. هذه وحدها كانت ستمنع عطلَي `System.Object[]`
   و`Count`.

**إن أردت، أكتب لك ملف `Tests/Bridge.Tests.ps1` جاهزًا وتشغّله عندك — هذا
أعلى بند عائدًا في الخطة كلها.**

### المرحلة الأولى — إصلاحات السلامة (١-٤ أعلاه)

بالترتيب: تنقيح الأسرار ← منع إعادة تنفيذ الأوامر القديمة ← خفض المهلات
وتوازي الإخفاء ← كتابة ذرّية للإعدادات.

هذه الأربعة تمسّ الهواء أو الأمان مباشرة، ومجموعها تغيير محدود ومركّز.

### المرحلة الثانية — تقليص المساحة

مراجعة صادقة للمزايا: أيّها استُخدم فعلًا خلال أسبوع تشغيل؟ ما لم يُستخدم
**يُحذف** أو يُعطَّل افتراضيًا. الهدف: تقليل عدد الإعدادات من ٢٦ إلى ما
يُستعمل حقًا.

### المرحلة الثالثة — البنية

تقسيم الملف إلى وحدات: `Config.psm1`, `Telegram.psm1`, `AirActions.psm1`,
`Media.psm1`, `Ui.psm1`، مع بقاء `TelegramBridge.ps1` منسّقًا رفيعًا.
تُنفَّذ **بعد** وجود اختبارات، لا قبلها.

### المرحلة الرابعة — النضج التشغيلي

مؤشر إصدار، حفظ حالة الهواء، مراقبة خارجية (ملف نبضة يقرأه نظام مراقبة)،
وتقرير يومي مختصر.

---

## الخلاصة

النظام **يعمل الآن ويؤدي وظيفته**، وبنيته الأمنية وتصميمه غير المتزامن
سليمان. لكن ثقتي فيه محدودة بحدّ واضح: **لم يُشغَّل سطر منه في بيئتي قط**،
وثلاثة أعطال وصلت إليك تثبت أن التحليل الساكن وحده غير كافٍ لـ PowerShell.

أهم توصية واحدة: **لا تعتمد على أي دفعة قادمة قبل اختبار دخان عندك**، ودعنا
نبني الاختبارات الآلية قبل إضافة أي ميزة جديدة.

الترتيب الذي أنصح به: المرحلة صفر ← البنود ١-٤ ← ثم إعادة التقييم.
