# مهام الإصدار 4.0.0 — تحسينات CinegyTelegramBridge

مرتّبة حسب الأولوية. كل بند له الملف/الدالة ومكان التحقق.

## ✅ منفّذ في 4.0.0

### 1. إعادة تفعيل تنبيه التغيير الخارجي (regression from 3.1.1 fix) — ✅
- أُعيد بناء `$changes` في `Update-OnAirStateFromCinegy` عند الإزالة الفعلية.
- التحقق: اختبار Pester يولّد Changes غير فارغ عند إزالة طبقة بسبب IsOnAir=false.

### 2. إصلاح اختبار الدخان — ✅
- `Tests/Smoke-OnAir.Pester.ps1` (5/5) يستخدم mock لـ Save/Import-OnAirState
  (لا لمس للقرص أو السيرفر). يُشغَّل تلقائيًا ضمن Run-Checks.ps1.

### 3. onair.json dirty tracking — ✅
- `$script:OnAirDirty` يُضبط عند تحديث ActiveId أو الإزالة؛ `Save-OnAirState`
  تُستدعى عند التسخ أو الإزالة (لا فقط عند الإزالة).

### 4. رفع مهلة المزامنة — ✅
- `CinegyMonitorTimeoutSeconds` من 1 إلى 3 ثوانٍ (افتراضي).

### 8. تنظيف ملفات الاختبار المؤقتة — ✅
- حُذف `run_smoke.ps1` و`test_*.ps1` المؤقتة. اختبار الدخان في `Tests/`.

### 10. توثيق السلوك + رفع الإصدار — ✅
- `CHANGELOG.md` يحوي 4.0.0. `$script:BridgeVersion = '4.0.0'`.

## ⏳ مؤجَّل إلى 4.1 (أكبر/أخطر — يحتاج اختبار دخان على الإنتاج)

### 5. ربط الطبقة بالقالب عبر Layer لا الاسم
- Cinegy يسمّي المشهد `مسائي On` بينما القالب `بنر عاجل` → `Test-OnAirTemplateMatch`
  يعتمد على الاسم/المسار فيفشل الربط. الحل: الاعتماد على `$template.Layer`
  كمعرّف موثوق (الطبقة 7 = بنر عاجل دائمًا حسب templates.json).
- ملاحظة: بعد إصلاح 4.0.0 لم يعد هذا يسبب فقدان السجل، لذا أقل إلحاحًا.

### 6. تسجيل سبب واضح عند إزالة الإقلاع
- عند إقلاع يمسح طبقة مخفية، تسجيل السبب صراحةً في bridge.log.

### 7. تقسيم الملف الواحد
- `TelegramBridge.ps1` ~5087 سطرًا. فصل إلى وحدات (OnAirState.ps1, CinegySync.ps1,
  StatusReporting.ps1) تُحمّل عبر dot-source (كما فُعل مع CinegyAirTitler.psm1).

### 9. عمود "آخر تحديث" في الحالة
- عرض وقت آخر مزامنة ناجحة مع Cinegy في قائمة الحالة.
