# Changelog

## 8.17.0 — 2026-09-09

حارس الجداول، بعد التصنيف الذي كان شرطه.

كان هذا البند مفتوحًا في الخطة منذ 8.6، ورفضتُ في 8.15 شحنه بسرعة لأن قياسي
الخشن (`grep` على أسماء الدوال) قال إن **عشرين** جدولًا مخالف — وحارسٌ يُستثنى
منه عشرون ليس حارسًا.

### 🔢 والتصنيف قال شيئًا آخر

تحليلٌ نحوي (AST) لكل `Get-*Blocks` في `Parts` — خمسٌ وعشرون دالة:

| | |
|---|---|
| محدودة بآلية مسمّاة | **١٢** (`Select-RichTableRows`، `Get-BridgePageWindow`، `PageSize`، `Select-Object -First`) |
| صفوفها قائمة ثابتة | **١٢** (فحوص الصحة، ملفات التشغيل، الطبقات على الهواء، أيام التقرير، المشغّلون…) |
| **تنمو بلا حدّ** | **١** |

الفرق بين «عشرون» و«واحد» هو الفرق بين `grep` وتحليل نحوي. ولو بُني الحارس على
التقدير لوُلد بقائمة استثناءات تُفرغه من معناه.

### 📜 والمخالفة الوحيدة

`Get-TemplateHistoryBlocks` تقرأ سجل التدقيق بحثًا عن قالب، فطولها **تاريخ
المحطة** لا شيء تتحكم به الشاشة: قالبٌ استُخدم خلال شهرٍ مزدحم يعيد جدولًا يعبر
حدّ الحمولة، فيسقط الجدول كلّه إلى نصّ. قُصّت بـ`Select-RichTableRows`، ومعها
السطر الذي يقول كم صفًّا أُخفي — لأن جدولًا ينقص أقدم صفوفه بصمت يُقرأ على أنه
القصة كاملة.

### 🛡 والحارس نفسه

في `BridgeUiPaging.Tests.ps1` بجانب حارس تصفيح الأزرار: أي `Get-*Blocks` يبني من
مجموعة بحلقة يجب أن يكون محدودًا بآلية مسمّاة، أو مُدرجًا في `FixedRowScreens`
**بسببٍ مكتوب**. ومعه اختبارٌ يرفض استثناءً يسمّي دالة لم تعد موجودة.

وأمسك حالةً وهو يُبنى: `Get-NewsTickerReorderBlocks` مُصفَّحة فعلًا لكن **بيدها**
عبر `Get-NewsTickerPageSize` — الحساب نفسه مكتوب مرّتين في الملف. محدودة، لا
بالمساعد المشترك؛ سُمّيت آليتها صراحةً بدل تمريرها بنمطٍ فضفاض، فيبقى ذلك
التكرار معروفًا لا مخفيًّا.

وجُرِّب سلبيًّا: نُزع القصّ من `Get-TemplateHistoryBlocks` فسقط الحارس، ثم أُعيد.

## 8.16.0 — 2026-09-09

فحص الخطط — وما وجدَته الخطة نفسها.

### 📖 «الدليل الكامل» لم يُعرض مطويًّا ولا مرة

`bridge.log` كان يحمل السطر منذ أمس، ولم ينظر إليه أحد — وأنا أوّلهم:

```
18:44:49 [WARN] Rich payload for '📖 دليل الجسر' is 21861 characters,
                182% of the limit - cap its rows before it loses its table
```

و`AGENTS.md` يقول عن هذا السطر بالحرف: «راقب ذلك السطر بعد كل إضافة». أضفتُ إلى
المساعدة في 8.11 و8.12 ولم أراقبه.

القياس اليوم: **115% للمشغّل، 182% للمشرف**. أي أن `Send-TelegramRichMessage`
كان يرفض الرسالة **في كل مرة**، فيسقط القارئ إلى النصّ المُصفَّح — والفصول
القابلة للطيّ، وهي سبب وجود هذه الشاشة، **لم تُعرض ولا مرة لأي مستخدم**. لم
يكن عطلًا صاخبًا: البديل يعمل، فالدليل يصل. لكن الميزة كانت ميتة بصمت.

> ولم أُحدثه أنا: إضافاتي زادته ولم تُنشئه. كان فوق الحدّ قبلها.

صار البناء **مسقوفًا بالقياس**: تدخل الأبواب حتى لا يتّسع التالي، ثم سطرٌ يسمّي
ما بقي — لأن دليلًا يتوقف صامتًا أسوأ من دليلٍ يقول أين يكمل. والقياس **خطّي**
لا تربيعي (الصياغة الأولى كانت تُعيد تسلسل القائمة كلها عند كل باب، فأبطأت
البوابة أكثر من دقيقة).

وسقط باختباري اختبارٌ قائم كان يقارن **عدد** كتل المشرف بعدد كتل المشغّل — والسقف
يقصّ من الطرف للاثنين فتتساوى الأعداد. نُقل التأكيد إلى الخاصية التي تهمّ فعلًا:
ألّا يظهر عنوان بابٍ إداري في نسخة المشغّل، ولا في السطر الذي يسمّي المحذوف.

### 📋 والخطة كانت تصف نظامًا عمره تسع نسخ

ترويستها تقول **8.6.0** والكود عند 8.16. وبندها الثالث يطلب «البحث في السجل
بالوقت» في المدير — **وقد وصل في 8.7**، والحقل يقول ذلك بنفسه.

وبندها الأول كان ينتظر «تشغيل يوم كامل ثم القياس». ثلاثة أسابيع من السجل
(17 أغسطس – 9 سبتمبر) أعطت الجواب: **تحذير حمولة واحد في المدة كلها**، ولشاشة
واحدة هي الدليل. فكل شاشة أخرى بقيت دون 70% مع بيانات محطة حقيقية، وحدّ 12000
ليس ضيّقًا. أما «خمس رسائل محجوزة» فما يزال بلا بيانات: فترة الهدوء معطّلة هنا،
و**صفر** احتجاز في المدة كلها. كُتب ذلك في الخطة بأرقامه.

## 8.15.0 — 2026-09-09

بقية بنود تقارير الفحص، ومعها انحدارٌ سببته أنا.

### 📅 الانحدار أولًا: وضع اليد الواحدة كسر كل المنتقيات

في 8.9.0 نقلتُ `ConvertTo-OneHandLayout` إلى وقت الإرسال ليشمل كل الشاشات — وكان
ذلك صحيحًا ومقصودًا. لكنه شمل معها **الشاشات التي يحمل صفُّها معنى**:

| الشاشة | قبل | بعد النقل |
|---|---|---|
| تقويم الجدولة | ٩ صفوف | **٤٦** |
| منتقي الساعة | ٦ | **٢٧** |
| منتقي الدقيقة | ٤ | **١٥** |
| مدى إعداد صغير | ٥ | **٢٥** |

وتعليق الكود نفسه يقول: «الصفُّ الذي يقع عليه التاريخ هو ما تجده به العين». أي
أن إصلاحًا صحيحًا كسر شيئًا آخر، ولم يكشفه أي اختبار لأن اختباري وقتها قاس
القائمة الرئيسية وشاشات الأفعال لا الشبكات.

العلاج: انسحابٌ صريح `KeepRows` تحترمه طبقة التسلسل الوحيدة وتحذفه قبل الإرسال
(فليس حقلًا في Bot API). الشبكات الخمس تعلنه؛ وكل ما عداها ما يزال ينقسم. ومعه
اختبارٌ يقيس الأربع.

### 📝 وكتابة postbox المؤجَّلة صارت معنونة بمشهد

كانت تُرسل إلى الطبقة وحدها، والطبقة تُستبدل خلال `PostShowDelayMs`. فقيم القالب
الذي غادر تُكتب داخل القالب الذي حلّ محلّه — ويقرأ المشاهد العنوان القديم تحت
الرسم الجديد. صار العنصر يحمل `Key` و`ActiveId`، ويُسقَط إن لم تطابق الطبقةُ ما
حُجز له. والإخفاء والخروج يُلغيان الكتابة بالاختبار نفسه، بلا آلية ثانية.

### ⏱ وفحص السواد لم يعد ينام على حلقة التحكم

`Start-Sleep` بين اللقطتين كان يوقف **كل شيء**: مؤقتات الإخفاء التلقائي،
والجدولة، وانتهاء المهل، والنبضة — تنتظر انقشاع تلاشٍ. صارت اللقطة الثانية
**تُحجَز لموعد** وتلتقطها التكّة التي تصل بعده.

> والالتقاط نفسه ما يزال متزامنًا: القرار موثَّق في الكود («أبسط من تمرير مهمة
> غير متزامنة أخرى عبر التك»)، ونقضُه إعادة هيكلة لا سطر. أزلتُ الانتظار الميت
> ولم أنقض القرار.

### 🧹 وثلاثة أصغر

فشل تفريغ الطبقة قبل إعادة العرض صار يُسجَّل دائمًا لا في وضع التصحيح وحده —
وهو الشرط الذي يبقي العنوان السابق على الهواء. والتنويهات المنتهية لها سقف
خمسين خلف الحيّة. وسياق الاختبارات صار يمنع تشغيل عمليات الوسائط كما يمنع
الشبكة منذ زمن.

### ✅ ومسحُ انحدارٍ على تعديلات الجلسة كلها

فُحصت وسلِمت: ملفات الحالة السبعة تدور ذهابًا وإيابًا وتكتب نسخها الاحتياطية،
والإعدادان المحذوفان لا يقرؤهما شيء، **والموجز ما يزال يتوقف فعلًا عند إخفاءٍ
مسموح** — وهذا آخر ما يجب أن يُنسى بعد نقل الأثر تحت البوابة، فحارسٌ يثبت الرفض
وحده يرضيه حذفُ الأثر أصلًا. اختباران جديدان يثبتان النصفين.

### ⏳ وبندٌ واحد لم يُنفَّذ، بسببه

حارس الجداول (`Get-*Blocks` يجب أن تمرّ بـ`Select-RichTableRows`): من ست وعشرين
دالة تمرّ **ستٌّ**. فالحارس يحتاج تصنيف الستّ والعشرين أولًا، وإلا وُلد بقائمة
استثناءات تُفرغه. حارسٌ يُستثنى منه عشرون ليس حارسًا. مكتوبٌ في
`DEVELOPMENT-PLAN.md` بهذا الرقم.

## 8.14.0 — 2026-09-08

استكمال المستوى الرابع من خطة تقارير الفحص: صيانةٌ لا عطل ظاهر، وكلّ بندٍ قوبل
بالكود قبل تنفيذه.

### 🗄️ سبعة ملفات حالة خارج النمط الموثّق

`AGENTS.md` يشترط لملف الحالة: **ملف مؤقّت فريد، ثم نسخة احتياطية صالحة،
واستبدال ذري**. و`Write-BridgeValidatedJson` يفعل الثلاثة. وسبعة مواضع كانت
تكتب بـ`Set-Content` خامًا ثم `Move-Item`:

الإعلانات، وعدّاد الاستخدام، والمفضّلة، وأسماء المستخدمين، والقيم الأخيرة،
ومسودات العرض، وحالة تشغيل الموجز.

فلا نسخة احتياطية لأيٍّ منها — ملفٌ تلف يعني فقدان المفضّلة أو الأسماء أو
المسودات بلا مرجع. **وأسوأ**: اسم الملف المؤقّت كان **ثابتًا** (`.tmp`) لا
فريدًا، فحفظان متزامنان للملف نفسه يتنازعان عليه. السبعة الآن على الكاتب
المشترك.

> التقرير عدّها خمسة؛ العدّ الفعلي سبعة، ولم يذكر مسألة الاسم الثابت.

### 🖥 وأربعة موارد في المدير

- **ستة مؤقّتات من سبعة لا تُحرَّر** عند الإغلاق (التقرير قال خمسة من ستة؛
  الفعلي ستة من سبعة). أُنشئت بـ`new` لا داخل حاوية مكوّنات النموذج، فلا شيء
  يحرّرها.
- **`Process.Start` لفتح مجلد السجلات** يعيد كائنًا يملك مقبضًا نظاميًّا ولو لم
  يُستعمل، ويُنفَّذ عند كل ضغطة. صار `using`.
- **تبنّي جسرٍ انتهى يسرّب مقبضًا**: `Process.GetProcessById` ثم `HasExited`
  يعود `false` بلا تحرير — وهي الحالة الشائعة (جسر مات بين النبضة وقراءتها).
- **`StopForSettingsSave` يجمّد النافذة عشر ثوانٍ**: `WaitForExit(10000)` على
  خيط الواجهة. لا يمكن جعله غير متزامن دون إعادة هيكلة الحفظ، لكن نافذةً تتوقف
  عن الرسم بلا مؤشر ولا رسالة تُقرأ كتعليق — وأول ما يفعله المشغّل عندها أن
  يقتلها في منتصف الحفظ. صار لها عنوانٌ ومؤشر انتظار يُعادان في `finally`.

### ⏸ وبندٌ مؤجَّل بقصد

قراءة `bridge.liveness` و`bridge.log` متزامنةً على خيط الواجهة كل ثانية. صحيحة
كملاحظة، لكن علاجها إعادة هيكلة إلى قراءة غير متزامنة مع ما يتبعها من إعادة
دخول — خطرٌ أكبر من قيمته هنا، لأن السجلّات محليّة لا على مشاركة شبكية. تُترك
مذكورةً لا مصلَحة.

## 8.13.0 — 2026-09-08

طلبٌ يُرفض لا يجوز أن يغيّر الهواء.

أصل هذه النسخة تقارير فحصٍ خارجية، وقوبل كل بندٍ منها بالكود قبل العمل به.

### 🔒 ثلاثة مواضع، وعيبٌ واحد

النمط واحد: **أثرٌ يمسّ الهواء يُنفَّذ قبل البوابة التي تقرّر هل يحقّ له ذلك** —
لأن الأثر كُتب في أعلى الدالة بوصفه «تنظيفًا» لا فعلًا.

- **العاجل الممنوع كان يسحب الموجز ثم يُرفض.** `Clear-MojazForUrgent` كان يُستدعى
  فور تحديد القالب (`Bridge.AirOperation.ps1:617`)، أي **قبل** `Test-TemplateAccess`
  و`Test-TemplateShowPolicy` بستة أسطر. فمشغّل مصرَّح له بالبوت وممنوع من قالب
  العاجل كان **يُنزل نشرة الموجز عن الهواء** ثم يُقال له «لم يتم الإرسال». صار
  الاستدعاء بعد البوابتين وبعد التحقق الحيّ من Cinegy، وقبل بدء العملية مباشرة.
- **والإخفاء والخروج المحجوبان بوضع الصيانة** كانا يوقفان الموجز أولًا
  (`Bridge.ShowFlow.ps1:398` و`460`) ثم يرفضان العملية. فلا HIDE يصل Cinegy،
  والموجز مُسجَّل منتهيًا والشريط مستدعًى، والمشهد ربما ما يزال يمشي صفوفه على
  الهواء. صار الإيقاف بعد البوابة.

**وحارسٌ يمنع عودة النمط**: `Describe 'A refused request changes nothing on air'`
واختباران في بوابة الصيانة. جُرِّبت بإعادة الترتيب القديم فسقطت ثلاثة منها.

### 📡 بثٌّ حيّ بلا تتبّع

كتابة `relay.pid` بلا حماية: ffmpeg يكون قد بدأ فعلًا عند ذلك السطر، فإخفاق
الكتابة كان يُقرأ «لم يبدأ البث» بينما البث حيّ ولا شيء يعرف رقمه فيوقفه. صار
يُبلَّغ أنه يعمل، ويُكتب في السجل أن الرقم تعذّر حفظه.

> وحارس المستودع نفسه ردّ الصياغة الأولى: اختبار «لا نصّ استثناء خام في مسارٍ
> يحمل سرًّا» أسقط البوابة، لأن هذا الملف يتعامل مع مفاتيح RTMP. مرّ النصّ عبر
> `Protect-SensitiveText`.

### ⚙️ وفقدانٌ صامت لتعديلٍ يدوي

`Save-Config` حين تعجز عن إعادة قراءة الملف كانت تكتب بـ`Write-Host` — لا في
`bridge.log` ولا في التدقيق — ثم تكتب نسخة الذاكرة فوق الملف، فيضيع كل ما عُدِّل
بيدٍ على القرص بلا أثر. صارت `Write-BridgeLog` مع رفع `LastConfigSaveFailed`.

### 🌙 وسقفٌ لطابور الهدوء

ديْنٌ من 8.9.0: أُضيف الحفظ ولم يُضَف حدّ. ليلة طويلة مع مصدر ثرثار تُنمي الملف
ورسالةَ موجزٍ تتجاوز حدّ تيليجرام فلا تصل أصلًا. السقف 200، الأقدم يسقط أولًا،
والعدد الساقط يُذكر في الموجز.

### 🧪 واختبارٌ كان يخرج إلى الشبكة

«Quiet runtime orchestration» يموّه اثنتي عشرة خطوة من **سبع وعشرين**، ومن غير
المموّه `Update-OutputBlackWatchdog` الذي يشغّل ffmpeg نحو الرابط النائب في
`config.example.json`. صار يموّه قائمة التك كاملة بترتيبها.

### 🖥 ومفاتيح المدير داخل القائمة

`Theme.Apply` يمشي على `Control.Controls`، ومفاتيح «☰ خيارات» الستة مستضافة في
`ToolStripControlHost` خارج تلك الشجرة — وأحدها مفتاح الوضع الليلي نفسه. كانت
القائمة تُفتح بمفاتيح فاتحة على أرضية داكنة. صارت تُعاد تلوينها عند كل فتح.

## 8.12.0 — 2026-09-08

مقارنة الدليل بالكود، لا قراءته وحده.

المراجعة السابقة (8.11.0) قرأت الدليل بحثًا عن التناقض والنقص. هذه قابلت كل
ادّعاءٍ قابل للعدّ فيه بما في الكود فعلًا — فظهر عطلان لا يكشفهما إلا العدّ.

### 🚪 «الأبواب الثمانية» وهي تسعة

`$script:SettingCategoryDefinitions` فيها **تسع** فئات، والدليل يعدّها ثمانيًا
ويسرد ثمانيًا. والغائب — بعد `🗄️ الملفات والاحتفاظ` — هو **`🔔 الإشعارات
والتنبيهات`**، أي الباب الذي شرح هذا الدليلُ نفسه محتواه في فصلٍ كامل قبل
إصدارٍ واحد. مشرفٌ يبحث عن ساعات الهدوء في شاشة الإعدادات كان يقرأ ثمانية أبواب
ليس بينها بابها.

### 🔒 وقائمة الحماية تذكر خمسة من سبعة

الدليل يقول «**وهي:**» ثم يسرد خمسة. و`$script:ProtectedSettings` تحمل سبعة:
الخمسة، ومعها `BlockRejectedRequesters` و`LeaveUnknownGroups`. و«وهي» ادّعاء
حصر، فإمّا أن يكون كاملًا أو لا يُكتب. اكتمل.

### 🖤 وسطرٌ يناقض السطر الذي بعده بأربعة أسطر

في شرح مراقبة المخرج: «الجسر يلتقط لقطة كل *(قيمة الإعداد)*» ثم بعده «اضبط
`OutputMonitorMinutes` بـ 0 لإيقاف المراقبة». فمن يتبع النصيحة يقرأ بعدها
«يلتقط لقطة كل **0 دقيقة**». صار السطر يقول «المراقبة متوقفة الآن» حين تكون كذلك.

### 🧪 وحارسان يمنعان عودة العدّ اليدوي

اختبارٌ يقابل نصّ الباب بكل فئةٍ في جدول الفئات، وآخر يقابله بكل اسمٍ في
`ProtectedSettings`. جُرِّبا بإعادة العطلين فسقطا، ثم أُعيد الملف.

### ✅ وما قُوبل فوجد صحيحًا

حتى لا يُعاد فحصه: نوافذ سجل العمليات (24/48/72/168 ساعة)، ومعدّل الإشعار
(ثلاثون ثانية = «نصف دقيقة»)، وأن `OutputMonitorMinutes = 0` يوقف الحلقة فعلًا،
وأن التصدير يخرج كتلة `Settings` وحدها فلا توكن ولا قوائم مستخدمين، وأن حذف
الموجز يُلغي مواعيده ويُمنع وهو على الهواء، وأن الشيت الفارغ يُرفض ولا يمسح
الشريط، وأن `/الغاء` و`/قائمة` و`تنبيهاتي` موجودة كما وُعد بها.

## 8.11.0 — 2026-09-08

مراجعة الدليل: ما يقوله، وما نسي أن يقوله.

### ❌ سطرٌ يصف سلوكًا انتهى

في باب ⚙️ الإعدادات كان الدليل يناقض نفسه في شاشةٍ واحدة. أوّله:

> 🎛 وأغلب القيم تُضبط بالضغط لا بالكتابة… الأرقام: ➖ و➕ بخطوة تناسب حجم الرقم.

وبعده بأسطر:

> ↳ إعداد رقمي **يطلب القيمة كتابة**؛ السالب وغير الرقم يُرفضان.

والثاني بقيّةٌ من زمنٍ سبق المنتقيات، وقاعدة المستودع نفسها تقول «لا يُدخَل رقمٌ
بالكتابة». صار السطر يصف ما يجري فعلًا: ➖ و➕، و⌨️ لمن يعرف الرقم، وما خرج عن
المدى يُرفض.

### 🕳 وثلاث ميزات لم يذكرها الدليل أصلًا

بحثتُ في الملف عن «الهدوء» و«اليد الواحدة» و«مسودتك» فلم يردْ أيٌّ منها ولا مرة:

- **🌙 فترة الهدوء** لم تكن في باب 🔔 التنبيهات — وهو بابها. أُضيفت: ما ليس
  عاجلًا يُحتجَز ليلًا ويصل مجموعًا، وما يعني أن الشاشة خطأ الآن يصل فورًا،
  والمحتجَز محفوظ فلا يضيع بإعادة تشغيل ليلية.
- **🤏 وضع اليد الواحدة** لم يُذكر رغم أنه إعدادٌ ظاهر. أُضيف إلى باب الإعدادات،
  ومعه أنه يشمل **كل** الشاشات لا القائمة وحدها (وهذا ما صحّحه 8.9.0).
- **↩️ عودة المسودة بعد إعادة التشغيل** أُضيفت إلى باب ✏️ التعديل، بجانب تنبيه
  المهلة: فلا تذهب رسالتك التالية نصًّا لقالبٍ دون أن تدري.

وأُضيف إلى باب 🛑 الإنهاء ما استحدثه 8.10.0: أن الإخفاء غير المؤكَّد يُقال كما
هو، وأن الطبقة تبقى معروضة حتى يصل التأكيد.

### ✍️ واتّساق الإملاء

«يدوياً» وحيدةً بين خمسٍ وثلاثين «ـًا» في الملف. صارت «يدويًا».

قِيست الأبواب بعد الإضافة: أكبرها 2472 محرفًا مقابل حدّ تيليجرام 4096، والدليل
الكامل (9024) يمرّ بـ `Send-TelegramPagedText` كما كان.

## 8.10.0 — 2026-09-08

ثلاث لحظاتٍ تقول فيها الواجهة غير ما يحدث.

### 🙈 «تم الإخفاء» فوق زرٍّ يقول إنها ما تزال على الهواء

ظهر في تجربة الرحلات لا في قراءة الكود: بعد إخفاء الطبقة ٧ وصلت رسالتان
متناقضتان على شاشة واحدة —

```
✅ تم إخفاء الطبقة 7.
   [🔴 إخفاء 7 · Urgent] [⏱ مؤقت] ...
```

والسبب أن `Hide` نجح فعلًا وقبِله Cinegy، لكن قراءة التأكيد بعده فشلت،
**والاحتفاظ بالسجل حينها قرارٌ صحيح ومقصود** يحرسه اختبارٌ قائم: أن تفقد أثر
رسمٍ قد يكون ما يزال على الشاشة أسوأ من أن تُبقيه معروضًا. لكن نصّ النجاح كان
يُقال بلا شرط، فيقرأ المشغّل «أُخفيت» ويرى تحتها زرّ إخفاءٍ لطبقة حيّة — وهو ما
يُقرأ كعطلٍ في الجسر لا كحالةٍ غير مؤكَّدة.

صار النصّ يتبع ما جرى: «أُرسل أمر الإخفاء وقبِله Cinegy، لكن لم يُؤكَّد رفعها
بعد. تبقى معروضة هنا كأنها على الهواء حتى يصل التأكيد.»

### ⬅️ سطر الرفض كان يختفي تحت سبعة عشر صفًّا

«هذا الخيار للمشرفين فقط.» كانت تأتي ومعها **القائمة الرئيسية كاملة**. سبعة عشر
صفًّا تحت جملةٍ واحدة تدفع الجملة نفسها خارج شاشة الهاتف، ولا واحد من السبعة
عشر هو ما أراده القارئ — أراد الشيء الذي مُنع منه.

صار لحرّاس الرفض الأربعة، ولانتهاء مهلة الإدخال، زرُّ رجوعٍ واحد
(`Get-NoticeKeyboard`). القائمة تبقى حيث طُلبت، لا حيث اعتُذر.

### 🖥 وأزرار المدير تُنطق باسم أيقونتها

كل زرٍّ هناك يبدأ برمزٍ — «⚙  الإعدادات»، «🧹  مسح الشاشة» — وقارئ الشاشة ينطق
اسم الرمز أولًا، فيسمع المشغّل «gear» ثم ينتظر ليعرف ما الزرّ. صار الاسم
يُشتقّ في المصنع `BaseButton` نفسه لا عند كل استدعاء، فيغطّي النافذتين معًا وأي
زرٍّ يُضاف لاحقًا. والإسناد الصريح ما يزال يعلو عليه.

### 🧪 وثلاثة اختبارات تحرس الثلاثة

لأن درس الإصدار السابق كان أن سلوكًا يُشحن بلا اختبارٍ في البوابة هو نفس شكل
العطل الذي أصلحه ذلك الإصدار.

## 8.9.0 — 2026-09-08

مراجعةٌ بحثت عن المفاتيح التي لا تُقرأ والحدود التي لا تُطبَّق.

### 🤏 وضع اليد الواحدة كان يعمل على شاشة واحدة

`ConvertTo-OneHandLayout` مكتوبٌ في توثيقها أنها «تُطبَّق كمرور أخير على لوحة
جاهزة كي لا تحتاج أي شاشة لمعرفتها» — وكان لها **موضع استدعاء واحد**:
`Get-MainMenuKeyboard`. أي أن مشغّلًا يشغّل الوضع يحصل على عمودٍ واحد في القائمة
الرئيسية، وصفوفٍ ثلاثية في الشاشات الاثنتين والخمسين خلفها: الحقول والقوالب
والإعدادات والموجز والأخبار والجدولة.

صار المرور داخل `ConvertTo-TelegramReplyMarkupJson` — «المكان الوحيد الذي تصير
فيه اللوحة JSON» وحيث يُقرأ `EnableButtonStyles` أصلًا — فيغطّي كل لوحة تُرسل.
قاعدةُ تخطيطٍ على كل شاشة أن تشترك فيها طوعًا هي قاعدةٌ ستفوت أكثر الشاشات.
والشريط الثابت 🏠/🆘 يحمل `keyboard` لا `inline_keyboard` فلا يُعاد تشكيله.

### ⚙️ أربعة حدودٍ كُتبت لأسماءٍ غير موجودة

`$script:SettingConstraints` حمل أربعة مفاتيح لا يقابلها إعداد: `MaxNewsItems`
بدل `NewsMaxItems`، و`AuditMaxLines` بدل `AuditMaxSizeMB`، و`AutoHideMaxSeconds`
بدل `AutoHideDefaultSeconds`، و`TelegramPollTimeoutSeconds` بدل
`PollTimeoutSeconds`. والحارس في `Set-Setting` سليمٌ ومكتوب، لكنه مشروط بـ
`ContainsKey` — فلم يعمل قط. النتيجة أن الشاشة تطبع مدًى تحت حقلٍ يقبل
2,147,483,647.

صُحّحت الثلاثة الأولى بأسمائها الحقيقية. و`AuditMaxSizeMB` أخذ حدًّا أدناه
**صفر** لا مئة، لأن الصفر جوابٌ حقيقي فيه («بلا أرشفة»)، وسقفه بالميغابايت لا
بعدد الأسطر الذي كُتب له في نسخةٍ سابقة. أما `PollTimeoutSeconds` فمفتاحٌ عُلوي
لا يمرّ بـ `Set-Setting` أصلًا، فحُدَّ عند موضع قراءته: تيليجرام يرفض مهلة فوق
50 ثانية ويردّ بخطأ على كل استطلاع، وهو ما يُقرأ في السجل كأن البوت واقف.

وأُضيف اختبارٌ يرفض أي قيدٍ باسمٍ لا يقابله إعداد. حارسٌ مخطئ الهجاء ليس حارسًا
أضعف؛ هو لا حارس، ويبدو تمامًا مثل الحارس.

### ↩️ المسودة تعود بعد إعادة التشغيل، وصاحبها لا يعلم

`Import-DraftStates` تستعيد تدفّق التجهيز المفتوح عند الإقلاع — بفحص المهلة
والقالب والقفل — ثم تكتب سطرًا في `bridge.log` ولا تخبر أحدًا. والمشغّل لم يرَ
إعادة التشغيل، فالرسالة التالية التي يكتبها عن شيءٍ آخر تُبتلع نصًّا لخبرٍ عاجل.
السطر كان يذهب لمن يقرأ السجل، وهو أثناء المناوبة لا أحد.

صار يصله «مسودتك لم تضِع» باسم القالب وزرّ إلغاء.

### 🔔 التنبيهات المؤجّلة كانت تُفقد بإعادة التشغيل

طابور فترة الهدوء عاش في الذاكرة وحدها، فإعادة تشغيلٍ في الثالثة فجرًا تُسقط كل
ما احتُجز بلا أثر: لم يُرسل، والدليل الوحيد سطرُ سجلٍّ كُتب قبل ساعات. صار
يُحفظ في `logs/quiet-hours-queue.json` ويُستعاد عند الإقلاع. وكل الطوابير الحرجة
الأخرى كانت محفوظةً أصلًا — الهواء والإخفاء التلقائي والتذكيرات والمسودات
والجدولة — وكان هذا الاستثناء الوحيد.

### 🗑 «المدة تتبع المقطع»: سلسلةٌ كاملة لا تعمل

المفتاح `MojazHoldFollowsClip` كان معروضًا في شاشة إعدادات الموجز بوصفٍ وتسمية،
و**لا يُقرأ في أي سطر**. والحقل `MediaSeconds` الذي يقوده كان يُقرأ في
`New-MojazRunSnapshot` و**لا يُكتب في أي موضع من كود الإنتاج** —
`Add-MojazBulletinRow` تنشئ الصف بـ `Id/ImageMode/Image/Title/Text` فحسب. وحدها
الاختبارات كانت تصنعه بيدها (`MediaSeconds = 42.5`)، فمرّ الفرع خضراءً وهو ميتٌ
على الجهاز. و`Get-FfprobePath` — الدالة التي كانت ستقيس المقطع — بلا مستدعٍ في
المستودع كله.

حُذفت السلسلة: المفتاح، والفرع، والحقل `HoldFromClip`، والدالة الميتة،
والاختبارات التي كانت تلفّق مُدخلها. المدة تُكتب كما كانت تُكتب دائمًا. وحُذف
معها `SharedFavoritesEnabled` — مفتاحٌ «محجوز» ظاهرٌ للمستخدم لا أثر لتبديله.

### 🔐 معالج إنشاء القالب لم يكن في إعادة فحص الصلاحية

`Test-PendingStateAdmission` تعيد التحقق من صفة الإدارة عند استهلاك كل رسالة،
وفيها `template_definition_json` منذ البداية — أما `template_create_*`، الذي
يكتب التعريفات نفسها حقلًا حقلًا، فلم يكن. صار يُطابَق بالبادئة لا بالسرد، كي لا
تُضاف خطوةٌ خامسة إلى المعالج دون أن يتذكّرها أحد هنا.

## 8.8.0 — 2026-09-08

مهلةُ التقاطٍ واحدة ليست مصدرًا ميتًا.

### 📡 ما حدث فعلًا في 16:09

```
16:08:36  Bridge starting
16:09:00  [WARN] Output monitor capture timed out after 8s
16:09:00  [WARN] Output monitor source unavailable for 1 consecutive capture(s)
```

التقاطٌ واحد تجاوز ثماني ثوانٍ **بينما كان الجهاز يبني المانجر وينشره** —
والمصدر سليم طوال الوقت. وثلاثة عيوب اجتمعت:

- **محاولة واحدة تكفي للتحويل حين يكون هناك احتياطي** (بقصدٍ مكتوب في الكود:
  لا تترك مصدرًا ميتًا فترةً أخرى). **صار هناك محاولة ثانية بعد ثانية ونصف**
  قبل احتساب أي فشل: ثماني ثوانٍ ثمنًا مقابل ساعةٍ على المصدر الخطأ.
- **والفحص التالي بعد ساعة** (`OutputMonitorMinutes = 60`) — **وهذا سبب عدم
  وصول إشعار العودة**: الفحص لم يحن بعد. صار التحويل يجدول فحصًا **خلال
  دقائق**، والرسالة تقول ذلك.
- **والأسوأ: «عاد المصدر الأساسي» كانت تُقال بلا فحصه.** الالتقاط الناجح يأتي
  من المصدر **النشط** — وهو الاحتياطي حينها — فكان النجاح عليه يُقرأ عودةً
  للأساسي. صار `Test-PrimaryMonitorSourceBack` يسأل الأساسي مباشرةً ثم يعيد
  الحالة كما كانت مهما كان الجواب.

### 🖥 وتوازن عناصر المدير

- **عرضٌ واحد لكل أزرار الصفّ** (كانت سبعة عروض بين 118 و140): العين تجمع
  بالشكل قبل أن تقرأ الكلمة.
- **وقائمة الخيارات عمودٌ مستقيم**: عرضٌ أدنى موحّد لكل مفتاح، فتصطفّ علامات
  الاختيار بدل حافةٍ مسنّنة تُمسح بالنظر.

## 8.7.0 — 2026-09-08

عناصر المدير تقول ما هي قبل أن تُقرأ.

- **أيقونة على كل زرّ ومفتاح**: ▶ تشغيل · ■ إيقاف · ↻ إعادة تشغيل · ⚙ الإعدادات
  · 📂 السجلات · 🧹 مسح · ☰ خيارات، والمفاتيح: ↻ 🩺 🪟 🧹 ↩ 🌙. **والكلمة باقية
  معها**: أيقونةٌ وحدها تخمين، وهذه أزرارٌ تُضغط تحت ضغط.
- **وقائمة الخيارات صارت تتبع المظهر**: `RenderMode.System` وألوانٌ تُضبط عند كل
  فتح — العارض الافتراضي يرسم إطارًا فاتحًا يبقى فوق اللوحة الداكنة، **والمظهر
  يُبدَّل من داخل هذه القائمة نفسها**.
- **ومجموعتاها صارتا بأسمائهما الحقيقية**: «🔴 ما يحدث للقناة» و«🖥 هذه النافذة» —
  لا «التشغيل» و«العرض» اللتين تحتاجان تفسيرًا.
- **وقائمة شريط النظام تحمل الأيقونات نفسها**: هي ما يراه المشغّل حين تكون
  النافذة مخفيّة، وهو أكثر الوقت، وقائمةٌ لا تشبه الأزرار التي تعكسها شيءٌ ثانٍ
  يُتعلَّم.
- **وصندوق التصفية يعترف أنه يبحث في الوقت**: كل سطر يبدأ بطابعه الزمني، فـ
  «07:2» تصفيةٌ على عشرين دقيقة كما أن الكلمة تصفيةٌ على كلمة — كان ذلك يعمل
  دائمًا ولا شيء يقوله.

## 8.6.0 — 2026-09-08

نافذة المدير تعود إلى ما وُجدت له: السجل.

### 🖥 ثلاثة تحسينات، والمانجر صار v8

- **أربعة عشر عنصرًا كانت تفصل أعلى النافذة عن أول سطر من السجل** — والنافذة
  كلّها وُجدت لتُشاهَد. الستة مفاتيح انتقلت خلف زرّ **`⚙️ خيارات`**، فعاد صفٌّ
  كامل إلى ما يُقرأ فعلًا. وهي مفاتيح تُضبط مرة وتُترك شهورًا (اثنان منها عن
  التفاف الأسطر والوضع الليلي)، فثمنها نقرةٌ واحدة.
- **والمفاتيح داخل القائمة نفسها لا نُسخٌ عنها**: كل قراءةٍ لحالتها في الكود
  تعمل كما كانت، وما تحفظه لم يتغيّر.
- **زرٌّ رئيسي واحد في كل حالة**: «تشغيل» حين يكون متوقفًا، و«إيقاف» حين يعمل —
  لا ثلاثة أزرار متساوية الوزن اثنان منها ميتان. أثناء عطلٍ الزرّ الممكن هو
  الموجود.
- **وسطر فوق السجل يقول ما يجري** دون قراءته: `▶ آخر عملية: ✅ عرض «عاجل» —
  14:22` و`⚠ 3 أخطاء في آخر ساعة` (بلون التنبيه حين تكون هناك أخطاء). السجل
  تيّارٌ يمرّ؛ وهذا خلاصةٌ تُلمح.
- **ورقم إصدار المدير يتبع الجسر تلقائيًا** — `Build-BridgeManager.ps1` يقرأ
  8.5.0 ويختم **v8**؛ لم يكن يحتاج تغييرًا، كان يحتاج أن يُبنى بعد وصول الجسر
  إلى الثامنة.
- البناء نظيف و**121 فحصًا ذاتيًّا** تمرّ، منها ثلاثة جديدة لقراءة سطر العملية.

## 8.5.0 — 2026-09-08

الدليل يلحق بما تعلّمه الجسر، ومفاتيح المدير تقول أيّها يحكم الهواء.

### 📘 الدليل

- **فصلٌ جديد: `🔔 التنبيهات`** — ما يصلك فيه (النصّ والطبقة والمدة ومن
  نفّذ)، وأنه لا يصل صاحب العملية، وزر «أوقف تنبيهاتي» وأمر `تنبيهاتي`
  للعودة، والاحتجاز أثناء الكتابة، والاستثناء الوحيد الذي يصل فورًا: أن
  يُعرض أو يُرفع القالب الذي تجهّزه أنت.
- **`🔍 المتابعة`**: سجل العمليات ونوافذه الأربع والتنقّل يومًا بيوم، ومن أين
  يُفتح، وأنه يقرأ من سجلّ التدقيق لا من الذاكرة.
- **`✏️ التعديل`**: التعديل يبدأ من النصّ الحالي، والنسخ بضغطة، وتنبيه المهلة
  وزرّ «تمديد» — للحقول ولمسودة الشريط.
- **`⚙️ الإعدادات`**: القيم تُضبط بالضغط — أزرار للأرقام، وشبكة للساعات وأيام
  الأسبوع، وساعة لنافذة الصيانة، ومنتقٍ للقوائم، ومدًى للأرقام الحسّاسة.
- **`🚪 من يدخل البوت`**: الموافقة بضغطتين، وإبلاغ كل المشرفين بها، وشاشة
  الطلبات السابقة، وأن الإشعار يصل كل مشرف في القائمتين.

### 🖥 المدير

- **الستة مفاتيح صارت مجموعتين مسمّاتين**: «التشغيل» (إعادة التشغيل التلقائية،
  كشف التعليق، مع بدء ويندوز) و«العرض» (المسح اليومي، التفاف الأسطر، الوضع
  الليلي).
- **ثلاثةٌ منها تقرّر ما يحدث للقناة حين لا يراقب أحد، وثلاثةٌ تقرّر شكل نافذة**
  — ومتجاورةً بالنمط نفسه تُقرأ كأنها سواء، فيمتلئ نصف الصفّ الذي يمسحه
  المشغّل أثناء عطلٍ بتفضيلاتٍ عن التفاف الأسطر.
- البناء نظيف و**118 فحصًا ذاتيًّا** تمرّ.

## 8.4.0 — 2026-09-08

الإشعار ينتظر دوره، والتعارض على القالب صار يُقال لصاحبه.

### ⏸ لا يقاطع من يكتب

- **موظفٌ يُدخل خبر الشريط الثالث لا يفقد مكانه** حين تصل رسالة — الحالة محفوظة
  في الخادم لا في آخر رسالة — **لكن السؤال الذي كان يقرأه ينزلق خارج الشاشة**،
  وعلى الهاتف هذا هو الشيء نفسه. ومن يُدخل صفّ موجز أو نصّ بنر أو عنوان عاجل
  يفعل بالضبط ما يتحدّث عنه الإشعار؛ ومقاطعته ليوصف له أسوأ لحظة ممكنة.
- **فصار الإشعار يُحتجَز لمن هو داخل عملية إدخال**، ويصله **مجموعًا في رسالة
  واحدة** لحظة انتهائها: «📣 حدث أثناء انشغالك (3)». محجوزٌ لا مُلغى — ما خرج
  على الهواء يبقى جديرًا بأن يُعرف حين يرفع رأسه.
- **ومحدودٌ بخمسة**: عمليةٌ طويلة في ساعة مزدحمة يجب ألّا تنتهي بجدارٍ من
  الرسائل، وهو نوعٌ آخر من المقاطعة.
- **ومكان التفريغ `Clear-PendingState`** — النقطة التي تمرّ منها كل نهايات
  العمليات، لا اثنتا عشرة نقطة تفترق.

### ⚠️ والتعارض على القالب

- **قفل الطبقة يمنع شخصين من بدء عمليةٍ على الطبقة نفسها** — وهو موجود منذ
  زمن — **لكن لا شيء كان يمنع قالبًا مجدولًا أو إعدادًا مسبقًا أو مشغّلًا على
  طبقةٍ أخرى من نشر القالب نفسه الذي يكتب فيه أحدهم**. وأوّل ما كان يعلمه: نصّه
  لا يظهر، أو الأسوأ، يحلّ محلّ ما كان صحيحًا.
- **الآن يُنبَّه فورًا**: «⚠️ Urgent عُرض على الهواء بواسطة فلان أثناء تجهيزك —
  راجع ما أعددته قبل الإرسال». يشمل من يجهّز **القالب نفسه أو الطبقة نفسها**.
- **ويصل حتى لمن أوقف تنبيهاته**: الكتم يعني «لا تخبرني بما تفعله القناة»، لا
  «لا تخبرني أن عملي تجاوزه غيره».

## 8.3.0 — 2026-09-08

الرفع عن الهواء خبرٌ كالعرض، وثلاثة تحسينات في قسم الإشعارات.

### ⚫️ الرفع عن الهواء

- **يُعلَن بالقاعدة نفسها** (`TemplateNotifyRules`): قاعدةٌ واحدة للقالب تغطّي
  ظهوره واختفاءه، لا إعدادين يفترقان.
- **ومعه كم بقي**: «⏱ بقي 4 دقائق». وهو السؤال الذي يثيره الرفع — شريطٌ بقي
  أربع دقائق قُرئ، وآخر بقي أربع ثوانٍ خطأٌ صحّحه أحدهم بالفعل.
- **ويُرسل حتى في الإخفاء الصامت**: مؤقّتٌ تلقائي يرفع الشريط هو بالضبط
  التغيّر الذي لم يره أحد في الغرفة.

### 📡 ثلاثة وجدتها في قسم الإشعارات

**١ — دفعةٌ من الضغطات كانت دفعةً من الرسائل.** مشغّل يصحّح عنوانًا يعرض القالب
ثلاث مرات في عشرين ثانية؛ الغرفة ترى تغيّرًا واحدًا، وثلاث مقاطعات متطابقة هي
الطريقة التي يصير بها التنبيه شيئًا يُطفأ. صار الإشعار **مرّة كل نصف دقيقة**
لكل قالب وطبقة وفعل — ونصف الدقيقة قصيرة عمدًا: عرضٌ ثانٍ بعد دقائق خبرٌ من
جديد.

**٢ — لا مخرج لمن لا يريد.** كانت الجهة قرارًا جماعيًّا (`admins` أو `all`) بلا
جواب فردي. صار **على كل إشعار زرّ `🔕 أوقف تنبيهاتي`**، وقراره محفوظٌ في ملفه
يبقى بعد إعادة التشغيل. **وبوتٌ بلا مخرج بوتٌ يُكتم من نظام التشغيل** — وعندها
يضيع التنبيه الذي كان يهمّ أيضًا.

**٣ — ومن كتم ثم أغلق الرسالة كان يفقد الطريق.** أمر **`تنبيهاتي`** يقول حالتك
ويعطي الزرّ المقابل.

## 8.2.0 — 2026-09-08

الإعدادات كفّت عن طلب الكتابة. فحصٌ للـ174 إعدادًا كلّها، ثم خمسة إصلاحات.

**ما وجده الفحص:** 58 إعدادًا منطقيًّا بأزرار تبديل (سليم)، و7 قوائم خيارات،
و4 منتقيات — **و90 رقمًا كلّها تُكتب**، و14 نصًّا حرًّا نصفها لا يحتاج نصًّا.

### 🔢 الأرقام بالضغط (90 إعدادًا)

- شاشة واحدة: `➖ الخطوة` · القيمة · `➕ الخطوة`، ثم `الافتراضي` و`الأدنى`
  و`الأعلى`، وزرّ `⌨️ اكتب رقمًا` لمن يعرف الرقم بالضبط.
- **والخطوة من حجم الرقم نفسه** لا من جدول: مهلةٌ بالثواني تتحرّك بواحدة،
  واحتفاظٌ بخمسة آلاف سطر لا يحتمل خمسين ضغطة ليبلغ 5500.
- **والحدّ عند السقف يُقصّ لا يُرفض**: من يضغط ➕ عند الأعلى يقصد «إلى أقصاه»،
  ورسالةُ خطأٍ ردًّا على زرٍّ ما كان يجب أن يُعرض خطأُ الشاشة لا خطؤه.

### 🛡 ولكل رقم حسّاس مدًى

- **البنية تحمل `Minimum`/`Maximum` منذ كُتبت، ولم يعلنها إعدادٌ واحد.** أي أن
  `CinegyStateCheckSeconds = 0` كان مقبولًا — جسرٌ يسأل محرّك الهواء بلا توقّف —
  ومهلةٌ سالبة مقبولة، وعددُ محاولاتٍ بعشرة آلاف مقبول.
- **عشرون إعدادًا تُعلن مداها الآن**، وهي التي يُحسّ خطؤها على الهواء أو على
  المحرّك. **والحارس في `Set-Setting`** لا على الشاشة: الاستيراد والاستعادة
  وشاشتان كلّها تمرّ من هناك، وقاعدةٌ تُفرض عند بابٍ واحد قاعدةٌ لها ثلاثة
  مسالك.

### 🕑 والمدى الصغير يُعرض كاملًا

- **ساعةُ اليوم أربعٌ وعشرون إجابة، ويوم الأسبوع سبع** — والوصول إليها ضغطةً
  ضغطةً حسابٌ في موضع اختيار. صارت شبكة، والقيمة الحالية معلَّمة، واليوم باسمه
  لا برقمه.
- **ونافذة الصيانة تُختار من ساعة**: كانت نصًّا حرًّا، و«8:00» و«٨:٠٠» و«20.00»
  والفراغ كلّها تبدو إجابةً بينما النافذة لا تفعل شيئًا. ضغطتان: الساعة ثم
  الربع، وزرّ `🚫 بلا توقيت` لأن إلغاءها إجابةٌ أيضًا ولم يكن لها زرّ.

### 📋 والقوائم من منتقٍ

- **`DisabledTemplateKeys` و`SensitiveTemplateKeys` و`ReservedLayers`** كانت
  تُكتب، **والمنتقي موجودٌ أصلًا لأربعة إعدادات مثلها**. وخطأُ هجاءٍ في قائمة
  صلاحيات يُقرأ «ليس في القائمة» — فالصلاحية لا تحمي شيئًا بصمت، ولا يُكتشف ذلك
  إلا حين يستخدم أحدهم ما لم يكن ينبغي.
- **و`AutoHidePresetSeconds`** صارت منتقي مدد تُقرأ كمدد لا كأرقام.
- **و`NewsItemSeparator`** خمسة خيارات: مسافةٌ تُكتب بعد الفاصل كانت تصير جزءًا
  منه، فيكسب كل خبرٍ على الشريط فراغًا لا يُرى في الإعداد.

## 8.1.0 — 2026-09-08

إعداد الإشعار يُضبط بالضغط، لا بالكتابة.

- **«Urgent=all, Banner=admins» صيغةٌ تُحفظ، واسمُ قالبٍ يُهجّى بدقّة، وكلمةُ
  جهةٍ بلغةٍ لا تستخدمها الشاشة** — ثلاث طرقٍ للخطأ الصامت في إعدادٍ غايته
  كلّها أن يسمع أحد.
- **الآن شاشة**: كل قالب في صفّ، ومعه ما سيفعله عند العرض. اضغط عليه فيتنقّل:
  🔕 لا أحد ← 👮 المشرفون ← 📢 الجميع. لا هجاء، ولا صيغة، وكل الأجوبة ظاهرة
  دفعةً واحدة.
- **مُصفَّحة كبقية المنتقيات** ومعنونة بالموضع: النداء يحمل 64 بايتًا، واسم
  القالب لا يتّسع فيها دائمًا. وتُعاد رسمها في مكانها مع كل ضغطة فلا تتكاثر
  نسخُها في المحادثة.
- **وما يعني الصمت لا يُخزَّن**: `none` هي الحال الافتراضية، فلا تُكتب في
  الإعداد. **وقاعدةٌ لقالبٍ لم يعد موجودًا تبقى**: قالبٌ أُعيدت تسميته صباحًا
  واستُعيد ظهرًا لا يجوز أن يفقد قرار غرفة الأخبار بينهما.
- **والإعداد نفسه لم يتغيّر** — نصّه كما هو في `config.json` وفي النسخ
  الاحتياطية: الشاشة تكتبه، ولا تستبدله بمخزنٍ ثانٍ يجب أن يبقى متوافقًا معه.

## 8.0.0 — 2026-09-08

القالب يُعلن عن نفسه على الهواء، ولا تنتهي مهلةٌ دون سابق إنذار.

### 🔴 إشعار العرض، قالبًا بقالب

- **`TemplateNotifyRules` إعدادٌ واحد يكتبه المشرف من هاتفه**: «Urgent=all,
  Banner=admins». **والعاجل مثال لا قاعدة** — «عاجل» عند محطة هو «Breaking»
  عند أخرى، وأيّ الغرافيك يستحقّ مقاطعة غرفة الأخبار قرارُ غرفةِ أخبار لا
  استنتاجُ برنامج.
- **الجهة لكل قالب**: `none` لا أحد · `admins` المشرفون · `all` كل المصرّح لهم.
  واسمٌ بلا جهة يعني `all`: من كلّف نفسه ذكر قالبٍ يريد أن يُبلَّغ أحد،
  والقراءة الآمنة لقاعدة ناقصة هي التي تُخبر لا التي تُسكِت.
- **والرسالة تحمل ما يُسأل عنه فعلًا**: النصّ الذي سيُقرأ على التلفزيون داخل
  `<code>`، والطبقة، **والمدة** — «يُخفى تلقائيًا بعد 30 ثانية» أو «يبقى حتى
  يُخفى يدويًا» — ومن نشره.
- **ولا تصل صاحبَها**: هو ينظر إلى تأكيده، ورسالةٌ ثانية تخبره بما فعله للتوّ
  هي الضجيج الذي يُكتم البوت بسببه — والبوت المكتوم يخسر الرسالة التي كانت
  تهمّ. **وما لم يُذكر قالبه لا يُرسل عنه شيء**: المحطة تعرض عشرات الغرافيك
  يوميًا.

### ⏳ لا انتهاء بلا إنذار

- **ميزة التمديد تشمل كل أنماط الإدخال** — الموجز والشريط والبنر والجدولة
  والقوالب — لأن حارس المهلة محايدٌ تجاه النمط.
- **لكن مسودة الشريط كانت خارجه**: مهلتها منفصلة (`NewsDraftTimeoutMinutes`)
  **وتمحو الشريط كلّه**، لا حقلًا واحدًا نصف مكتوب — والمحرّر لا يعلم إلا من
  الرسالة التي تعدّ له ما خسره.
- **صار لها تنبيهٌ قبل خمس دقائق** بعدد أخبارها، ومعه زرّ **«⏳ تمديد»** يعيد
  المهلة من الآن، وزرّ لفتحها. والانتهاء يبقى — المسودة المهجورة تحجز الشريط —
  لكنه صار قرارًا لا مفاجأة.

## 7.99.0 — 2026-09-08

السجل يقول أي فترة يغطّيها، ويُقرأ يومًا بيوم، وطلبات الوصول صار لها تاريخ.

### 📜 الفترة التي يغطّيها السجل

- **`📜 السجل` كان يكتب `HH:mm:ss` فقط** بينما يستعيد خمسين سطرًا من
  `audit.jsonl` قد تمتدّ أيامًا. فـ«07:22» تعني إحدى أربع صباحات، ولا سبيل
  للقارئ إلى معرفة أيّها.
- **اليوم يبقى بالوقت وحده** — وهو الحالة الشائعة، والتاريخ في كل سطر ضجيج —
  **وما قبله يحمل تاريخه** (`09-07 21:40:03`).
- **وفوق الأسطر سطران**: الفترة المعروضة فعلًا، وعدد ما عُرض من عدد ما هو
  محفوظ. الشاشة تعرض عددًا ثابتًا من الأسطر لا مدةً زمنية، فكم تبلغ من الماضي
  يتوقّف كليًّا على انشغال المحطة.

### 📆 يومًا بيوم

- **«ماذا خرج يوم الثلاثاء» سؤال لا تجيبه نافذةٌ تنتهي الآن**: بحلول الخميس
  يكون الثلاثاء قد سقط من كل نافذة يمكن قراءتها.
- **`📆 يوم أمس` من شاشة الساعات**، ومنها سهمان: `◀` إلى اليوم السابق
  و`▶` إلى التالي — **ولا سهم بعد اليوم**، فما بعد الآن لا شيء فيه، وزرٌّ
  جوابه «لا توجد عمليات» بالتصميم زرٌّ يعلّم الناس ألّا يثقوا بالشاشة.
- **واليوم يُسمّى كما يقوله الناس**: «أمس · 2026-09-07»، والتاريخ بجانبه فلا
  تكذب شاشةٌ بقيت مفتوحة حتى منتصف الليل.
- **ومنافذ الوصول**: زرّ في `📜 السجل` وآخر في `📊 التقارير` —
  غريزة المشرف أن يبحث هناك، وكانت الشاشة لا تُفتح إلا من `🧾 عملياتي`.

### 👤 طلبات الوصول صار لها تاريخ

- **القرارات كانت تُسجَّل جملةً عربية داخل رسالة واحدة**، وحقولها المنظّمة
  فارغة — فلا شاشة تُبنى عليها إلا بتحليل نصّ. صارت **بيانات**: الطلب والموافقة
  والرفض، ثلاثتها بحقول (`event=access`).
- **وزرّ `📜 الطلبات السابقة`** في شاشة الطلبات المعلّقة: من طلب، وما
  الحالة (مقبول / مرفوض / بانتظار القرار)، ومتى صدر القرار، **وبواسطة من**،
  وكم انتظر الطالب.
- **مبنيّة من مصدرين لأن أيًّا منهما وحده ناقص**: ملف التدقيق يحمل الرفض — وهو
  لا يترك أثرًا في أي مكان آخر — وسجل المستخدمين يحمل الموافقات الأقدم من هذا
  الإصدار ويعيش أطول من تدوير السجلات. والشاشة **تقول ذلك صراحة** بدل أن
  تُقرأ كأنها القصة كاملة.

## 7.98.0 — 2026-09-08

سبب المشرف الصامت: الترقية نفسها.

- **`Set-AdminRole` كانت تضيف إلى `AdminUserIds` فقط**، وتُبقي `AdminChatIds`
  على ما كانت عليه. فكل من رُقّي من داخل البوت صار **مشرفًا بلا إشعارات**: يملك
  الموافقة على منح الوصول إلى الهواء، ولا يصله طلبٌ قطّ. لم يكن الأمر إعدادًا
  أُسيء ضبطه — كان مسار الترقية نفسه.
- **صارت تملأ القائمتين.** إحداهما «من يقرّر» والأخرى «من يُبلَّغ أن هناك ما
  يُقرَّر»، وتجميد الثانية يصنع الأولى عمياء.
- **والحذر الأصلي باقٍ**: معرّف المجموعة سالب، فلا ترقية تحوّل مجموعةً إلى
  جمهورٍ لإشعارات المشرفين. المضاف دائمًا معرّف محادثة خاصة، وهو رقم المستخدم
  نفسه.
- **وإصلاحٌ لمرة واحدة عند الإقلاع** (`Repair-AdminChatIds`): من رُقّي بنسخة
  أقدم يبقى حيث هو مهما أُصلح المسار — وهو لا يستطيع اكتشاف ذلك بنفسه، لأن ما
  ينقصه رسائل لم تُرسل أصلًا. يضيف فقط، ويكتب في السجل أسماء من أضافهم بدل أن
  يغيّر ملف الإعداد صامتًا.

## 7.97.0 — 2026-09-08

حادثتان من الهواء: طلبُ وصولٍ لم يبلغ صاحبه، وكتابةٌ أُلغيت في منتصفها.

### 📢 مشرفٌ لا يصله شيء

- **قائمتان يُفترض أن تتّفقا، ولا شيء يتحقّق:** `AdminUserIds` تحمل ثلاثة،
  و`AdminChatIds` اثنين. فالثالث يملك الموافقة على منح الوصول إلى الهواء **ولا
  يُبلَّغ بطلبٍ قطّ** — ولا بتنبيه Cinegy ولا بالموجز. وصل الطلب إلى اثنين،
  ووافق أحدهما خلال دقيقة، وعلم الثالث من زميل.
- **`Send-AdminBroadcast` صار يراسل القائمتين** (`Get-AdminNotifyIds`): في
  المحادثات الخاصة رقم المحادثة هو رقم المستخدم نفسه. ومن يملك القرار يجب أن
  يُبلَّغ به. وطُبِّق على تنبيه سواد المخرج وإشعار الشيت أيضًا.
- **و«فحص النظام» صار يسمّي من لا يصله شيء** — العطل صامتٌ بطبعه: الغائب لا
  يلاحظ رسالةً لم تصل قطّ.

### 🔑 منح الوصول صار يُسأل عنه

- **كل فعلٍ خطر في الجسر يؤكَّد** — الإخفاء، إعادة التشغيل، مسح السجل — **إلا
  الذي يسلّم غريبًا مفاتيح الهواء.** ومشرفٌ هنا أبلغ عن موافقةٍ لا يذكر أنه
  منحها، وضغطةٌ عابرة على رسالةٍ قابعة في محادثة هي بالضبط ما يفعل ذلك ولا
  يترك أثرًا يذكّره.
- **ضغطتان الآن**: الأولى تسأل باسم الطالب، والثانية زرٌّ أحمر مكتوب عليه «نعم،
  امنح الوصول» — لا «نعم» مبهمة تُقرأ بعدها على غير معناها.
- **وكل موافقة تُبلَّغ لكل المشرفين**: «مُنح الوصول: فلان — بواسطة فلان». منحُ
  سيطرةٍ على الهواء لا يجوز أن يراه صاحبه وحده.

### ⏳ الكتابة لم تعد تُلغى فجأة

- **محرّرٌ يكتب عنوانًا قُطع في منتصف جملته** بـ«انتهت مهلة الإدخال، ابدأ من
  جديد» — وكانت تلك أوّل مرة يعلم فيها بوجود مهلة أصلًا. لم يكن يتخلّى عن
  العملية؛ كان يُقاطَع ببقية عمله (تذكيران وصلاه في الدقائق نفسها).
- **تنبيهٌ قبل دقيقة، ومعه زرّ «⏳ تمديد»** يعيد المهلة من الآن. والانتهاء يبقى
  صحيحًا — العملية المهجورة تبتلع الرسالة التالية لصاحبها — لكنه لم يعد مفاجأة.
- **والمهلة المُوزَّعة صارت عشر دقائق** بدل خمس. خمسٌ أقصر من كتابة عنوانٍ
  والهاتف يرنّ. **التنصيب الذي ضبط قيمته بنفسه يحتفظ بها** — غيّرها من
  `⚙️ الإعدادات` إن أردت.

## 7.96.0 — 2026-09-08

وأسبوع كامل، لأنه المدى الذي تُراجع به غرفة الأخبار.

- **`🗓 7 أيام` انضمّت إلى 24 و48 و72 ساعة.** «هل عُرض ذلك البنر يوم
  الثلاثاء» سؤالٌ أسبوعي، لا سؤال ثلاثة أيام.
- **ولا تكلّف شيئًا لا تدفعه النوافذ الأخرى**: الجدول مقيَّد بأربعين صفًّا مهما
  اتّسعت المدة، و`Get-ReportRecords` تقرأ العدد المحدود نفسه من الأسطر الذي
  تقرأه تقارير الشهر — وتقول صراحةً إن بلغته.
- **وتُسمّى «7 أيام» لا «168 ساعة»**: الرقم صحيح والتسمية عديمة النفع، فلا أحد
  يسأل عن مئة وثمانٍ وستين ساعة.
- **وأزرار النوافذ صارت اثنين في كل صفّ** كفترات التقارير: أربعة أزرار في صفّ
  واحد على الهاتف تترك كل تسمية أضيق من أن تُقرأ، وهو نقيض الغرض من تسميتها.

## 7.95.0 — 2026-09-08

سجل العمليات صار يمتدّ إلى 72 ساعة، ويُقرأ من الملف لا من الذاكرة.

### 📅 نافذة ساعات

- **`🧾 آخر عملياتك` كانت تعرض عشر عمليات ولا شيء أقدم.** ومصدرها الذاكرة:
  عشرون سجلًّا لكل شخص، تُنسى عند إعادة التشغيل. فمشرفٌ يسأل «ماذا خرج ليلة أمس»
  كان يُعطى قائمة هذا الصباح.
- **الشاشة الجديدة تقرأ `audit.jsonl`** عبر آلية التقارير نفسها — وهي السجل
  الدائم، يبقى بعد إعادة التشغيل ويعود إلى الأرشيفات المدوَّرة، وهو ما تحتاجه
  نافذة اثنتين وسبعين ساعة.
- **ثلاث نوافذ: 24 و48 و72 ساعة.** يومان هما سؤال تسليم الوردية، وثلاثة تغطّي
  عطلة نهاية الأسبوع — أوضح من «الأخيرة» بلا تحديد. والنافذة المقروءة مُعلَّمة
  على زرّها.
- **جدول**: الوقت (يوم وساعة، لأن «21:40» وحدها ملتبسة عبر ثلاثة أيام)،
  والعملية، والقالب والطبقة، والنتيجة. **وحكمٌ قبل الجدول**: «🟢 30 عملية،
  كلّها ناجحة» أو عدد الفشل والرفض — فلا تُعدّ الإخفاقات بقراءة أربعين صفًّا.
- **وللمشرف زرّ `👥 كل المشغّلين`.** والحارس في الأمر نفسه لا على الزرّ
  وحده: النداء قد يصل دون ضغطة، فمشغّلٌ يطلب الجميع يُعطى عملياته هو.
- **مقيَّدة بأربعين صفًّا** كبقية الجداول، وتقول ما أخفته، وتقول أيضًا إن كان
  السجل قد بلغ حدّ القراءة.

### 🐛 خطأ كشفه الاختبار قبل أن يراه أحد

- **`foreach ($hours in ...)` داخل دالةٍ معاملُها `-Hours`.** وأسماء المتغيّرات
  في PowerShell **لا تفرّق بين حالة الأحرف**، فالحلقة كانت تكتب فوق المعامل
  نفسه: كل النوافذ الثلاث ظهرت مُعلَّمة كأنها المقروءة، وزرّ «كل المشغّلين» حمل
  آخر نافذة دارت عليها الحلقة لا النافذة المعروضة.

## 7.94.0 — 2026-09-08

فحصُ استقرار: ثلاث شاشات كانت ستكرّر عطل 7.87، وانهيارٌ كامن، وكودٌ ميت.

### 📏 ثلاثة جداول بلا حدّ

قياسٌ فعليّ على المُسلسِل نفسه (الحدّ 12000 حرف):

| الصفوف | الحمولة | من الحدّ | النتيجة |
|---|---|---|---|
| 40 | 4088 | 34% | تُرسَل |
| 250 | 24399 | 203% | تُرفض — الشاشة تفقد جدولها |
| 700 | 68049 | **567%** | تُرفض |

- **`📑 تقرير الموجزات`**: صفٌّ لكل تشغيل في المدة. محطة تشغّل موجزًا كل
  ساعة تصنع ~700 صف في تقرير الشهر — **أسوأ من تقرير البنرات الذي أسقط جداول كل
  الشاشات في 7.87**. يومها قُيّد البنر وحده وبقي هذا.
- **`📑 الموجزات المحفوظة`**: صفٌّ لكل موجز، **ولا شيء يقلّم المكتبة** —
  الشاشة الوحيدة التي لا ينخفض عدد صفوفها أبدًا.
- **`✅ مراجعة النشر`**: استبدال الشريط كلّه = كل خبر قديم محذوف وكل جديد
  مضاف، أي صفّان لكل خبر؛ شريط من 40 خبرًا يصير 80 صفًّا. والإضافات أوّلًا لأنها
  ما سيظهر على الهواء.
- **والحدّ صار في مكان واحد** (`Select-RichTableRows`, `$script:RichTableMaxRows`):
  حدٌّ مكتوبٌ أربع مرات هو حدٌّ يُرفع في ثلاثة مواضع ويُنسى في الرابع. وكل جدول
  مقصوص **يقول ما أخفاه** بدل أن يُقرأ كأنه القصة كاملة.

### 🛠 انهيار كامن في تقرير الموجزات

- **`Get-MojazRunDuration` كان يقرأ `$Run.DurationMs` مباشرة.** وسجلّ التشغيل
  يحمل ما عرفته النسخةُ التي كتبته فقط، والمخزن يعيش أطول من النسخة — وتحت
  StrictMode الحقلُ الغائب ليس صفرًا بل **انهيار**، على شاشة التقارير، ولكل
  تشغيلٍ في المدة. صار يمرّ بـ`Get-JsonProp`. كشفه اختبارٌ بُني ببيانات واقعية
  ناقصة الحقول.

### 🗑 كودٌ ميت

- حُذفت خمس دوال لا يناديها شيء في المستودع كلّه، ولا اختبار:
  `Get-BridgeMediaDurationSeconds`، `Get-LayerDevice`،
  `Get-NewsTickerItemsKeyboard`، `Get-PinnedAnnouncementLine`،
  `Test-OnAirTemplateMatch` — 124 سطرًا.

### ✅ ما وُجد سليمًا

لا `catch {}` فارغ واحد في الكود كلّه؛ وحلقة التشغيل محصّنة بـ`try/catch` حول
الاستطلاع وحول كل تحديث وكل معالِج على حدة؛ وسجلّ عمليات المستخدم مقيَّد بعشرين.

## 7.93.1 — 2026-09-08

«مرة واحدة لكل شاشة» كانت تعني مرة لكل رقم.

- **تحذير الحجم الذي أُضيف أمس كان مفتاحه عنوان الشاشة كما هو مكتوب** — وثمانية
  عناوين تحمل عددًا أو مدةً بداخلها: «الأحداث القادمة (12)»، «تقرير الموجزات —
  هذا الأسبوع». فكان التحذير يتكرّر كلّما تغيّر الرقم، وجدول الشاشات المحذّر
  عنها يكسب مفتاحًا جديدًا مع كل عدد، ما دام الجسر يعمل.
- **صار المفتاح العنوانَ بلا أرقامه.** فالتحذير يُقال مرة، والجدول يبقى بحجم
  عدد الشاشات لا عدد الأعداد. وُجد بفحصٍ للاستقرار بعد ساعةٍ من إصداره.

## 7.93.0 — 2026-09-07

كل شاشة صارت تقيس حمولتها بنفسها، فلا يكتشف الحدَّ مشغّلٌ اختفى جدوله.

### 📐 قياس الحمولة

- **انقطاع 7.87 لم يُكتشف إلا بعد أن توقّف تقريرٌ عن الظهور جلسةً كاملة.** لا
  شيء كان يراقب الحجم حتى صار كبيرًا؛ والشاشة تعبر الحدّ **تدريجيًا** مع امتلاء
  يوم المحطة، فأوّل من يعلم كان المشغّل الذي اختفى جدوله.
- **فصار كل إرسالٍ يُقاس وهو يمرّ** (`ConvertTo-RichMessagePayload`)، ويُسجَّل
  تحذيرٌ **مرة واحدة لكل شاشة** حين تبلغ 70% من الحدّ — أي وهي لا تزال تعمل،
  وحين يبقى وقتٌ لتقييد صفوف جدولها.
- **و`📈 أرقام التشغيل` صار فيها سطر `أكبر شاشة أُرسلت`**: النسبة من
  الحدّ واسم الشاشة كما هو في عنوانها. سؤالٌ كان لا يُجاب إلا بعد وقوع العطل.

### 🛡 ثغرة وُجدت أثناء المراجعة

- **`editMessageText` كان يبني حمولة ثرية بلا أي فحصٍ للحجم** — بينما
  `sendRichMessage` يفحصها منذ 7.87. وحمولةٌ كبيرة هنا تعني 400، و400 يضيّق أو
  يعطّل نوع كتلة لبقية الجلسة. **وشاشة إعادة الترتيب تُعاد رسمها مع كل ضغطة
  سهم**، فكانت ستدفع هذا الثمن مرارًا. صار لها الفحص نفسه، وصار الفشل يحمل طول
  الحمولة فلا يُلام نوعٌ على مشكلة حجم.

### 🧪 الاختبارات كانت تخرج إلى الشبكة

- **خمسة عشر اختبارًا كانت تطلب `api.telegram.org` فعليًا** وتنتظر ردّ 401
  لكلٍّ منها — ثانية تقريبًا لكل واحد في كل تشغيل للبوّابة. السبب أن الشاشات
  صارت تجرّب `sendRichMessage` قبل نصّها، فأي اختبار يقود شاشة حقيقية ويموّه
  `Send-TelegramMessage` وحده كان مساره الثريّ يتجاوز التمويه. والجواب لم يكن
  مفيدًا أصلًا: الرمز في `config.example.json` عيّنة.
- **مُوّه عند الحدّ الحقيقي** — `Invoke-RestMethod` داخل `BridgeTelegram` — لا
  عند الغلاف: هكذا يبقى كل ما فوقه يعمل ويُقاس، بما فيه إعادة المحاولة والمهلة
  اللتان تموّهان الأمر نفسه في اختبارهما فيغلب تمويههما هذا.
- **ولا تختبر مجموعةُ اختبارات ما تعتمد فيه على الشبكة.**

## 7.92.1 — 2026-09-07

الزرّ نفسه لا يمكن أن يتغيّر بعد الضغط، فتغيّر قبله.

### 🔵 لون الزرّ

- **السؤال كان: هل يتبدّل نصّ الزرّ أو لونه ليدلّ على أن النسخ تمّ؟** والجواب
  بعد مراجعة سجلّ تغييرات Bot API حتى **10.3** (24 أغسطس 2026، وهو الأحدث):
  **لا.** ضغط `copy_text` لا يرسل أي تحديث إلى البوت، فلا يصل شيءٌ يُبنى عليه
  تعديل. ولا يوجد حقلٌ يجعل الزرّ يبدّل شكله بنفسه بعد الضغط. اللون والنصّ
  يُختاران عند إرسال الرسالة أو لا يُختاران أصلًا.
- **فصُرف اللون على ما *هو* الزرّ لا على ما فَعَل.** الأزرق في الجسر هو لون
  الفعل الذي تعرضه الشاشة، وزرّ النسخ كان آخر زرٍّ في هذه الشاشات بلا لون —
  فبقي الزرّ الوحيد الذي يسلك سلوكًا مختلفًا عن الجميع يبدو مثلهم تمامًا.
- **والقيد قديم لا جديد**: `style` أُضيف في 9.4، والعملاء الأقدم يتجاهلونه،
  و`EnableButtonStyles` تُسقطه من السلك حين تُطفأ — فالتسمية تبقى هي التي
  تحمل المعنى.

### 📌 تصحيح

- **`copy_text` من Bot API 7.11 (31 أكتوبر 2024)، لا 8.0.** كُتب «8.0» في
  إصدار 7.91.0 وتكرّر في 7.92.0 — بينما كان الرقم صحيحًا في 6.9.6 حين أُضيف
  الزرّ أول مرة. صُحّح في الكود وفي هذا السجلّ وفي README.

## 7.92.0 — 2026-09-07

زرّ النسخ صار يقول ما سيفعله، لأن أحدًا لا يستطيع أن يقول ذلك بعد الضغط.

### 📋 تنويه النسخ

- **الطلب كان: بيّن للمستخدم أن النسخ تمّ.** والصريح أولًا: **لا يستطيع البوت
  ذلك.** زرّ `copy_text` (Bot API 7.11) ينفّذه تيليجرام على الجهاز نفسه ولا يرسل
  أي `callback` إلى الجسر — فالجسر لا يعلم أصلًا أن الزرّ ضُغط، ولا يملك ما
  يردّ به بعده. تيليجرام وحده يُظهر تأكيدًا قصيرًا على الشاشة.
- **فالتنويه كُتب قبل الضغطة بدل أن يُنتظر بعدها.** سطرٌ يسمّي الزرّ كما هو
  مكتوب، ويقول أين يذهب ما نُسخ: يجعل تأكيد تيليجرام **متوقّعًا** لا مفاجئًا،
  ويحوّل زرًّا كان يبدو بلا أثر إلى فعلٍ مفهوم.
- **وصياغته في مكان واحد** (`Get-CopyButtonNotice`): شاشةٌ تعد بشيءٍ بصياغة
  مختلفة هي شاشة يكفّ المشغّل عن تصديقها.

### 🔗 الشاشتان اللتان فيهما الزرّ

- **شاشة تعديل النص** (خبر الشريط، وصفّ الموجز): «الصقه في صندوق الرسالة ثم
  عدّله» — وهي الوجهة التي وُجدت الشاشة من أجلها.
- **شاشة `🧾 آخر عملياتك`**: «أرسله للمشرف مع وصف ما حدث» — لأن المرجع
  إنما يُنسخ ليُبلَّغ عنه.
- **ومن يقرّر وجود الزرّ صار دالةً واحدة** (`Get-MyOperationsCopyReference`):
  نصّ الرسالة يُبنى في ملف وأزرارها في آخر، ونسختان من هذا القرار كانتا ستفترقان
  حتى تَعِد الشاشة بزرٍّ لا تعرضه.

## 7.91.0 — 2026-09-07

تعديل النص يبدأ من النص، وما سيظهر على الشاشة صار مميّزًا.

### ✏️ النسخ للتعديل

- **شاشة تعديل خبر الشريط كانت تقول «أرسل النص البديل للخبر:» ولا تعرض شيئًا.**
  محرّرٌ يصحّح كلمة واحدة كان يعيد كتابة العنوان كلّه، من ذاكرته أو بالرجوع
  إلى شاشة سابقة.
- **ولا يستطيع أي بوت أن يملأ صندوق إدخال المستخدم** — تيليجرام لا يتيح ذلك.
  فأقرب شيء هو نسخةٌ بضغطة، وهي معطاة مرتين:
  - **النص الحالي داخل `<code>`** — وتيليجرام يجعل ذلك ينسَخ بضغطة واحدة.
  - **وزر `📋 نسخ النص الحالي`** (`copy_text`، Bot API 7.11) لمن لا يعرف ذلك.
    وهو الزرّ الوحيد في الجسر بلا `callback_data`: النسخ يجري على الجهاز
    والجسر لا يسمع بالضغطة أصلًا.
- **والنص يُعرض كاملًا مهما طال.** «حالي» مقتطعٌ أسوأ من لا شيء: من ينسخه يفقد
  ذيله دون أن يُقال له، وهو الخطأ الذي وُجدت هذه الشاشة لمنعه.
- **وطُبِّق على صفّ الموجز أيضًا** — كان يعرض «الحالي: …» نثرًا في آخر سطر،
  مقروءًا لكن لا يُنسخ على الهاتف إلا بضغطةٍ طويلة دقيقة.

### 📺 تمييز ما سيظهر على الشاشة

- **شاشة المراجعة قبل الإرسال** — وهي التي يقرّر فيها المشغّل وضع الغرافيك على
  الهواء — كانت تعرض النصّ بلا تمييزٍ عن أسماء الحقول التي تصفه.
- **وتيليجرام لا يمنح البوت تلوينًا للنص.** أقوى تمييزٍ يملكه هو الخط الثابت،
  فصارت القاعدة واحدة: **كل ما سيظهر على التلفزيون داخل `<code>`، ولا شيء
  غيره.** وهو تمييزٌ ينسخ بضغطة أيضًا.
- **وحقلٌ متروك لا يُلبَس لباس النصّ**: يقول «(متروك)» مائلًا بدل أن يجلس في
  خانةٍ توحي بأنه سيظهر.

## 7.90.0 — 2026-09-07

المدد المنقضية تُقرأ بأشهرها وأسابيعها، لا بدقائقها.

- **`👥 نشاط المستخدمين` كان يقول «منذ 4320 د».** حسابٌ يقسمه القارئ مرتين قبل أن
  يعني شيئًا — على الشاشة نفسها التي يقرّر فيها المشرف إن كان أحدهم ما زال
  يحتاج الوصول. صار يمرّ بـ`Format-DurationMinutes`.
- **و`Format-DurationMinutes` صار يسمّي الأشهر والأسابيع**: أسبوعان كانا يُقرآن
  «14 يومًا»، وهو ليس ما يقوله إنسان.
  - **والشهر ثلاثون يومًا والأسبوع سبعة، ولا يدّعيان الدقة**: هذا جواب «منذ متى»
    لا تقويم، وشهرٌ دقيق كان سيجعل المدة الواحدة تُقرأ مختلفةً بحسب الشهر الذي
    وقعت فيه.
  - **ووحدتان لا أكثر**: «شهر و12 يومًا و7 ساعات و20 دقيقة» دقيقٌ ولا يقوله أحد،
    ومن يقرّر إن كان حسابٌ خاملًا لا يعنيه فرقُ الدقائق.
- **وفحصتُ المواضع الأخرى**: أكثر ما يذكر «دقيقة» في الجسر **إعداداتٌ ضبطها
  المشرف** («كل 60 دقيقة»، «تنبيه بعد 30 دقيقة») — وهي صحيحة كما هي. الحالات
  المنقضية غير المحدودة كانت أربعًا: نشاط المستخدمين، وثلاثة في تقرير البنرات
  حيث بنرٌ بقي المساء كلّه كان يُقرأ «300 د».
- **واختبارات تقرير البنرات صارت تُحمّل `Bridge.Core.ps1`** لتقيس ما تطبعه
  الشيفرة فعلًا: مُهيّئٌ بديل كان سيكون له رأيٌ خاصٌّ في كيف تُقرأ «300 دقيقة».

## 7.89.0 — 2026-09-07

القوائم الأربع الباقية صارت جداول.

- **📅 الجدولة القادمة** — القالب والموعد والتكرار. والنسخة النصّية تحتفظ بكيان
  `tg-time` الذي يُري كل قارئ الموعد بتوقيته هو؛ خانةُ جدولٍ لا تحمل كيانًا،
  فيُكتب توقيت المحطة صراحةً — وهو التوقيت الذي تُكتب به جداول البثّ أصلًا.
- **👤 طلبات الوصول المعلّقة** — المعرّف والاسم ومنذ متى. من يمنح القدرة على وضع
  غرافيك على الهواء يقارن هذه الثلاثة بين الطلبات، وهذه مقارنةُ جدول لا فقرةٍ
  لكل طلب.
- **📑 مكتبة الموجزات** — الصفوف والمراجعة والحالة: أربع حقائق كان النصّ يضمّها
  في سطرٍ ثانٍ تحت كل اسم.
- **👤 من استخدم قالبًا** — متى ومن والعملية والنتيجة.
- **والقائمة الفارغة تقول ذلك** بدل رسم جدولٍ بترويسةٍ فوق لا شيء. و«من استخدم»
  بلا نصِّ بحث لا يعيد كتلًا إطلاقًا، فيسقط المستدعي إلى النصّ الذي يحمل
  الإرشاد.
- **وسجلٌّ عارٍ لا يُسقط شاشة الطلبات**: تُكتب هذه السجلات في أكثر من موضع،
  ومفتاحٌ غائب تحت StrictMode كان سيُسقط الشاشة التي يفتحها المشرف ليجيب شخصًا
  ينتظر.

## 7.88.1 — 2026-09-07

`📊 ملخص الاستخدام` صار جدولين — وتصحيحٌ لما ادّعيتُه في 7.88.0.

- **قلتُ في 7.88.0 «لم تبقَ شاشة بلا جدول»، وكان ذلك غير دقيق.** بقيت شاشات بلا
  بانيَ كتل، وهذه إحداها. أكثر البقيّة مطالباتٌ وتأكيدات وقيمٌ مفردة، والجدول
  ليس شكلها الصحيح — لكن بينها قوائم تستحقّه: الجدولة القادمة، وطلبات الوصول،
  ومكتبة الموجزات، ونسخ الإعدادات، وسجلّ استخدام قالب.
- **وجدولان لا واحد**، لأن الشاشة تجيب عن سؤالين: أي القوالب يحمل العمل، وكيف
  انتهت عمليات الوردية. جدولٌ واحد كان سيحتاج عمودًا يعني شيئًا في نصف صفوفه
  وشيئًا آخر في النصف الآخر.
  - الترتيب: `# · القالب · مرات · آخر مرة` — والقالب الذي لم يُستخدم بعدُ يأخذ
    شَرطة لا خانةً فارغة.
  - النتائج: `✅ ناجحة · ❌ فاشلة · ⛔ مرفوضة`.
  - وثالثٌ لأسباب التراجع، يظهر فقط إن سُجِّل سبب.
- **والملخص الأسبوعي المجدول يبقى نصًّا** عمدًا: يخرج عبر `Send-AdminBroadcast`
  التي تحتجز الإشعار غير العاجل لملخّص ساعات الهدوء ولا تحفظ إلا النص — فجدولٌ
  فيها يصل بعد ساعات لا شيءَ فيه.

## 7.88.0 — 2026-09-07

لم تبقَ شاشة بلا جدول.

ثلاث شاشات لم يكن لها مسارٌ غنيٌّ **إطلاقًا** — كانت نصًّا مهما نُسّق نصُّها، ولهذا
بقيت مختلفة عن البقيّة مهما رُتِّبت سطورها:

- **📋 شاشة القائمة** — وهي التي يهبط عليها المشغّل أكثر من أي شاشة. ما على
  الهواء صفوفُ أعمدة بطبيعته: **الطبقة · القالب · منذ · المشغّل** — أربع حقائق
  كان النصّ يضطر إلى ضمّها في سطرين لكل طبقة.
  - والحكم العام صار **عنوان** الرسالة، لأنه ما يجب أن يصل قبل أن يُقرأ شيء.
  - **ولا سقف هنا**: النسخة النصّية تقف عند أربع طبقات لأن كلًّا منها يكلّفها
    سطرين، أما الجدول فيُقرأ نزولًا — والسقف كان سيُخفي غرافيكًا حيًّا بلا مقابل.
- **📈 أرقام التشغيل** — قائمةُ بندٍ وقيمة هي جدولٌ من عمودين بطبيعتها.
- **🗂 صحة ملفات التشغيل** — الشاشة الأخرى للسؤال نفسه كان لها جدول منذ 10.1
  وهذه لم يكن لها، فبدا نصفا السؤال الواحد منتَجَين مختلفَين. والملفات المعطوبة
  أولًا، كما يفعل مركز الصحة.
- **وقرار «الجدول ثم النص» صار في موضع واحد** لشاشة القائمة (`Show-MainMenuScreen`)
  بدل ثلاثة مواضع تتخذه كلٌّ على حدة.

## 7.87.1 — 2026-09-07

صفُّ أزرارٍ واحد كان يُسقط تقارير الموجز والأخبار والبنرات.

- **تيليجرام قال السبب بنفسه**، بفضل ما أُضيف في 7.86.2:

  ```
  Bad Request: expected an Array of InlineKeyboardButton
  ```

  ليست مسألة HTML إطلاقًا — بل **لوحة الأزرار**. ولأن الرسالة الغنيّة وبديلها
  النصّي يحملان اللوحة نفسها، كان المساران يفشلان معًا.
- **والسبب حرفٌ واحد من PowerShell**: صفُّ «⬇️ تحميل الملف» كان مكتوبًا
  `$(if (...) { , @($زر) })` — و`$( )` يفكّ الغلاف الذي بنته الفاصلة للتوّ، فيصل
  الصفُّ **زرًّا عاريًا** لا مصفوفةً من زرٍّ واحد:

  ```
  ..., {"callback_data":"repdl:mojaz:today", ...}, ...
          ↑ والمطلوب  [ { ... } ]
  ```
- **و`تقرير العمل` هو النوع الوحيد بلا صفِّ تحميل** — ولذلك كان الوحيد الذي يعمل،
  وهو ما لاحظتَه بالضبط.
- **صارت الصفوف تُضاف واحدًا واحدًا** بدل سردها داخل `@( )` واحدة، فلا موضع تفكّ
  فيه الفاصلةُ نفسَها.
- **وفحصٌ يمسح كل لوحات الجسر** التي تُبنى بلا وسائط ويتحقّق أن كل صفٍّ فيها
  مصفوفة. العطل حرفٌ واحد ويمكن كتابته في أيٍّ منها.
  - وقد أمسك الفحصُ شكلًا لم أتوقّعه: لوحة الأزرار الدائمة تحمل `keyboard` لا
    `inline_keyboard`، وسؤالُ مفتاحٍ غير موجود تحت StrictMode يرمي استثناءً.

## 7.87.0 — 2026-09-07

تقريرٌ واحد ضخم كان يسلب كلَّ الشاشات جداولها حتى إعادة التشغيل.

هذا هو سبب «تدهور التصميم» — ولا علاقة له بتعديلات النصّ الأخيرة.

### السلسلة

1. **تقرير البنرات هو التقرير الافتراضي**، وحمولته ككتلٍ غنيّة **٤٤٬٩٣٧ محرفًا**
   لشهر، و٣٠٬١٠٥ لأسبوع. أكبر ما ثبت أنه يُعرض على هذا التركيب هو تقرير الأخبار
   عند ٤٬٠٩٥.
2. تيليجرام يرفضه بـ 400.
3. ولأنه قد يكون **أول رسالة غنيّة في الجلسة**، لا يجد منطقُ التضييق نوعًا «سبق
   أن نجح»، فيلوم الأنواع الأربعة كلها ويُعطّلها: `heading, table, paragraph,
   details`.
4. وهي الأنواع التي تبني **ℹ️ الحالة** و**📊 الحالة الكاملة** و**🩺 مركز الصحة**
   و**🕘 ماذا فاتني** وكل التقارير — فتسقط جميعها إلى بديلها النصّي بصمت.

والسجل يقول الأمرين بوضوح:
```
18:59:54  refused this message, but every block type in it has rendered
          before, so nothing was disabled      ← جلسة نجحت فيها كتلة أولًا
19:11:39  refused a payload using details, heading, paragraph, table;
          those block types are disabled for this session   ← جلسة بدأت بالتقرير
```

فالعطل **يعتمد على الترتيب**: إن فُتحت شاشة صغيرة أولًا لم يكلّف التقريرُ إلا نفسه.

### الإصلاح

- **حمولة أكبر بكثير مما ثبت عرضه لا تُرسل أصلًا** (`Test-RichPayloadSize`، الحدّ
  ١٢٬٠٠٠ محرف). تسقط هذه الشاشة وحدها إلى النص، ولا يُلام نوعُ كتلة.
- **ورفضُ حمولةٍ ضخمة لا يُعطّل نوعًا أبدًا**: 400 على ٤٥ كيلوبايت مسألةُ حجم لا
  مسألةُ قدرة. والتضييق باقٍ لما وُجد له: رفضٌ بحجمٍ عادي ما زال يُعطّل النوع غير
  المُثبَت.
- **وجدول تقرير البنرات محدود بأربعين صفًّا** — ٤٥ كيلوبايت ← ٩٬٧٨٩ — ويقول
  «أحدث 40 من 300» بدل أن يوهم أنها كلّها. والنسخة النصّية ما زالت تحمل كل صف.

## 7.86.2 — 2026-09-07

الرفض صار يقول سببه.

- **`Invoke-RestMethod` لا يعطي إلا سطر الحالة**: «Response status code does not
  indicate success: 400 (Bad Request)» ولا شيء غيره — وهو ما حمله السجل حين
  توقّف قسم التقارير، ولذلك تطلّب العثورُ على السبب إعادةَ بناء المسار كاملًا على
  بيانات التدقيق الحيّة بدل سطرٍ واحد.
- **والجواب في جسم الرد**، ويضعه PowerShell في `ErrorDetails.Message`:

  ```
  {"ok":false,"error_code":400,
   "description":"Bad Request: can't parse entities: Unclosed start tag at byte offset 91"}
  ```

  الوصف بكلمات تيليجرام نفسه: يسمّي الوسم الذي تعثّر فيه، وموضعه بالبايت.
- **ويُقرأ الآن ويُلحق برسالة الخطأ** في كل إخفاق — 400 و429 وما بينهما. وإن لم
  يكن الجسم JSON متوقَّعًا (صفحة خطأ من بوابة مثلًا) يُؤخذ خامًا مقتطعًا عند 300
  محرف، فصفحةُ خطأ لا تملأ السجل.

### وتحقُّقٌ من إصلاح 7.86.1

أعدتُ بناء مسار التقارير وشاشات الحالة الأربع على بيانات التدقيق الحقيقية
(‏604 كيلوبايت)، وفحصتُ كل رسالة تُرسَل:

| الشاشة | الطول | النتيجة |
|---|---|---|
| تقرير البنرات (شهر) | 43,210 | ✅ يتنازل إلى نصٍّ عارٍ صالح (4,059) |
| تقرير العمل / الموجزات / الأخبار | 838 / 1,822 / 2,112 | ✅ HTML صالح |
| ℹ️ الحالة · 📊 الكاملة · 🩺 الصحة · 🗂 الملفات | 410 / 1,873 / 617 / 385 | ✅ HTML صالح |

لا رسالة واحدة غير متوازنة ولا متجاوزة للحدّ.

## 7.86.1 — 2026-09-07

قسم التقارير توقّف في 7.86.0. هذا سببه وإصلاحه.

- **الصفحة كانت تُقصّ داخل الإطار.** `Split-TelegramText` يقصّ عند حدّ المحارف
  ولا يعرف ما الوسم، فقصٌّ داخل `<blockquote>` يترك الصفحة الأولى بوسمٍ مفتوح
  والثانية بوسمِ إغلاقٍ لا يغلق شيئًا — وتيليجرام يردّ **400 على الصفحتين معًا**.
- **وتقرير البنرات هو التقرير الافتراضي**، ويبلغ **43 كيلوبايت** على شهر:
  ثلاث عشرة صفحة، كلها مرفوضة. القياس من بيانات التدقيق الحقيقية:

  | المدة | الطول | الصفحات | قبل | بعد |
  |---|---|---|---|---|
  | اليوم | 4,683 | 2 | ❌ | ✅ |
  | أسبوع | 24,428 | 8 | ❌ | ✅ |
  | شهر | 43,210 | 13 | ❌ | ✅ |

  والتقارير الثلاثة الأخرى كانت تعمل، لأنها تقلّ عن 4096 محرفًا فلا تُقصّ.
- **`Repair-TelegramHtmlChunks`**: تُغلق كل صفحة ما تركته مفتوحًا، وتُعيد فتحه في
  أول التالية — من الخارج إلى الداخل فيبقى التداخل كما كان. والوسم يُحفظ كاملًا،
  فيعود `<blockquote expandable>` قابلًا للطيّ لا اقتباسًا عاديًّا.
  - و`code` و`pre` **لا يُعاد فتحهما أبدًا**: لا يحتملان كيانات أخرى، فإعادة فتح
    أحدهما تبتلع بقية الصفحة نصًّا حرفيًّا.
- **وإن تجاوزت صفحةٌ الحدّ بعد إضافة وسوم الإغلاق**، يُنزَع التنسيق ويُرسل النص
  عاريًا — التنازل نفسه الذي تفعله `Send-TelegramMessage` للسبب نفسه، بدل إرسال
  ما سيُرفض.
- **والإصلاح في الصفحة لا في التقرير**، فالمساعدة و«ما الجديد» ومعاينة الموجز
  تمرّ بالمسار نفسه وكانت معرَّضة للعطل ذاته.

## 7.86.0 — 2026-09-07

بقية الشاشات أخذت تصميم مركز الصحة.

- **📊 ملخص الاستخدام**: ذيلُه كان ثلاث جُملٍ بثلاثة أشكال مختلفة بعد ترتيبٍ
  مبنيٍّ بعناية — تقديرُ أسبوع، ومجموعُ تشغيلة، وحصرُ نتائج، لا يشبه أحدها الآخر
  ولا القائمة فوقه. صار الرقمان اللذان تُفتح الشاشة لأجلهما في المقدمة، وحصر
  النتائج تحتهما في إطار.
- **والتقارير الأربعة**: العمل، والموجزات، والأخبار، والبنرات. **الإجمالي أولًا**
  ثم التفصيل في إطار يُطوى إن تجاوز ثلاثة صفوف.
  - في تقرير العمل كان الإجمالي **أسفل** الأشخاص: في يومٍ مزدحم هو السطر الذي
    يمرّر المشغّل أحد عشر سطرًا ليصل إليه.
- **ولم يبقَ في المستودع سطرُ `━━━` واحد يرسم فصلًا.** الإطار يفصل فعلًا، ويُطوى،
  ولا يُرسم.
- **والتقارير تُرسَل الآن بـ`parse_mode=HTML`** — وكل حقولها مُهرَّبة بـ
  `ConvertTo-HtmlText`، المُهرِّب المحلي في ملف التقارير الموجود أصلًا لأن هذه
  الشاشات تُبنى من نصٍّ كتبه الناس: أسماء المشغّلين، وأسماء القوالب والموجزات،
  ونصّ البنر نفسه.

## 7.85.1 — 2026-09-07

الجدول الثابت صغّر النص، والمقايضة كانت خاسرة.

- **`<pre>` يحاذي أعمدته فعلًا، وتيليجرام يرسمه بمقاسين أصغر.** على هاتف يخسر
  ذلك من الوضوح أكثر ممّا يكسبه من الاصطفاف — وشاشات التقارير، وهي التي تُقرأ
  جيدًا، لم تستعمله يومًا.
- **صار الإيقاع من فتح كل سطر بخانتَي رمزٍ متطابقتين**: الحالة ثم الهوية. عمودٌ
  تتبعه العين بلا حساب محرفٍ واحد، وبحجم النص الكامل.
- **وبمفردات شاشات التقارير نفسها**: `·` فاصلًا و`—` بين العنوان وقيمته، والاسم
  عريضًا — لا شيء جديدًا يتعلّمه المشغّل.
- **وحُذف `Format-BridgeTextTable`.** لم يبقَ له مستدعٍ، وتركُه دالةً ميّتة بعد
  فحصٍ أحصى الدوال الميّتة قبل يومٍ واحد يناقض الفحص نفسه.

## 7.85.0 — 2026-09-07

جداول حقيقية، و«لماذا» صار مكتوبًا حيث يُسأل عنه.

### جدولٌ لا أسطر

- **`Format-BridgeTextTable`**: تيليجرام لا يملك وسم جدول، و`<pre>` هي السطح
  الوحيد ثابت العرض فيه — وداخله تصطفّ الأعمدة فعلًا، فيُقرأ عمود الحالة نزولًا
  بدل تفكيك كل سطر وحده. وهو الشكل نفسه الذي يعطيه جدول `sendRichMessage` حيث
  يتوفّر، وهذه الشاشات بديلُه حين لا يتوفّر.
- **طُبِّق على ثلاث**: `🩺 مركز صحة النظام` و`📈 أرقام التشغيل` و`📊 ملخص الاستخدام`.
- **الرموز عمودٌ بذاتها**: كل صف يحمل رمزًا واحدًا في كل عمود رموز، فتتساوى
  عروض الصفوف مهما كان عرض الرمز — بدل حشوها بحروفٍ لن تطابقها أبدًا.
- **والعمود الأخير لا يُحشى**: المسافات في آخر السطر لا تشتري شيئًا وتكلّف موضع
  الالتفاف على هاتف ضيّق.

### لماذا صارت Cinegy «غير سليمة»

- **السجل كان يقول الكلمة ولا يقول السبب.** `unhealthy` لم تعن يومًا «غير متصل»:
  Air يردّ، وقياساته تتجاوز حدّ تسامح. أيّ حدٍّ وبكم كان يُحسب لتنبيه المشرفين
  وحده، وبعد بلوغ عتبة الإنذار فقط — فالسطر يقول «تغيّرت حالة Cinegy إلى
  unhealthy» ولا شيء غيره، ولا سبيل لمعرفة السبب إلا قراءة القياسات يدويًّا بعد
  فوات اللحظة.
- **صار السبب على السطر نفسه**، وفي `⚠️ آخر الأخطاء` بدل «قياسات غير سليمة» التي
  كانت تكرّر ما يقوله لونُ الصف.

### وتعافي Cinegy يُعلَن دائمًا

- **كان يُعلَن فقط إن سبقه تحذير.** والتحذير ينتظر عتبةً من الإخفاقات المتتالية،
  فانخفاضٌ زال قبلها كان يترك الشاشات تقول «غير سليم» دقائق ثم يصمت — ومن نظر في
  أثنائه لم يُخبَر أنه انتهى.
- **والإشعار صار يحمل ما يحمله التحذير**: مدة الخلل، وسببه، والقياسات بعده —
  ويقول صراحةً إن لم يبلغ حدّ التنبيه، فلا يتساءل المشغّل لماذا يُخبَر بانتهاء
  شيءٍ لم يُخبر ببدايته.
- **وبدءُ التشغيل ليس تعافيًا**: `unknown` ← `healthy` لا يُرسل شيئًا، وإلا أعلن
  الجسر تعافيًا عند كل إقلاع — وهكذا تُكتم قناة التنبيهات.

## 7.84.0 — 2026-09-07

تصميم شاشتين: التي يُكتب فيها، والتي يُبحث فيها عن عطل.

### ✏️ شاشة إدخال النص

- **صارت تقول ما يجب فعله.** كل شاشة أخرى في الجسر تُجيب ضغطة زرٍّ بأزرارٍ أخرى،
  فمن ضغط زرًّا ينتظر التالي — وهذه هي الشاشة الوحيدة التي تريد كتابةً بدلًا منه.
  كانت تقول «أرسل نص الحقل» داخل جملةٍ عن القالب وتترك الباقي مفهومًا ضمنًا. صار
  السطر الأخير: **«⌨️ اكتب النص في صندوق الرسالة بالأسفل وأرسله.»**
- **والتقدّم يُرسم لا يُحسب**: `▰▰▱  2/3` — يُقرأ بلا عملية حسابية، والرقم باقٍ لمن
  يريده. ومحدود: قالبٌ بعشرين حقلًا لا يرسم شريطًا أعرض من الشاشة.
- **وأربعة أسطر بترتيب السؤال**: أيّ قالب، وأين أنت منه، وما هذا الحقل، وماذا تفعل.

### 🩺 مركز صحة النظام

- **لكل مكوّن رمزه**: 📡 تيليجرام · 🎛 Cinegy · 👁 مراقبة المخرج · 📶 البث المرحّل ·
  💾 التخزين · 📅 الجدولة · ⚠️ آخر الأخطاء. عمودٌ من سبع 🟢 يقول إن كل شيء سليم
  ولا يقول أيّ صفٍّ هو أيّ — والرموز تُميَّز من لمحة ومن بُعد ذراع، وهكذا تُقرأ هذه
  الشاشة فعلًا.
- **وما يحتاج انتباهك يُرفع فوق** بلا إطار — لأنه جواب الشاشة — والسليم يُطوى خلف
  عدد. النَّسَق نفسه الذي اعتمدته 🗂 صحة الملفات، فعادةٌ واحدة تكفي للشاشتين.
- **والترتيب داخل كل مجموعة لم يتغيّر.** فرزُ القائمة كلها بالخطورة كان سينقل
  Cinegy من الصف الثاني إلى حيث يضعه لونُه اليوم، فيخسر المشغّل المكان الذي تعلّمه.
- **وصفٌّ بلا رمز هوية لا يُسقط الشاشة** — قرأه `Get-JsonProp` الذي يقرأ الجدول
  والكائن معًا؛ أمسك الاختبارُ هذا قبل أن يصل.

## 7.83.0 — 2026-09-07

شاشات التقارير أخذت نَسَق «الحالة الكاملة»: حكمٌ أولًا، ثم الدليل تحته.

- **`🩺 مركز صحة النظام` صار يبدأ بحكم.** كان يجيب عن سؤاله الوحيد — هل من خلل؟ —
  بأن يجعل المشغّل يقرأ سبعة صفوف وينتبه إلى لون. صار السطر الأول يقوله، والصفوف
  تحته دليلًا عليه.
  - والحكم **مقروء من أيقونات الصفوف نفسها** لا محسوبًا من جديد، فلا يمكن أن يقول
    العنوان «كل شيء سليم» فوق صفٍّ أحمر — وهو العطل الذي أُضيف الحكم لمنعه.
- **و`📈 أرقام التشغيل` صار له حكم أيضًا.** كان أرقامًا فقط: من يفتحه لأن «شيئًا
  ما يبدو غريبًا» كان عليه أن يعرف مسبقًا شكلَ الرقم السيّئ. يُحكم عليه بمدة
  التشغيل — فجسرٌ يعمل منذ إحدى عشرة دقيقة جسرٌ يُعاد تشغيله — وبحدّ تيليجرام
  بجانبها، وهو الشيء الآخر الذي يكون معطلًا بينما كل رقم على حدة يبدو عاديًّا.
- **و`🗂 صحة ملفات التشغيل` صارت ترفع ما يحتاج انتباهك فوق**، وتطوي السليمة خلف
  عدد. ستة عشر سطرًا من «لم يُكتب بعد» كانت تمنح تركيبًا سليمًا وزنَ تركيبٍ معطوب
  على الشاشة — وهو عكس ما تُوجد شاشة الصحة من أجله.
- **وكل شاشة تقرير صارت تحمل ساعتها.** لقطة الشاشة التي تُرسَل إلى زميل صارت تقول
  متى أُخذت بدل أن يُسأل عنه.

## 7.82.0 — 2026-09-07

سرٌّ كان يمكن أن يخرج في رسالة خطأ.

- **`Protect-SensitiveText` موجودة منذ إصدارات وتُنقّي رمز البوت ومفتاح البثّ،
  لكن عشرة مواضع كانت تتجاوزها.** رابط تنزيل الملف من تيليجرام يحمل **رمز البوت
  في مساره**، ورابط خرج البثّ هو `rtmp://host/s/<مفتاح البث>` — وكلاهما ينتهي
  داخل رسائل استثناءات المكتبات، ولكلٍّ محادثةٌ وسجلٌّ يتسرّب إليهما.
- **كل نصّ استثناء يصل محادثة صار يمرّ بالمنقّي** — سبعة مواضع. المحادثة أوسع
  جمهورٍ للجسر: فشلُ استيرادٍ يقرؤه كل مشغّل في المجموعة. والتنقية لا تكلّف شيئًا
  على رسالةٍ لا تحمل سرًّا، فمرورُ الجميع بها أسلم من مناقشة كل موضع على حدة.
- **وثلاثة مواضع في السجل**، من المسارات التي تحمل سرًّا وحدها: تبديل مصدر البثّ،
  وإعادة تشغيله تلقائيًّا، وتنزيل صورة الموجز. أضيق من قاعدة المحادثة عمدًا:
  `bridge.log` خاصّ، وتنقيةُ فشلِ كتابةِ ملفٍ محلّي تجعل السجل أصعب قراءةً بلا مقابل.
- **وثلاثة اختبارات تحرس القاعدة**: واحدٌ يثبت أن المنقّي يمسح رمزًا ومفتاحًا
  حقيقيَّي الشكل، واثنان يمسحان ملفات المصدر فلا يعود موضعٌ جديد يفلت.

### ومن الفحص نفسه

- **٩٠٧ دوال، خمسٌ منها فقط غير مستدعاة إطلاقًا** (‏٠٫٥٪) وتسعٌ مغطّاة باختبارات
  بلا مستدعٍ في الإنتاج — لم أحذف شيئًا، فالقائمة أنفع من الحذف الآن.
- **ولا موضع واحد يسجّل رابطًا خامًا أو `$apiBase`.**

## 7.81.0 — 2026-09-07

آخر الشاشات النصية — ولم يبقَ نصًّا عاريًا إلا ما يجب أن يبقى.

- **شاشة إدخال نص الحقل** — وهي الشاشة التي يكتب فيها المشغّل فعلًا — صار اسم
  الحقل المطلوب فيها عريضًا، واسم المتغيّر تحته بخط ثابت. المتغيّر معرّفٌ في
  Titler مثل `Ajel.center`، والخط الثابت هو ما يقول «هذا اسم الآلة للحقل، لا
  عنوانٌ تقرؤه». وحين لا يعطي القالب عنوانًا مفهومًا يظهر الاسم الخام وحده.
- **وشاشة تغيير الإعداد** صارت تضع القيمة الحالية والافتراضية بخط ثابت، فتصطفّ
  إحداهما تحت الأخرى وتُقارَن بنظرة، وتبقى أرقامهما من اليسار لليمين.
- **وملاحظة مزامنة الموجز** صار حكمها عريضًا وأرقامها بخط ثابت، ومعها
  `LoopEndFrame` — وهو اسم حقل يُكتب في Titler لا كلمة تُقرأ.
- **وكل هذا مُهرَّب**: مفتاح القالب وعنوان الحقل واسم المتغيّر ووصف الإعداد
  وقيمتاه. `templates.json` يُكتب باليد، فقد تصل `<` من أيٍّ من الثلاثة — والشاشة
  تعرضها جميعًا معًا. مثبَّت باختبار على قالبٍ معادٍ فيه الثلاثة.

## 7.80.1 — 2026-09-07

المدير يسجّل خروجه، فيصير غيابُ السطر دليلًا.

- **كل مخرج إرادي من البرنامج صار يترك سطرًا في `manager.log`.** كان إغلاق
  الشاشة من أيقونة شريط النظام لا يُسجَّل إطلاقًا، فبرنامجٌ اختفى من الشريط
  و«أغلق نفسه» و«أنهاه شيءٌ من الخارج» كانا يتركان الأثر نفسه: لا شيء.
  - قيمة السطر في غيابه أكثر منها في وجوده: إن اختفى المدير ولم يُكتب شيء، فقد
    أُنهي من الخارج — مدير مهام، أو مثبّت، أو بناءٌ احتاج الملف التنفيذي غير
    مقفول.
- **وهذا ما حدث فعلًا اليوم**: بلاغ «المدير يغلق لوحده» كان أوامرَ بناء تُنهي
  العملية لتحرير `BridgeManager.exe` قبل النشر — لا عطلًا في البرنامج. السجل لم
  يكن يستطيع قول ذلك، والآن يستطيع.

## 7.80.0 — 2026-09-07

جولة تمتين: عطلان يظهران للمشغّل، وتقسيم أكبر ثلاثة ملفات.

### ما كان يظهر للمشغّل

- **تنبيه «لا يزال يعمل» كان يظهر في كل إغلاق.** التعليق في الكود يقول «مرة
  واحدة» منذ إضافته، والكود لم يفعلها قط — فمن يُخفي هذه النافذة عشر مرات في
  الوردية يرى عشرة إشعارات، ويقرؤها كأن النافذة تستأذن في الإغلاق. صار يُعرض
  مرة واحدة، محفوظةً في إعدادات المدير فلا تعود مع كل فتح.
- **وخطأ في واجهة المدير كان يفتح نافذة كل ثانية.** مؤقّت الساعة ينبض كل ثانية،
  والخطأ داخله يُلتقط ويُبلَّغ ثم يُرمى ثانيةً في النبضة التالية — نافذةٌ مانعة كل
  ثانية، أسرع مما يُغلقها المشغّل، بينما الجسر الذي كان يراقبه يعمل بلا مشكلة.
  صار يُبلَّغ عن الخطأ الأول ويُسجَّل ما بعده، وتُفتح النافذة ثانيةً بعد عشر دقائق.

### فحص لم يجد ما خُشي منه

- `Measure-Object -Maximum` الوحيد **محروسٌ** بسطر فوقه.
- **صفر `catch { }` فارغة** في `Parts/` و`Modules/` والملف الرئيسي.
- الـ٨٤ موضعًا التي بدت كتمًا للأخطاء أكثرها `-ErrorAction SilentlyContinue`
  على تنظيفٍ بأفضل جهد، و١٤ منها تُعيد **مغلّف فشلٍ** يفحصه المستدعي.
- **الفرز وجد واحدًا حقيقيًّا**: فشلُ إحصاء عمليات الجسر الأخرى كان يُعيد قائمة
  فارغة، فيطبع المستدعي تفسيرًا واثقًا خاطئًا: «القفل محجوز لكن لا توجد عملية
  جسر أخرى — يحجزه طرفيّة بقيت مفتوحة». صار الفشل يُقال، فلا يختلط «لا يوجد»
  بـ«تعذّر النظر».
- والذاكرة سليمة: مفاتيح قواميس الحالة الخمسة والعشرين محادثات ومستخدمون وطبقات
  وقوالب — محدودة بحجم المحطة لا بالزمن.

### التقسيم

ثلاثة ملفات تجاوزت الحدّ ثلاثة أضعاف، وصارت اثني عشر:

| كان | صار |
|---|---|
| `Bridge.Mojaz` 2733 | 402 · `.Design` 558 · `.Screens` 1031 · `.Playback` 785 |
| `Bridge.ShowFlow` 2733 | 948 · `Bridge.WhatsNew` 673 · `Bridge.Help` 577 · `Bridge.AirOperation` 577 |
| `Bridge.Admin` 2149 | 346 · `.Templates` 274 · `.Health` 651 · `.Config` 464 · `.Diagnostics` 469 |

نقلُ نصٍّ لا أكثر: الأجزاء تُحمَّل بـ dot-source فتتشارك نطاقًا واحدًا، ولا يهمّ
ترتيب تحميلها. و**١٣٩٨ اختبارًا لم يتغيّر منها واحد** — وهو ما يقول إن النقل كان
نقلًا خالصًا لا إعادة كتابة.

## 7.79.0 — 2026-09-07

شريط الاقتباس صار له معنًى واحد.

- **المشكلة لم تكن الشكل، بل أنه كان يعني شيئين.** مرة «مادة منقولة من مكان
  آخر»، ومرة «قائمة تفاصيل» — والمشغّل تحت الضغط لا يتعلّم شيئًا من علامةٍ تعني
  شيئين. صار معناها واحدًا: **تفصيل يمكن تخطّيه**، والحكم فوقه لا داخله.
- **فأُزيل من `👥 نشاط المستخدمين`.** تلك القائمة هي الشاشة كلها، لا دليلًا تحت
  حكمٍ سبقها؛ وشريطٌ حول الشيء الوحيد المعروض لا يعلّم شيئًا — وهي الوظيفة
  الوحيدة لعلامةٍ متّسقة.
- **وبقي حيث المادة دليل**: صفوف `🩺 مركز الصحة`، وقائمة `🗂 صحة الملفات`،
  وعدّادات `📈 أرقام التشغيل` الثانوية، وترتيب `📊 ملخص الاستخدام`، وأحداث
  `🕘 ماذا فاتني` — وهذه الأخيرة منقولة فعلًا من السجل.
- **والقوائم الطويلة تُطوى تلقائيًا (`expandable`).** الطيّ هو الشيء الوحيد الذي
  يعطيه الاقتباس ولا تعطيه المسافات: يبقى الحكم فوق القائمة والنصيحة تحتها في
  أول شاشة الهاتف بدل أن يُمرَّر عليهما. الحدّ خمسة صفوف — وعشرة أسطر في صحة
  الملفات، لأن كل ملف فيها سطران.

## 7.78.0 — 2026-09-07

شاشة القائمة: بلا شريط اقتباس، ومع من عرض الطبقة ومتى.

- **أُزيل الـ`<blockquote>` من الشاشة.** شريطه الثقيل على الجانب يُقرأ نقلًا عن
  مكان آخر، وهذا جواب الشاشة نفسها — على شاشةٍ تُفتح عشرات المرات في الوردية.
  الفصل صار بسطر فارغ فوق الكتلة وآخر تحتها: يجمع الكتلة كما يجمعها الاقتباس،
  ولا يدّعي ما يدّعيه.
- **وتحت كل طبقة سطرٌ يقول منذ متى هي على الهواء ومن عرضها.** السؤالان اللذان
  يسألهما المشغّل عن غرافيك لم يضعه هو. الحقيقتان كانتا مسجّلتين أصلًا — شاشة
  تأكيد الإخفاء تطبعهما منذ إصدارات — والشاشة التي يهبط عليها المشغّل لا تقولهما.
  - ومشهدٌ لم يضعه الجسر لا مشغّل له يُسمّى، فيقول «Cinegy (خارج الجسر)» — وهي
    الطريقة التي يعرف بها المشغّل أنه جاء من المحرّك لا من البوت.
- **والقائمة تُقتصر على أربع طبقات.** كل طبقة صارت سطرين، وثماني طبقات كانت
  ستدفع سطر المحرّك وسطر المشغّل خارج الشاشة. ولا يضيع شيء: تحت الرسالة زرُّ
  إخفاء لكل طبقة حيّة باسمها، والشاشة تقول كم طبقة لم تُدرِج. والعدد في العنوان
  هو العدد الحقيقي لا المعروض.
- **وسجلٌّ بلا وقت ولا مشغّل لا يُسقط الشاشة.** هذه السجلات يكتبها عشرات المواضع
  ولا يحمل كلٌّ منها إلا ما يعنيه، ومفتاحٌ غائب تحت StrictMode كان سيُسقط الشاشة
  التي تُفتح حين يكون شيء قد ساء بالفعل.

## 7.77.0 — 2026-09-07

برنامج التحكم صار يحمل رقم إصداره هو، لا رقم الجسر.

- **`BridgeManager.exe` يُختم بالرقم الرئيسي وحده: `v7`.** الاثنان يتباعدان
  عمدًا — الجسر يصدر عدة مرات في اليوم، والبرنامج لا يُنشَر إلا حين يتغيّر هو —
  فمديرٌ مختوم بـ 7.76.0 بجانب جسرٍ عند 7.79.0 يُقرأ قديمًا وهو محدَّث. والرقم
  الرئيسي هو الادّعاء الوحيد الذي يستطيع الوفاء به: هذا مديرُ الإصدار السابع،
  لجسرٍ من الإصدار السابع.
- **وشريط الحالة صار يسمّي كل رقم.** كان يقول «يعمل منذ … · المعرّف 28728 ·
  الإصدار v7» وكل رقم آخر على السطر يخصّ الجسر — فإصدار المدير كان يُقرأ إصدارًا
  للجسر. صار يقول «الجسر v7.76.0 · المدير v7»، وإصدار الجسر مقروء من السطر الذي
  يعلنه الجسر عند إقلاعه، على السجل الذي يتابعه المدير أصلًا.
  - ويُنسى عند التوقف: إصدار جسرٍ متوقّف هو إصدار التشغيلة السابقة، والتشغيلة
    التالية تعلن إصدارها بنفسها.
- **والسطر لا يُقصّ صامتًا.** طال بإضافة الرقمين، وعنوانٌ ثابت الارتفاع يقطع
  النص من آخره — حيث الإصدارات — بلا ما يدلّ على القطع. صار ينتهي بثلاث نقاط،
  وكاملًا عند المرور بالمؤشر.
- **والاختصاران صارا مرئيَّين.** `F5` و`Ctrl+L` وُجدا إصدارًا كاملًا ولا شيء على
  الشاشة يقول ذلك، وهو ما يعادل عدم وجودهما. صارا مكتوبَين على تلميح الزرّين —
  التعرّف أسهل من التذكّر، وهو المبدأ نفسه الذي يجعل `(Ctrl+F)` مكتوبًا في خانة
  التصفية.

## 7.76.0 — 2026-09-07

آخر أربع شاشات إلى `parse_mode=HTML` — ولم يبقَ شيء يُرسَل نصًّا إلا ما يجب.

- **`📈 أرقام التشغيل` و`📊 ملخص الاستخدام` و`ℹ️ معاينة القالب` و`⭐ المفضلة`.**
  مدة التشغيل تتصدّر عريضة لأنها الرقم الذي وُجدت الشاشة من أجله — جسرٌ يعمل منذ
  إحدى عشرة دقيقة جسرٌ يُعاد تشغيله — وكل رقم بجانبها `<code>` يبقى من اليسار
  لليمين ويُنسخ بلمسة.
- **ومعاينة القالب صارت تقول أولًا ما يُقرأ قبل العرض**: اسم القالب، ثم هل هو على
  الهواء الآن. والتفصيل تحتهما في اقتباس بدل سطرٍ من `━━━` فوق عمودٍ واحد.
- **و`👥 المفضلة` صار سطر التفسير مائلًا**: هو نقطة الشاشة كلها — يشرح الحالتين
  اللتين لا تستطيع علامات الصح شرحهما — فكان يُقرأ تعليمةً ثانية.
- **و`📤 نسخ الحالة` بقي نصًّا عاريًا عمدًا.** تعليقه يقول لماذا: هو ملخّصٌ
  يُلصَق في تطبيق آخر، والتنسيق هو الشيء الوحيد الذي يكسره هناك.
- **والفاحص صار يغطّي هذه الشاشات أيضًا**، بقالبٍ معادٍ فيه `<` و`&` في الاسم
  والوصف والتصنيف وأسماء الحقول — يخرج مُهرَّبًا ويعود سليمًا عند العرض.
  - وقد أمسك الفاحص خطأً كتبتُه أثناء هذا العمل نفسه: `<code>` داخل `<i>` في شاشة
    المفضلة. صارت الأرقام هناك أرقامًا عادية داخل الجملة المائلة: تكسير الجملة
    إلى ثلاثة مقاطع لشراء خطٍّ ثابت كان سيكلّف الجملة.

## 7.75.1 — 2026-09-07

سطران من 7.75.0 كانا سيمنعان شاشتيهما من الوصول أصلًا.

- **`<b>…<code>رقم</code>…</b>` ليس تداخلًا مقبولًا.** وثيقة كيانات
  تيليجرام صريحة: العريض والمائل والمسطّر والمشطوب والمخفي لا تُجمع مع
  `code` و`pre`، والواجهة تردّ الرسالة كلها بـ 400 بدل أن تُسقط أحد الكيانين — وهذا
  على الهاتف زرّ لا يفعل شيئًا. الموضعان كانا في سطر «فشل/مرفوض» في «ماذا
  فاتني»، وسطر «ملفات تحتاج انتباهك» في صحة ملفات التشغيل. صارا جنبًا إلى
  جنب بدل واحد داخل الآخر.
- **وفاحص دائم يمنع عودته.** يتحقّق من الوسوم المسموحة، وتوازنها، وألا يُداخل
  اقتباس اقتباسًا، وألا يجتمع `code` مع توكيد — على الشاشات المبنية فعلًا وعلى
  ملفات المصدر جميعًا، لأن المزج سهل الكتابة ومستحيل الرؤية.

## 7.75.0 — 2026-09-07

بقية الشاشات إلى `parse_mode=HTML`، وشاشة القائمة صار فيها بناءٌ لا رسمُ بناء.

- **كتلة «على الهواء» في القائمة صارت `<blockquote>`.** الشاشة كانت تُرسَل HTML
  منذ 7.73.0، لكنها تنفق التنسيق على التوكيد وحده، فتبقى عمودًا مسطّحًا مخدوشًا
  بسطرَي `━━━`. الاقتباس هو البناء نفسه: تيليجرام يرسم الشريط والإزاحة — وهذا ما
  يعنيه فصلُ كتلة — فيصير الحكمُ فوقها وسطرُ المحرّك تحتها **خارج** شيء، لا
  محصورَين بين صفَّي شرطات.
  - ويُطوى الاقتباس (`expandable`) إذا تجاوزت الطبقات أربعًا: استوديو بثماني
    طبقات كان يدفع سطر المحرّك وسطر المشغّل خارج الشاشة، وهما السطران اللذان
    يقتبسهما المشغّل حين يبلّغ عن العطل الذي ينظر إليه.
- **وخمس شاشات أخرى تحوّلت**: `🕘 ماذا فاتني`، و`🩺 مركز صحة النظام`، و`🗂 صحة
  ملفات التشغيل`، و`👥 نشاط المستخدمين`، و`👤 من استخدم`. العناوين عريضة تفصل
  الأقسام، والأوقات والأعداد `<code>` تبقى من اليسار لليمين وتُنسخ بلمسة، وما
  اقتُبس من مكان آخر داخل `<blockquote>`.
  - وفي «نشاط المستخدمين» صار التحفّظ مائلًا ومفصولًا عن القائمة: «نشط» هنا تعني
    «تحدّث إلى البوت حديثًا» لا «متصل الآن»، وقراءتُها سطرًا من القائمة هي سوء
    الفهم الذي وُضع التحفّظ لمنعه.
- **وكل ما يكتبه إنسان في هذه الشاشات مُهرَّب**: أسماء القوالب، وأسماء المشغّلين،
  والألقاب، ورسائل السجل الحرّة، ونصُّ البحث الذي يكتبه من ينفّذ `/who` — وهو
  أقرب النصوص إلى يد المستخدم على أي شاشة هنا. علامة `<` واحدة كانت ستكلّف
  الشاشة كلها ردَّ 400، وهو ما يظهر على الهاتف كزرٍّ لا يفعل شيئًا.

## 7.74.0 — 2026-09-06

تقصير القائمة، وشاشةُ سجلٍّ لا تُترك فارغة، ونبضةٌ تُعاد المحاولة قبل الشكوى.

- **القائمة صارت أقصر بثلاثة صفوف.** كانت ستة عشر صفًّا للمشرف — جدارٌ على
  الهاتف، وكل صف فوق أزرار الهواء صفٌّ يُمرَّر عليه بينما غرافيك خاطئ على الشاشة.
  الاقتران بالمعنى لا لتوفير المساحة: `ℹ️ الحالة` و`📊 الحالة الكاملة` سؤالٌ واحد
  بعمقين، و`📅 الجدولة` و`📊 تقارير` كلاهما «ما القادم / ما الذي جرى»، و`📰 شريط
  الأخبار` و`📑 الموجز` هما الكلام الخارج تحت الصورة.
  - و«وضع اليد الواحدة» ما زال يفكّ كل اقتران إلى صفٍّ مستقل: الاقتران خيار
    تخطيط، وإعلان المشغّل أنه يمسك الهاتف بيدٍ واحدة يعلو عليه.
- **برنامج التحكم لم يعد يترك شاشة السجل فارغة.** الشاشة الفارغة ليست محايدة:
  عمل هذه النافذة كله أن تقول إن الجسر حيّ، فالعمود الخالي يُقرأ «توقّف عن
  الكتابة» — وهو ما تنتجه تصفيةٌ لا تطابق شيئًا، وليس تحتها إلا «المعروض: 0 من
  1842» بخطٍّ صغير في أسفل النافذة. صارت الشاشة تقول أيّ الحالات الثلاث هي، وكيف
  الخروج منها: لم يصل شيء بعد، أو التصفية أقصت كل شيء، أو «الأخطاء والتحذيرات
  فقط» فعلت — وهذه الأخيرة تقول إن نظافة السجل هي المطلوب لا عطل.
- **واختصاران**: `F5` لإعادة التشغيل و`Ctrl+L` لمسح الشاشة. كلاهما يمرّ بالزرّ
  نفسه، فتأكيدُ الفأرة هو تأكيد لوحة المفاتيح، ولا يعمل `F5` وزرُّه معطَّل — وإلا
  أعادت لوحةُ المفاتيح تشغيل جسرٍ هو أصلًا في طور التوقف.
- **ونبضة الحياة تُعاد المحاولة مرة قبل أن يُقال شيء.** العطل الذي يصيب هذا الملف
  فعلًا هو قارئٌ يصادف لحظةَ الكتابة: أربع مرات في أسبوعين على هذا التركيب، كلها
  زالت في الدورة التالية، وكلٌّ منها مسجَّلة كأن المراقب توقّف — وهذا ما يُعلّم
  المشغّل أن هذا السطر لا يعني شيئًا.

## 7.73.0 — 2026-09-06

شاشات الحالة صارت تُبنى بـ`parse_mode=HTML`، ومعها تنبيهٌ للموجز كان ينقصه.

- **ℹ️ الحالة و📊 الحالة الكاملة وشاشة القائمة صارت منسّقة، لا عمودًا مسطّحًا.**
  الحكم العام بخط عريض لأنه السطر الوحيد الذي يجب أن يصل في لمحة، وعناوين
  الأقسام الستة في الحالة الكاملة عريضة تفصل ثلاثين سطرًا كانت تُقرأ كتلةً واحدة.
  - الشاشة التي يقرأها المشغّل فعلًا هي هذه: `sendRichMessage` من Bot API 10.1
    وأكثر الخوادم ترفضه، فالبديل النصي هو التصميم لا نسخةً منقوصة منه — وكان
    يُرسَل بلا تنسيق أصلًا.
- **وكل رقم يقتبسه المشغّل صار `<code>`**: العنوان، والقناة، والساعة، والعدادات.
  ليس زخرفًا: الخط الثابت يُعرض من اليسار لليمين فتتوقّف الأرقام عن الانقلاب
  بجانب العربية، وتيليجرام يجعل كلًّا منها يُنسخ بلمسة واحدة لمن يكتب بلاغ عطل.
- **وكل ما يكتبه إنسان صار يُهرَّب**: اسم القالب، واسم المشغّل، وأخطاء المخزن.
  علامة `<` واحدة في اسم قالب كانت ستكلّف الشاشة كلها ردَّ 400 من تيليجرام، وهو
  ما يظهر على الهاتف كزرِّ قائمةٍ لا يفعل شيئًا.
- **و«/بدء» و«إلغاء» صارا يعرضان الحالة تحت التحية.** كان النصّ الممرَّر يحلّ
  محلّ الشاشة كلها، فالبابان الأكثر استعمالًا للدخول إلى الجسر ما زالا ينتهيان
  إلى «اختر من القائمة:» — وهو السطر الخالي من البيانات الذي أُعيدت هذه الشاشة
  من أجل الكفّ عن عرضه.
- **والموجز صار ينبّه إن كانت مدة الصف لا توافق طول لوب القالب.** موجزٌ على
  الهواء كان يمسك كل صف ١٥٠٠ إطار على لوبٍ طوله ٧٥٠، فيُعرض كل خبر مرتين ويُقرأ
  للمشغّل كموجزٍ معلّق على خبر واحد. الرقمان كانا معروضين على الشاشة نفسها،
  ولم يقارنهما أحد. التنبيه يظهر تحت الرقمين، وفي شاشة تعديل المدة، وفي السجل
  عند بدء التشغيل — ويصمت إن كانت «مزامنة الظهور» مفعّلة، فهي تأخذ إيقاعها من
  اللوب ولا يمكن أن تخالفه.

## 7.72.0 — 2026-09-06

- **شاشة القائمة صارت تسمّي المشغّل الذي فُتحت له.** سطر `👤` أسفلها، مبنيٌّ
  بـ`Format-UserAuditActor` نفسها التي تستعملها ℹ️ الحالة — لا بصياغة ثانية:
  فالشخص الواحد يُكتب بطريقة واحدة على الشاشتين، والمعرّف بين قوسين يبقى مثبّتًا
  إلى اليسار بعد اسم عربي بدل أن ينقلب إلى `)8201739556(`.
  - ويُحذف السطر إن لم يُمرَّر مشغّل، فلا يُطبع صفرٌ عارٍ.

## 7.71.0 — 2026-09-06

تنسيق شاشة القائمة، ومعها خللٌ كشفه اختبارُ التنسيق نفسه.

- **الشاشة صارت ثلاث كتل مفصولة**: الحكم العام أولًا، ثم ما على الهواء، ثم
  المحرّك والقناة. متتابعةً بلا فصل كانت تُقرأ فقرةً واحدة يفكّكها المشغّل تحت
  الضغط، والسطر الأول هو ما يُنظر إليه في اللمحة الأولى.
- **وكل طبقة على الهواء في سطر خاص بها.** كانت مجموعةً بفاصل `|` في صفٍّ واحد،
  فإيجاد طبقة بعينها يتطلب قراءة الصف كلمةً كلمة.
- **والحكم العام مأخوذ من شاشة ℹ️ الحالة بصياغته نفسها**، لكن مقروءًا من طزاجة
  آخر فحص محفوظ لا من مسح حيّ لـ Cinegy: هذه الشاشة تُفتح عند كل ضغطة قائمة ولا
  تحتمل رحلةً إلى المحرّك في كل مرة.
- **ولا يُقال «كل شيء سليم» إلا بعد تحقّق ناجح.** كشفه الاختبار المكتوب للتنسيق:
  جسرٌ **لم يصل إلى Cinegy ولا مرة** كان يعلن «🟢 كل شيء سليم» — وهو ادّعاء بلا
  سند، ونفس الطمأنة الكاذبة التي يعطيها ادّعاء «على الهواء» قديمٌ يشبه الحديث.
  صار الأخضر مشروطًا بقراءة مؤكَّدة، وما عداها — قديمة أو متعذّرة أو لم تحدث بعد
  — يُقال عنه إنه غير مؤكَّد.

## 7.70.0 — 2026-09-06

شاشة القائمة كانت تبدأ برسالة لا بيانات فيها.

- **ضغطة القائمة كانت تُنتج رسالتين، أولاهما فارغة من المعنى.** الأولى تقول
  «استخدم زر 🏠 القائمة أسفل الشاشة…» ولا وظيفة لها إلا تثبيت لوحة الأزرار
  الدائمة، والثانية هي القائمة. وتيليجرام يُبقي لوحة الأزرار مثبّتة حتى تُستبدل،
  فإعادة إرسالها في كل ضغطة لم تكن تشتري شيئًا — وكانت تكلّف المشغّل رسالةً
  أعلى الشاشة تخبره أن القائمة موجودة، فوق القائمة التي فتحها للتوّ. صارت
  تُثبَّت **مرة واحدة لكل محادثة**، فما يراه من يضغط القائمة هو القائمة نفسها.
- **وصارت القائمة تجيب بدل أن تشير.** كانت سطرًا واحدًا عمّا على الهواء؛ صارت
  تقول ما على الهواء، **وعمر آخر تحقّق** من Cinegy (فادّعاء «على الهواء» قديمٌ
  يشبه الحديث تمامًا، وهذا ما ترك مشهدًا خارجًا دون أن يُلاحَظ)، **ومحرّك Air
  والقناة** اللذين يتحدث عنهما — فالمشغّل الذي يفتح القائمة يجد جوابه فيها بلا
  نقرة إضافية على ℹ️ الحالة.

## 7.69.0 — 2026-09-06

استكمالٌ لما تركه استلامُ الجسر في 7.68.0 ناقصًا، وإجابةٌ عن سؤال لم يكن الرأس
يجيبه.

- **«يعمل» لم تكن تعني أن الجسر يعمل.** كانت تعني أن **العملية** حيّة فقط. وجسرٌ
  رفضه تيليجرام أو لا يصل إلى محرّك Air حيٌّ وعديم الفائدة في آنٍ واحد، ومعرفة
  ذلك كانت تتطلب قراءة أسطر السجل. والجسر يعلن الانتقالين بنفسه
  (`Telegram connection changed …` و`Cinegy health changed …`)، فصار الرأس يقرأ
  هذين السطرين وهما يمرّان ويُبقي الجواب على الشاشة: **«تيليجرام: متصل»**
  و**«Cinegy: سليم»**، بالأخضر إن كانا سليمين وبالأحمر إن انقطعا. وعند كل إعادة
  تشغيل يعودان إلى «—» حتى يقول الجسر الجديد حاله، فلا تُعرض حالةُ جسرٍ سابق على
  أنها حالة الحاضر.
- **ولوحة السجل لم تعد فارغة عند الاستلام.** الجسر الذي لم تشغّله هذه النافذة
  يملك خرجه، فكانت الشاشة تأتي بيضاء وتحيل المشغّل إلى ملف في المستكشف — وهو آخر
  ما تركه الاستلام ناقصًا. والجسر يكتب تلك الأسطر نفسها إلى `logs/bridge.log`،
  فصارت اللوحة تتابع الملف: تُبذَر بآخر مئة سطر (شاشةٌ فارغة لجسرٍ هادئ ساعاتٍ
  لا تُميَّز عن شاشةِ جسرٍ معطَّل)، ثم تُلحق الجديد كل ثانية.
  - **ولا تُعرض نصف كتابة**: القراءة قد تقع في منتصف سطر، فيُحتفظ بما بعد آخر
    سطر جديد ليُضمّ إلى القراءة التالية، وإلا رُسم نصف السطر ثم رُسم كاملًا مرة
    أخرى.
  - **ودوران السجل مكشوف**: `bridge.log` يدور عند `LogMaxSizeMB`، والقراءة من
    الإزاحة القديمة في ملف جديد كانت ستقفز فوق بدايته كلها.
  - وتتوقف المتابعة لحظة تشغيل جسر من هذه النافذة، وإلا طُبع كل سطر مرتين: مرة
    من الخرج ومرة من الملف.
- **والفحوص من 77 إلى 101.** كلها للمنطق الصافي الجديد: تقسيم الأسطر الكاملة،
  وكشف الدوران، وقراءة سطر الحالة ووجهته لا مصدره.

## 7.68.0 — 2026-09-06

ثلاثة أصلحتها مراجعةُ سجل ثلاثة أيام، لا تقريرُ مستخدم.

- **تقرير العمل كان يخلط الرفض بالعطل.** كان يعرض ثلاثة أرقام لكل مشغّل:
  الإجمالي، ناجح، «فاشل» — و«فاشل» يجمع `blocked` مع `failed`. وهما سؤالان
  مختلفان تمامًا: **مرفوضة** تعني شخصًا مُنع (صلاحية ناقصة، أو نافذة صيانة)،
  و**فاشلة** تعني المحرّك لم ينفّذ ما أُمر به. مدموجتين، كانت وردية مليئة
  بالرفض تبدو كوردية مليئة بأعطال Cinegy — والاستجابة لكلٍّ منهما معاكسة
  للأخرى. صارتا رقمين، ولا يُذكر أيٌّ منهما إن كان صفرًا: سطرٌ يقول «🚫 0 · ⚠️ 0»
  في كل وردية نظيفة يُعلّم القارئ تخطّي الرمزين في الوردية التي ليست نظيفة.
  - وأُضيف ما يجيب أسئلة المشرف الثلاثة بعد «كم»: **كم عملية وصلت الهواء فعلًا**
    (SHOW ناجحة — فالإخفاء ليس وقت بثّ، والمرفوضة لم تغادر المبنى)، **أكثر قالب**
    اشتغل عليه، **وآخر نشاط**. ومجموع للوردية حين يكون المشغّلون أكثر من واحد.
  - **والتقرير شُحن في 7.66.0 بلا اختبار واحد.** صار له ثمانية، وأولها التقط
    فورًا خطأ `.Sum` على مجموعة فارغة الذي كنت قد أعدت إدخاله في سطر المجموع —
    وهو الخطأ نفسه الذي أوقع تقرير الموجز في يوم هادئ سابقًا. اليوم الهادئ هو
    بالضبط اليوم الذي يُفتح فيه التقرير للاطمئنان.
- **المدير كان يقول «متوقف» بينما الجسر على الهواء.** إغلاق المدير يترك الجسر
  عاملًا عمدًا، لكن فتحه من جديد كان يعرض «متوقف» — والأسوأ أن إعادة التشغيل
  التلقائي وكشف التعليق يظلّان خاملين، لأنهما يراقبان عملية لم يبدأها هذا
  البرنامج. صار الجسر يكتب معرّف عمليته في `logs/bridge.liveness` (سطر ثانٍ،
  والقارئ القديم يقرأ السطر الأول كما هو)، والمدير يستلمه عند الفتح إن كانت
  النبضة حديثة — فلا يُنزل الرسومات ثوانيَ ليستعيد إشرافًا كان قائمًا. وما لا
  يستعيده هو لوحة السجل: الخرج ملك من شغّل العملية، فالنافذة تقول ذلك بدل أن
  تبدو معطّلة.
- **وقراءة المدير كانت تمنع نبضة الجسر.** ظهر في السجل ثلاث مرات:
  `Could not write the liveness stamp … used by another process`. السبب أن
  `File.ReadAllText` يفتح بـ`FileShare.Read` فيحجب الكاتب. الجسر كان يعيد
  المحاولة في الدورة التالية فلم يقع ضرر، لكن القارئ لا شأن له بمنع النبضة التي
  يراقبها — والنص كان يزعم أن الكشف سيتعطّل وهو غير صحيح.
- **وقياس صورة الموجز كان يسقط بسبب لوحة واحدة.**
  `Where-Object { [string]$_.File -eq $token }` يمرّ على كل عقد `//Plate`، وأيّ
  عقدة بلا سمة `File` ترمي تحت `Set-StrictMode -Version Latest`، فيضيع القياس
  كله وتُرسم الصورة بحجم افتراضي. صار الانتقاء بـ XPath مباشرة
  (`//Plate[@File="…"]`) فلا تُقرأ خاصية غائبة أصلًا.

## 7.67.0 — 2026-09-06

- استعادة الموجز تتحقق من هوية المشهد المؤكدة قبل إعادة الصف أو الإخراج، لأن
  تطابق اسم القالب وحده قد يعيد نص نشرة قديمة فوق تشغيل جديد للقالب نفسه.
- إعادة إرسال Telegram المؤجلة تعمل بطلب خلفي واحد؛ كانت ثلاث مهل شبكة متتالية
  تستطيع تأخير مهام الهواء بنحو 45 ثانية. ملفات الطابور تُنسخ بملكية مستقلة
  وبسقف 64 MiB لكل تشغيل، وتُنظّف عند النجاح أو الإسقاط أو الإغلاق المعتاد، حتى
  لا يحذف مُصدّر التقرير الملف قبل إعادة الإرسال.
- «حفظ وإعادة التشغيل» يتحقق من توقف الجسر قبل كتابة الصلاحيات؛ القفل وحده لم
  يمنع الجسر من إعادة نسخته القديمة بين الحفظ وإعادة التشغيل. فشل الحفظ يبقيه
  متوقفًا، وفشل التوقف يمنع الكتابة. تحرير القفل مضمون حتى عند أخطاء القرص.
- ملف الإعدادات المؤقت يُقيّد قبل كتابة الرمز ويُحذف عند الفشل، لأن وراثة صلاحيات
  المجلد وترك الملف بعد فشل الاستبدال كانا يكشفان نسخة من الإعدادات.
- ترتيب عرض السجل يتبع وصول الأحداث؛ أولوية التحذير تؤثر في الاحتفاظ عند الضغط
  دون تقديمه على حدث أقدم. رقم المدير يطابق رقم الجسر كاملًا في مخرجات البناء.
- اختبارات استعادة الهوية، الشبكة البطيئة، عمر المرفقات، وفشل الحفظ تحمي هذه
  المسارات دون تشغيل الجسر الفعلي أو إرسال أمر إلى الهواء.

## 7.66.0 — 2026-09-06

ملخص الاستخدام الأسبوعي لم يعد يقرأ عدّاد العملية الحالية فقط؛ يقرأ سجل التدقيق
لآخر سبعة أيام، فيبقى التقدير صحيحًا بعد إعادة التشغيل. وأضيف «تقرير العمل» إلى
قسم التقارير ليعرض حصيلة عمليات الهواء الناجحة والمتعثرة لكل مشغّل حسب المدة.

## 7.65.2 — 2026-09-06

يحفظ مدير الإعدادات الآن حقول الاتصال التي عدّلها المشغّل فقط. فتح النافذة ثم
حفظ صلاحية لا يعيد عنوان Air أو القناة أو الرمز بقيمة قديمة فوق تعديل أحدث كتبه
الجسر أو مدير آخر.

## 7.65.1 — 2026-09-05

تكملة إصلاح حد الإغراق في Telegram: لم يكن طابور 7.65.0 يؤجل إلا الرسائل النصية،
فكانت الصورة أو الملف أو الرسالة الغنية تضيع عند 429. صارت هذه الطلبات تُعاد عبر
نقطة النهاية الأصلية في الطابور المحدود نفسه. تأكيد زر Telegram لا يُؤجّل عمدًا؛
قد تنتهي صلاحية الاستعلام قبل موعده، ولا يجوز أن يحجب ذلك دورة الجسر.

## 7.65.0 — 2026-09-05

إصلاح ثبات ما يظهر للمشغّل وما ينتظر Telegram تحت الضغط. كان سجل المدير يحتفظ
بطابور بلا سقف ويفرّغه في نبضة واجهة واحدة؛ ومع دفقة بدء أو خطأ طويلة قد لا تستعيد
النافذة التحكم. صار الطابوران محدودين، وتصل التحذيرات والأخطاء أولًا، ولا تعالج
الواجهة أكثر من ٣٠٠ سطر في نبضة. السطر الضخم يُقصّ في العرض فقط والسجل الكامل
يبقى في `bridge.log`، و«مسح الشاشة» يمسح ما ينتظر أيضًا.

- شاشة السجل تلوّن الطابع الزمني ومستوى السجل ومعرّف عملية Air والطبقة والنتيجة
  بدل لون واحد للسطر كله؛ لا تُستخرج من الأسرار ولا تُعرض.
- طابور Telegram المؤجل عند 429 صار سقفه ٢٠٠ رسالة، يفسح للرسالة التحذيرية قبل
  العادية، ويرسل ثلاث رسائل مستحقة فقط في دورة الاستطلاع؛ فلا يبتلع انقضاء حد
  الإغراق المراقبة أو العمل المجدول.
- البحث في سجل المدير مؤجّل ٢٥٠ms بدل إعادة رسم آلاف السطور مع كل ضغطة مفتاح.
  وواجهة الشاشات الضيقة تجمع أزرار التشغيل معًا، وتمنع فتح `config.json` يدويًا
  ما دام الجسر يعمل، وتبقى نافذة الإعدادات قابلة لتغيير الحجم.
- جرى تصحيح دليل الوكلاء وكاتب ملفات حسابات المستخدمين: وحدات التخزين ليست
  منطقًا خالصًا، وملفات الحالة تكتب باسم مؤقت فريد في المجلد نفسه قبل الاستبدال
  الذري.

## 7.64.0 — 2026-09-05

مراجعة اعتمادية وأمان لمسارات التحكم الحي. صار كل تدفق نصي معلّق يعيد التحقق من
صلاحية صاحبه قبل التنفيذ، وتُمسح تدفقاته عند سحب الصلاحية، مع منع سحب صلاحية
المالك صراحة. كما أُعيد فحص صلاحية القالب عند تأكيد الإخفاء أو الخروج.

- استعادة الموجز تتحقق من هوية العنصر النشط في Cinegy، وتحفظ الصورة والهوية،
  وتدفع الصف الحالي إلى الـPostbox قبل استكمال الجدول.
- إصلاح سباق إعادة التشغيل، وتنسيق كتابة `config.json` بقفل مشترك وأسماء مؤقتة
  فريدة مع الحفاظ على قوائم الصلاحيات التي لم يحررها المستخدم.
- انتظار Telegram عند الازدحام انتقل إلى طابور tick، وبدء التشغيل لا يعالج
  التحديثات القديمة إن فشل تفريغها مؤقتًا.
- فشل إعادة تشغيل relay السريعة لا يلغي المحاولات المتبقية، و«أمس» لا يضم اليوم،
  واستيراد شيت غير صالح لا يهدم مسودة محرر آخر.
- كتابة JSON لا تستبدل الأصل قبل نجاح تجهيز النسخة الاحتياطية.

## 7.63.0 — 2026-09-04

مراجعة كاملة لبرنامج التحكم `BridgeManager.exe`. كل بند هنا عن خللٍ قائم في
البرنامج، لا عن ميزة كان يُتمنّى وجودها.

- **«تشغيل تلقائي مع بدء ويندوز» كان يعيد المدير وحده.** الخانة تكتب مفتاح Run،
  وتعليقها في الكود يقول إن غايتها سدّ الفجوة التي يسقط فيها الإشراف بعد انقطاع
  كهرباء أو تحديث ويندوز — لكن البرنامج لم يكن يشغّل الجسر عند إقلاعه. فالجهاز
  يعود، والنافذة تظهر فوق شاشة الـ playout، **والجسر متوقف** حتى ينتبه أحد.
  صار مفتاح Run يحمل `--autostart`، فيبدأ البرنامج في شريط النظام مباشرة ويشغّل
  الجسر بنفسه.
- **تعديل قوائم الصلاحيات أثناء عمل الجسر كان يُلغى بصمت.** `Save-Config` تعلن
  `AllowedChatIds` و`AdminChatIds` و`AllowedUserIds` و`AdminUserIds` ضمن
  `$managed`، أي أنها تكتب نسختها في الذاكرة **فوق** ما في القرص. فمن يضيف
  Chat ID من شاشة المدير ويضغط «حفظ» يجد إضافته قد اختفت عند أول حفظ يقوم به
  الجسر — بلا رسالة ولا أثر في السجل. والأسوأ أن «حفظ» هو زر `AcceptButton`،
  فمجرد ضغط Enter كان يسلك هذا الطريق. صار البرنامج يوازن ما حُرِّر بما قُرئ،
  وإن مسّ إحدى الأربع والجسر يعمل عرض ثلاثة خيارات صريحة: أعد التشغيل ليثبت، أو
  احفظ على أي حال، أو عُد للتحرير.
- **رمز البوت كان معروضًا بنصّه.** وهذا يخالف قاعدة مكتوبة في `AGENTS.md`:
  «ولا يجوز للبرنامج أن يعرض توكن». صار الحقل مخفيًّا مع زر «إظهار». وحين تكون
  حماية DPAPI مفعّلة كان القرص يحوي `dpapi:BotToken` فيعرضه البرنامج **كأنه
  الرمز** — ما يغري المشغّل بأن «يصلحه» فيلصق الرمز الحقيقي، فيهبط سرًّا صريحًا
  على القرص في تركيبٍ اختار تشفيره. صار الحقل يُعطَّل في هذه الحالة مع سطر يشرح
  السبب ويحيل إلى `scripts\Protect-BridgeSecrets.ps1`، والمرجع يبقى على القرص
  كما هو.
- **كتابة `config.json` كانت غير ذرّية.** الجسر يكتب عبر `.tmp` ثم `Move-Item`
  ويحتفظ بـ`.bak` وبمجلد نسخ مؤرَّخ، وتعليقه يشرح السبب: ملف مبتور يعني جسرًا
  **لا يُقلع أصلًا**، لأنه يحمل الرمز وقائمة الصلاحيات. أما المدير فكان يستعمل
  `File.WriteAllText` مباشرة. صار يكتب `.tmp` ثم `File.Replace` — وهو يحتفظ
  بـ`.bak` ويُبقي صلاحيات الملف الأصلية — ويطبّق ACL للمستخدم وSYSTEM
  وAdministrators حين يُنشأ الملف لأول مرة، على غرار `Protect-BridgePathAcl`.
- **إغلاق ويندوز كان يُلغى.** `FormClosing` كان يُلغي كل أسباب الإغلاق، بما فيها
  `WindowsShutDown`، فيعرض ويندوز شاشة «برنامج يمنع إيقاف التشغيل» ثم يقتل
  العملية قسرًا: بلا سطر في السجل، وبأيقونة شريط نظام تبقى شبحًا. صارت أسباب
  الإغلاق الثلاثة (إغلاق النظام، إنهاء من مدير المهام، خروج البرنامج) تمرّ
  وتوقف الجسر وتكتب ما جرى.
- **حالة الخانات لم تكن تُحفظ.** من يُطفئ إعادة التشغيل التلقائي عمدًا — لصيانة
  المحرّك مثلًا — كان يجدها مفعَّلة عند الفتح التالي. صارت `AutoRestart` و
  `AutoClearDaily` و`WordWrap` تُحفظ في `BridgeManager.settings.json`.
- **كشف الجسر المعلّق.** الإشراف كان يعتمد على حدث خروج العملية وحده، فجسرٌ
  عالق — استطلاع طويل لا يعود، جمود — يبقى «يعمل» بالأخضر إلى الأبد. **ولم
  يُبنَ الكشف على صمت السجل**، وهذا جوهر القرار: قياسًا على `bridge.log` لهذا
  التركيب نفسه، الجسر السليم يصمت من ٢٢:٣٠ إلى ٩:٠٠ ليلة بعد ليلة، ومرةً
  ١٧٫٨ ساعة متصلة — فأي عتبة صمت قصيرة بما يكفي لالتقاط تعليق كانت ستعيد تشغيل
  جسر سليم **على الهواء كل ليلة**. صار الجسر يكتب `logs/bridge.liveness` بعد كل
  دورة استطلاع، والمدير يراقب هذه النبضة. وكل شك يُحسم نحو عدم التصرف: جسر في
  طور الإقلاع، أو نسخة أقدم لا تكتب نبضة، أو نبضة من تشغيل سابق — كلها «ليس
  معلّقًا»، والحالة الثانية تُعلَن مرة واحدة بدل أن يبدو الكشف عاملًا وهو ليس كذلك.
- **شاشة السجل**: تلوين للأخطاء والتحذيرات، وتصفية بالبحث أو بـ«الأخطاء
  والتحذيرات فقط» (Ctrl+F)، وعدّاد أسطر **يُحصى** لا يُقدَّر كما كان.
- **الأداء والتنظيف**: الأسطر تُجمَّع كل ١٠٠ms بدل `BeginInvoke` لكل سطر — دفقة
  خرج كثيفة كانت تُغرق طابور الرسائل وتجمّد النافذة؛ وتُحرَّر مقابض العمليات
  والأيقونات بدل تسريب مقبض عند كل إعادة تشغيل؛ والنوافذ صارت RTL (عدا لوحة
  السجل، فسطورها تبدأ بطابع زمني لاتيني ومرآتها تُصعّب المسح البصري)؛ وحوارات
  التأكيد من شريط النظام تُظهر النافذة أولًا بدل أن تفتح خلف كل شيء؛ وقفل
  النسخة الواحدة صار `Global\` فلا يشرف مديران من جلستَي ويندوز على جسر واحد.
- **والواجهة أُعيد تصميمها.** كانت أحد عشر عنصرًا في صف واحد بأوزان بصرية
  متساوية: زر «إيقاف» يشبه زر «مسح الشاشة»، وحالة الجسر كلمة بخط 11 محشورة
  بينها. صار هناك **رأس** يقول الحالة بخط 16 ملوّنًا بشريط جانبي — يُقرأ من آخر
  الغرفة — ومعه مدة التشغيل ورقم العملية؛ و**تدرّج في الأزرار**: ما يغيّر الهواء
  (تشغيل/إيقاف/إعادة) ممتلئ بلون حالته، وما يفتح شاشة فقط هادئ ومحدَّد بإطار؛
  و**شريط سفلي** يقول عدد الأسطر وعمر آخر نبضة ومسار الجسر؛ وتلميح على كل عنصر.
  - **والوضع الافتراضي فاتح**، والداكن اختياري بمفتاح 🌙 يُطبَّق فورًا بلا إعادة
    تشغيل. البرنامج يُفتح نهارًا بجانب Explorer وعميل Cinegy، ونافذة سوداء
    وحيدة بينها تبدو برنامجًا آخر في كل مرة. والداكن يبقى لمن يجلس ليلًا بجانب
    شاشة البث.
  - **والخيارات صارت أزرارًا ممتلئة/مفرَّغة** لا مربّعات تأشير: أول بناء داكن
    أظهر خمسة خيارات لا تُقرأ حالتها إطلاقًا، لأن مربّع التأشير المسطّح يُرسم
    بألوان الأب — داكن على داكن. (وأول نسخة من الأزرار الممتلئة ظهرت بلا نص
    أصلًا: `AutoSize` مع `Padding` على `CheckBox` بمظهر زر يطرح الحشو من مستطيل
    نصٍّ سبق أن قيس على النص وحده.)
- **وقائمة الحسابات بدل خمسة حقول نصية.** كان إدخال الصلاحيات خمسة مربّعات فيها
  أرقام مفصولة بفواصل. ورقم تيليجرام عشرة أرقام لا يُقرأ منها شيء، فرقمٌ مكرّر أو
  ناقص رقمًا لا يُرى، ونقل مشغّل من «مسموح» إلى «إداري» يعني تحرير سلسلتين يدويًا
  وإبقاءهما متسقتين. صار جدولًا: صف لكل حساب، عمود للدور وآخر للنوع، وإضافة
  بحقل ورقم دور وزر، وحذف بتأكيد يسمّي الأرقام التي سيُسحب وصولها، وسطر يلخّص
  العدد لكل دور.
  - **والتحقق يرفض اتجاهًا واحدًا فقط**: الرقم السالب في دور *مستخدم* خطأ لأنه
    يسمّي مجموعة حيث يجب فرد. أما الرقم الموجب في دور *مجموعة* فليس خطأ —
    تيليجرام يعطي المحادثة الفردية رقم صاحبها، و`AllowedChatIds` في هذا التركيب
    مليء بها. أول نسخة من هذا التحقق كانت ترفضها، فكانت ستمنع المشغّل من إعادة
    إضافة رقم يعمل به الجسر منذ شهور — وظهر ذلك عند فتح الشاشة على الإعدادات
    الحقيقية، لا في اختبار.
- **وتأكيد قبل كل ما يُضعف السيطرة**: إعادة التشغيل (تسحب التحكم ثوانيَ)، وإطفاء
  إعادة التشغيل التلقائي، وإطفاء كشف التعليق، وحذف حسابات. والتأكيد في اتجاه
  الإطفاء فقط — صندوقٌ يظهر في الاتجاهين يُعلّم الناس تجاهله. ولا يُسأل مراقب
  التعليق ولا «حفظ وإعادة التشغيل»، فهما يستدعيان إعادة التشغيل بلا أحد أمام
  الشاشة.
- **و`--selftest` صار يُنفَّذ فعلًا.** كان مكتوبًا منذ إصدارات ولا يستدعيه
  `Run-Checks.ps1` ولا `Build-BridgeManager.ps1` ولا CI — أي أنه غير موجود
  عمليًّا. صار `Build-BridgeManager.ps1` يشغّله بعد كل نشر ويُفشل البناء إن رجع
  بغير صفر، وارتفع من ٦ فحوص إلى ٣٠ تغطّي المنطق الجديد كله.

## 7.62.0 — 2026-09-04

- **تنبيه حين لا يكون اللوغو أو الشريط على الهواء.** قالبٌ يُفترض ألّا ينزل
  أبدًا، ونزوله لا يُلاحَظ إلا بالنظر إلى الشاشة.
  - **يُسأل Cinegy لا سجل الجسر**، وهذا هو بيت القصيد: المهم أن الرسم غائب، لا
    من أنزله. وسجل الجسر لا يصف إلا ما فعله الجسر بنفسه — فالغياب الذي يقع من
    خارجه كان سيمرّ بلا أثر.
  - **والمجموعة مُعلَنة سلفًا**: القالب الموسوم `longRunning` في `templates.json`
    هو بالضبط ما لا يُنتظر نزوله — واللوغو والشريط موسومان به فعلًا. وإعدادٌ
    جديد يعيد سردهما كان سيصير موضعًا ثانيًا يُنسى.
  - **ولا يتكلّم قبل التأكّد**: القالب الدائم ينزل مشروعًا للثواني التي يستغرقها
    استبدال، وتنبيهٌ عند كل لحظة كهذه تنبيهٌ لا يُقرأ. **«فحوص تأكيد الغياب»**
    (`MissingGraphicConfirmChecks`، فحصان).
  - **ويقولها مرة**، ثم مرة أخرى **حين يعود**. إنذارٌ متكرر عن حالة يعالجها أحدهم
    ضجيج.
  - **وانقطاع القراءة ليس غيابًا**: طبقةٌ لم يُجب المحرّك عنها لا تقول شيئًا عمّا
    عليها، فتُترك.
  - الإعداد: **«تنبيه غياب قالب دائم»** (`NotifyAdminsOnMissingGraphic`، مفعّل)،
    في قسم 🔔 الإشعارات.
  - ولا يكلّف نداءً إضافيًّا: يستعمل مسح الطبقات الذي يجريه فحص Cinegy الدوري.
- **وسجل التشغيل صار يقول من أين أُخذ زمن الموجز.** كان يسجّل رفض مرساة المحرّك
  ولا يسجّل قبولها، فـ«ارتُبط بساعة Cinegy» و«لم يعطِ المحرّك شيئًا فعُدنا لساعة
  الجسر» يبدوان سواءً من الخارج. ميزةٌ دعواها الدقة بالملّي ثانية يجب أن تقول ما
  فعلته.

## 7.61.0 — 2026-09-04

- **الاحتفاظ بصور الموجز صار ٤٨ ساعة** بدل ٢٤ (`MojazImageKeepHours`). وهذا
  تغيير للافتراضي فقط: النسخة العاملة تحمل القيمة في `config.json` بالفعل،
  فتُغيَّر من ⚙️ **الإعدادات ← 🔴 التشغيل على الهواء ← «الاحتفاظ بصور الموجز»**،
  أو بـ«إعادة إلى الافتراضي» بعد الترقية.
- **زمن الموجز صار مضبوطًا على ساعة المحرّك.** بحثٌ سبقه: Cinegy لا يكشف عدّاد
  لفّات ولا موضع فريم — ثلاث نقاط فقط تُجيب على هذا التركيب (`/metrics`
  و`/gfx_N/status` و`/gfx_N/status/active`) وما عداها ٤٠٣، و`/metrics` صحّة
  قناة لا موضع مشهد. لكن العنصر النشط يحمل `ScheduledAt` بدقة الملّي ثانية،
  **ولا تتحرك ما دام المشهد يلفّ** — فصارت هي صفر الزمن بدل ساعة إيقاف تبدأ
  لحظة عودة أمر `SHOW`، أي بعد رحلة الشبكة وارتجاف حلقة الاستطلاع.
  - ويُرفض الإرساء إن لم يكن مصدَّقًا: سالبٌ يعني ساعتين متخالفتين، وكبيرٌ يعني
    أن الطبقة تحمل شيئًا آخر. والرفض يعني ساعة الجسر كما كان دائمًا — فمرساة
    خاطئة تُخرج كل كتابة من الفيضة، وذلك أسوأ من الارتجاف الذي جاءت لتزيله.
  - والمرساة تُحفظ مع التشغيل، فالموجز المستأنف بعد إعادة تشغيل **يستعيد طور
    اللوب الحقيقي** بدل تقريبه. الإعداد: **«ضبط الزمن من Cinegy»**
    (`MojazAnchorToAirClock`، مفعّل).
- **وأساس شاشة الصف المولَّدة**: ما يُسأل عنه الصف صار يأتي من التصميم — ترتيب
  السجل التحريري أولًا (العنوان قبل الخبر)، والأسماء العربية من `fields`،
  والمتغيّر الذي لا يستهلكه عنصر يُستبعَد لأن ملأه يضع القيمة في لا مكان. وقراءة
  قيمة الصف تجرّب القاموس ثم الحقول الثلاثة القديمة، فنشرة كُتبت قبل التصاميم
  تُشغَّل عليها بلا إعادة كتابة.

## 7.60.0 — 2026-09-04

- **«🎬 مزامنة الظهور» صار مفهومًا من الشاشة، لا من التجربة على الهواء.** الزر
  كان يقول «نعم/لا» ولا شيء غير ذلك، وهو خيار يغيّر **إيقاع النشرة كلها**:
  - الزر صار يقول أثرها: «نعم · الإيقاع من اللوب» أو «لا · الإيقاع من المدة».
  - وسطر الخطة يقول ثمنها صراحة حين تكون مفعّلة، ومعه طول اللوب بالثواني —
    فمن يضبط المدة على ٨ ثوانٍ ولوبُه دقيقة يعرف **من الشاشة** لماذا يبقى كل
    خبر دقيقة، بدل أن يكتشفه وهو على الهواء.
  - وباب **📑 الموجز** في المساعدة يشرحها: المشهد يلفّ، وعند اللفّ يختفي
    المحتوى ويعود؛ المطفأة تكتب الخبر في وقته وقد يُرى وهو يتبدّل، والمفعّلة
    تكتبه داخل تلك الفيضة فيظهر كأنه دخل لتوّه — وثمنها أن الإيقاع يصير طول
    اللوب، ويُغيَّر بقصّ `LoopEndFrame` في Titler.
- **أول خطوات المعالج**، خلف `MojazMultiDesign` المطفأ: عند إنشاء موجز جديد
  يُسأل عن التصميم **قبل** الاسم، لأنه هو ما يحدّد عمّ سيُسأل الصف أصلًا.
  - والسؤال يُطرح **فقط حين يكون هناك اختيار**: تصميم واحد ليس اختيارًا،
    والخيار مطفأ يعني أن المدمج هو التصميم الوحيد.
  - والقالب لا يصير تصميمًا بالتخمين بل بإعلانه (`bulletin: true`) — فليس كل
    قالب فيه لوب وحقل نص موجزًا.
  - وكل تصميم يُقرأ من مشهده: كم حقل وسائط وكم نص، وهل يمشي في عدة أخبار أم
    يحمل خبرًا واحدًا. وتصميمٌ معلَنٌ لا يمكن قراءته يبقى في القائمة **ومعه
    سببه**، لأن من أعلنه ولم يجده يستحق أن يُخبَر لماذا.
  - وتبديل تصميم موجز **على الهواء** مرفوض: الصفوف الجارية كانت ستُكتب في
    متغيّرات لم يسمع بها التصميم الجديد.
- **وعلّة في دالة كتبتُها للتوّ**: `Set-MojazBulletinDesign` كانت تُرجع قيمتين.
  `Send-TelegramMessage` تُخرج قيمة، وسطرٌ بلا `| Out-Null` يضعها في مخرجات
  الدالة — فـ`return $false` كان يعني في الحقيقة `$true, $false`، والمستدعي
  يقرأ الأولى. الفخ موثّق في `AGENTS.md` ووقعتُ فيه.

## 7.59.0 — 2026-09-04

- **موجز على الهواء صار ينجو من إعادة تشغيل الجسر.** بلاغ: النشرة الأخيرة بقيت
  تلفّ عند الخبر الثامن واضطُرّ المشغّل لإخراجها يدويًّا. والسجل يقول السبب، وهو
  ليس الخبر الثامن:

  ```
  11:08:49  Mojaz playback started (10 rows, 750 frames each)
  11:12:31  Started after stopping 1 running instance(s) on request.
  11:12:35  Bridge v7.58.0 starting
  ```

  أُعيد التشغيل بعد ٣ دقائق و٤٢ ثانية من تشغيل مدته خمس دقائق — أي عند الصف
  الثامن عند ٣٠ ثانية للصف. وCinegy لا يعرف شيئًا عن ذلك، فبقي المشهد يلفّ كما
  هو؛ والذي مات هو الخطة التي تكتب الصفوف وترسل أمر الخروج.

  وكل ما يحمله الجسر كان ينجو من إعادة التشغيل — ما على الهواء، ومؤقتات
  الإخفاء، وتنبيهات القوالب، والجدولة — **وتشغيل الموجز وحده كان الاستثناء**.
  صار يُكتب في `logs/mojaz-playback.json` عند البدء ويُحذف عند الانتهاء:
  - **ما زال في وقته** ← يُستأنف عند الصف الذي تقوله الساعة. خطة التشغيل
    بلحظات مطلقة من البداية، فالزمن المنقضي وحده يحدّد الصف المستحق، والتشغيل
    يعود إلى جدوله لا إلى أوله.
  - **تجاوز وقت خروجه** ← يُخرَج، بدل مشهد يلفّ بلا من ينهيه.
  - **الطبقة لم تعد للموجز** ← لا يُلمس شيء: أحدهم عالجها والجسر نائم، والتصرّف
    هناك يُخرج ما حلّ محلّها.
  - والمشرفون يُخبَرون في الحالتين.
- **مدة بقاء يضعها المشغّل** (`HoldFrames`): من لحظة ظهور المشهد حتى الخروج.
  التقرير المصوَّر خبرٌ واحد لا قائمة، فلا شيء يمشي فيه؛ وطلبُ التعبير عن «أبقه
  أربعين ثانية» بمدة صف زائد مقدمة زائد بقاء الصف الأخير هو طلب حسابٍ ليقول
  المرء شيئًا بسيطًا. صفر = التوقيت بالصفوف كما هو، وهو حال كل موجزاتك اليوم.
  ولا يُجدوَل صف بعد لحظة الخروج: كتابةٌ تصل بعد أمر الخروج تكتب في لا شيء.
- **والمدة تتبع المقطع** (`MojazHoldFollowsClip`، مفعّل): مقطعٌ يعرف طوله يقول
  كم يبقى، ما لم يكتب المشغّل مدة — فما يكتبه لا يُراجَع أبدًا. ومدةٌ يتعذّر
  إثباتها تعود صفرًا أي «توقيت المشغّل هو الحاكم»: رقم خاطئ هنا يُخرج الموجز
  مبكرًا، فرفض التخمين هو الفشل الآمن الوحيد.
- **وأساسٌ لتعدد التصاميم، خلف خيار مطفأ** (`MojazMultiDesign`): الجسر صار يقرأ
  حقول التصميم من ملف المشهد نفسه — الاسم والنوع والعنصر المستهلك ومقاسه
  بالبكسل — بدل وصفها بيد في JSON يفارق المشهد بصمت. ومعه تصحيح قاعدتين
  وضعتُهما وكانتا خاطئتين: لا يُشترط عنوانٌ ونصّ (فتصميم الفيديو-فقط مشروع)،
  ولا يُشترط لوب (فمن يحمل خبرًا واحدًا يدخل ويعرض ويخرج). المعالج والشاشات لم
  تُبنَ بعد.

## 7.58.0 — 2026-09-03

- **📢 التنويهات**: يكتب المشرف نصًّا فيصل إلى من يختار. السجل والتدقيق يقولان
  ما فعله الجسر؛ وهذا يقول ما يحتاج **شخصٌ** أن يعرفه الآخرون — وهو الرسالة
  الوحيدة التي يرسلها البوت دون أن يطلبها أحد، فكل ما فيها محدود:
  - **من يصله**: الجميع، أو المشغّلون وحدهم، أو المشرفون وحدهم. والقائمة تُقرأ
    لحظة الإرسال لا لحظة الكتابة، فيصل من مُنح الوصول صباحًا ولا يصل من سُحب
    وصوله ظهرًا.
  - **كم يعيش**: ساعة أو ٦ أو ٢٤ أو ٧٢، ثم ينتهي وحده.
  - **كم يتكرر**: مرة واحدة، أو كل ١ أو ٣ أو ٦ أو ١٢ ساعة — بالساعات لا
    بالدقائق عمدًا: هذا يقاطع أناسًا يعملون، والتنويه الذي يُلحّ يعلّمهم تجاهل
    ما بعده.
  - **زر «تم الاطلاع»**: يوقف التكرار عمّن ضغطه، وهذا كل الغرض منه. والشاشة
    تقول كم شخصًا اطّلع.
  - **التثبيت**: سطر واحد فوق القائمة الرئيسية لمن لم يطّلع بعد، مقصوص إلى ٨٠
    حرفًا لأنه يجلس على شاشة تُستخدم تحت الضغط.
  - ولا يصل كاتبَه، ولا يُرسَل تكراره داخل ساعات الهدوء — يُؤجَّل إلى أول فحص
    بعدها ولا يسقط.
  - ونصّه كتبه إنسان لا الجسر، فيُهرَّب من HTML كأي نص غير موثوق.
- إعداداته في قسم **🔔 الإشعارات**: تفعيله، وأقصى طول (٥٠٠ حرفًا)، والمدة
  الافتراضية (٢٤ ساعة).

## 7.57.0 — 2026-09-03

- **إصلاح: تقرير الموجزات كان يفشل بالفتح في كل يوم هادئ.** `Measure-Object`
  على مجموعة فارغة يعيد كائنًا بلا `Sum` أصلًا، وStrictMode يرمي عند قراءتها —
  فأي فترة بلا موجز كانت تعطي رسالة خطأ بدل «لم يُشغَّل أي موجز». ورأس ملف
  اختبارات التقارير يحذّر من هذا الفخ بعينه، فأُضيف اختبار انحدار له.
- **وعلّة صامتة معه:** `Get-ReportRecords` لم تكن تُمرّر `operationId` ولا
  `durationMs` رغم أن `Write-AuditRecord` يكتبهما — والتقرير يقرن البداية
  بالنهاية عبر معرّف العملية، فما كان يقرن شيئًا: تشغيلان لنشرة واحدة يندمجان،
  والمدة تضيع.
- **قسم «🔔 الإشعارات والتنبيهات»** في الإعدادات. كل ما يقرّر أن البوت سيكلّم
  أحدًا كان موزّعًا على خمسة أقسام — المراقبة والجدولة والهواء والأخبار والقوالب —
  فمن يسأل «لماذا أيقظني في الثالثة فجرًا» كان يقرأ أربع شاشات. صارت ٢٣ إعدادًا
  في مكان واحد: ساعات الهدوء، وتنبيهات المشرفين الخمسة، والإشعار المسبق
  للجدولة، وإشعارات الموجز، وتنبيه القالب المنسي، والنبض، والملخص الأسبوعي.
- **مفتاحان جديدان:**
  - **«إشعار طلبات الوصول»** (`NotifyAdminsOnAccessRequest`، مفعّل): الطلب
    يُسجَّل في الطابور على كل حال؛ هذا يقرّر فقط أيقاطع أحدًا أم لا.
  - **«إشعار الحظر التلقائي»** (`NotifyAdminsOnBlockedChat`، مفعّل): الحارس كان
    يحظر المحادثة ويكتب سطرًا في السجل ولا شيء غير ذلك — وحظرٌ لا يعلم به أحد لا
    يعرف أحدٌ أن يرفعه. الإشعار للمشرفين فقط؛ **الحظر يبقى صامتًا تجاه المحظور**
    كما كان، ورفضٌ قام به مشرف للتو ليس خبرًا له فلا يُشعَر به.

## 7.56.0 — 2026-09-03

- **قسم «📑 الموجزات» في التقارير**، ولزمه تسجيلٌ جديد: كانت النشرة تصل الهواء
  بوصفها `air_control` عاديًّا لمشهد الموجز — أي «ظهر رسم» ولا شيء عن **أي** موجز
  كان، ولا كم صفًّا حمل، ولا أشغّله إنسان أم جدولة. فصار كل تشغيل يكتب سجلًّا
  دائمًا `mojaz_run` عند البداية وآخر عند النهاية تحت معرّف عملية واحد.
  - التقرير يقرن الاثنين، فالنشرة التي ما زالت على الهواء لها بداية بلا نهاية
    **وتقول ذلك** بدل أن تُحذف أو تُحسب منتهية.
  - والمدة تُؤخذ من ساعة التشغيل نفسها لا من الفارق بين الختمين، فهي الرقم الذي
    تسأل عنه غرفة الأخبار.
  - وجدولٌ من أربعة أعمدة كأخويه — عدد الصفوف يركب مع الاسم، لأن عمودًا خامسًا
    يسرق من اسم النشرة ما يجعلها تُعرف. و🕒 تسبق ما شغّلته الجدولة.
  - وله ملف HTML للتنزيل مثل البقية.
  - **والنهاية تُغلَق من مكانين**: توقّف النشرة نفسها، وأخذُ الطبقة منها بشيء آخر
    (إخفاء الكل مثلًا). ولو أُغلقت من واحد فقط لبقي التشغيل في التقرير «على
    الهواء» إلى الأبد.
- **الفترة التي تقرؤها صارت مُعلَّمة** بنقطة في أزرار التقارير: أربعة أزرار
  متشابهة فوق تقرير لا يكرّر نافذته كانت تترك المشغّل يخمّن أيّها ضغط.
- **تنظيف صور الموجز**: الآلية موجودة وشروطها سليمة — ملفات سمّاها الجسر نفسه
  فقط، داخل مجلد صور الموجز فقط، لا يشير إليها صف محفوظ ولا لقطة تشغيل جارٍ،
  وأقدم من يوم. لكنها كانت تمسح المجلد **في كل دورة استطلاع** وعلى خيط الاستطلاع
  نفسه، لعملٍ كل معناه أن شيئًا لم يلمس تلك الملفات منذ يوم. صارت **كل ساعة**،
  ومدّتها إعداد: **«الاحتفاظ بصور الموجز»** (`MojazImageKeepHours`، ٢٤ ساعة؛
  وصفر يوقف الحذف تمامًا).

## 7.55.0 — 2026-09-03

مراجعة تثبيت بعد موجة الإضافات. فُحصت أربعة محاور آليًّا، وخرجت منها علّة واحدة
منهجية.

- **ثلاث شاشات تُبنى من قوائم تنمو ولا تُصفَّح** — العلّة نفسها التي أوقفت شاشة
  صلاحيات القوالب قبل إصدارين:
  - **📑 مكتبة الموجزات** (صف لكل موجز): عشرة في الصفحة.
  - **🕒 مواعيد الموجزات** (صف لكل موعد): عشرة في الصفحة.
  - **📋 الأحداث القادمة**: **ثلاثة أزرار** لكل حدث، فكانت تمتلئ أسرع من كل ما
    عداها — أسبوعان من حدث يومي يتجاوزان ما ترسله تيليجرام. ثمانية في الصفحة،
    ونصّها صار مُصفّحًا معها بعد أن كان يُرسَل كاملًا فيصل منزوع التنسيق.
- **والأهم: العلّة نفسها أُصلحت أربع مرات في أربع شاشات.** إصلاحها خامسةً باليد
  ليس خطة، فصار **الفحص الآلي يفرض القاعدة**: كل لوحة مفاتيح تدور على مجموعة
  يجب أن تُصفّحها. والشاشة التي لا تنمو مجموعتها تُدرَج في قائمة استثناء **ومعها
  سببها** في سطر واحد؛ وفحصٌ ثانٍ يُسقط أي استثناء يسمّي دالة لم تعد موجودة، فلا
  يتحوّل الاستثناء إلى غطاء لعلّة تالية.
- **وما فُحص فكان سليمًا** (يُذكر ليُعرف أنه فُحص): ١٦٢ إعدادًا كلها مقروءة
  ومسمّاة ومصنّفة بلا إعداد ميت؛ ولا موضع يُدخل نصًّا غير موثوق في HTML بلا
  تهريب؛ ولا فرع نداء يغيّر شيئًا بلا بوابة صلاحية.

## 7.54.0 — 2026-09-03

- **سُحب الادعاء من أصله.** تسمية العنصر المجهول من سجل القوالب (٧.٥٢) ثم تمييزه
  بعلامة صفراء (٧.٥٣) كانا يعالجان العَرَض: البوت ما زال يقول إن هناك موجزًا ولا
  موجز. فحُذفت القاعدة كلها — **لا يُتبنّى عنصر لا يسمّيه المحرّك** — وهذا هو
  الصواب ما دام لا شيء يفرّق بين طبقة خرج منها المشهد وطبقة تعرض.
- **والسجل القديم يُمسح تلقائيًا.** السجل المكتوب في ٢١:١٩ بقي في `onair.json`
  وعاش إعادة التشغيل، فظلّ يدّعي الهواء. صارت المطابقة تُسقط أي سجل **مُكتشَف**
  لا يسمّي المحرّك ما على طبقته. أما ما كتبه الجسر بنفسه فلا يُمسّ: هو معرفة
  الجسر بما فعله، وقراءةٌ لا تستطيع تمثيل الخروج ليست دليلًا ضدّه.
- **ولم يضِع التحكم الذي طُلب أصلًا:** شاشة **🎚 الطبقات** تقرأ Cinegy حيًّا
  وتعرض «🙈 إخفاء» لكل طبقة يقول المحرّك إنها تعمل — بلا حاجة إلى سجل في
  `onair.json`. وهي المكان الصادق لذلك، لأن الجسر لا يستطيع التحقق من الظهور
  بنفسه.

## 7.53.0 — 2026-09-03

- **البوت كتب «موجز على الهواء» ولا شيء على الهواء.** الموجز خرج بـ EXIT في
  ١٨:١٤ وأسقط الجسر سجله وقتها كما ينبغي؛ وعند إعادة التشغيل في ٢١:١٩ تبنّاه
  الاكتشاف الجديد من جديد.
- **والسبب مكتوب في الشيفرة منذ زمن**، في تعليق `Remove-OnAirRecord`: الإخفاء
  يجعل Cinegy يعلّم العنصر بـ `IsEmpty="y"`، أما `EXIT_SCENE_LOOP` فينهي الحركة
  ويترك عنصر القائمة **نشطًا بالمعرّف نفسه بلا أي علامة** — فطبقةٌ أنهت خروجها
  تُقرأ تمامًا كطبقة تعرض الآن. والقياس يؤكده: الطبقة ٥ (موجز خرج) والطبقة ٨
  (شريط ظاهر) متطابقتان في **كل** حقل يكشفه المحرّك — `IsOnAir` و`ActiveId`
  و`LogId` و`ScheduledAt` و`Duration` و`ManualEnd` و`OutputState` — و`/status/active`
  و`/item/<id>` كلاهما يردّ 403 على هذا التركيب.
- **فلا دليل يفصلهما، والادعاء يُصحَّح لا يُلغى:** العنصر بلا اسم يثبت أن مشهدًا
  **محمّل** على الطبقة، لا أن شيئًا يُعرض. صار يُتبنّى — فالمشغّل ما زال يحتاج زر
  تحرير الطبقة — لكن بمصدر `cinegy-unconfirmed`:
  - في القائمة الرئيسية علامته **🟡** لا 🔴.
  - وفي شاشة الحالة: «مشهد محمّل من Cinegy · قد لا يكون ظاهرًا».
  - ومشهدٌ يسمّيه المحرّك بنفسه يبقى `cinegy` كما كان: الاسم من المحرّك دليل عرض.
- **وزر «⏹ إخفاء الموجز» لم يعد يظهر إلا والموجز على الهواء.** كان ثابتًا في
  القائمة — باهتًا حين لا شيء ليُخفى — كي يتعلّم المشغّل مكانه؛ لكن زرًّا يعرض أن
  يُخفي شيئًا بينما لا شيء على الهواء يُقرأ كادّعاء بأن هناك شيئًا، وهو الالتباس
  نفسه الذي وُجد الزر لإنهائه. والطبقة المستنتَجة لا تُحسب موجزًا على الهواء —
  صفّ الإخفاء العادي يحرّرها.

## 7.52.0 — 2026-09-03

- **الاكتشاف الخارجي لم يكن معطّلًا في المراقبة الدورية وحدها — كان ميتًا في كل
  مكان.** `Resolve-BridgeCinegyLayerState` يشترط «دليلًا إيجابيًا» على أن الطبقة
  تعرض شيئًا حقيقيًا، وكان الدليل الوحيد المقبول **اسمًا** يصف المشهد. ومحرّكنا لا
  يسمّي شيئًا مما يعرضه: كل عنصر نشط يعود هكذا
  `<Item Id=… LogId=… ScheduledAt=… Duration=… ManualEnd=…/>` بلا `Name` وبلا
  `Description`. فكانت القاعدة ترفض **كل** طبقة، ولم يُكتشف مشهد خارجي واحد منذ
  كُتبت — لا عند الإقلاع، ولا في ℹ️ الحالة، ولا في 🎚 الطبقات.
- **طريق ثانٍ لا يخترع اسمًا هو الآخر:** دليل صلب على أن عنصرًا حقيقيًا يعمل،
  زائدًا اسمًا يعرفه الجسر أصلًا — القالب المسجَّل لتلك الطبقة. والدليل الصلب:
  مُعرِّف نشط غير صفري، و`LogId` غير صفري، و`ScheduledAt` يُقرأ كتاريخ، وألّا
  يقول المحرّك `IsEmpty="y"`. فإن غاب الدليل، أو لم يطالب بالطبقة قالب واحد
  بعينه — وقالبان يتشاركان طبقةً تهيئةٌ واردة — تُترك الطبقة كما هي. **الغموض لا
  يضيف سجلًا أبدًا.**
- القياس على الهواء وقت الإصلاح يفصل الحالتين فصلًا تامًّا: الطبقات ٥ و٨ و٩ تحمل
  `LogId` بلا `IsEmpty`، والطبقة ٧ — قشرة عنصر انتهى — بلا `LogId` ومعها
  `IsEmpty="y"`. القاعدة الجديدة تتبنّى الثلاث وترفض الرابعة.
- **انقطاعات Telegram الـ٣٨ في أمسية واحدة لم تكن انقطاعًا.** كل واحدة استطلاعٌ
  طويل تجاوز مهلة النقل: `Polling error … HttpClient.Timeout of 25 seconds` ثم
  `connected → disconnected` ثم عودة بعد ثوانٍ. واستطلاعٌ يتأخر جوابه ليس اتصالًا
  مفقودًا — وكان يُسجَّل خطأً ويقلب الحالة ويكلّف مهلةً كاملة من الاستطلاع الميت
  في كل مرة.
  - المهلة الإضافية فوق الاستطلاع صارت إعدادًا: **«مهلة الاستطلاع الإضافية»**
    (`TelegramPollMarginSeconds`، ٢٠ ث بدل عشرٍ ثابتة) — فهي تغطي فتح الاتصال
    ورحلة العودة أيضًا، وعشرُ ثوانٍ فوق استطلاع من خمس عشرة كانت تقطع طلبات حيّة.
  - و**«تأخر الاستطلاع المحتمل»** (`TelegramPollTimeoutTolerance`، مرتان) يحدّد
    كم تأخرًا متتاليًا يُحتمل قبل اعتبار الاتصال مفقودًا. والفشل الحقيقي يسلك
    الطريق القديم من أول مرة، بلا تأخير.

## 7.51.0 — 2026-09-03

- **ما شُغِّل من خارج الجسر صار يظهر فيه.** الشريط الإخباري كان على الهواء فعلًا
  (`gfx_8` يحمل مشهدًا نشطًا) والقائمة لا تذكره. السبب أن الفحص الدوري الوحيد —
  `Update-CinegyStateWatchdog` — كان يُستدعى بلا `-DiscoverExternal` وبلا لوحة
  الطبقات، فيتحقّق من الطبقات التي أرسلها الجسر بنفسه لا غير. الإقلاع كان يكتشف
  الخارجي، وℹ️ الحالة و🎚 الطبقات تكتشفه، **ولا شيء بينها**. صار الفحص الدوري
  يتبنّاه، بإعداد **«تبنّي الطبقات الخارجية»** (`DiscoverExternalLayers`، مفعّل).
  ونشر الأخبار من البوت يكتب ملف الشريط ولا يرسل SHOW — فلم يكن هناك سجل أصلًا.
- **شاشة المستخدمون: سطر واحد لكل شخص.** كانت ترسم لكل مستخدم خمسة أزرار —
  التعطيل والاسم والنشاط والترقية والسحب — فعشرة أشخاص خمسون صفًّا، والبحث عن
  واحد يمرّ فوق أزرار سحبِ تسعة غيره. القائمة الآن تسمّي الناس، والضغط على اسم
  يفتح **بطاقته**: الحالة والدور ورقم المستخدم ومتى أُضيف وبواسطة من وكم يومًا
  صمت، وتحته أزراره وحده. وصفّك أنت مُعلَّم بـ «(أنت)».
- **شاشة صلاحيات القوالب صارت مُصفّحة وعمودين.** سجلٌّ فيه مئة قالب كان يرسم مئة
  صف في رسالة واحدة، وهو ما لا ترسله تيليجرام — فالشاشة التي تحدّد من يستخدم
  قالبًا كانت تتوقف عن الفتح على التركيبات الكبيرة وحدها، أي على التي تحتاجها.
  والتأشير يعيدك إلى صفحتك لا إلى الأولى.
- **برنامج التحكم `BridgeManager`** صار مذكورًا في «ما الجديد» وفي دليل الوكلاء:
  تطبيق Windows Forms يشغّل الجسر ويوقفه ويعيد تشغيله، ويتابع `bridge.log` حيًّا،
  ويحرّر الإعدادات في نافذة بدل تحرير `config.json` بيد.
- **`AGENTS.md`**: دليل من يفتح المستودع بعدنا — طقوس الإصدار، المواضع الأربعة
  لتسجيل أي إعداد، مصائد PowerShell التي كلّفتنا إصدارات، كيف يُكتب الاختبار
  ولماذا `$script:` في ملف الاختبار ليس دائمًا كائن الجسر، وقائمة ما يُراجَع قبل
  قول «تم».

## 7.50.0 — 2026-09-03

خمسة أقفال على بوتٍ يجده الغرباء، ولكلٍّ منها مفتاحه في الإعدادات.

- **رفض الطلب صار يحظر.** كان الرفض يزيل الطلب ولا شيء غير ذلك، فتعود المحادثة نفسها لتطلب بعد ثانية، بلا نهاية — `MaxPendingApprovals` يحدّ طول الطابور لا عدد مرات دخول شخصٍ واحد إليه. الآن يُسجَّل المحظور في `logs/access-guard.json` بسببه وتاريخه، وشاشة **🚫 المحظورون** (من شاشة الطلبات المعلّقة) ترفع الحظر بزر. الإعداد: **«حظر من رُفض طلبه»** (`BlockRejectedRequesters`، مفعّل افتراضيًا).
- **الحظر صامت تجاه المحظور** عن قصد: من يُخبَر أنه محظور يعرف أن البوت يستحق حسابًا ثانيًا. ولهذا شاشة المحظورين هي السجل الوحيد للمشرف، فتحمل السبب والتاريخ ومن حظر.
- **رمز الانضمام** (`JoinSecret`، فارغ افتراضيًا = معطّل). حين يُضبَط، يُسأل الغريب عنه **قبل** أن يصل طلبه إلى أي مشرف. الرمز الخاطئ يُعدّ ولا يُشرح، وبعد **«محاولات رمز الانضمام»** (`JoinSecretMaxAttempts`، ٣) في اليوم تُحظر المحادثة بلا رسالة — رفضٌ يخبرك كم اقتربت هو لعبة تخمين.
  - الرمز يُسأل مرة واحدة: من نجح لا يُسأل مجددًا في كل رسالة بينما ينتظر طلبه.
  - اسم الإعداد يحوي `Secret`، فيُعرض ويُسجَّل بطوله لا بنصّه — في السجل وفي التدقيق ورسالة التأكيد. وأرسل `-` لمسحه.
- **حدّ يومي للطلبات** من المحادثة الواحدة: **«طلبات الوصول يوميًا»** (`MaxAccessRequestsPerDay`، ٣؛ صفر يعطّله). النافذة تبدأ من أول محاولة وتُستأنف بعد ٢٤ ساعة لا تنزلق — عدّادٌ لا يُصفَّر يحوّل عصرًا مزدحمًا إلى رفضٍ دائم لم يخترْه أحد.
- **بلاغ الخمول.** **«أيام خمول المستخدم»** (`DormantUserDays`، ٦٠؛ صفر يعطّله) يُرسل للمشرفين مرة كل ٢٤ ساعة قائمةً بمن صمت، ومعها عدد أيام صمت كلٍّ منهم. و**«تعطيل الخامل تلقائيًا»** (`AutoDisableDormantUsers`، مطفأ افتراضيًا) يعطّله فعلًا. المالك لا يُدرج أبدًا: الحساب الذي لا يُقفَل عليه هو ما يجب ألّا يجرفه مؤقّت. ومن لا تاريخ له لا يُحسب — غياب السجل ليس دليل غياب.
- **مغادرة المجموعات المجهولة** (`LeaveUnknownGroups`، مفعّل افتراضيًا). كان البوت إن أُضيف إلى مجموعة يجلس فيها يتجاهل كل رسالة — حاضرًا وصامتًا ويقرؤه من أضافه. صار يغادر مرة واحدة ويخبر المشرفين أين كان. ومجموعة أدرجها أحدهم في `AllowedChatIds` ليست مجهولة فلا تُغادَر.
- `BlockRejectedRequesters` و`LeaveUnknownGroups` من الإعدادات المحمية: إطفاؤهما يطلب تأكيدًا.

## 7.49.0 — 2026-09-03

- **زر «👁 معاينة النص كاملًا»** في شاشة الموجز: كل صف بعنوانه ونصه **بلا اقتصاص**، ومعه الصورة التي سيعرضها فعلًا بعد حساب «يتبع السابق».
- **الجدول يبقى جدولًا.** اقتصاصه إلى ٢٤ حرفًا للعنوان و٦٠ للنص هو ما يجعله أربعة أعمدة تصطفّ على شاشة هاتف — لكنه كان المكان الوحيد الذي يظهر فيه النص، فلم يكن المحرّر يستطيع قراءة ما كتبه ليتأكد منه. صار لكلٍّ دوره: الجدول للمسح السريع، والمعاينة للقراءة.
- المعاينة **مُصفّحة**: عشرة صفوف بأربعمئة حرف تتجاوز ما تقبله تيليجرام في رسالة واحدة، والنشرة الطويلة هي بالضبط ما يستحق أن يُقرأ قبل بثّه.
- النص يُهرَّب من وسوم HTML: قوسٌ في خبر لا يكسر الشاشة.

## 7.48.0 — 2026-09-03

- **مدة الصف صارت بالإطارات كالتوقيتَين الآخرين**، فوحدةٌ واحدة تصف الموجز كله بدل ثوانٍ هنا وإطارات هناك. زر ⏱ يقبل إطارات ويعرض ما يعادلها بالثواني، وإعداد **«إطارات صف الموجز»** (`MojazRowFrames`، ٢٠٠ إطارًا = ٨ ث عند ٢٥) يحمل الافتراضي لكل موجز جديد ولكل موجز لم يُحدَّد له رقم.
- موجزٌ يحمل `DelaySeconds` من قبل يُقرأ ويُحوَّل بمعدل الإطارات، والترحيل من `mojaz.json` يحوّل مدده الثلاث كذلك — فلا يضيع ضبط سابق.
- **طالب الوصول يُسأل عن اسمه**، ويصير ذلك الاسم **alias** له لحظة الموافقة، فلا يكتبه المشرف يدويًا: السجل وكل سطر تدقيق يقرآنه من الدقيقة الأولى.
  - **السؤال بعد إبلاغ المشرفين لا قبله**: من لا يجيب يبقى طلبه في الطابور، وهذا هو الغرض منه. وما يرسله يحلّ محلّ معرّف تيليجرام في الاسم المعروض.
  - وهذا هو المسار الوحيد الذي تصل إليه محادثة بلا صلاحية، فهو **يتحقّق بنفسه** أن المحادثة في الطابور فعلًا، ويقصّ النص إلى ٦٠ حرفًا ويُسطّح ما يكسر سطر السجل.
  - **اسمٌ اختاره المشرف من قبل لا يُدهس** — من اختاره اختاره عن قصد.
  - إعداد **«سؤال طالب الوصول عن اسمه»** (`AskRequesterName`، مفعّل افتراضيًا).

## 7.47.0 — 2026-09-03

- **إشعار حين ينتهي الموجز وحده ويخرج عن الهواء.** كل الطرق الأخرى لإنهائه تقول ذلك أصلًا — ضغطة زر، أو عاجل يسحبه، أو صف يفشل — أما الانتهاء الطبيعي فيقع بعد دقائق من انصراف المحرّر عن الشاشة، ولم يكن يقول شيئًا.
- **وتنبيه قبل بدء موجز مجدول** بدقيقة افتراضيًا. يُرسل **مرة واحدة** ويُعلَّم في سجل الموعد، لأن النبضة تعمل كل ثانية والموعد يظل داخل نافذة التنبيه طوال الدقيقة التي تسبقه.
- **مفتاح لكل إشعار**، لا مفتاح واحد للجميع: «إشعار انتهاء الموجز» (`MojazNotifyOnFinish`) و«تنبيه قبل الموعد» (`MojazScheduleNoticeSeconds`، صفر يوقفه).

## 7.46.0 — 2026-09-03

- **توقيت الموجز صار بالإطارات، لأن الحركة مقطوعة بالإطارات لا بالثواني.** زرّا **⏩ الأول** و**⏹ الأخير** يقبلان عدد إطارات ويعرضان ما يعادلها بالثواني ومعدل إطارات المشهد.
- **وزال التقريب إلى ثانية كاملة** — وكان يضيف ما يقارب ثانية كاملة إلى نشرة قصيرة: دخول 30 إطارًا كان يصير 2 ث وهو 1.2 ث، وخروج 168 إطارًا كان يصير 7 ث وهو 6.72 ث. الآن تُستعمل كما قُطعت، واللحظات كسور تُحسب عليها الساعة.
- **إعدادان للمدد الافتراضية** كما طلبت: **«إطارات الصف الأول»** (`MojazIntroExtraFrames`) و**«إطارات الصف الأخير»** (`MojazLastRowFrames`). الترتيب: رقم الموجز نفسه، ثم هذان الإعدادان، ثم المشهد — فالإعداد «افتراضي» يعلو على المشهد وإلا لما عمل أبدًا مع مشهد يمكن قراءته.
- **«معدل إطارات القناة»** (`BroadcastFps`): **25** افتراضيًا أو 50 أو 60، يُستعمل حين لا يذكر المشهد معدله. المشهد أولى بنفسه حين يذكره — فهو الشيء الموقوت ولا يخطئ في وصف ذاته.
- موجزٌ يحمل توقيتًا بالثواني من قبل يُقرأ كما هو ويُحوَّل بمعدل الإطارات، فلا يضيع ضبطٌ سابق.
- حلّ `MojazIntroExtraFrames` محلّ `MojazIntroExtraSeconds`؛ مفتاحٌ قديم باقٍ في `config.json` لا يضرّ ولا يُقرأ.

## 7.45.0 — 2026-09-03

- **قائمة القوالب تُفلتَر بصلاحية من يفتحها.** قالبٌ لا يملك المستخدم عرضه لا يظهر له أصلًا، بدل أن يُعرض ثم يُرفض عند الضغط — فقائمةٌ تعرض ما سيُرفض تُعلّم الناس أن يضغطوا ليروا، وهو نقيض الصلاحية. تشمل الفلترة البحث والتصنيفات والعرض المؤقّت وتحديث الحقول والجدولة، فلا باب خلفيًّا إلى قالب محميّ.
- **الطبقات ليست لكل مستخدم.** إعداد **«من يرى زر الطبقات»** (`LayersScreenAccess`): **الجميع** افتراضيًا، أو **المشرفون**، أو **المالك**. شاشة الطبقات أدوات خام — إخفاء وخروج وعرض على رقم طبقة — وغرفة أخبار قد تريدها لمن يملك الرندَون وحده.
- **والزرّ المخفيّ ليس بابًا مغلقًا:** الاستدعاء نفسه يرفض كذلك، لأن من يحتفظ بالزر القديم في محادثته يستطيع ضغطه.
- استدعاء قائمة القوالب بلا مستخدم لا يُفلتر شيئًا: تلك رؤية السجل لنفسه، لا شخصٌ يختار ما يعرضه.

## 7.44.0 — 2026-09-03

- **من يريد إخفاء قالب يرى نصّه أولًا.** شاشة تأكيد الإخفاء والخروج كانت تقول الطبقة واسم القالب ومنذ متى ومن أرسله — أما ما يقوله الغرافيك فلا. ورقم الطبقة ليس شيئًا يُطابقه المخرج بالشاشة تحت الضغط، أما الكلمات فنعم. أُضيف سطر «النص: …».
- **الحقول الحسّاسة تُذكر بأسمائها دون قيمها** (`•••`): الغرض أن يتعرّف المخرج على ما هو معروض، والاسم يكفي لذلك حيث تكون القيمة تسريبًا.
- **حقل مستقلّ عن سجل التدقيق.** النص المعروض يُحفظ في `ScreenCopy` لا في `AirCopy`: الثاني محكوم بإعداد `AuditTemplateValues` لأن `audit.jsonl` يُؤرشف إلى الأبد، أما هذا فيُقرأ مرة على شاشة تأكيد ويزول مع السجل. ربط أحدهما بالآخر كان سيجعل خيارًا يُطفئ الآخر بلا سبب.
- قالبٌ عُرض قبل تفعيل الخيار أو من خارج الجسر يقول «غير مسجّل» بدل أن يبدو كأن الطبقة فارغة.
- إعداد **«عرض النص قبل الإخفاء»** (`ShowOnAirTextOnRemoval`، مفعّل افتراضيًا).

## 7.43.0 — 2026-09-03

- **«طبقات للمشرفين» و«طبقات للمالك» تُختار بالضغط كذلك.** كل طبقة يعرفها الجسر تظهر **باسمها** — «9 · الشعار» لا «9» — لأن المشرف يحمي الشعار لا رقمًا؛ والمخزَّن هو الرقم، والمعروض هو الاسم.
- **منتقٍ واحد للأربعة.** ما كتبته للقوالب في الإصدار السابق صار عامًّا يخدم القوالب والطبقات معًا (`$script:SettingPickers`)، والفرق الوحيد بينهما هو من أين تأتي القائمة: سجل القوالب أو الطبقات المعروفة. نسخةٌ ثانية من المنتقي كانت ستفترق عن الأولى بعد أول تعديل.
- الحفظ يمرّ بالمسار نفسه في الحالتين، فالسجل والتدقيق لا يفرّقان بين قيمة كُتبت وقيمة اختيرت.

## 7.42.0 — 2026-09-03

- **«قوالب للمشرفين» و«قوالب للمالك» تُختار من سجل القوالب بالضغط**، بدل كتابة الأسماء. كل قالب سطرٌ يحمل ✅ إن كان محدَّدًا و⬜ إن كان متاحًا للجميع، والضغط يقلبه، ومعها زر لإفراغ القائمة دفعة واحدة.
- **السبب ليس الراحة:** اسمٌ مكتوب خطأً يُقرأ «ليس في القائمة»، فتحمي الصلاحية لا شيء وتظن أنك حميت. المُنتقى من السجل لا يُكتب خطأً أصلًا.
- الحفظ يمرّ بالمسار نفسه الذي يمرّ به الإدخال الكتابي، فالسجل والتدقيق يقرآن سواء بأيّ الطريقين ضُبطت القيمة.
- «طبقات للمشرفين» و«طبقات للمالك» تبقى أرقامًا تُكتب — رقم الطبقة قصير ولا يُخطئ فيه أحد كما يُخطئ في اسم عربي.

## 7.41.0 — 2026-09-03

- **«تشغيل لاحقًا» في الموجز صار يُسأل كما تُسأل جدولة القوالب**: أزرار **⏱ +15 · +30 · +60** و**📅 اختر من التقويم** ثم يوم ← ساعة ← دقيقة، بدل كتابة الوقت وحدها.
- **الكتابة باقية**: `+30` و`بعد 90` و`21:45` و`غدًا 07:00` تعمل كما كانت. الأزرار طريق آخر للدخول لا بديل عنه.
- **لوحة واحدة لا اثنتان.** الأزرار والتقويم هي `Get-ScheduleTimePromptKeyboard` نفسها التي تستعملها القوالب، والاختيار يمرّ بالمحلّل ذاته — فلا يُنتج المنتقي لحظةً كان المحلّل ليرفضها، ولا تفترق طريقتان في سؤال واحد.
- الفرق الوحيد بين المسارين هو أين تذهب اللحظة بعد اختيارها، وذلك في `Set-BridgeChosenMoment` وحدها.

## 7.40.0 — 2026-09-03

- **باب جديد في دليل المساعدة: «📑 الموجز»**، متاح لكل المحرّرين لا للمشرفين وحدهم — فكتابة النشرة عمل المحرّر. يشرح المكتبة وتسمية الموجزات ونسخها وحذفها، وإضافة الصفوف وتعديلها وترتيبها، و**أوضاع الصورة الثلاثة** بما فيها «يتبع السابق» الذي يجعل صورةً واحدة تخدم عدة صفوف، وفحص الصور وقياسها، والتوقيت و«مزامنة الظهور» وأن اللوب يُقصَّر من Titler لا من الشاشة، والمواعيد وأنها تشغّل آخر نسخة محفوظة، وأولوية العاجل، وزر إخفاء الموجز.
- **باب الإعدادات صار يشرح صلاحيات العرض والإخفاء:** القوائم الأربعة، وأن الفارغ يعني للجميع، وأن الأشدّ يفوز حين تنطبق قاعدتان، وأن الإخفاء الآلي لا يُمنع أبدًا.
- **واختبارات تُبقي الدليل صادقًا:** تتحقّق من وجود الباب ومن ذكره ما لا يُخمَّن من الأزرار — الأوضاع الثلاثة، والمزامنة، وأولوية العاجل، و«آخر نسخة محفوظة» — فلا يشرح الدليل ميزةً تغيّرت تحته.

## 7.39.1 — 2026-09-03

- **إصلاح: إعدادات الصلاحيات الأربعة لم تكن مسجّلة في `DefaultSettings`.** الكود يقرؤها في مسارَي العرض والإخفاء، لكنها بلا قيمة ولا اسم ولا موضع في شاشة الإعدادات — فكانت الميزة معطّلة صامتة، واختباراتها تمرّ لأنها تضع القيم في الذاكرة مباشرة. سُجّلت الآن في المواضع الأربعة: القيم الافتراضية، والشروح، والتصنيف `templates`، والأسماء العربية.
- **وحارس يمنع تكرار الصنف:** اختبار يمسح كل `Get-Setting`/`Get-SettingInt` في `Parts` و`Modules` والملف الرئيس — ١٤٣ اسمًا — ويرفض أيّ اسم يقرؤه الكود ولا يُعلنه الجدول. تحقّقت أنه يسقط فعلًا لو غابت الأربعة، لا أنه يمرّ فحسب.

## 7.39.0 — 2026-09-03

- **صلاحية العرض والإخفاء لكل قالب على حدة.** أربعة إعدادات جديدة، كلها فارغة افتراضيًا — أي **للجميع**، كما كان الجسر قبلها:
  - **«قوالب للمشرفين»** (`AdminOnlyTemplateKeys`) و**«قوالب للمالك»** (`OwnerOnlyTemplateKeys`).
  - **«طبقات للمشرفين»** (`AdminOnlyLayers`) و**«طبقات للمالك»** (`OwnerOnlyLayers`) — فالشعار محميّ بمكانه كما هو محميّ باسمه، أيًّا كان القالب الذي يُوضع عليه.
- **الأشدّ يفوز حين تنطبق قاعدتان.** قالبٌ للمشرفين على طبقةٍ للمالك يصير للمالك: صلاحيةٌ تتراخى بإضافة قاعدة ثانية ليست صلاحية.
- **الإخفاء محكوم كالعرض**، فطلبك كان لكليهما: إخراج غرافيك محميّ من الهواء تغييرٌ للشاشة كإدخاله.
- **لكن الإخفاء الآلي لا يُمنع أبدًا** — مؤقّت الإخفاء، «إخفاء الكل»، وخروج الموجز من تلقائه. المنع يقع حيث ضغط إنسانٌ زرًّا فقط: رفض عرضٍ يعني ألّا يخرج شيء إلى الهواء، أما رفض إخفاءٍ فيعني بقاء غرافيك على الهواء — وهو أسوأ مما تحميه الصلاحية.
- القوائم تُكتب كبقية قوائم الجسر: مفصولة بفواصل أو أسطر، ولا تُفرّق بين حالات الأحرف.

## 7.38.0 — 2026-09-03

- **مقاس صورة الصف صار إعدادًا: `MojazImageWidth`=538 و`MojazImageHeight`=303.** حجم اللوحة في المشهد `525.38;291.61` بوحدات المشهد، بينما التصدير الفعلي من Titler **538×303** — والرقم المقيس أوثق من المحسوب.
- **صفرٌ في أيّهما يعيد القراءة من لوحة القالب** كما كانت، فتغيير اللوحة في Titler يظل يسري لمن لا يضبط الإعداد.
- الصور المرفوعة قبل هذا الإصدار قيست 525×292؛ فرقها صغير وسيمطّها Cinegy قليلًا. أعِد رفع ما يهمّك منها ليخرج بالمقاس الصحيح تمامًا.

## 7.37.1 — 2026-09-03

- **الشريط الإخباري يبقى على الهواء أثناء الموجز**: صار `MojazHidesTicker` مطفأً افتراضيًا بعد أن جرّبته الغرفة واختارت إبقاء الشريط. السلوك نفسه كما هو ويُشغَّل من ⚙️ الإعدادات ← «إخفاء الشريط أثناء الموجز» متى أُريد.
- التغيير في الافتراضي وحده: إعدادٌ محفوظ سابقًا يبقى على قيمته حتى يُغيّره المستخدم من الشاشة.

## 7.37.0 — 2026-09-03

- **شريط الأخبار يقف للموجز ثم يعود.** الاثنان يتقاسمان أسفل الشاشة، فعند بدء الموجز يخرج الشريط بأمر EXIT — بحركة خروجه لا بقطع — ويعود بعد انتهاء الموجز. اللوغو لا يُمسّ.
- **العودة بعد الخروج لا في أثنائه:** المشهد ما زال يعرض حركة خروجه لثوانٍ، وإعادة الشريط تحته تضع الاثنين على الشاشة معًا — وهو ما وُجد الإخفاء ليمنعه. فالموعد يُحجز بطول حركة الخروج المقروءة من القالب، والنبضة تنفّذه؛ وهذا يُبعد الإعادة أيضًا عن مكدّس ما أنهى الموجز، فلا عرضٌ داخل عرض.
- **لا يُعاد شريط لم يكن على الهواء أصلًا.** يُسجَّل فقط ما أخرجه الموجز بنفسه، وإلا لصار الموجز يقرّر شكل القناة بدل المخرج.
- ينطبق على كل نهاية: انتهاء طبيعي، أو إيقاف يدوي، أو سحب الطبقة من القائمة، أو **سحب العاجل للموجز** — فالشريط يعود في الحالات كلها.
- إعداد جديد **«إخفاء الشريط أثناء الموجز»** (`MojazHidesTicker`، مفعّل افتراضيًا) لإيقاف السلوك كله.

## 7.36.0 — 2026-09-03

- **الأولوية للعاجل، بلا استثناء.** أي شيء يضع العاجل على الهواء — زر، موعد مجدول، تراجع — يسحب الموجز الجاري أولًا ويُعلم المُرسِل. القاعدة موضوعة في خط العرض نفسه لا خلف زر، لأن كل عرض يمرّ به: فعاجلٌ آلي لا ينتظر أحدًا ولا يُحجب.
- **ويُسأل المستخدم قبل ذلك، لا أثناءه.** لحظة خروج العاجل ليست وقت سؤال؛ فالسؤال عند تأكيد الإرسال:
  - **الموجز يعمل والعاجل جاهز** → «🚨 الآن — يخرج الموجز» أو «⏳ بعد انتهاء الموجز». والمؤجَّل يخرج وحده فور انتهاء النشرة، بلا ضغطة أخرى.
  - **العاجل على الهواء والموجز يُراد تشغيله** → «⏳ بعد خروج العاجل» أو «▶️ ابدأ الآن رغم العاجل». والانتظار يُحجز موعدًا عاديًّا مستحقًّا الآن، فيبقى بعد إعادة التشغيل ويظهر في شاشة المواعيد ويُلغى منها كأيّ موعد.
- **الموعد المجدول ينتظر العاجل ولا يسقط:** الطابور كان يؤجّل لموجزٍ آخر على الهواء، وصار يؤجّل للعاجل كذلك بسببٍ مذكور (`urgent_on_air`) وإشعار واحد، ويبدأ فور خلوّ الهواء دون تدخّل.
- التشغيل المجدول يمرّ بـ`-Force`: الفحص جرى في الطابور، ولا أحد ينتظر ليجيب سؤالًا في الثالثة فجرًا.

## 7.35.0 — 2026-09-03

- **الصورة تُفحص وتُقاس قبل أن تصل إلى مجلد المشهد.** صورة الجوال تأتي بعرض ٤٠٠٠ بكسل وبنسبة أبعاد لا تشبه اللوحة، وهذا سببٌ معروف لألا تظهر الصورة على الهواء أصلًا. صار الملف يُنزَّل إلى مجلد مؤقّت أولًا، ثم يُفكّ ترميزه — وهو الفحص نفسه — ويُقاس ويُحفظ **PNG** كصورة القالب الأصلية، ولا يصل إلى `Mojazot` إلا الناتج.
- **المقاس يُقرأ من القالب لا من إعداد:** اللوحة التي تعرض الصورة تُعلن حجمها بنفسها — `<Plate Name="img 01" Size="525.38;291.61" File="${mojaz_img}" />` — فتغيير حجم اللوحة في Titler يغيّر مقاس الصور المرفوعة بلا تعديل هنا.
- **ملء وقصّ من الوسط لا سحب:** صورة ١٦:٩ محشورة في لوحة بنسبة أخرى تُشوّه الوجوه؛ الملء ثم القصّ أهون.
- **ملفٌّ ليس صورة يُرفض** برسالة «هذا الملف ليس صورة يمكن قراءتها»، بدل أن يُحفظ ويصير صفًّا لا يعرض شيئًا. والمؤقّت يُحذف في كل الأحوال.
- **زر «⏹ إخفاء الموجز» صار ثابتًا في القائمة الرئيسية** لا يظهر ويختفي: مكانٌ يُحفظ. لونه أحمر حين يكون الموجز على الهواء فعلًا، وضغطه وهو غير باثٍّ يردّ «الموجز ليس على الهواء».

## 7.34.0 — 2026-09-03

- **زر «⏹ إخفاء الموجز» في القائمة الرئيسية**، يظهر ما دام الموجز على الهواء — سواء كانت نشرة تعمل أو مشهدًا بقي بعد انتهائها. يوقف النشرة ويخرج بأمر EXIT فتعمل حركة الخروج كاملة، بدل القطع الذي يفعله الإخفاء العادي.
- **إصلاح: إخفاء الطبقة كان يترك النشرة تعمل في الخفاء.** أزرار «🔴 إخفاء 5» و«خروج» و«إخفاء الكل» لا تعرف شيئًا عن الموجز، فكانت الطبقة تنزل بينما النشرة تواصل كتابة صفوفها في مشهد لا يراه أحد، ثم ترسل أمر خروج خاصًّا بها بعد دقائق. صار أي إخفاء أو خروج لطبقة الموجز يُنهي النشرة معه ويُعلّم موعدها منتهيًا.
- الحارس في مكان واحد يمرّ به الطريقان معًا (`Invoke-HideLayer` و`Invoke-ExitLayer`)، لا نسخة في كل زرّ.

## 7.33.0 — 2026-09-03

- **التبديل صار مخفيًا خلف حركة الظهور.** المشهد عند التفاف اللوب يُخفي محتواه فورًا (`Opacity` من ١٠٠ إلى صفر) ثم يُظهره خلال ٣٠ إطارًا. زرّ **🎬 مزامنة الظهور** يجعل كل صف يُكتب بُعيد الالتفاف بقليل، فيكون الخبر الجديد موجودًا قبل أن يعود المحتوى مرئيًا — لا يرى المشاهد نصًّا يتبدّل.
- **عند تشغيلها يملك المشهد الإيقاع:** مدّة الصف تصير طول اللوب نفسه، لأن نافذة الإخفاء لا تأتي إلا عند الالتفاف. لتسريع النشرة قصِّر `LoopEndFrame` في Titler — القالب الحالي لوبه ٦٠ ث، والشاشة تحذّر من ذلك بدل أن تُشغّل الصفوف دقيقةً لكلٍّ منها بصمت.
- **الخروج يقع على الالتفاف لا بعده:** أمر EXIT يقفز بالمشهد إلى `LoopEnd` حيث المحتوى ما زال كامل الظهور، فيكون الوصل سلسًا؛ ولو أُرسل بعد الالتفاف بلحظة لعاد المحتوى ظاهرًا ثم اختفى.
- **مواعيد مطلقة وساعة لا تتراجع.** كل صف صار له لحظة تُقاس من بدء الجولة (`Stopwatch`)، لا تأخيرًا يُقاس من الإرسال السابق: كان زمن كل طلب HTTP يتراكم على طول النشرة، والآن الصف المتأخّر يتأخّر وحده.
- **الحلقة تتسارع ما دامت نشرة على الهواء.** `Get-EffectivePollTimeout` كانت تعرف باللقطات والإخفاء والمرحّل ولا تعرف بالموجز، فكان إيقاعه رهينة إعداد `CinegyHealthCheckSeconds`. وعندما تقترب اللحظة ينتظرها الجسر بدقّة بدل أن ينتظر النبضة التالية — فالنافذة ١٫٢ ث والنبضة ثانية.
- **إعدادان للمعايرة على الهواء:** `MojazSyncOffsetMs` (٤٠٠ افتراضًا) كم بعد الالتفاف يُكتب الصف داخل الظهور، و`MojazSyncLeadMs` (١٢٠) كم يُرسل مبكرًا لتعويض زمن الشبكة. العتاد يحتاج مفتاح ضبط؛ عاير وأنت تنظر إلى الهواء.
- الدقّة الواقعية بهذا الطريق **±١٠٠–٢٥٠ مللي ثانية ≈ ٣–٦ إطارات**، وهي داخل نافذة الظهور بمريح. القفل على الإطار تمامًا يحتاج أن يملك المشهد التسلسل كاملًا.
- **إصلاح:** اختبار تقرير الأخبار كان يضع سجلّه «قبل ساعتين» ويطلب تقرير «اليوم»، فينكسر في الساعات الأولى بعد منتصف الليل. صار السجلّ لا يسبق بداية اليوم.

## 7.32.1 — 2026-09-02

- **إصلاح: شاشة تعديل الصف كانت ترفضها تيليجرام.** الضغط على ✏️ لم يُظهر شيئًا على الهاتف وكتب في السجل `400 (Bad Request)` عند كل ضغطة.
- السبب: `@()` تُسطِّح المصفوفات المتداخلة، فلوحة أزرار من صفّين تحتاج فاصلة قبل **كل** صف كما في بقية لوحات الجسر. كانت الفاصلة على الصف الأول وحده، فانسكب الصف الثاني أزرارًا عارية في مكان صفّ — وهو شكل ترفضه واجهة تيليجرام.
- أُضيف حارس في الاختبارات يفحص شكل كل لوحة في شاشات الموجز: كل عنصر في `inline_keyboard` صفٌّ من أزرار، وكل زر يحمل نصًّا وبيان استدعاء. الحارس أمسك محاولة إصلاح أولى كانت خاطئة أيضًا.

## 7.32.0 — 2026-09-02

- **تعديل الصف بدل إعادة كتابته.** زر ✏️ برقم الصف يفتح شاشته: 🖼 الصورة · 📝 العنوان · 📰 النص، ومنها الحذف والعودة للجدول. خطأ مطبعي في الخبر الثالث صار ثلاث ضغطات لا حذفًا وكتابةً من جديد.
- **ثلاثة اختيارات صريحة لصورة كل صف:**
  - 🖼 **صورة خاصة** — يُرسل مسارها إلى المشهد.
  - ↑ **يتبع الصف السابق** — لا يُرسل متغيّر الصورة إطلاقًا، فتبقى الصورة المعروضة كما هي. **هذا ما يجعل صورة واحدة تخدم عدة صفوف:** ضع الصورة في الصف الأول واترك ما بعده على «يتبع».
  - ▫️ **صورة القالب** — يُرسل المسار الذي يعلنه المشهد نفسه (`<Var Name="mojaz_img" Value=".\Mojaz\Pic01.png" />`). كان ناقصًا: من أعطى صفًّا صورة خاصة لم يكن يستطيع العودة إلى صورة القالب إلا بكتابة مسارها من الذاكرة.
- **الجدول يقول ما سيظهر فعلًا.** عمود 🖼 يعرض رمز الوضع لكل صف مع مفتاحٍ تحته، فترى أين تتكرر الصورة وأين تتغيّر بدل أن تستنتجه. والصف الأول الذي «يتبع» يُعرض ▫️ لا ↑ — إذ لا صفَّ فوقه يتبعه، فما يظهر هو صورة القالب.
- **♻️ إعادة استخدام صورة مرفوعة:** عند خطوة الصورة تظهر أزرار بصور هذا الموجز المستخدمة فعلًا، فتُختار بضغطة بدل رفع الملف نفسه مرتين. تُعنون بموضعها في القائمة لأن حمولة الزر ٦٤ بايتًا لا تتّسع لمسار.
- صورة القالب تُقرأ من إعلان المتغيّر في `.cintitle` وتُخزَّن على وقت كتابة الملف كالتوقيت، وتُقرأ مستقلةً عنه فمشهدٌ مؤشرات لوبه غير صالحة يظل يُعطي صورته.
- صورة القالب جزء من لقطة التشغيل أيضًا: صفٌّ على «صورة القالب» يعرض ما كان في المشهد لحظة بدء الجولة، لا ما عُدِّل فيه أثناءها.

## 7.31.0 — 2026-09-02

- **مكتبة موجزات بدل موجز واحد.** احفظ «الصباحي» و«المسائي» وما شئت، لكل واحد صفوفه ومدّته وتوقيته الخاص. شاشة 📑 الموجز صارت قائمة بها كل موجز مع عدد صفوفه ورقم مراجعته وعدد مواعيده القادمة، وتفتح أيًّا منها بضغطة.
- **الصفوف تُحفظ في مكانٍ واحد.** كل تحرير — إضافة صف، حذفه، تحريكه، تغيير المدة — يُكتب في `mojaz-bulletins.json` مباشرةً بكتابة ذرّية مُتحقَّق منها. إن فشلت الكتابة لا يتغيّر شيء في الذاكرة ولا تقول الشاشة إنها حفظت، فما تقرأه بعد إعادة التشغيل هو بالضبط ما رأيته آخر مرة.
- **ترحيل مرّة واحدة بلا فقد:** الجدول القديم في `mojaz.json` يصير أول موجز في المكتبة باسم «الموجز الحالي» بمدده وصفوفه وصوره الموروثة، ولا يُؤرشف الملف القديم إلا بعد نجاح حفظ المكتبة — ففشل الترحيل يُعاد في التشغيل التالي بدل أن يضيع الجدول.
- **إدارة الموجز من شاشته:** ✏️ إعادة تسمية، 📋 نسخة منه (بصفوفها، غير مشتركة مع الأصل)، 🗑 حذفه بعد تأكيد أحمر، و⬆️ ⬇️ لترتيب الصفوف. الاسم المكرَّر مرفوض، والحذف ممنوع ما دام الموجز على الهواء.
- **«تشغيل لاحقًا» صار موعدًا محفوظًا.** كان وعدًا في الذاكرة يضيع مع إعادة التشغيل؛ صار سجلًّا في `mojaz-schedules.json` بحالته (`scheduled` / `queued` / `running` / `completed` / `failed`). شاشة 🕒 المواعيد تعرض كل المواعيد وتلغي أيًّا منها، ولموجز واحد أن يكون له عدة مواعيد.
- **الموعد يستخدم آخر تحديث محفوظ**، لا نسخة الموجز وقت الحجز: عدّل الجدول صباحًا ويعمل الموعد بالمعدَّل مساءً.
- **لا تصادم على الطبقة.** موعد يحين وموجز آخر على الهواء يدخل الانتظار مع إشعار واحد فقط، ويبدأ بعد انتهاء الجاري بترتيب مواعيده الأصلية لا بترتيب ملاحظتها. وموعد كان `running` لحظة إيقاف الجسر يُعلَّم `failed` عند التشغيل التالي بدل أن يُقفل الطابور إلى الأبد.
- **التشغيل معزول عمّا يُحرَّر.** الجولة تعمل من لقطة تُؤخذ عند البدء ولا تقرأ الجدول المحفوظ مرة أخرى، فتعديل الصفوف أو مسحها أثناء البث يغيّر ما سيُعرض في المرة القادمة لا ما هو على الهواء الآن. والتوقيت في اللقطة هو نفسه المكتوب في سطر الخطة، فما يُعرض هو ما وُعد به.
- **تنظيف الصور اليتيمة**، مضبوط عمدًا: فقط الملفات التي سمّاها الجسر بنفسه، داخل مجلد صور الموجز وحده، ولا يذكرها أي صف محفوظ ولا اللقطة الجارية، وبعد مرور يوم — فصورة رُفعت لصفٍّ ما زال يُكتب لا تُسحب من تحته.
- الحذف يمسّ ملفين معًا أو لا يمسّ شيئًا: مواعيد الموجز تُلغى أولًا، وإن فشلت كتابة المكتبة بعدها تُعاد المواعيد كما كانت، فلا يبقى موعد يشير إلى موجز غير موجود.

## 7.30.0 — 2026-09-02

- **توقيت الموجز يُقرأ من المشهد لا من رقم مكتوب باليد.** المشهد نفسه يقول أين يبدأ اللوب وأين ينتهي، وهذه المؤشرات هي بالضبط المدد الثلاث التي تحتاجها النشرة: `0 ← LoopStartFrame` حركة الدخول، `LoopStart ← LoopEnd` الجزء المتكرر، `LoopEnd ← Duration` حركة الخروج.
- بالقالب الحالي (`Fps=25`، `LoopStart=30`، `LoopEnd=1530`، `Duration=1698`): **دخول 1.2 ث · لوب 60 ث · خروج 6.72 ث** — فزيادة الصف الأول صارت ٢ ث والصف الأخير يبقى ٧ ث قبل أمر الخروج، مجبورتين لأعلى حتى لا تُقصّ حركة.
- **إعادة توقيت المشهد في Titler تُعيد توقيت النشرة** دون تعديل شيء هنا: القراءة مخزّنة على مسار الملف ووقت كتابته، فتتحدّث وحدها عند حفظ القالب.
- الشاشة تقول من أين جاءت الأرقام: «من القالب: دخول 1.2 ث · لوب 60 ث · خروج 6.72 ث».
- الترتيب: رقم النشرة إن حُدِّد يدويًا، ثم المشهد، ثم القيم القديمة (إعداد الدخول ونصف المدة) إن تعذّرت قراءة المشهد.

## 7.29.0 — 2026-09-02

- **التوقيتان الخاصان صارا زرَّين في شاشة الموجز.** كانت زيادة الصف الأول في ⚙️ الإعدادات ومدة الصف الأخير نصف المدة دائمًا بلا طريقة لتغييرها. الآن «⏩ الأول: +2 ث» و«⏹ الأخير: 4 ث» بجانب «⏱ المدة»، وكلاهما يُحفظ مع الجدول لا في الإعدادات — نشرة واحدة قد تحتاج توقيتًا خاصًا بها. إرسال 0 يعيد الافتراضي: قيمة الإعدادات للأول، ونصف المدة للأخير.
- **«🕒 تشغيل لاحقًا».** الموعد يُكتب بنفس صيغة شاشة الجدولة ويُقرأ بنفس الدالة — `+30`، `بعد 90`، `21:45`، `غدًا 07:00` — فليست هناك لغتان لقول «متى». الشاشة تعرض «🕒 يبدأ 18:42 — بعد 30 دقيقة» ومعها زر إلغاء، والتشغيل يبدأ من الـtick في موعده.
- الموعد المؤجّل في الذاكرة فقط: جسرٌ يُعاد تشغيله قبل حلوله لا يبدأ نشرة لا أحد ينتظرها. الجدول نفسه يبقى محفوظًا كما كان.
- خطة النشرة في أعلى الشاشة تحسب الإجمالي من الأرقام الثلاثة الفعلية.

## 7.28.1 — 2026-09-02

- **إصلاح عاجل: البوت كان يردّ على كل رسالة نصية بـ«لا يُنتظر منك صورة الآن».** فرع الصور الذي أُضيف في 7.28.0 اختبر وجود الصورة بـ`@(Get-JsonProp $message 'photo').Count -gt 0` — و`@($null)` مصفوفة فيها عنصر واحد، فالعدّ واحد دائمًا. النتيجة: كل رسالة — بما فيها `/بدء` و`/menu` — تدخل فرع الصور وتُبتلع، ولا تُفتح القائمة أبدًا.
- صار الاختبار دالة مسمّاة، `Get-TelegramMessagePhotoId`، تعيد معرّف أكبر مقاس أو نصًّا فارغًا، ومعها اختباران: رسالة بلا صورة لا تُعدّ صورة، ورسالة بصور تعطي أكبرها.

## 7.28.0 — 2026-09-02

- **شاشة «📑 الموجز»: نشرة كاملة من جدول.** المحرر يكتب صفوفًا — صورة، عنوان، خبر — ويحدد مدة الصف، ثم يضغط ▶️ تشغيل. الجدول يُعرض كجدول حقيقي بأربعة أعمدة، وأرقام صفوفه هي أرقام أزرار الحذف تحته.
- **عرض واحد ثم تحديث قيم، لا عرض لكل صف.** مشهد الموجز يدخل خلال أول 30 إطارًا ويدور بين 30 و285 ويخرج على EXIT؛ فإعادة عرضه لكل خبر كانت ستُعيد حركة الدخول وتُومض الشاشة بين الأخبار. الصف الأول `SHOW`، وما بعده `postbox` داخل المشهد الجاري.
- **التوقيت كما طُلب:** الصف الأول يبقى مدته + زيادة حركة الدخول (`MojazIntroExtraSeconds`، افتراضيًا ٢ ث)، وكل صف بعده مدته، والأخير يبقى نصف المدة ثم يُرسَل EXIT فتعمل حركة الخروج كاملة.
- **الصورة تُرفع من الجوال.** صورة أو ملف، تُحفظ في `D:\cingy cg\Mojaz\bot\` — بجانب المشهد لا في مجلد رفع البوت الذي يُكنَس دوريًا، فلا تُحذف صورة ما زالت على الهواء — وتُرسل للقالب كمسار نسبي. صفٌّ بلا صورة يحذف المتغيّر من الإرسال فتبقى صورة القالب بدل أن تُمحى.
- التشغيل يمرّ بخط العرض المعتاد: وضع الصيانة، وسياسة الطبقة، والتحقق الحيّ من Cinegy، وسجل التدقيق — النشرة غرافيك يذهب إلى الهواء كغيره. وفشل أي صف يوقف النشرة ويخرج بدل أن يكمل على العمياء.
- الجدول يُحفظ في `mojaz.json` فيبقى بعد إعادة التشغيل؛ التشغيل الجاري لا يبقى.
- القالب نفسه أُضيف إلى سجل القوالب على **الطبقة 5**، وحقل `mojaz_img` من نوع `File` كما في `mojaz.cintitle`.

## 7.27.0 — 2026-09-02

- **زر 🏠 القائمة كان معطّلًا في سبع شاشات.** الموجّه يعرف `menu` ولا يعرف `menu:main`، وسبع شاشات ترسل الثانية: التشخيص، والبداية السريعة، وشاشتا التقارير، وباب المساعدة، وفهرس المساعدة، و🧾 عملياتي. الضغطة كانت تسقط إلى الفرع الافتراضي فتردّ «خيار غير معروف.» — ثم تعرض القائمة، فيبدو الأمر كأنه نجح مع رسالة خطأ.
- **والأسوأ أنها لم تكن تلغي ما بدأه المشغّل.** فرع `menu` يمسح الحالة المعلّقة؛ الفرع الافتراضي لا. فمن ضغط 🏠 وسط إدخال قيمة بقي داخل الإدخال، وأوّل ما يكتبه بعدها يُبتلع في تدفّق ظنّ أنه خرج منه. زر الخروج كان الزرّ الوحيد الذي لا يُخرج.
- **الفحص الذي وجدها صار اختبارًا:** كل `callback_data` تصدره اللوحات يُقابَل بكل نمط يعالجه الموجّه؛ زرٌّ بلا معالج يسقط الاختبار بدل أن ينتظر من يضغطه.
- تحقّقٌ عرضي من الفحص نفسه: لا لوحة مفاتيح واحدة بلا طريق رجوع، و`menu:refreshstatus` و`news:noop` معالِجان بلا أزرار عمدًا — لوحات قديمة في محادثات قديمة تبقى تعمل.

## 7.26.0 — 2026-09-02

- **شاشتا النسخ كانتا سطرًا فوق أوقات.** «اختر نسخة لمراجعة استعادتها:» ثم أزرار عليها طابع زمني — والطابع الزمني لا يقول ما في النسخة، والضغطة تضع نصّها على الهواء. صارت قائمة: رقم لكل نسخة، ووقتها، وكم خبرًا فيها، ومنذ متى حُفظت. ونسخ الإعدادات مثلها، ومعها ما تتطلبه الاستعادة.
- أرقام الأسطر تطابق أرقام الأزرار في الشاشتين.
- **عيبٌ كامن ظهر أثناء العمل:** سرد نسخ الإعدادات كان مكتوبًا ثلاث مرات بصيغة `$files = if (...) { @(...) } else { @() }`. الأنبوب يفكّ نسخة واحدة إلى ذاتها ولا نسخة إلى `$null`، و`.Count` عليه يتعطّل — على الشاشة الفارغة تحديدًا. صار `Get-ConfigBackupFiles` قارئًا واحدًا للنص وللأزرار وللاستعادة نفسها، واختبار يغطّي غياب المجلّد.
- «منذ 0 دقيقة» صارت «الآن» / «حُفظت الآن».

## 7.25.0 — 2026-09-02

- **قائمة المستخدمين كانت أزرارًا بلا قائمة.** أربعة أزرار لكل شخص — الاسم مكرَّرًا ثلاث مرات — ولا معرّف مستخدم في أي منها: وهو الشيء الوحيد الذي يربط الشخص بسطره في السجل، وبمرجع العملية، وبطلب الوصول الذي وُوفق عليه قبل قليل. عشرة أشخاص كانوا أربعين زرًّا وصفر أسطر.
- صار فوق الأزرار نصّ يُقرأ: حصيلة (كم مستخدمًا، كم بصلاحية إشراف، كم معطّل)، ثم لكل شخص اسمه ودوره وحالته، وتحته معرّفه بخط موحّد العرض وحالة نشاطه.
- أرقام الأسطر تطابق أرقام الأزرار، فالصف الذي يُضغط هو السطر الذي قُرئ.
- **مَن لا اسم تشغيلي له يُقال ذلك عنه** بدل طباعة معرّفه مرتين في سطرين — `Get-UserDisplayName` ترتدّ إلى المعرّف، فكان السطران يقولان الشيء نفسه ويخفيان أنّ أحدًا لم يسمّه.
- الـAlias يكتبه مشرف، فيُهرَّب ويُقصّ مثل أي نص لم يأتِ من هنا.

## 7.24.0 — 2026-09-02

- **قرار أمني كان يُتَّخذ باسم وحده.** شاشة «طلبات الوصول المعلّقة» كانت سطر عنوان ثم أزرار عليها اسم — اسمٌ اختاره الطالب نفسه — فالمشرف يمنح القدرة على وضع الغرافيك على الهواء دون أن يرى لمن يمنحها. صارت تعرض لكل طلب: الاسم، ومعرّف المستخدم ومعرّف المحادثة بخط موحّد العرض، ومنذ متى الطلب، ومتى ينتهي تلقائيًا.
- **الاسم يُهرَّب ويُقصّ.** هو النص الوحيد على هذه الشاشة الذي كتبه غريب، والرسالة صارت `HTML`؛ فلا يمرّ منه وسم.
- زر ✅ الموافقة صار أخضر كنظيره في شاشة الطلب الواحد، وزر ❌ يبقى بلا لون — الرفض لا يهدم شيئًا، وتلوين النصفين يلغي الإشارة.
- الأرقام في النص تطابق أرقام الأزرار، فالصف الذي يُضغط هو السطر الذي قُرئ.
- شاشة بلا طلبات تقول ذلك بدل عنوان فوق فراغ.

## 7.23.0 — 2026-09-02

- **«♻️ استعادة الافتراضي» كان يكتب فوق كل إعداد بضغطة واحدة.** كل شقيق له يقف ليسأل — مسح السجل، سحب الصلاحية، استعادة نسخة، حذف قالب — وهذا وحده كان ينفّذ ثم يخبر. صار يسأل أولًا، ويقول كم إعدادًا سيعود إلى قيمته الأصلية ويسمّي أوّلها بالعربية مع شرحه، ويذكّر بأن نسخة من الحالي تُحفظ قبل الكتابة.
- **سياسة التلوين كانت مكتوبة وغير مطبّقة على ستة أزرار.** النصّ في `New-Button` يقول إنّ المسح والسحب والاستعادة وإعادة التشغيل خطر؛ ومع ذلك كانت 🧹 مسح سجل التشغيل و🧹 مسح سجل التدقيق و🗑 سحب الصلاحية و♻️ إعادة تشغيل الجسر و🗄 استعادة نسخة و↩️ استعادة المشهد السابق بلا لون. لُوِّنت.
- «إخفاء الكل» في القائمة يبقى بلا لون عمدًا: هو تنقّل إلى شاشة التأكيد لا الفعل نفسه — والفعل هناك ملوّن منذ البداية.
- زر الإلغاء يبقى بلا لون في كل تأكيد: تلوين النصفين معًا يترك الإبهام بلا إشارة.

## 7.22.0 — 2026-09-02

- **«✅ EnableSnapshot = False» كان آخر ما بقي بالإنجليزية.** الأزرار والقوائم والبحث كلها عربية منذ 7.21.0، ثم يأتي التأكيد بمفتاح JSON وقيمة كما تطبعها PowerShell. صار: اسم الإعداد بالعربية، ثم «من كذا إلى كذا».
- **ولم يكن يقول ما كانت عليه القيمة.** من ضغط زرًّا بالخطأ لم يكن أمامه ما يعيده إليه. القيمة السابقة صارت جزءًا من التأكيد في المسارات الأربعة: التبديل، والرقم، والنص، والخيار المحدود.
- **زر ↩️ صار يسأل سؤالًا مفهومًا:** «إعادة «طول نص الحقل» وحده إلى الافتراضي؟ الآن: ٥٠٠ حرفًا · سيصير: ٢٠٠ حرفًا» — بدل اسم بالإنجليزية بلا رقمين.
- `Get-SettingValueDisplay`: القيمة بكلمات شاشتها — مفتاح نعم/لا يُقرأ مفعّل/معطّل هناك لا True/False.
- تحذير تعذّر حفظ `config.json` يبقى سطرًا مستقلًا داخل التأكيد الجديد، فلا يُقال «تم» لتغيير لم يُحفظ.

## 7.21.0 — 2026-09-02

- **٧٧ زرًّا كانت جملة كاملة.** الإعداد بلا اسم قصير كان يرث وصفه اسمًا، فصار الزر «تسجيل نص القالب الذي ظهر على الهواء لعرضه في التقارير» بدل «تسجيل نص القالب». لكل إعداد الآن اسم قصير خاص به، والوصف انتقل إلى السطر فوق الزر — حيث صار له مكان أصلًا منذ 7.20.0. أطول اسم اليوم ٣١ حرفًا، واختباران يمنعان عودة الحالين.
- **شاشات القوائم والبحث تقول ما وجدته.** كان عنوان وحده فوق لوحة أزرار: لا عدد، ولا شرح، وبحث بلا نتيجة يبدو شاشة فشلت في التحميل. صارت تقول «١٦ إعدادًا · صفحة ١ من ٢»، وتشرح كل إعداد في الصفحة، وتقول صراحةً حين لا يطابق شيء وأين يُبحث بدلًا من ذلك.
- `Get-SettingsExplainedLines`: باني الأسطر نفسه للأبواب وللقوائم، ويقرأ صفحته من `Get-BridgePageWindow` — الشريحة التي سترسمها الأزرار نفسها.
- **`$records = switch (...)` كان فخًّا.** `switch` أنبوب، والأنبوب يفكّ نتيجة واحدة إلى ذاتها ولا نتيجة إلى `$null`؛ فأول `.Count` عليه كان سيتعطّل على شاشة «لا نتائج» بالذات. مُلِفَّ بـ `@(...)`، واختبار يغطي الحالة الفارغة.

## 7.20.0 — 2026-09-02

- **كل إعداد صار له شرح.** ٥٣ إعدادًا من ١٣٤ لم يكن لها وصف أصلًا، وما كان له وصف لم يكن يظهر إلا في شاشة إدخال رقم — فالمفاتيح المنطقية لم تشرح نفسها قطّ. اكتملت الأوصاف، وصار كل باب يعرضها تحت عنوانه.
- **كل باب صار له سطر يقول ما فيه.** ثمانية عناوين هي قائمة أسماء، والاسم لا يقول أيّ الشاشات الثماني تحوي ما جاء المشرف يبحث عنه. `Summary` في تعريف الباب، تُعرض `<i>` فوق قائمة إعداداته.
- `Get-SettingsCategoryPageNames`: شريحة واحدة للصفحة يقرأها النصّ ولوحة الأزرار معًا — شريحتان من قائمة واحدة جوابان ينتظران أن يختلفا.
- **📄 زر يرسل `CHANGELOG.md` نفسه** من شاشة «ما الجديد»: الشاشة تقول ما تغيّر على شاشة المشغّل، والملف يقول لماذا بكلام من يصون الجسر.
- **«نسخ مرجع» كان ينسخ المرجع الخطأ.** الشاشة تطبع المرجع بجانب الفشل وحده، والزر كان ينسخ مرجع أحدث عملية — فإن نجحت الأخيرة أعطى المشغّل ثمانية أحرف لا وجود لها على الشاشة التي يقرأ منها. صار يفضّل أحدث عملية غير ناجحة.
- قراءة `Result` صارت عبر `Get-JsonProp`: سجلّ عملية يحمل ما عرفه كاتبه فقط، و`StrictMode` يحوّل حقلًا غائبًا إلى تعطّل — في الشاشة التي تُفتح للإبلاغ عن تعطّل.
- تحقّق من 🧾 عملياتي و🕘 ماذا فاتني بمدخلات حقيقية: بلا سجلّ، بسجلّ واحد، بسجلّ ناقص الحقول، وبأحداث بلا `action` — لا تعطّل في أي منها.

## 7.19.0 — 2026-09-01

- **📖 المساعدة صار فيها باب «⚙️ الإعدادات».** الشاشة التي تغيّر كل سلوك الجسر كانت الوحيدة بلا شرح: الأبواب الثمانية، «المعدّل فقط» كأول ما يُنظر إليه حين يتصرف الجسر خلاف المتوقّع، سلوك كل نوع إعداد عند التعديل، إعدادات الحماية الخمسة، النسخ والاستعادة، والتصدير الذي لا يخرج `BotToken`. للمشرف وحده.
- **صفحات المساعدة تُرسل بـ `parse_mode=HTML`.** الدليل هرم — باب، قسم، خطوة — وكان النص العادي يعطي الثلاثة وزنًا واحدًا. قاعدتان تُقرآن من النص لا تُكتبان في كل سطر: السطر المنتهي بنقطتين قسم فيُعرَّض، والكلمة التي هي اسم إعداد حقيقي تُوضع بخط موحّد العرض لتُنسخ إلى «🔎 بحث» كما هي.
- أسماء الإعدادات تُبنى من `$script:DefaultSettings` نفسها، فاسم لا وجود له لا يُنسّق كأنه إعداد.
- كل وسم داخل سطر واحد: `Split-TelegramText` يقطع على حدود الأسطر، واختبار يتحقق من التوازن في كل سطر من الدليل كاملًا.

## 7.18.1 — 2026-09-01

- **«ما الجديد» تُرسل بـ `parse_mode=HTML`.** الشاشة قائمة إصدارات داخلها قائمة تغييرات، والنص العادي كان يعطي الاثنين نفس الوزن. رقم الإصدار `<b>` والإصدار الجاري `<code>`.
- **كل وسم داخل سطر واحد.** `Split-TelegramText` يقطع على حدود الأسطر، فوسم يفتح في سطر ويغلق في آخر — `<blockquote>` مثلًا — هو الشيء الوحيد الذي لا ينجو من القطع؛ اختبار يتحقق من توازن الوسوم في كل سطر.
- `Send-TelegramPagedText` تحمل `-ParseMode` مع الصفحات: زر 📄 المزيد يصل في دور لاحق، وجزء يُرسل دونه كان سيعرض الوسوم نفسها.

## 7.18.0 — 2026-09-01

- **تأكيد استعادة نسخة الإعدادات صار يقول ما سيتغيّر إلى ماذا.** كان يسرد أسماء الإعدادات المختلفة فقط — نفس عيب «سُتستبدل ٣ أخبار» — والمشرف يكتب فوق إعداداته الحيّة دون أن يرى الفرق. صار جدولًا من ثلاثة أعمدة: الإعداد، الحالي، في النسخة.
- **لا قيمة سرّية تُطبع.** `config.json` هو الملف الوحيد الذي يحمل `BotToken` ورمز الشيت، فكل اسم يقرأ `token|secret|password|apikey` يُعرَض بطوله لا بمحتواه، والقوائم — ومنها قوائم المسموح لهم — بعدد عناصرها.
- `Get-ConfigDifferenceRows` تقرأ الخاصية مباشرة لا عبر `Get-JsonProp`: قائمة من عنصر واحد تعود من دالة مفكوكة إلى عنصرها، فتُقرأ قائمة فيها مشرف واحد كمعرّفه لا كعدد.
- الرسالة النصية القديمة باقية كاحتياط، وأي رفض للإرسال الغنيّ يسقط إليها.

## 7.17.2 — 2026-09-01

- **نتيجة البحث بالمرجع صارت كتلة `pre` موحّدة العرض.** سطر السجل أعمدة يمسكها الفراغ — `id=` و`action=` و`result=` — والخط المتناسب هو الشيء الوحيد على هذه الشاشات الذي يكسرها فعلًا: تتوقّف الأعمدة عن التحاذي بين الصفوف فتفقد العين العمود الذي كانت تتبعه.
- لا كتلة تُرسم حين لا يوجد سطر مطابق؛ الرسالة النصية تقول ذلك كما كانت، وأي رفض يسقط إليها.
- يصلح رقم الإصدار: الكوميت السابق أنزله إلى `7.17.0` وقد كان `7.17.1`.

## 7.17.1 — 2026-09-01

- **رفضٌ واحد للإرسال الغنيّ لم يعد يُطفئ كل الشاشات.** كان أول رفض يضبط راية واحدة، فتفقد الحالة والتقارير وشريط الأخبار جداولها حتى إعادة التشغيل. صار `404` وحده — الطريقة غير موجودة هنا — يوقف الجلسة، لأنه الحقيقة الوحيدة التي تصدق على كل شاشة.
- **`400` يُلام عليه ما لم يُعرض من قبل فقط.** نوع الكتلة الذي نجح مرّة يُسجَّل في `RichBlockTypesProven`، فالرفض اللاحق يُنسب إلى الجديد في الحمولة لا إلى كل ما فيها. وإن كانت كل أنواعها مُثبتة سلفًا فالعطل في هذه الرسالة بعينها — حجمها أو حقل فيها — فلا يُعطَّل شيء، وإلا كلّف تقريرٌ ضخم واحد نوعَ كتلة يعمل بشكل دائم.
- `Get-RichBlockTypes` تمشي داخل `details` و`cells` أيضًا: هي أرجح أسباب رفض الرسالة كلّها، ولومٌ لا يراها يلوم الخطأ.
- الأثر: تبنّي كتلة جديدة صار يخاطر بتلك الكتلة وحدها — وهو شرط أي استفادة من كتل 10.2/10.3 قبل أن تُكتب.

## 7.17.0 — 2026-09-01

- **المفضلة كانت تضيع من أول إضافة ثانية.** `$x = if (...) { @(...) } else { @() }` لا ينجو: فرع `if` أنبوب، والأنبوب يفكّ المصفوفة القصيرة، فيصير `$x` نصًّا ويصير `+=` بعده دمج نصوص. مفضلتان تصيران مفتاحًا واحدًا لا يسمّي قالبًا، فيرشحه كل قارئ وتختفي المفضلة كلّها دون خطأ في أي مكان. صارت تُبنى بـ `List[string]`.
- **شاشة إدارة المفضلة كانت تقرأ القائمة المقصوصة** (`FavoritesCount`) لتقرّر علامة الاختيار، فأي اختيار بعد الحدّ لم يظهر مختارًا قطّ، وكل ضغطة كانت تُعيد إضافته فلا يُزال أبدًا. الاختيار والعرض سؤالان مختلفان ولهما الآن دالتان: `Get-UserFavoriteSelection` و`Get-FavoriteTemplateKeys`.
- الشاشة تقول ما لا تقدر العلامات على قوله: أنّك لم تختر شيئًا وأنّ القائمة تخمّن من الاستخدام، أو أنّك اخترت أكثر ممّا يُعرض. وفشل الحفظ لم يعد صامتًا.
- **«الحالة» و«الحالة الكاملة» تعرضان معرّف المستخدم** فوق الطيّة لا داخلها — وهو ما يُطلب من المشغّل عند طلب وصول أو الإبلاغ عن عطل، وحقيقة تحتاج فتح قسم لرؤيتها تُلتقط صورتها خطأً. يمرّ عبر `Format-UserAuditActor` فيحلّ الاسم البديل ويثبّت اتجاه الأرقام.
- **قفل الأخبار أمام طلبين في اللحظة نفسها**: الضغط المتكرر من الطالب نفسه كان يُعيد تصفير المهلة في كل مرّة، فلا يأتي المنح التلقائي أبدًا ويتلقّى المالك تنبيهًا جديدًا في كل ضغطة. صار يُبلّغ بالمتبقّي وحسب.
- **التسليم لا يحذف مسودة تغيّر مالكها** أثناء فتح النافذة. كان من ينشر ثم يبدأ غيره مسودة جديدة يخسرها في خلاف لم يكن طرفًا فيه.
- **المكان المُفرّغ يُحجز لمن مُنح القفل** لمدة `NewsLockGrantHoldSeconds` (افتراضيًا ١٢٠ ثانية). مفاوضة دامت خمس دقائق كان يربحها أسرع من يضغط في الثانية التالية للمنح.
- **السحب من الشيت صار داخل القفل**: «⬇️ سحب ونشر» و«📝 سحب إلى المسودة» يعيدان كتابة الشريط كأي تحرير آخر، فلا يُرسمان ولا يُقبلان إلا لمن يحمل القفل. كان أي مشغّل له حقّ السحب يقدر على إعادة كتابة ما على الهواء ما دامت لا مسودة مفتوحة.
- إخفاء الزر ليس قاعدة: تيليجرام يُبقي الرسائل القديمة حيّة، فالرفض موضوع في `Get-CallbackRefusal` أيضًا.
- مشرف لا يحمل القفل لا يرى زرّي السحب أيضًا — أمامه «🔓 إلغاء القفل (مشرف)» ثم «✏️ بدء التحرير»، فيبقى كاتب واحد في كل لحظة.

## 7.16.0 — 2026-08-31

- **شاشة الترتيب صارت جدولًا**: الرقم · الخبر · عدد الأحرف. الترتيب يُقرأ ترتيبًا، وهو ما هذه القائمة عليه.
- **عمود الأحرف هو ما يجعل الجدول يستحق مكانه**: الشريط يفرض `NewsMaxItemLength`، ولم يكن للمحرّر سبيل ليعرف أيّ عنوان يقترب من الحدّ حتى يُرفض النشر. والعنوان الذي تجاوز تسعين بالمئة من الحدّ يُعلَّم بـ⚠️، لأن رقمًا يُقارَن بإعداد لا يراه المحرّر ليس تحذيرًا.
- **ما على الهواء صار تحت المسودة مباشرة** في كتلة مطويّة تقول `+3 −1` وتسرد ما سيُضاف وما سيُحذف. كان ذلك يتطلّب «👁 معاينة» ثم رجوعًا، ولا يرى المحرّر الاثنين معًا — وهو ما يحتاجه ليقرّر.
- **تأكيد النشر صار يعرض الكلمات لا العدد.** «سيُستبدل ٣ أخبار» عدد؛ ومن يضع نصًّا على الهواء يقرّر إن كانت هذه هي الكلمات الصحيحة، ولذلك يحتاج الكلمات.
- **`Edit-TelegramRichMessage`**: الشاشة تُحرَّر في مكانها كما النصية، فضغطات ⬆️/⬇️ المتكرّرة تبقى على رسالة واحدة ولا تخلّف واحدة لكل ضغطة.
- الكتل لا تُبنى أصلًا متى عُرف أن الإرسال الغنيّ مرفوض: هذه الشاشة تُعاد رسمتها عند كل ضغطة سهم، وبناؤها يقرأ ملف الهواء للمقارنة — عملٌ لا يراه أحد.
- **لم أُلغِ الصفحات.** `InputRichBlockButtons` صفّ واحد لكل كتلة ولا سقف موثّق لعدد الكتل، ولن أبني إلغاء الصفحات على افتراض غير مُثبت — وهو الخطأ الذي كلّف الإصدار السابق سحبًا.

## 7.15.0 — 2026-08-31

- **يسحب تغييرَي اتجاه الجداول من `7.14.0` و`7.14.1`.** عزل اتجاه الخلية وقلب ترتيب الأعمدة كلاهما بُني على وصف ما يظهر على الشاشة، لا على شيء يقوله التوثيق عن كيفية رصف تيليجرام للجدول — والفحص على الجهاز أظهر أن الجداول لم تكن معكوسة أصلًا. تغييرٌ يصحّح ما ليس معطوبًا هو تغيير يكسر.
- الجداول تعود إلى ما كانت عليه في `7.13.0`: الخلايا كما تُكتب، والأعمدة بترتيب كتابتها.
- ما تبقّى من `7.14.x` — تقسيم «الحالة الكاملة» إلى أقسام مسمّاة — كان في الكوميت نفسه فعاد معه؛ يمكن إعادته وحده إن أردت.
- رقم الإصدار يتقدّم ولا يرجع: نسخة سُحبت تبقى في السجل مسحوبةً لا ممحوّة.

## 7.13.0 — 2026-08-31

- **«الحالة» و«الحالة الكاملة» صارتا بالكتل**: الحكم في العنوان، والطبقات على الهواء في جدول، والتفصيل الآليّ مطويّ تحته.
- **ما على الهواء هو الشيء الجدولي الوحيد في شاشة حالة** — الطبقة وما عليها ومنذ متى ومَن — وكان جملة متصلة. صار جدولًا بأربعة أعمدة كبقية الجداول.
- **الحالة الكاملة كانت أطول شاشة في الجسر**: أقسام متتابعة يمرّ عليها القارئ كلها ليعرف إن كان شيء معطّلًا. الحكم صار أول ما يُقرأ، وما تحته يُفتح عند الحاجة.
- **الأسطر تُمرَّر ولا تُعاد بناءً**: الشاشتان النصية والكتلية تستعملان النصوص نفسها حرفيًّا، فلا تختلفان — القاعدة نفسها المطبَّقة على الملخّص والتقارير والصحّة.
- السطر الفارغ لا يصير فقرة فارغة: في النص فاصل، وفي الكتل فجوة تبدو كأن شيئًا لم يُحمَّل.
- «منذ 0 ثانية» صارت «الآن».

## 7.12.0 — 2026-08-31

- **مركز الصحة صار جدولًا يُقرأ عموده عموديًّا.** سبع جمل تبدأ كلٌّ منها بدائرة ملوّنة تُقرأ سطرًا سطرًا؛ عمودٌ من الدوائر يُمسح بنظرة واحدة — وهذا هو عمل الشاشة كلّه: العثور على الأحمر.
- **العطل يتقدّم على السليم.** شاشة تُفتح لأن شيئًا تعطّل يجب ألّا يضع العطل في الصف السادس.
- **التفصيل يبقى في عموده ولا يُطوى**: شاشة صحّة تُخفي *لماذا* الشيء أحمر هي شاشة تُفتح مرتين.
- **`Get-BridgeHealthRows` مصدر واحد للشاشتين**، فلا تختلفان يومًا على ما إذا كان النظام سليمًا — القاعدة نفسها التي طُبِّقت على شاشتَي الملخّص.
- النسخة النصية تبقى كما كانت حرفًا بحرف، ويحرسها اختبار يقارنها بالصفوف.

## 7.11.0 — 2026-08-31

- **الإخفاء والخروج صارا يسمّيان البنر الذي خرج ويقتبسان نصّه.** كانا يسجّلان رقم الطبقة ولا شيء غيره — بلا اسم قالب وبلا نص — فكانت «عملياتي» تقول «أُخفيت الطبقة 7» ولا تقول أيّ شريط كان عليها. لم تكن المشكلة في العرض: **النص لم يكن مُسجَّلًا أصلًا**.
- **سجلّ الهواء صار يحمل النسخة معه** (`AirCopy`). كان يحمل المفتاح والوقت والمستخدم فقط، فلم يكن لدى الإخفاء ما يقوله حتى لو أراد. تُلتقط قبل تشغيل العملية، لأن العملية تُفرغ الطبقة والجواب لا يتوفّر بعدها.
- **العرض الفاشل والمرفوض صارا يسجّلان ما كان سيُعرَض.** خمسة مواضع كانت تسجّل أيّ قالب حوولت ولا تسجّل ماذا كان سيقول — فلا تجيب «ماذا كنت أحاول رفعه؟».
- طبقة كانت فارغة أصلًا تُسجَّل بنصّ فارغ لا بتخمين.

## 7.10.1 — 2026-08-31

- **«عملياتي» كانت تنفق خمسة أسطر على العملية الواحدة** — الجملة والتصنيف والنص والمرجع والنصيحة، كلٌّ في سطر — فثلاث عمليات تملأ خمسة عشر سطرًا. وأكثر ذلك كان صحيحًا في كل صف، فلم يكن يقول شيئًا.
- **العملية الناجحة صارت سطرًا واحدًا**: `✅ 21:40 — عُرض urgent · أخبار`. التصنيف انتقل بجانب الاسم الذي يصفه.
- **المرجع لا يظهر إلا حيث يُطلب.** على عملية ناجحة هو ثمانية أحرف بلا معنى في كل صف؛ وعلى عملية فاشلة هو ما يحتاجه المشرف — فيصل على السطر نفسه مع ما يجب فعله: `🔖 5023333b · افحص الاتصال ثم أعد المحاولة`.
- **حصيلة في الأعلى**: `10 عملية · ✅ 8 · ❌ 1 · ⛔ 1`. السؤال الذي تُفتح الشاشة لأجله هو «هل فشل شيء ممّا فعلت؟»، وقراءة عشرة صفوف للجواب هي العمل الذي وُجدت الشاشة لتوفيره.
- **الأحدث أولًا** لا أخيرًا: المطلوب غالبًا هو ما فُعِل للتوّ.
- الفواصل حلّت محلّ التداخل: كانت قائمة داخل كتلة مطويّة داخل قائمة، وصار كل سطر يملك عرض الرسالة.

## 7.10.0 — 2026-08-31

- **«ماذا فاتني» تبدأ بما على الهواء الآن.** كانت تنتهي به — بعد ما عُرض والإخفاءات والأحداث والإخفاقات — وهو أول سؤال لمن يدخل الغرفة متسلّمًا وردية، وكان آخر سطر يصل إليه.
- الإخفاقات تبقى مفتوحة لأنها عادةً سبب فتح الشاشة، و**«أحداث تستحق الانتباه» تُطوى** لأنها تُقرأ عند التتبّع وحده. وجدول «ما عُرض» أربعة أعمدة كبقية الجداول.
- **قارئ واحد للسجل** يخدم شاشتَي الملخّص: قارئان يرشّحان سجلًا واحدًا كلٌّ على حدة هما جوابان ينتظران أن يتناقضا.
- **نصوص البنرات عادت إلى تقريرها** في كتلة مطويّة تحت الجدول. كانت قد سقطت كليًّا حين نزل الجدول إلى أربعة أعمدة — ولا عمود فيه يتّسع لجملة، لكن النص هو ما يعرف به المشغّل بنره.
- **«عملياتي» تُبقي الثلاث الأحدث مفتوحة وتطوي ما قبلها.** عشر عمليات كلٌّ منها بنصّها ومرجعها شاشة لا يصل أحد إلى آخرها، والمطلوب فيها هو الأخير غالبًا.
- **يصلح `.Count` على قيمة مفردة، للمرة الثالثة في هذا المسار**: تعبير `if` يُخرج نتيجته إلى الأنبوب، والأنبوب يفكّ المصفوفة أحادية العنصر — فصار اللفّ حول التعبير كلّه لا داخل فرعيه.
- **يزيل علامات BOM أُضيفت سهوًا** إلى تسعة عشر ملفًا خلال تحرير هذه الجلسة؛ لم تكن في أيٍّ منها قبلها.

## 7.9.0 — 2026-08-31

- **يعيد تصميم تقرير الأخبار لمحرر يعدّل الشريط عشر مرات يوميًا.** تيليجرام يقسم عرض الجدول بالتساوي بين أعمدته، فستة أعمدة تعطي كل واحد سدس شاشة الهاتف — **وهو الخطأ نفسه الذي عالجته `6.9.3` حين أخرجت عنوان الخبر من الأزرار**. عشرة تعديلات جعلته غير مقروء لا مجرد ضيّق.
- **أربعة أعمدة قصيرة معروفة العرض**: اليوم · عدد التعديلات · ما بقي على الهواء · **المدى** (`12–18`). رقمان وشرطة يحملان ما كان مسار من عشر قراءات ينفق عمودًا كاملاً ليقوله.
- **التفصيل انتقل إلى كتلة مطويّة تحت الجدول**، حيث العرض هو الرسالة كلها: مسار القراءات كاملاً بلا قصّ، ووقت أول وآخر تعديل، والمشغّلون.
- **جدول البنرات نزل إلى أربعة أعمدة أيضًا**: رقم الطبقة — خانة واحدة — كان يأخذ من عرض الشاشة قدر ما يأخذه اسم البنر، فصار يركب على الاسم: `urgent · ط7`.

## 7.8.0 — 2026-08-31

- **يصلح رقمًا خاطئًا في تقرير الأخبار.** الشريط شيء واحد يجري تعديله، لا سلسلة نشرات منفصلة — وكل نشر يسجّل كم خبرًا كان يحمله الشريط في تلك اللحظة. جمع تلك القراءات كان يعدّ العناوين نفسها مرة لكل تعديل: شريط من 14 خبرًا عُدِّل ثلاث مرات كان يُبلَّغ **«40 خبرًا»**. الرقم الآن هو ما انتهى إليه اليوم فعلًا، والمفردات صارت «تعديلًا» و«على الهواء» لا «نشرة» و«خبرًا».
- **حجم كل تعديل صار ظاهرًا**: عمود يسرد عدد أخبار الشريط بعد كل تعديل — `12 ← 15 ← 14` — فيُقرأ منه اتجاه التعديل وحجمه معًا، وآخر رقم فيه هو ما بقي على الهواء. يوم طويل يُقصّ إلى أحدث ستّ قراءات مع `…` تقول إنه قُصّ.
- **اليوم الذي لم يُعدَّل فيه الشريط صار صفًّا يقول ذلك، لا غيابًا.** `Group-Object` لا يُنتج إلا الأيام التي فيها سجلات، فأسبوعٌ لم يُلمس فيه الشريط يوم الأربعاء لم يكن له صفّ أربعاء إطلاقًا — **والفجوة هي ما يفتح المشرف التقرير ليجدها**. واسم اليوم صار بجانب تاريخه، فـ«08/27» لا تقول إن اليوم الصامت كان جمعة.
- **سطران جديدان يجيبان ما لم يكن التقرير يجيبه**: منذ متى والشريط دون تعديل — شريط كُتب قبل ست ساعات هو نصّ قديم على الهواء الآن — وكم يومًا في الفترة مرّ بلا تعديل.
- **ومتى بدأ العمل ومتى انتهى** في كل يوم (`07:10 ← 21:40`): «ثلاثة تعديلات» لا تقول إن كانت موزّعة على النشرة أم دُفعت كلها دقيقةً واحدة عند التسليم.
- **📖 الدليل كاملًا صار رسالة واحدة** بأبواب مطويّة (`details`، Bot API 10.2) بدل رسائل متتابعة يُمرّ فيها على كل باب للوصول إلى المطلوب.
- **🧾 عملياتي صار قائمة منظَّمة** لا نصًّا مُزاحًا باليد. وعمدًا ليس جدولًا: أهمّ سطر فيه هو النص الذي وصل الهواء، وهو جملة — وعمود يتّسع لها يخنق ما عداه، وهو سبب إخراج نصّ البنر من جدول التقارير أصلًا.
- **يصلح انحدارًا أدخلَته تعبئة الأيام الصامتة**: صارت الفترة الفارغة تبدو غير فارغة، فاختفت «لم يُنشر شريط أخبار في هذه الفترة». الفراغ يُسأل عنه عدد التعديلات لا عدد الصفوف.

## 7.7.0 — 2026-08-31

- **تقرير الأخبار صار جدولًا كتقرير البنرات**: اليوم · النشرات · الأخبار · المشغّلون. أربعة أرقام لليوم الواحد مصفوفةً بفواصل `·` هي بالضبط ما تُوجد الجداول له.
- **حصيلة المشغّلين صارت عمودًا رابعًا** بدل سطر ثانٍ مُزاح تحت كل يوم — لم تكن النسخة النصية تملك مكانًا آخر تضعها فيه.
- اليوم بلا مشغّل مسجَّل يُكتب `—`، فلا يبقى عمود فارغ يُقرأ عطلًا.
- الارتداد نفسه ينطبق: أي رفض من `sendRichMessage` يسقط إلى النص الذي كانت الشاشة ترسله دائمًا، بلا كلمة للمشغّل.

## 7.6.0 — 2026-08-31

- **حقل تاريخ وحقل وقت بدل الكتابة.** تيليجرام لا يملك منتقيًا أصليًا، فالتقويم مبنيّ من أزرار سطرية: شبكة شهر بصفوف أسابيع تحت ترويسة أيام، ثم شبكة ساعات ستّ في الصف، ثم دقائق بخطوة خمس. الشاشات الثلاث تُحرَّر في مكانها فلا تخلّف شبكات قديمة وراءها.
- **اليوم الماضي والساعة المنقضية يظهران معطَّلين لا محذوفين** (`disabled`، Bot API 10.3): شهرٌ فيه ثقوب يكفّ عن أن يُقرأ تقويمًا — الصفّ الذي يقع عليه التاريخ هو ما تجده العين به. وترويسة أيام الأسبوع أزرار معطَّلة للسبب نفسه: تشغل أعمدتها دون أن تُضغط.
- **ثلاثة إزاحات جاهزة** بجانب المطالبة: `+15` و`+30` و`+60` — تغطّي أكثر ما يُجدوَل، بضغطة واحدة.
- **المنتقي يمرّ بالمحلّل نفسه** الذي يمرّ به النص، فلا ينتج لحظةً كان المحلّل ليرفضها: دقيقة مضت، أو وقتًا داخل فجوة التوقيت الصيفي.
- **الكتابة تحسّنت أيضًا، ولم تُستبدل.** `21:45` وحدها تكفي (وتنتقل للغد إن مضى وقتها اليوم)، و«غدًا 21:45» و«اليوم 21:45» و«+30» و«بعد 90» و«09-15 21:45» — إضافةً إلى الصيغة الكاملة بفواصلها `-` أو `/` أو `.`.
- **الأرقام العربية الهندية تُقبل** (٢١:٤٥). مَن يكتب بالعربية يكتب بلوحة عربية، وكل محلّل تواريخ في الثقافة الثابتة يرفضها — فكان الحقل الوحيد المكوَّن من أرقام بحتة يتطلّب تبديل لوحة المفاتيح.
- `25:00` و`21:70` تُرفضان بدل أن تنزلقا صامتتين إلى الغد أو الساعة التالية عبر `AddHours`/`AddMinutes`.

## 7.5.0 — 2026-08-31

- **قائمة الأحداث القادمة تحمل اللحظة لا رسمها.** كل حدث يحمل كيان `tg-time` (Bot API 9.5) بالطابع الزمني وبالتنسيق `wdt` — يوم الأسبوع والتاريخ والوقت — فيراه كل قارئ **بمنطقته الزمنية ولغته**. ومنطقة المحطة تبقى بجانبه: جدول الهواء يُكتب بتوقيت المحطة، ومَن يقرأ من مكان آخر يحتاج الاثنين لا تحويلًا صامتًا.
- **`Format-ScheduleEventHtml` دالة مستقلة، و`Format-ScheduleEvent` لم تُمسّ.** للأخيرة أربعة مستدعين يرسلون نصًّا خامًا، وتحويلهم جميعًا إلى HTML دفعةً واحدة هو بالضبط ما يُسقط شاشة عند أول مفتاح قالب فيه `<`. ونصّ العنصر نفسه يبقى وقتًا مقروءًا لتطبيق لا يعرف `tg-time`.
- **يصلح عيبًا كامنًا من `7.0.0`: رسالة HTML أطول من حدّ الإرسال كانت تُرسل بوسومها ظاهرة.** كان المشغّل يرى `<b>` و`<blockquote expandable>` نصًّا — وهو بعينه ما وُجد الهروب لمنعه، قادمًا من الجهة الأخرى. `ConvertFrom-TelegramHtmlText` تنزع الوسوم ثم تفكّ الكيانات؛ الترتيب مقصود، فالعكس يحوّل `&lt;b&gt;` — عنوانًا يحوي `<b>` فعلًا — إلى وسم ثم يحذفه.

## 7.4.1 — 2026-08-31

- **يصلح انهيارًا: البحث بالمرجع كان يرمي حين يجد سطرًا واحدًا بالضبط** — وهي أرجح نتيجة له. PowerShell يفكّ المصفوفة أحادية العنصر عند الإرجاع، فيعود السطر الواحد نصًّا لا مصفوفة، و`.Count` على نصّ ترمي تحت StrictMode: `Unhandled error processing message ... The property 'Count' cannot be found`. النتيجة تُلَفّ بـ`@()` قبل عدّها.
- **يصلح: «لا نتائج» كانت تُبلَّغ «ليس مرجعًا».** المصفوفة الفارغة تعود من PowerShell `$null` تمامًا كالقيمة الغائبة، فلم تكن الدالة الواحدة تستطيع التفريق بين مرجع مشوّه وسجل لا يحوي ذلك السطر — والسجل المُدوَّر كان يُبلَّغ خطأً في الإدخال. فحص الشكل صار دالة مستقلة `Test-OperationReference`، والبحث يبحث وحده.
- ثلاثة اختبارات على الحالات الثلاث: مطابقة واحدة، ولا مطابقة، ومدخل ليس مرجعًا.

## 7.4.0 — 2026-08-31

- **تقرير البنرات صار جدولًا حقيقيًا** عبر `sendRichMessage` (Bot API 10.1): صفّ ترويسة، وأعمدة يرسمها تيليجرام بنفسه — البنر · الطبقة · المشغّل · الوقت · المدة. النسخة النصّية كانت تصفّ الأعمدة بمسافات، والخط المتناسب على الهاتف لا يصفّها إطلاقًا، فيلتفّ كل صف على نحو مختلف ويُقرأ التقرير قائمةً لا جدولًا.
- **`is_rtl` مُمرَّرة**: كل شاشات الجسر عربية، وجدول يُرسم من اليسار يضع العمود الأول حيث تصل عين القارئ أخيرًا.
- **الجدول يُجرَّب ولا يُعتمد عليه أبدًا.** فشل الإرسال — أو واجهة بلا هذه الطريقة أصلًا — يسقط إلى النص نفسه الذي كانت الشاشة ترسله دائمًا، بلا رسالة خطأ للمشغّل. وأول رفض للطريقة ذاتها يُعطّل المسار لبقية الجلسة، وإلا دفع كل تقرير رحلتين ليكتشف الشيء نفسه من جديد.
- **نصّ البنر يبقى خارج الجدول** عمدًا: هو جملة كاملة تصل الهواء، وعمود يتّسع لها يخنق الأعمدة الأربعة الباقية.
- **تقرير الأخبار بقي نصًّا**: لا أعمدة فيه ليكسبها.
- الشكل مأخوذ من التوثيق لا من التخمين: `InputRichMessage` يقبل **واحدًا فقط** من `blocks` أو `html` أو `markdown`؛ و`InputRichBlockTable` يأخذ `cells` مصفوفةَ مصفوفات من `RichBlockTableCell` بحقل `is_header`؛ و`RichText` يقبل نصًا عاديًا فلا حاجة إلى بناء شجرة كيانات.

## 7.3.0 — 2026-08-31

- **زر نسخ المرجع كان نصف ميزة؛ هذا نصفها الآخر.** «🔎 ابحث بمرجع عملية» في شاشة التشخيص: يلصق المشرف المرجع الذي نسخه المشغّل فيرى سطر العملية. قبله كان المرجع يُنسخ ويُقتبَس، ثم على المشرف أن يصل إلى جهاز الإخراج ويعمل grep على السجل بيده.
- **هذا بحث بالمرجع لا بحث في السجل، والفرق أمني لا أسلوبي.** سجل التشغيل أقلّ ما يكتبه الجسر تنقيحًا — ولهذا وُجدت حزمة التشخيص المنقّحة أصلًا — فشاشة تُعيد أي سطر مطابق تصير طريقًا لقراءته كلمةً كلمة. لذلك: المدخل يجب أن يكون **ثمانية أحرف ست عشرية بالضبط** وإلا رُفض، ولا يُعاد إلا سطر `AIR_OP` منظَّم. سطرٌ يذكر المرجع عرضًا لا يُعاد.
- المرجع يُقبل بأي حالة أحرف لأنه يُلصق لا يُكتب، والسقف عشرون سطرًا، والسجل المُدوَّر يُبلَّغ بلا خطأ.
- صلاحية المشرف تُفحص مرتين: عند رسم الزر وعند وصول النص — الشاشة قد تكون فُتحت قبل أن يتغيّر الدور.

## 7.2.0 — 2026-08-31

- **الرفض اليومي صار نافذة على الزر.** بوّابة `Get-CallbackRefusal` تقرّر قبل الإقرار، لأن تيليجرام يقبل إجابة واحدة لكل ضغطة: رفضٌ يُقرَّر بعد إنفاق الإجابة لا يملك إلا أن يصل رسالةً جديدة تدفع اللوحة خارج الشاشة.
- **يصلح: زرّا استعادة نسخ الأخبار كانا يرفضان بصمت تام.** `news:restore:*` و`news:restoreconfirm:*` كانا يخرجان بـ`break` مجرّد — تُقرّ الضغطة، ولا يحدث شيء، ولا يُقال لماذا؛ وهذا لا يُفرَّق عن جسر أسقط الضغطة. صارا يقولان إن الاستعادة للمشرف وحده.
- بوّابة السحب تغطي **أربع** وجهات (`news:sheet` و`news:sheetdraft` وتأكيداهما) لا الزرّين اللذين يُرسمان فقط، لأن تيليجرام يُبقي الأزرار حيّة في الرسائل التي سلّمها.
- المعالجات تحتفظ بفحوصها: القاعدة كانت ولا تزال أن الزر **لا يُرسم ولا يُنفَّذ**.
- **زر نسخ لمرجع العملية** (`copy_text`, Bot API 7.11). المرجع ثمانية أحرف ست عشرية يقتبسها المشغّل للمشرف فيجدها بـgrep واحد؛ كان يُطبع في الرسالة ويُعاد كتابته من الهاتف، وحرف واحد خطأ يعني grep لا يجد شيئًا. للأحدث وحده — عشرة منها تدفن الزرّين تحت جدار من الست عشري.

## 7.1.1 — 2026-08-31

- **يصلح: تفعيل «تأكيد الإخفاء» كان يُضعف الشاشة بدل أن يقوّيها.** زر الإخفاء المباشر أحمر، لكن «✅ نعم، أخفِ» في لوحة التأكيد كان بلا لون — فمن يُفعّل `ConfirmLayerRemoval`، وهو الإعداد الأكثر أمانًا، كان يحصل على إشارة أضعف ممن يُطفئه. الأحمر صار على نصف التأكيد المُثبِت في الإخفاء والخروج معًا.
- «✅ إعادة هذا الإعداد» صار أحمر كما «♻️ استعادة الافتراضي»: كلاهما يستبدل إعدادًا حيًّا، وتلوين أحدهما دون الآخر يجعل اللون قاعدةً لا يمكن الوثوق بها.
- «✅ موافقة» على طلب وصول صارت خضراء؛ و«❌ رفض» تبقى بلا لون لأنها لا تمنح شيئًا ولا تُتلف شيئًا، شأن كل إلغاء.

## 7.1.0 — 2026-08-31

- **الرفض صار نافذة على الزر، لا رسالة جديدة في المحادثة.** تيليجرام يقبل إجابة واحدة لكل ضغطة، والجسر كان ينفقها على إقرار فارغ في أول السطر — فلم يبقَ للرفض طريق إلى الزر، فكان يصل رسالةً تدفع اللوحة خارج الشاشة ويضطر المشغّل إلى البحث عنها. الحُرّاس انتقلوا قبل الإقرار، وكلٌّ منهم يُجيب الاستعلام في طريق خروجه.
- **سبب تقديم الإقرار لم يُفقد**: هو ألّا تبقى الدوّامة تدور خلال عملية هوائية بطيئة. الحُرّاس الثلاثة فحوص في الذاكرة لا تكلّف شيئًا، والضغطة المسموح بها تُقرّ كما كانت تمامًا قبل التنفيذ.
- **الضغطة غير المصرّح بها لا تترك أثرًا في محادثة قد لا يقرأها صاحبها ثانية** — تُجاب بنافذة `show_alert` وحدها.
- نصّ الإجابة يُقصّ عند 200 حرف بعلامة حذف، فتيليجرام يقطع ما زاد في منتصف الكلمة.
- **آخر أربعة أفعال نالت لونها**: «نعم، انشر» لسحب الشيت إلى الهواء أحمر لأنه يستبدل ما على الهواء بمحتوى لم يكتبه الضاغط، و«نعم، حمّل المسودة» أخضر لأن التأكيد نفسه يقول إنه لا يصل الهواء، و«نعم، اختبر القالب» أخضر، و«نعم، إلغاء» للحدث المجدول أحمر.

## 7.0.1 — 2026-08-31

- **اللون امتدّ من شاشات الأخبار إلى بقية الشاشات، بسياسة مكتوبة لا برشّ.** `-Style` صار معاملًا في `New-Button` — المدخل الذي تمرّ به 191 زرًا — والقاعدة موثّقة فوقه: **أحمر** لما يُنزل شيئًا عن الهواء أو يُتلف عملًا أو يستبدل حالة حيّة (الإخفاء، الخروج من المشهد، حذف قالب أو نصّ جاهز، مسح السجلّات، سحب الصلاحية، استعادة نسخة، استعادة الافتراضي، إعادة التشغيل)، و**أخضر** لما يُثبّت ما كتبه المشغّل للتوّ (تأكيد الإرسال، تأكيد الجدولة، حفظ التغيير، اعتماد الاستيراد، تطبيق الإعدادات).
- **مدخل القائمة لا يُلوَّن، الفعل وحده يُلوَّن.** «🙈 اخفاء طبقة» في القائمة الرئيسية يفتح شاشة ولا يُخفي شيئًا؛ الأحمر على «🙈 إخفاء هذا (طبقة 7)» حيث تقع الضغطة فعلًا. تلوين الاثنين يُفرغ اللون من معناه.
- **نصف التأكيد المُثبِت وحده يُلوَّن.** «🚨 نعم، إخفاء الكل» أحمر و«❌ إلغاء» بلا لون: تلوين النصفين يترك الإبهام بلا إشارة.
- أربعة اختبارات تحرس السياسة نفسها لا مواضعها — أن الفعل ملوّن ومدخل القائمة ليس كذلك، وأن الإلغاء بلا لون، وأن كل قيمة مستعملة من الثلاث التي يعرّفها تيليجرام (قيمة خارجها تُرفض بها الرسالة كاملة، فيصير الخطأ المطبعي شاشةً لا تصل).

## 7.0.0 — 2026-08-31

- **الأزرار صار لها لون.** `style` أُضيف في Bot API 9.4 بثلاث قيم موثّقة — `danger` أحمر و`success` أخضر و`primary` أزرق — ولا يشترط Premium. الحذف وإلغاء المسودة ومسح الكل حمراء، و«✅ مراجعة ونشر» خضراء. حتى 6.9.5 كان الكود يقول في تعليقه إن «تيليجرام لا يمنح الزر لونًا ولا حدًّا»، فبُني تمييز الأخبار على التسمية وحدها: رقم يركب كل زر وعلامة `▪️`/`▫️` تتناوب. هذا لم يعد القيد.
- **الزرّان الخاملان صارا معطّلين فعلًا.** طرفا القائمة (⬆️ لأول خبر و⬇️ لآخره) كانا زرّين حيّين يشيران إلى `news:noop`: يُضغطان ويُستجاب لهما ولا يفعلان شيئًا — وهذا لا يُفرَّق عن ضغطة أسقطها الجسر. `disabled` من Bot API 10.3 يجعلهما جامدين. المعالج `news:noop` باقٍ في التوجيه لأن الأزرار تبقى حيّة في الرسائل التي سلّمها تيليجرام أصلًا. وأسهم شاشة الخبر تُعطَّل بدل أن تُخفى، فلا يتغيّر عرض الصف تحت الإبهام.
- **قائمة الترتيب تُرسل HTML داخل اقتباس قابل للطيّ.** `<blockquote expandable>` (Bot API 7.6) يجعل تيليجرام يطوي السرد إلى أسطر قليلة مع «إظهار المزيد» الخاص به، فصفحة طويلة لم تعد تدفع لوحة أزرارها خارج الشاشة. ميزانية الحروف لم تتغير: `expandable` يحكم الارتفاع لا الطول.
- **كل نص يكتبه إنسان يُهرَّب قبل الإرسال.** `ConvertTo-TelegramHtmlText` تستبدل `&` و`<` و`>` بترتيب `&` أولًا. عنوان خبر فيه `<` واحد كان بلا أثر، ومع `parse_mode=HTML` يجعل تيليجرام يرفض الرسالة كاملة — أي «قائمة لا تتحدّث»، وهو العطل نفسه الذي أُضيفت الصفحات لعلاجه. ورسالة HTML تجاوزت حدّ الرسالة الواحدة تُرسل نصًا خامًا بدل أن تضيع في 400.
- **ميزانية السرد تُقاس بعد الهروب لا قبله، وبحدّ الجسر الفعلي (`TelegramTextLimit`) لا برقم خاص بها.** الهروب لا يفعل إلا التوسيع: `&` واحد يصير خمسة أحرف، فصفحة عناوين مثل «AT&T» كانت تُقاس 3000 وتخرج 6000 مقابل حدّ 3500 — فتنقسم الرسالة ويسقط وضع التحليل، فيرى المشغّل `<b>` و`<blockquote expandable>` نصًا ظاهرًا بدل قائمة. و`Get-NewsListEscapedLine` تقصّ العنوان الخام ثم تهرّبه، لأن قصّ نص مهرَّب قد يقع داخل `&amp;` فيترك `&am` — وهذا ليس كيانًا، فيرفض تيليجرام الرسالة كلها لا ذلك السطر.
- **`EnableButtonStyles`** (افتراضيًا مفعّل) يطفئ الألوان لمن يراها صاخبة. تُقرأ عند الإرسال في `ConvertTo-TelegramReplyMarkupJson` لا عند بناء الزر، فيبقى بناء اللوحة دالة خالصة من المسودة، ويُقرأ الإعداد مرة لكل رسالة لا مرة لكل زر.
- التطبيقات الأقدم من هذه الحقول تتجاهلها، فالتسمية تبقى هي التي تحمل المعنى: اللون يشحذ الشاشة ولا يشرحها.

## 6.9.5 — 2026-08-31

- **الشكل المكدّس صار يميّز كل خبر عن أزرار جاره.** تيليجرام لا يمنح الزر لونًا ولا حدًّا ولا تباعدًا، فالتسمية هي الوسيلة الوحيدة: رقم الخبر يركب كل زر إجراء (`⬆️ 3` · `🗑 3`)، وعلامة العنوان تتناوب `▪️`/`▫️` فتقرأ الأخبار المتتالية كأشرطة منفصلة. بلا زرّ إضافي، فعدد الأخبار في الصفحة لم يتغيّر.
- **`compact`: شكل رابع، زرّ واحد لكل خبر.** النص كاملًا في متن الرسالة، وتحته شبكة أرقام خمسة في الصف؛ الرقم يفتح شاشة الخبر حيث ⬆️ ⬇️ ✏️ 🗑 موجودة أصلًا. هذا وحده ما يُدخل ٣٠ خبرًا في شاشة واحدة: الأشكال الثلاثة الأخرى تصرف ٤ أو ٥ أزرار للخبر، فثلاثون منها ١٢٥ أو ١٥٥ زرًا وتيليجرام يرفض اللوحة كاملة.
- سقف `compact` أربعون خبرًا لا خمسة وثمانين: كل زر يحتاج سطرًا مقروءًا فوقه، وخمسة وثمانون سطرًا تقتسم ميزانية الرسالة حتى لا يبقى منها ما يُقرأ.

## 6.9.4 — 2026-08-31

- **`NewsListLayout` بثلاث قيم بدل مفتاح `NewsListStackedLayout` الثنائي**: `text` (الافتراضي، نص الخبر في متن الرسالة وصف أزرار مضغوط)، `stacked` (الخبر بزر يملك سطره وأزراره تحته)، `inline` (الخبر داخل الصف مع أزراره — سلوك ما قبل 6.9.3، أُعيد لأنه الأكثر إيجازًا ولأن المقارنة تحتاج الثلاثة).
- `Get-NewsListLayout` هي القارئ الوحيد للقيمة وترتدّ إلى `text` عند قيمة غريبة، فلا تختلف الشاشة والنص وحساب الصفحة على ما هو مفعّل.
- **مَن كان مفعّلًا `NewsListStackedLayout` يعيد الاختيار مرة واحدة**: المفتاح القديم لم يُهاجَر — قيمة واحدة في إعداد واحد لا تستحق كود ترحيل يبقى للأبد.

## 6.9.3 — 2026-08-31

- **نص الخبر خرج من الزر إلى متن الرسالة في شاشة الترتيب.** تيليجرام يقسم عرض الصف بالتساوي على أزراره، فالعنوان الذي يشارك ثلاثة أزرار لا ينال إلا ربع الشاشة ويلتف ثلاثة أسطر، وكان يُقصّ عند 24 حرفًا قبل ذلك أصلًا. الآن يُسرد كاملًا فوق الأزرار حيث يملك عرض الرسالة كلها، ويُقصّ عند `NewsListLabelLength` التي صارت 120 حرفًا ووصفها «طول نص الخبر في قائمة الترتيب».
- **صفوف الأخبار صارت بحجم واحد.** أول خبر بلا ⬆️ وآخر خبر بلا ⬇️ ⇐ ثلاثة أزرار لا أربعة ⇐ عرض مختلف وارتفاع مختلف لهذين الصفين. الصف الآن أربعة أزرار دائمًا: الرقم يفتح الخبر، وسهم موجود أو `▫️` خامل مكانه، ثم 🗑.
- **السرد محسوب ليبقى داخل رسالة واحدة**: طول السطر يُقصّ عند نصيبه من ميزانية 3000 حرف مقسومة على عدد أخبار الصفحة، فحتى صفحة من 21 خبرًا طويلًا لا تتجاوز حدّ تيليجرام.
- التخطيط المكدّس (`NewsListStackedLayout`) لم يُمسّ: هناك العنوان يملك سطره أصلًا.

## 6.9.2 — 2026-08-31

- **باب مستقل في المساعدة للربط مع Google Sheets.** كان الشيت سطورًا داخل باب الأخبار تصف الزرّين ولا تصف الشيت نفسه: أي عمود يصل الهواء، وماذا يحدث للصف الفارغ، ولمن يعود الرفض حين تكون المسودة بيد زميل. الباب يشرح الاتجاهين — الشيت إلى الهواء والنشر إلى الشيت — وباب الأخبار صار يشير إليه بدل تكرار نصفه.
- **خطوات الضبط تظهر للمشرف وحده** داخل الباب نفسه: رابط تصدير CSV، ووضع المزامنة وفترتها المقروءة من الإعدادات فعلًا، وأن رابط الكتابة وسرّه في `config.json` بجانب `BotToken` لا داخل `Settings`. المشغّل يرى كيف يعمل الشيت، لا أين يسكن السرّ.

## 6.9.1 — 2026-08-31

- **يصلح: `/help` وزر 🆘 مساعدة كانا يفتحان الدليل القديم.** فهرس الأبواب أُضيف في `6.8.0` لكن المدخلين ظلّا يستدعيان `Get-HelpText` مباشرة، فكان الفهرس يُرى من زر القائمة وحده. الثلاثة تفتح الآن `Get-HelpHomeText`.
- **الدليل الكامل صار يُبنى من الأبواب نفسها.** النسختان تباعدتا فعلًا: «📄 الدليل كاملًا» لم يكن فيه شريط الأخبار ولا سحب الشيت إطلاقًا. تفاصيل مراقبة المخرج وأرقام صحة Cinegy انتقلت إلى باب المشرف حتى لا تضيع، والدليل تجاوز حدّ الرسالة الواحدة فيصل مقسّمًا كما كان يفعل زرّه أصلًا.
- **يصلح: انعكاس الأقواس حول رقم المُنفِّذ.** سطر الإخفاء يبدأ بعربي، فاتجاه الفقرة RTL والقوسان محايدان بين اسم عربي وأرقام لاتينية ⇐ يأخذان اتجاه الفقرة ويُعكسان: `)8201739556(`. `Format-UserAuditActor` تضع الآن علامة LRM على الجانبين.
- **اسم المُنفِّذ في كل سطر تدقيق لا في عمليات الهواء وحدها.** 39 موضعًا كان يكتب الرقم مجرّدًا، و«user» و«by» و«schedule» و«Alias» الإنجليزية استُبدلت بالعربية — كلمة لاتينية وسط سطر عربي تقفز في العرض لنفس سبب الأقواس.
- **فشل ffmpeg بسبب المصدر لم يعد يُسجَّل كعطل في الجسر.** `Test-MediaSourceUnreachable` تقرأ سطر سبب ffmpeg: تعذُّر فتح المدخل أو ردّ 5XX من مضيف البث يصير WARN، وأي فشل آخر يبقى ERROR. ومراقبة المخرج تقول إن المصدر ساقط مرة واحدة لا مرة لكل محاولة — عدّاد الإخفاقات المتتالية وتنبيه العتبة موجودان أصلًا، وعشرون سطرًا متطابقًا فوقهما لا يضيفان شيئًا.
- **`400` من `answerCallbackQuery` صار WARN لا ERROR.** معناه زرّ قديم أو استعلام انتهت صلاحيته، وكان أكثر سطر ERROR تكرارًا في السجل فيغرق الأعطال الحقيقية.

## 6.9.0 — 2026-08-31

- **يحفظ الشريط في الشيت عند النشر من تيليجرام**، فيصبح التزامن باتجاهين: الشيت يغذّي الهواء، والتحرير في تيليجرام يعود إلى الشيت. الشيت يبقى السجل المقروء أيًا كان مكان التحرير.
- الكتابة عبر **Google Apps Script منشور كتطبيق ويب** لا عبر Sheets API: الأخير يتطلب OAuth أو مفتاح حساب خدمة مع توقيع RS256، ولا تفعل PowerShell ذلك دون مكتبة إضافية. السكربت الجاهز مع خطوات النشر في [`docs/news-sheet-writeback.gs`](docs/news-sheet-writeback.gs).
- **رابط الكتابة وسرّه في أعلى `config.json` بجانب `BotToken`، لا في `Settings`.** `Invoke-SettingsExport` لا يكتب إلا مفاتيح `DefaultSettings` — وهو نفسه ما يمنع تسرّب توكن البوت في ملف مُعاد توجيهه إلى محادثة. رابط فارغ يعطّل الميزة كليًا.
- **فشل الحفظ في الشيت لا يلغي النشر.** الشريط يكون قد وصل الهواء فعلًا، فيُبلَّغ الفشل ويُسجَّل ولا يُتراجَع عن شيء.
- ردّ Apps Script يكون `200` حتى حين يرفض السرّ، لذلك يتحقق الجسر من `"ok": true` في الجسم لا من رمز الحالة.
- لا حلقة لا تنتهي مع المزامنة التلقائية: النشر يكتب في الشيت، ثم تقرأ الدورة التالية المحتوى نفسه فتراه غير مختلف ولا تعيد النشر.

## 6.8.0 — 2026-08-31

- **يضيف «🚀 بداية سريعة»**: بطاقة واحدة تأخذ مشغّلًا لم يفتح البوت من قبل إلى أول قالب على الهواء في ست خطوات، ثم ثلاث قواعد تريحه. ليست قائمة ميزات — من يتسلّم وردية يحتاج الضغطات التي تعمل، والباقي في الفهرس.
- **يحوّل المساعدة إلى فهرس أبواب** بدل شاشة واحدة طويلة. مَن يسأل «كيف أخفي هذا» يريد هذه الإجابة لا أن يمرّ بالجدولة ليصل إليها. ثمانية أبواب للمشغّل وتاسع للمشرف، وكل باب شاشة واحدة.
- داخل كل باب **⬅️ السابق · 📖 الفهرس · ➡️ التالي**، فتبقى القراءة المتسلسلة ممكنة لمن يريد الدليل بترتيبه، دون إجبار الباقين عليها. و**📄 الدليل كاملًا** يبقى كما كان بآلية «المزيد».
- باب المشرف يُحذف من قائمة غير المشرف بدل عرضه مقفلًا، ورقم الباب يُحسب من الأبواب التي يراها القارئ فعلًا.
- **يصلح ثغرة: سحب الشيت كان يتجاوز قفل المسودة.** كسر قفل زميل فعل مشرف في كل شاشات الأخبار («🔓 إلغاء القفل (مشرف)»)، لكن سحب الشيت — بعد فتحه للمشغّلين في `6.6.0` — كان يمحو مسودة الزميل بتأكيد واحد. الآن يُمنع المشغّل ويُوجَّه إلى «🔓 طلب فكّ القفل»، ويبقى التجاوز للمشرف، والزرّان يختفيان أصلًا عمّن لا يملك ذلك.

## 6.7.0 — 2026-08-31

- **يطلب زرّا سحب الشيت تأكيدًا دائمًا**، لا عند التعارض فقط. بعد فتح السحب للمشغّلين في `6.6.0` صارت نقرة واحدة كافية لإعادة كتابة الشريط؛ الآن الضغطة الأولى تسأل والثانية تنفّذ.
- **رسالة تأكيد واحدة تحمل كل الحقائق** بدل «هل أنت متأكد» عامة يتبعها تحذير ثانٍ عن صاحب المسودة: تأكيد النشر يذكر كم خبرًا سيُستبدل على الهواء، وتأكيد المسودة يوضح أن شيئًا لن يصل الهواء قبل «مراجعة ونشر»، وكلاهما يسمّي صاحب المسودة الحالية إن كانت مشغولة.
- `news:sheet` و`news:sheetdraft` صارتا تسألان فقط، والتنفيذ في `news:sheetconfirm` و`news:sheetdraftconfirm` — فصل مقصود حتى لا يكون زر الشاشة هو نفسه الزر المنفّذ.

## 6.6.0 — 2026-08-31

- **يفتح سحب الشيت لكل المشغّلين، لا للمشرفين وحدهم** — الزرّان معًا: «⬇️ سحب ونشر» و«📝 سحب إلى المسودة». المحتوى محكوم بالشيت أصلًا، فالسحب ينشر نسخة تحريرية لا نصًا يكتبه الضاغط.
- يضيف `AllowOperatorsSheetPull` (افتراضيًا `true`) على نمط بقية مفاتيح `AllowOperators*` في قسم الأخبار، فيستطيع المشرف إعادة السحب خلف حاجز الإشراف بإعداد واحد. الفحص مزدوج كما كان: الزر لا يُرسم، والضغط عليه يُرفض — لأن تيليجرام يحتفظ بالأزرار في الرسائل القديمة.
- **يجعل `NewsSheetNotifyScope` افتراضيًا `all`**، فيصل تنبيه ما تغيّر إلى كل المحادثات المصرّح لها بدل المشرفين وحدهم. التنبيه نص للقراءة فقط ولا يمنح أحدًا زرًا ولا صلاحية.

## 6.5.0 — 2026-08-31

- **يضيف «📝 سحب إلى المسودة» بجانب «⬇️ سحب ونشر».** الأول يحمّل الشيت في المسودة ليراجعه المشرف ويرتّبه أو يصحّحه قبل أن يصل الهواء، والثاني ينشر مباشرة كما في `6.4.0`. الاختيار عند كل سحبة، بلا إعداد يُبدَّل.
- التحميل إلى المسودة يمر بمسار الاستيراد العادي، فتنطبق عليه حدود الطول والعدد ورفض التكرار نفسها. ويستبدل محتوى المسودة بدل أن يضيف إليه، فسحبتان متتاليتان لا تُنتجان نسخة مكررة.
- السحب إلى المسودة يعمل حتى حين يكون الشيت مطابقًا لما على الهواء: «لا تغيير» تعني أنه لا شيء لينشر، لا أن المشرف لا يريد مسودة يعمل عليها.
- **يستبدل `NewsSheetNotifyAdmins` بـ`NewsSheetNotifyScope`** بثلاث قيم: `none` أو `admins` (الافتراضي) أو `all`. القيمة الأخيرة تُنبّه كل المحادثات المصرّح لها، لغرفة أخبار تريد أن يرى الجميع ما خرج. إعداد واحد بدل منطقيّ ونطاق يمكن أن يتناقضا؛ المفتاح القديم يبقى غير ضار ويظهر تحت «متقدم».
- السحب إلى المسودة لا يرسل تنبيهًا: لم يصل الهواء شيء بعد.

## 6.4.0 — 2026-08-31

- **يُحدَّث شريط الأخبار من Google Sheets داخل الجسر.** يُضبط رابط تصدير CSV للشيت في `NewsSheetCsvUrl`، فيجلبه الجسر ويحوّله وينشره بنفسه. هذا يُلغي الحاجة إلى Power Automate والسكربت الوسيط معًا، والأهم أنه يُنهي السباق على `news.txt`: كان طرفان يكتبان الملف نفسه، والآن يمر كل نشر عبر `Publish-NewsTickerFile` بنسخته الاحتياطية وتحققه بعد الكتابة وكشف تعارضه.
- **وضعان قابلان للتبديل من الإعدادات:** `manual` (افتراضي) يضيف زر «⬇️ سحب من الشيت» للمشرف في شاشة الأخبار، و`auto` يزامن كل `NewsSheetSyncMinutes` دقيقة. الافتراضي يدوي عمدًا: مهمة غير مراقَبة تعيد كتابة ما على الهواء يجب أن يُشغّلها المشرف عن قصد.
- **المزامنة التلقائية تتنحّى لمن يحرّر.** إن كانت مسودة الأخبار بيد مشغّل، تُتخطّى الدورة وتُعاد المحاولة لاحقًا؛ الكتابة فوق مسودة نصف مكتوبة تُتلف عملًا لا يستعيده صاحبه ولا يعرف سببه. السحب اليدوي فعل مقصود فيمضي، لكنه يسمّي صاحب المسودة ويطلب تأكيدًا أولًا.
- **الشيت الفارغ لا يُنزل الشريط عن الهواء.** شيت لم يُحمَّل، أو أفرغه أحد سهوًا، يُرفض بدل أن يمسح الأخبار؛ المسح يبقى فعلًا يدويًا مؤكَّدًا.
- **يُقرأ الشيت كـCSV حقيقي لا كأسطر نص.** الخلية التي تحوي فاصلة يصدّرها Google بين علامتَي اقتباس، وكان السكربت السابق يضع الاقتباسين على الهواء؛ كما كان يشطر الخلية متعددة الأسطر إلى عنوانين ناقصين. الأعمدة بعد الأول ملاحظات المحرّر ولا تصل الشريط.
- التنزيل عبر https فقط، بمهلة محدودة وسقف بايتات (`NewsImportMaxBytes`)، لأن ما يُنزَّل يصل الهواء.

## 6.3.0 — 2026-08-31

- **يعرض «🧾 عملياتي» مرجعًا قصيرًا لكل عملية** (أول ثمانية محارف من معرّف `AIR_OP`). كان المعرّف داخليًا لا تُظهره شاشة، فلا يملك المشغّل ما يذكره حين يبلّغ عن خلل؛ الآن يقول «مرجع 1fbf8e20» ويجد المشرف سطرها في السجل بحثًا واحدًا. المعرّف الكامل 32 محرفًا لا يُقرأ على الهاتف ولا يعيد أحد كتابته.
- **يضيف شاشة «🗂 ملفات التشغيل»** إلى مركز الصحة (للمشرف): لكل ملف حالة — سليم، أو غير مكتوب بعد، أو تالف بنسخة احتياطية يستعيدها الجسر عند الإقلاع، أو تالف بلا نسخة. «غير مكتوب بعد» ليس عطلًا ولا يُلوَّن أحمر، وإلا تعوّد المشرف على تجاهل الشاشة.
- **يضيف سطر الاستخدام إلى مركز الصحة:** عمليات اليوم، عدد المشغّلين، عدد ما على الهواء، ومدة التشغيل. يُقرأ من الحالة في الذاكرة، فالشاشة ما زالت لا تنفّذ أي probe جديد.
- يؤشّر خطة شريط الأخبار (2026-08-23) بأثر رجعي: الميزة مشحونة منذ مدة والصناديق وحدها كانت متأخرة.

## 6.2.0 — 2026-08-30

- **يقسّم `Tests/Bridge.Tests.ps1`** (8095 سطرًا) إلى تسعة ملفات حسب الموضوع، مع إعداد مشترك واحد في `Tests/Bridge.TestContext.ps1` بدل نسخه في كل ملف. لا اختبار حُذف ولا نص تغيّر؛ النقل بحدود كتل `Describe` كاملة.
- **يضيف اختبارات لمسار الإنتاج في «♻️ استعادة الافتراضي» و«🔎 بحث».** كانت دالتا الوحدة المُختبَرتان محذوفتين ومسار الإنتاج بلا تغطية: التأكيد قبل الكتابة، تجاهل اسم إعداد غير معروف، قصّ نص البحث، وأن ما يظهر على الشاشة هو نتيجة `Find-BridgeSettings` نفسها.
- **يوحّد قراءة `audit.jsonl` في «🕘 ماذا فاتني».** كانت الدالة تعرّف قارئ حقول خاصًا بها وتحلّل الطابع الزمني بـ`TryParse` عاريًا؛ صارت تستدعي `Get-AuditRecordField` و`Read-AuditRecordStamp` مثل بقية القرّاء، فتعامل ختم `Z` بـ`RoundtripKind`.

## 6.1.0 — 2026-08-30

- **يضيف زر «📊 تقارير» بقسمَي البنرات والأخبار.** تقرير البنرات يقرن كل عرض بما أنهاه (إخفاء أو خروج أو استبدال على الطبقة نفسها) فيعرض النص الذي ظهر ومَن عرضه ومدة بقائه؛ وما لم ينتهِ يُعرض «ما زال على الهواء» بلا تخمين مدة. تقرير الأخبار يجمع النشر اليومي ويوزّعه على ناشريه. أربع مدد: اليوم، أمس، 7 أيام، 30 يومًا. المشغّل يرى عملياته، والمشرف والمالك يريان الجميع.
- **يتيح تحميل أي تقرير كملف HTML** مكتفٍ بذاته ومن اليمين إلى اليسار، بلا أي مرجع خارجي لأن آلة الإخراج قد تكون بلا إنترنت. الطباعة إلى PDF تتم من المتصفح مباشرة بعربية سليمة التشكيل. نصوص المشغّل مهرَّبة فلا تتحول إلى وسوم.
- **يسجّل نص القالب الذي ظهر على الهواء** في `audit.jsonl` لعمليتَي العرض والتحديث، بحد أقصى قابل للضبط (`AuditTemplateValuesMaxChars`) وبإمكانية التعطيل (`AuditTemplateValues`) لأن الملف دائم ويُؤرشف. تعرضه شاشة «🧾 عملياتي» مع تصنيف القالب.
- **يوضح «🕘 ماذا فاتني» مَن نفّذ ماذا.** يذكر مجموع العمليات، ويوزّع القالب المشترك على مشغّليه («محمد 3 · أحمد 2») بدل تسمية آخر شخص فقط، وينسب آخر إخفاء لصاحبه.
- يحدّ مسح التقارير بـ5000 سجل ويعلن ذلك في التقرير عند بلوغه؛ التقرير المقصوص الصامت يُقرأ كأنه شامل.
- **تتحدث شاشة «🧾 عملياتي» بالعربية بدل مصطلحات البروتوكول.** كانت تعرض `SHOW`/`HIDE` ورقم الطبقة وعدّاد المللي ثانية لمن يضغط الأزرار؛ صارت جُملًا عربية بأسلوب شاشة «📜 السجل»: رمز النتيجة أولًا، ثم الجملة، ثم الإرشاد في سطر مستقل.
- **تحذف وحدة `BridgeStateMigration` غير المستخدمة.** لم يكن أي من صادراتها الأربع مُستدعى من كود إنتاجي؛ الترحيل الفعلي يجري في `Import-OnAirState` عبر `ConvertTo-BridgeLiveSceneState`، والكتابة الذرّية مع النسخة الاحتياطية في `Write-BridgeValidatedJson`. صُحّح `docs/VERSION-6.md` ليذكر ما هو موصول فعلًا.
- **يوحّد قسم «✨ الجديد» حول الإصدارات المُسلَّمة.** كانت الشاشة تفتح على أربعة عناوين 6.0.0 متطابقة تقريبًا (الإصدار وثلاث نسخ preview داخلية لم تصل مشغّلًا قط).

- **تبقى شاشتا «📜 السجل» و«🧾 عملياتي» ممتلئتين بعد إعادة التشغيل.** كانت الشاشتان تقرآن من الذاكرة وحدها فتقولان «منذ آخر تشغيل»؛ صارتا تُعاد بناؤهما عند الإقلاع من `audit.jsonl` — السجل الدائم الذي يقرأ منه «🕘 ماذا فاتني» و`/who` أصلًا — بما في ذلك الأرشيف. لا ملف حالة جديد، ولا أي كتابة إضافية على مسار العرض/الإخفاء.
- يقرأ استرجاع الطوابع الزمنية بـ`RoundtripKind`، فلا يتحول ختم `Z` إلى توقيت محلي ثم يُزاح مرة ثانية عند العرض.
- **يصلح ازدواج تأكيد أزرار ترتيب الأخبار.** كانت `news:up`/`down`/`iup`/`idown` تؤكّد الـcallback مرتين لنفس الضغطة؛ أصبحت كل ضغطة تُؤكَّد مرة واحدة فقط.
- **يصلح ربط مؤقت الإخفاء التلقائي بهوية Cinegy الفعلية** عند اختلاف معرّف أمر SHOW عن معرّف العنصر الذي يعيده محرك Cinegy لنفس المشهد.
- يعيد تسمية `Queue-BridgeOperation` إلى `Add-BridgeOperation` (الاسم القديم يستخدم فعل PowerShell غير معتمد وكان يظهر كتحذير في Analyzer).
- يغلق بقية ملاحظات مراجعة v6 ويحسّن صفوف شاشة الإعدادات.

## 6.0.0 — 2026-08-29

- **يحسّن قراءة الإعدادات داخل الأقسام.** كل بند يحتفظ بصف كامل ويعرض اسمه العربي مع حالته أو قيمته الحالية ووحدتها؛ إعدادات طبقات الإخفاء وأسماء الطبقات تعرض ملخصًا مباشرًا، دون تغيير توزيع الأقسام أو callbacks أو الصلاحيات.
- يضمن أن كل مفاتيح الإعدادات الحالية تحمل تسمية عربية فعلية، وأن قيم الصفوف الكاملة تبقى ضمن حد عرض آمن (64 محرفًا) حتى لا تكسر لوحة Telegram.
- يحافظ قبول دفعات Telegram على ترتيب الأوامر التابعة للطبقة نفسها؛ لا يمكن أن يتحول SHOW ثم HIDE إلى HIDE ثم SHOW.
- يرفض تفعيل `Multi` إذا لم تثبت Cinegy هوية نشطة غير فارغة، ويحفظ `onair.json` جميع سجلات المشاهد canonical على الطبقة المشتركة عند العرض وإعادة الحفظ؛ يحدّث المشهد المُمثَّل فقط ويترك بقية كتالوج Multi محفوظًا.
- ربط سجل دورة العمليات المحدود بنتائج SHOW/HIDE/EXIT/UPDATE الفعلية باستخدام معرّف `AIR_OP` نفسه؛ تبقى العملية `queued` خلال التحقق والصلاحيات ولا تدخل `running` إلا قبل أول استدعاء إلى Cinegy.
- يمنع سجل دورة العمليات المحدود تكرار العملية نفسها عند الانتقال من `queued` إلى `running`، ويحافظ على حد السعة.

- **Version 6 official release.** The visible administrator audit trail and durable `AIR_OP` records retain the immutable numeric user id and, when configured, the corresponding administrator Alias. Template SHOW, HIDE, EXIT, undo, hide-all, live-value updates, and auto-hide timer actions use the same safe `Alias (ID)` representation; users without an Alias retain their numeric id only. The release keeps the existing configuration and template formats and may be merged into `main` through the documented side-by-side upgrade path.

## 6.0.0-preview.3 — 2026-08-29

- **Fixes timed auto-hide being discarded for the original operator without weakening replacement safety.** Cinegy can expose a different engine active id than the client EventId sent with `SHOW`; the bridge now resolves and persists that identity synchronously inside the successful SHOW using a positive exact template-name match. A missing or ambiguous identity does not arm a destructive timer, and a later watchdog observation can never transfer old work to a replacement—even one using the same template name. Attaching a timer to an existing scene performs the same live identity check.
- **Adds an explicit personal-reminder acknowledgement and one optional follow-up.** The notified operator gets a «تمت المعالجة» button; only that user can acknowledge it. If it remains unacknowledged and the same scene is still live, one follow-up is sent after `TemplateReminderFollowUpMinutes` (default 5 minutes, 0 disables). The acknowledgement identity and follow-up stage survive restart.
- **Adds approximate user activity to administrator tools.** Users are labelled recent, idle, or unknown from their cached last interaction. `UserActivityRecentMinutes` controls the window (default 5 minutes), and the UI states clearly that Telegram bots do not receive true online presence.
- Makes queue consumption transactional: if persisting removal fails, a due auto-hide or reminder remains pending and is not executed in a way that could repeat after restart.
- Adds regression coverage for exact SHOW identity, later-replacement safety, persistence failures, follow-up persistence and ownership, disabled follow-ups, and recent/idle/unknown user activity.

## 6.0.0-preview.2 — 2026-08-28

The complete Version 6 delivery record and merge gate are documented in [`docs/VERSION-6.md`](docs/VERSION-6.md).

- **Prioritizes emergency removal inside each Telegram update batch.** HIDE, EXIT, and hide-all callbacks run before SHOW and administration while equal-priority actions retain Telegram order. The bridge remains deliberately single-worker so shared PowerShell state is not exposed to unsafe runspace concurrency.
- **Rejects duplicate Telegram update ids through a bounded 4,096-entry in-memory ledger.** The polling offset advances from the highest fetched id before priority ordering, avoiding offset regression when processing order changes.
- **Adds an administrator health center.** One non-blocking screen summarizes cached Telegram and Cinegy health, output monitoring, relay state, storage warnings, pending schedules, and recent errors, with refresh, full-status, and diagnostic controls.
- **Introduces a unified setting-schema record.** Every existing default now composes into a record containing type, category, Arabic label, unit, description, protection flag, and constrained choices; future settings still fall back to Advanced.
- **Adds explicit state-migration primitives.** Legacy unwrapped data is version zero, ordered migration steps produce a current envelope, and missing or future schema versions fail with structured results.
- **Bounds large administrator catalogues.** Template administration, user administration, and pending access requests use absolute-index pages and remain below Telegram's 100-button keyboard limit. Stress coverage includes 1,000 templates, 250 users, and 200 requests.
- **Defines Cinegy shared-layer `Multi` mode without inventing unsupported scene targeting.** Several named template definitions may share a layer while Cinegy reports one active item; HIDE and EXIT remain layer-scoped and `Single` remains the default.
- **Adds Version 6 operation, settings-validation, migration-health, and readiness contracts.** Operation correlation states, numeric setting ranges, simple/advanced schema filtering, single-setting reset, timestamped state backups, cached readiness, and measured 1,000-item acceptance are covered by the release gate.
- **Completes the administrator settings discovery surface.** Search accepts Arabic labels/descriptions, modified-only and simple/advanced views are paged, and each setting can be reset independently after confirmation without resetting unrelated configuration.

## 6.0.0-preview.1 — 2026-08-28

- **Replaces the unbounded administrator settings keyboard with eight Arabic operational categories.** Security, on-air operation, templates/layers, news, scheduling, monitoring, storage, and advanced settings are now discoverable without scrolling through every technical key.
- **Pages large setting categories at eight options.** Previous/next controls preserve deterministic default-setting order while existing toggle, numeric, string, protected-setting, backup, layer-name, emergency-scope, and reset paths remain compatible.
- **Keeps every setting reachable.** A centralized navigation map assigns current settings to one category, and unknown future keys automatically fall back to Advanced rather than disappearing from Telegram administration.
- Adds administrator-gated `cfgcat:<category>:<page>` navigation and regression coverage for schema completeness, pagination, Arabic labels, callback routing, and role enforcement.
- Begins the Version 6 current-architecture program; no desktop application, service rewrite, or configuration migration is introduced in this preview.

## 5.7.9 — 2026-08-27

- **Keeps self-service access requests enabled by default while restricting approval controls to administrators and the owner.** Operators can still request access; only the authorized management roles see and can use the approval entry point.
- **Makes single-instance startup tolerant by default.** If Windows cannot enforce the mutex, the bridge continues with a warning; `-RequireSingleInstance` is available for deployments that must refuse startup when the lock cannot be verified.
- **Hardens settings imports.** JSON values are type-checked, all changes are saved in one transaction, and failed persistence restores the in-memory settings.
- **Raises the fixed JSON upload limits to 10 MiB.** Settings and template-registry imports now allow larger files for installations with many templates; the configurable news-import limit is unchanged.
- **Makes the template-registry count limit configurable.** `TemplateRegistryImportMaxTemplates` defaults to 1000, so the previous fixed 200-template ceiling no longer blocks larger catalogues.
- Rejects Telegram documents that already report an invalid size before downloading them, and makes analyzer execution failures fail the validation gate instead of reporting a misleading clean result.
- Adds regression coverage for exact bridge-process matching, role-gated approval visibility, typed/atomic settings import, and pre-download size limits.

## 5.7.7 — 2026-08-27

- **Enforces the role hierarchy `owner > administrator > operator`.** A configured owner inherits every administrator permission even when their id is not duplicated in `AdminUserIds`; administrators still cannot appoint or remove administrators or use other owner-only actions.
- **Routes personal template reminders to the initiating user.** When a template is launched from a Telegram group, its elapsed-time alert is sent directly to the user who launched it instead of being posted back to the group. Scheduled launches retain their creator's user id and follow the same rule.
- **Labels every operator snapshot with its real source.** Both the «📸 صورة من البث» and «📷 لقطة الآن» buttons use the same capture path; the sent photo now says whether its frame came from the primary broadcast or Cinegy backup. The cooldown-resend path retains the source captured with that frame rather than relabelling it based on the current failover state.
- Adds regression coverage for the primary and backup source labels.

## 5.7.6 — 2026-08-27

- **Adds per-template personal on-air reminders.** An administrator or owner can set `reminderMinutes` (0–1440) from a template's details. When an operator or their scheduled event shows that exact scene, the deadline is stored in `logs/template-reminders.json`; after a restart it resumes and messages that same operator only if the scene is still visible. A replacement or hidden scene cancels the reminder quietly.
- **Keeps permanent graphics quiet.** `longRunning: true` templates—logos and tickers intended to run 24/7—are excluded from personal reminders, even if a stale timer was saved before the template was marked long-running.
- Adds regression coverage for persistence, delivery to the original operator, replacement safety, long-running exclusion, and parsing `reminderMinutes`.

## 5.7.5 — 2026-08-27

- **Persists timed-show auto-hide deadlines across restart.** The remaining deadline is stored in `logs/autohide.json` and restored after the on-air state, so a duration selected before a bot restart still ends. The saved scene identity is checked before hiding; if the layer now carries a different scene, the stale timer is discarded safely.
- Adds regression coverage for restoring a timed-show deadline, hiding after the remaining duration, and refusing to hide a replacement scene.

## 5.7.4 — 2026-08-27

- **Turns an unreachable output source into an actionable alarm.** The output watchdog previously returned quietly whenever ffmpeg could not capture a frame, deliberately avoiding a false "black" claim but leaving a stopped HLS origin invisible to Telegram. It now counts consecutive capture failures and alerts administrators at `OutputMonitorFailureAlertThreshold` (default 2) when no backup is configured, once per outage, with one recovery notice when the primary source is captured again.
- **Adds automatic Cinegy SRT failover.** `LiveStream.BackupSourceType` and `LiveStream.BackupSourceUrl` define an optional standby source. The first failed primary probe immediately switches snapshots and the live relay to the standby; an already-running relay is restarted so ffmpeg opens the new input. The primary remains the only health probe, and a successful primary capture returns the bridge to it. A failed operator snapshot also retries once from the standby instead of returning an avoidable error.
- **Makes source status actionable for the right roles.** The full-status report now runs a manual primary-source probe and labels the monitoring server, active source, standby configuration, periodic-monitor state, and consecutive failures. The read-only report is available to administrators and the owner; other users remain excluded.
- The deployed configuration uses Cinegy Playout instance 0's local feedback stream, `srt://127.0.0.1:5421`, as the standby source. It was verified by capturing a real frame with ffmpeg before release.
- Adds regression coverage for unavailable-source alerts, fallback selection, one-time switching, and restoration to the primary source.
- **Keeps scheduled templates working across a bridge restart.** Pending events are restored from the validated schedule store before polling resumes. At the timer moment the current template registry is checked again; missing or invalid templates are recorded as a failed attempt instead of being sent, while valid templates still pass the existing live Cinegy layer verification immediately before SHOW. A regression test covers persist → simulated restart → timer execution exactly once.

## 5.7.3 — 2026-08-26

- **Fixes the duration formatting that 5.7.2 claimed to fix.** `Format-DurationSeconds` only promoted a value to minutes when it divided exactly by sixty, so the status screen still reported "منذ 107775 ثانية" for a ticker that had been up for a day. Uptime and scene age are almost never a round number of minutes, which is to say the fix reached almost nothing. Promotion now happens whenever there are enough seconds: leftover seconds are kept below an hour, where "دقيقة و30 ثانية" is useful, and dropped above it, where they are noise beside a day. Found reviewing 5.7.2, reported by the operator on the running channel within the hour.
- "آخر فحص ناجح" says "الآن" rather than "منذ 0 ثانية".
## 5.7.2 — 2026-08-26

- **Durations on the status screens read as durations.** They counted in a single unit for ever: a ticker on air since yesterday showed "1560 دقيقة", the stale-data label showed "متأخر منذ 372741 ثانية" - four days written as a number nobody divides out at a glance - and uptime showed "26س 5د". All three go through the duration formatter now, and `Format-Duration` delegates rather than duplicating, so the on-air row, the layer-lock notice, the auto-hide confirmation and the duration picker improve together instead of four of them drifting apart.
- "آخر فحص ناجح" leads with how long ago rather than a wall-clock stamp, because the question that line is asked is whether the reading is current. The clock time stays in brackets for anyone comparing against the log.
- **Archives the audit trail past `AuditMaxSizeMB` (110) instead of letting it grow without bound.** `bridge.log` has rotated for a long time; `audit.jsonl` - the permanent record of who put what on air - never had a limit at all. It now rolls into `audit-<stamp>.jsonl` and keeps every archive by default. That is the deliberate difference from `bridge.log`, which drops its oldest generation: right for a diagnostic log, wrong for the record whose whole purpose is answering questions about the past. `AuditArchiveKeepFiles` exists for a disk that genuinely demands pruning and defaults to 0, meaning keep everything.
- `Read-AuditRecords` reads back through the archives when the active file is short, so the first digest after a rotation does not report an empty morning.
- Two faults found while building the rotation, both introduced by it: `Invoke-AuditRotation` returned a boolean into `Write-AuditRecord`'s output stream, so `Invoke-ShowTemplateResult` came back as an array and lost its `.Success` - every operation that writes an audit record was affected; and the archive glob `audit-*.jsonl` adopted anything sitting in the log folder and read it as history, now narrowed to the exact stamped shape with the active file excluded.
- Test suite grows from 607 to 611.
## 5.7.1 — 2026-08-26

- **Makes the Cinegy health alert answer a question worth waking up for.** An administrator was warned about "الساقط 34، الخرج 1467" - thirty-four dropped frames, which sounds alarming and is 2.3% of what actually went out. A bare count cannot tell those apart, and the sample window is not a fixed size. Frame loss now has to cross both an absolute floor (`CinegyFrameLossTolerance`, 5) and a share of output (`CinegyFrameLossTolerancePercent`, 5%) before it counts as a fault: the count stops a tiny sample raising an alarm on percentages, the percentage stops a busy minute raising one on counts.
- Adds hysteresis to the health state itself. The tolerance filters the values; this filters the verdict. Even inside tolerance the channel kept crossing the line and back, and the log holds 332 health transitions - 23 of them today - on an engine that was fine every time anybody looked. `CinegyHealthConfirmChecks` (3) readings must now agree before the state moves at all. A monitor that changes its mind every minute is one operators stop reading, which costs more than the outage it was watching for.
- Explains the output monitor to administrators, in the help screen and in its own words: a frame every `OutputMonitorMinutes` taken from the channel output rather than from anything Cinegy says about itself, a confirming second frame after a dark one, an alert only when both are black so a deliberate fade wakes nobody, once, then a recovery notice. The health thresholds are spelled out beside it with their live values, so an administrator drowning in alerts can see which number to raise.
- The news draft timeout was thirty minutes, which expired a 28-item draft mid-edit. It is two hours now, and `NewsDraftTimeoutMinutes` finally appears on the settings screen - it had no metadata entry, so it could not be changed from the bot at all. Expiry also hands the items back as text instead of reporting a body count: an unpublished draft is somebody's work, and the lock hand-over has returned it since 5.5.
- Durations are spelled out wherever a setting is measured in time. `1200` reads as `20 ساعة` rather than a number the reader has to divide, and Arabic's own counting is respected - dual for two, plural three to ten, singular again from eleven - so `5 ثانية` and `كل 1 دقيقة` stop happening.
- Test suite grows from 592 to 607.
## 5.7.0 — 2026-08-26

- **Adds an owner role, and makes it the only one that can appoint administrators.** Until now the bot could disable, rename and revoke a user but never promote one, so adding an administrator meant hand-editing `config.json`. Promotion and demotion now live on the 👥 المستخدمون screen, behind a confirmation, and the person whose role changed is told directly rather than discovering it when a button stops working.
- The owner is whoever set the bridge up: with `OwnerUserIds` unset it is the first id in `AdminUserIds`. Deliberately the first id and not every administrator - the point of the tier is that appointing administrators is narrower than being one. Naming ids in `OwnerUserIds` overrides that.
- `OwnerUserIds` is never written by the bridge. It sits outside `Save-Config`'s managed keys, so ownership changes by hand in `config.json` and nowhere else - the bot cannot promote its way to controlling who controls it.
- Ownership matches on the user id alone, with no `RequireUserLevelAuth` relaxation. Administrator rights can be granted to a whole chat; owner rights are a person, and a group must never confer them.
- Guards: the owner cannot be demoted, the last administrator cannot be demoted, and only an already-authorized user can be promoted - so promotion never doubles as a way in. The promote/demote buttons are drawn only for an owner, because a button that always answers "not allowed" is worse than no button, and ownership is re-checked when the confirmation is tapped rather than only when it was drawn.
- Adds the news item where a ticker wants it: at the front. A newly typed item used to be appended behind however many older items the draft already held, which is the opposite of what a ticker is for. `NewNewsItemAtTop` (default on) turns it back into an append for a rundown ordered by hand, and the confirmation now says which end it landed on.
- The add-template wizard can finally reach a real scene. It refused every absolute path and required the file to sit inside the bridge's own project folder - scenes live in C:\Cinegy\Titler\Scenes, on a mapped drive or on a UNC share, never there - so the guided flow could not register a single real template and the only way in was the raw JSON screen, which has always accepted absolute paths. It now takes any path Cinegy can open, reports which file it actually looked at, and treats a file it cannot see as a warning rather than a refusal.
- The wizard's layer step also takes a device name, so the logo on `gfx_logo` no longer needs hand-edited JSON, and it names the templates already sitting on a layer number as it is typed. A fifth, skippable step captures description and category.
- Admin-only commands are no longer advertised to every operator. Each screen and handler was already gated, but `setMyCommands` registers one global list, so ⚙️ الإعدادات, 📜 سجل and the diagnostics commands appeared in everyone's ☰ menu and were refused only once tapped. The default scope now carries the twelve an operator can use; administrators get all eighteen in their own chat, and a demoted administrator's scope is cleared.
- Template paths accept `%PROGRAMDATA%`-style environment variables, and `TemplateBasePath` lets `templates.json` carry a bare file name. UNC shares and mapped drives already worked.
- Sweeps a class of StrictMode faults rather than the four instances that surfaced: every Cinegy entry point now returns the same field set on success and failure, so a caller reading `.Error` or `.StatusCode` on the wrong branch cannot crash the bridge exactly when the engine is in trouble.
- Adds a 📄 المزيد button for long screens. The release notes lead with the newest three versions and the audit log with its first page; the rest arrives only when asked for. Send-TelegramMessage already split past 4096 characters, but it fired every part at once - four unasked-for messages, with the beginning somewhere above. The release notes split at a version boundary rather than a character count, so the first screen ends where a release ends.
- Test suite grows from 557 to 592.

## 5.6.1 — 2026-08-26

- **Stops the staleness alert crying wolf on graphics that are meant to stay up.** The news ticker was reported to administrators as a stale record five times over two days while it was genuinely on screen - its Cinegy `Active Id` matched the record exactly. The alert measured elapsed time alone, and an alert that fires on correct behaviour teaches operators to ignore it. Two independent exemptions now answer it: Cinegy's own declared `Duration` for the active item (a ticker is scheduled as `24:00:00` with a manual end), honoured via `RespectCinegyItemDuration`; and a `longRunning` flag on the template for engines that declare nothing useful. An unreachable engine never silences the alert - "cannot check" and "fine" are different things.
- Gives the Cinegy health check a tolerance instead of demanding perfection. One dropped frame inside the sixty-sample window turned the whole channel unhealthy, and the next check - the window having rolled past it - turned it healthy again: 54 transitions in a single day on a channel that was fine. `CinegyFrameLossTolerance` (default 5 frames) and `CinegyReadErrorRateTolerance` (default 0.5%) set where a drop stops being television and starts being a fault. Counts are still reported in full: within tolerance never reads as nothing happened.
- `Get-TitlerLayerStatus` exposes `ActiveDurationSeconds` and `ActiveManualEnd`, parsed from Cinegy's own format rather than with `TimeSpan.TryParse`, which rejects the one value this exists for: `24:00:00` needs an hour field of 0-23.
- Its error path now returns the same field set as its success path. A caller reading a field that exists only when the call worked would crash under `StrictMode` exactly when the engine is already in trouble.
- Test suite grows from 547 to 557.
## 5.6.0 — 2026-08-25

- **The restart button now works when the bridge was started by hand.** It only ever signalled an exit and left the restarting to NSSM or the scheduled task; started from a terminal - which is how it is run during a shift, from the VS Code console - nothing brought it back, so the button refused rather than causing an outage with no way back in through the bot that just stopped. The bridge now relaunches itself into the same console with the same configuration, and refuses only when it can rebuild neither a supervisor nor its own command line. Under a supervisor it deliberately does *not* self-relaunch: two bridges long-polling one bot token means button presses vanish into whichever instance received them.
- Template buttons carry the name alone. The label used to append the layer number and the last-used time, which overran Telegram's 32-character button limit and cut the date mid-way - `News-Ticker (طبقة 8) · 🕘 08-25…` reads as noise. Layer, device, category, fields, usage count and last use moved onto the ℹ️ preview screen, which has room for them.
- 🕘 ماذا فاتني answers the questions asked at a handover instead of counting verbs. It used to report `SHOW 23, EXIT 12, أخرى 73`, where the largest bucket was simply every record without an action field. It now names which graphic went to air, who put it there and when, what failed and why, the notable activity notes verbatim, and the current on-air and engine state.
- Fixes a crash in that same digest: under `StrictMode` a single audit record missing a field threw, taking down the whole screen - and that screen is what an operator opens once something has already gone wrong. Records are normalised once on read, so an absent field is empty rather than fatal.
- Confirms before taking a live graphic off air, so a single tap cannot hide what is on screen.
- Addresses the Cinegy logo by its device name (`gfx_logo`) rather than a layer number, via an optional `device` on a template. The logo layer is not numbered, so it was previously uncontrollable.
- Pages the news reorder list. A 38-item draft rendered 152 buttons, past Telegram's keyboard cap, so the edit was rejected and the list appeared not to update - which read as "the last items are missing". Every item is now reachable.
- Adds `NewsListPaged` (default on): turn it off for one long news list instead of small pages. It is a cap rather than a promise - Telegram rejects an over-large keyboard, and a rejected edit looks exactly like a list that will not update - so the long list runs to the button budget, pages beyond it, and says on screen why.
- Adds delete-with-confirmation straight from the reorder list, a configurable news label length, and an optional stacked list layout (`NewsListStackedLayout`, default off).
- Stops calling a shared template layer harmless.
- De-duplicates the 5.1-5.5 changelog entries, each of which had been written twice during a merge.
- Test suite grows from 495 to 544.
## 5.5.0 — 2026-08-24

- **Makes the ticker usable alongside another writer.** A second system writes the same `news.txt`, so a publish conflict is the normal case rather than an accident, and refusing forever made the bot useless for the ticker. A conflict now offers two explicit choices: append the draft's items to whatever is live now, or replace it entirely. Never a silent overwrite - the other writer's work is only discarded by a deliberate choice, and the previous text is backed up either way.
- Adds a lock hand-over request. A non-owner can ask the operator holding the draft to release it; the owner has `NewsLockRequestMinutes` (default 5) to agree or refuse, and silence hands it over - an operator who has gone home cannot answer, and the ticker cannot wait for them. Previously the only way past someone else's draft was an administrator forcing it, which is the wrong tool for two operators on one channel.
- The owner's unpublished items are sent back to them as text before the draft is released. An unpublished draft is somebody's work: transferring it to another person, or dropping it silently, would both be worse than handing it back.
- The lock label now names the holder instead of showing a raw user id.
- Test suite grows from 484 to 495.
## 5.4.0 — 2026-08-24

- **Fixes a phantom on-air record.** On startup the bot reported a template on layer 7 while the screen was blank. Discovery treated "not marked empty" as evidence of a live scene, but a spent item stays Active - briefly with no `IsEmpty` at all - and reports Cinegy's placeholder name `Item` with no description, exactly as every empty layer does. Discovery now requires a real describing name. Ambiguity must never ADD a record, and still never removes one.
- Generalises undo. A push onto a verifiably empty layer now leaves an undo that clears the layer again - the commonest mistake there is. Deliberately not offered when correlation failed, since hiding would discard whatever the bridge could not identify rather than restore it.
- Names who holds a layer lock, what they are preparing, and for how long, instead of printing a bare user id. An optional 🔒 badge on the template list (`ShowLayerLockBadge`, default off) shows the clash before a field is typed.
- Asks why after an undo - wrong template, wrong timing, director asked, other - and reports the counts in the usage digest. `logs\cancel-reasons.json` holds counts only: no field text, no user ids.
- Adds `/digest` and 🕘 ماذا فاتني, and `/who <template>`. Both read `audit.jsonl` rather than a new events file, which would only drift out of sync with the audit trail.
- Adds a repeat radar: one template pushed three times inside an hour is queried, since that is almost always a paste slip. Asked after the push, never before.
- Adds quiet hours (`QuietHoursEnabled`, default off): non-urgent administrator notices are batched overnight and delivered as one message when the window closes. A black output or a stale on-air record is always urgent and always immediate.
- Adds an automatic maintenance window (`MaintenanceWindowStart`/`End`, `HH:mm`). Both it and quiet hours handle windows that cross midnight. An unset or malformed window is no window - neither may fail closed and block control of a live channel. The administrator emergency override still works inside one.
- Adds one-hand mode (`OneHandMode`) for thumb-only use, and optional text shortcuts (`EnableTextShortcuts`) where a bare template name starts a show. Exact match only: a fuzzy match would put the wrong graphic on air from a typo.
- `templates.json` is no longer tracked in git. It carries station-specific names and scene paths and belongs with `config.json`; `templates.example.json` remains for a fresh install.
- Test suite grows from 434 to 477.
## 5.3.0 — 2026-08-24

- Adds 📷 لقطة الآن and 📋 نسخ الحالة to the on-air row. The frame lets an operator check what the bridge claims is on air against the real output without leaving the chat - the exact gap that let an exited scene sit unnoticed for ninety minutes. The status text is deliberately plain so it survives being pasted into another app.
- Adds `/stats` and 📈 أرقام التشغيل: uptime, air operation outcomes, Telegram and Cinegy connection states, and the last daily heartbeat. Uptime is the number that matters - a bridge up for eleven minutes has been restarting. Telegram flood limits (429) are counted separately from ordinary send failures, since a rate limit is a capacity problem rather than a bug.
- Enables the pre-schedule warning by default at three minutes, and adds the Cinegy health line to it so the operator can decide whether to intervene. The mechanism already existed but shipped disabled.
- Fixes another `"$layer:"` interpolation, where PowerShell reads the colon as a scope delimiter and the whole load fails.
- Test suite grows from 427 to 434.
## 5.2.0 — 2026-08-24

- Surfaces an active rollback in the main menu with its remaining seconds. Undo was reachable only from the message that offered it, so navigating away lost it for the rest of its window - and the fastest human error is pressing the wrong template.
- `ReservedLayers` now admits administrators, stating that the layer is reserved, while still refusing operators. It previously refused everyone including the administrators who reserved the layer, so touching a protected layer meant editing `config.json`. A disabled template stays blocked for everyone.
- Warns administrators when a scheduled event is about to displace a live graphic (`NotifyOnScheduleOverwrite`, default on). The creation-time conflict check compares scheduled events only, and cannot know what an operator put up by hand.
- Adds settings export and import. Export carries only the keys declared in `DefaultSettings`, so the bot token and the operator whitelist never reach a chat. Import refuses a foreign or malformed document, refuses an unknown key rather than dropping it, previews exactly which keys differ, and applies nothing until the same administrator confirms.
- Adds a usage digest, on demand and weekly (`UsageDigestEnabled`, `UsageDigestDayOfWeek`): busiest templates, operation totals, and refusals.
- Settings export now creates the log directory if it is missing rather than assuming an earlier code path made it.
- Adds an administrator restart (`AllowRemoteRestart`, default off). It refuses unless a supervisor is actually present - the parent process is inspected, and NSSM or Task Scheduler both restart on exit - because exiting unsupervised is an outage with no way back in through the bot that just stopped. It confirms first, warns when scenes are recorded on air, and signals the polling loop to leave rather than exiting in place, so the finally block still stops the relay, saves counters and releases the single-instance mutex.
- Seeds the output monitor's timestamp to launch time so the first picture check lands one interval later, rather than probing the source during startup. This also stopped the test suite shelling out to ffmpeg against the example URL.
- Gives the approval keyboard a way back to the menu, so no screen is a dead end. A phone's own back button leaves the chat entirely and cannot be intercepted by a bot; the help now says so and points at the on-screen buttons.
- Release notes now carry a condensed summary of what version 4 brought, for operators who never saw its changelog.
- Adds `scripts\Test-BridgeReadiness.ps1` and `Modules\BridgeInstall.psm1`. Both installers now refuse to register a service over a configuration that cannot start: a supervisor wrapped around a broken config produces a bridge that crashes and is restarted for ever while `services.msc` reports it Running. It checks PowerShell 7, config.json validity, a real bot token, at least one administrator, and a parseable template registry; a missing ffmpeg warns rather than blocks.
- Catches the DPAPI account trap by name. Secrets are protected with `DataProtectionScope::CurrentUser` while both installers run the bridge as `SYSTEM`, so protecting the token as yourself and then installing the service produced a bridge that could not decrypt its own token. `Protect-BridgeSecrets.ps1` now records `ProtectedBy` in the store, and the readiness check refuses the mismatch and names both accounts.
- Both installers accept `-RunAsAccount`, and after starting they confirm the bridge actually came up by reading `logs\bridge.log` rather than trusting the service state. Repeated startup lines are reported as a crash loop.
- `RELEASE.md` gains a first-install section covering the readiness check, the account choice, and the DPAPI trap.
- Test suite grows from 384 to 427.
## 5.1.0 — 2026-08-23

- Alerts administrators about a bridge-pushed layer recorded on air longer than `StaleOnAirAlertHours` (default 6, 0 disables). Reports only, never removes: deleting a record the operator can still see on screen would be worse than a stale one. Cinegy-owned scenes are excluded, being legitimately up for days.
- The main menu now states how old the on-air claim is and flags it when Cinegy could not be reached. A stale claim reading identically to a fresh one is what let an exited scene go unnoticed.
- Adds `Invoke-BridgeSelfTest` (🧪 فحص المسار الحي): SHOW, read back, EXIT, read back, reporting what Cinegy said at each step. Refuses a layer that is busy or that production templates use, and always attempts a HIDE afterwards.
- Adds `ConfirmLayerRemoval` (default off): a confirmation before hide and exit naming the template, how long it has been on air, and who pushed it. Off by default because it costs the emergency path a tap.
- Deletes staged operator uploads past `UploadRetentionMinutes` (default 60). News and template registry uploads were staged, parsed, then left on the playout machine for ever.
- Adds an output monitor that looks at the picture itself: a frame every `OutputMonitorMinutes` (default 60), and on a dark frame a confirming second capture `OutputBlackConfirmSeconds` later. Alerts only when both are black, once, then reports recovery. Administrators always hear; operators only when `NotifyOperatorsOnBlackOutput` is on.
- Adds 🆕 ما الجديد and `/whatsnew`: operator-facing release notes describing what changed on screen rather than in the code.
- Rewrites the help screen grouped by task rather than by menu, covering the on-air rows, the freshness warning, the removal confirmation, the admin tools screen, the self-test and the output monitor.
- Test suite grows from 345 to 384.
## 5.0.0 — 2026-08-23

- Splits `TelegramBridge.ps1` from 7407 lines into a 973-line orchestrator plus 14 dot-sourced declaration files under `Parts/`. Parts are dot-sourced rather than imported because they must share the script scope and `$script:` state; only declarations move, while the 64 ordered top-level initialization statements stay put because they depend on `$config`.
- Replaces all 40 hand-counted `$data.Substring(N)` callback parses with `Get-CallbackArg $data '<prefix>'`, making the off-by-N class that broke news reordering unrepresentable. A renamed prefix now throws instead of slicing silently.
- Backs off Cinegy layer reconciliation while Air is unreachable: an unverifiable layer doubles the wait up to `CinegyStateBackoffMaxSeconds` (default 60) and the first success clears it, so a dead engine no longer freezes the polling loop for layers x timeout on every interval. The long-poll timeout follows the widened interval.
- Honours Telegram's `retry_after` on HTTP 429 instead of retrying at a fixed 400 ms, and raises sends to three attempts, so admin broadcasts and chunked long messages are no longer dropped under flood limits. Callback acknowledgements now use the same wrapper. `Get-TelegramUpdates` deliberately stays outside it, because the polling loop already is its retry policy.
- Renames the `Write-AuditRecord` parameter that shadowed the automatic `$Event` variable. The emitted audit field name is unchanged.
- Fails the verification gate on analyzer warnings, not only errors, and excludes `dist/` and `artifacts/` so stale release copies stop reporting findings already fixed. Clears the six real findings this exposed, including a `Publish-NewsTickerDraft` parameter that was never used and a DPAPI test teardown that restored into a local and therefore did nothing.
- Unpacks the semicolon-chained news draft functions, verified behaviour-preserving by comparing parsed token streams.
- Leads the main menu with what is on air: live-layer hide buttons and the emergency hide-all now render above everything that can put more on air, and the menu message reports the live state instead of a static prompt. Five rows of rarely used administrator configuration move behind a single `🗂 أدوات الإدارة` entry; settings and access requests stay one tap away, and operators see no change.
- Refuses a `TemplateTestLayer` that registered templates already occupy, naming the clashing templates, so a template test cannot go out on a programme layer.
- Test suite grows from 307 to 341.
## 4.2.44 — 2026-08-22

- Refreshes the display name of an already tracked external Cinegy scene when later live status exposes its actual `.cintitle` filename.
- Replaces legacy generic names such as `Cinegy Type Layer 8 On` without deleting the on-air record or changing its layer identity and preserves the original Cinegy event name as diagnostic metadata.
- Adds an explicit regression test for upgrading an existing live external record in place.
- Reorganizes full status into active scenes, Cinegy connection, service health, operations/scheduling, and access sections.
- Expands active-scene status with source, operator Alias, elapsed time, and retained Cinegy event metadata; main-menu hide buttons now include both layer and template name.
- Adds an explicit `Alias` action per authorized user, with interactive edit/delete flow, persistence, audit, and tests.

## 4.2.43 — 2026-08-22

- Adds `Modules/BridgeRuntimeState.psm1` as an explicit, isolated runtime-state model.
- Consolidates relay process ownership, relay intent/restart/watchdog fields, and Cinegy/Telegram monitoring timestamps and states under that model.
- Removes the separate global relay-process and monitoring variables while retaining the existing relay-state compatibility reference during gradual migration.
- Adds isolation/default tests and keeps relay, freshness, external-change, outage-threshold, and recovery coverage.
- Extracts the actual `.cintitle` template name from Cinegy's active-item `Description` for externally started or scheduled scenes, instead of presenting the generic layer event name; retains the Cinegy event name as diagnostic metadata.
- Shortens long Telegram button labels centrally at a configurable Unicode-aware visual limit (`ButtonTextMaxLength`, default `32`, `0` disables), without changing callback commands.
- Completes the planned modularization and gradual global-state reduction work without a broad rewrite.

## 4.2.42 — 2026-08-22

- Extracts live-relay watchdog timing, healthy/idle/wait states, disabled restart, retry-limit, and restart-count decisions into `Modules/BridgeRelayPolicy.psm1`.
- Keeps process ownership, PID cleanup, logging, administrator notifications, and actual restart execution in the bridge.
- Adds direct policy tests for every watchdog branch and completes the planned first separation of scheduling, live relay, and ffmpeg process handling.

## 4.2.41 — 2026-08-22

- Extracts Windows-safe argument quoting, ffmpeg input selection, and hidden redirected process launch into `Modules/BridgeMedia.psm1`.
- Routes both asynchronous snapshots and the live relay through the shared media-process launcher while retaining bridge-owned jobs, watchdogs, PID ownership, messages, and cleanup.
- Adds direct tests for paths with spaces, empty and quoted arguments, supported/unsupported inputs, and exact hidden redirected process launch parameters.

## 4.2.40 — 2026-08-22

- Extracts schedule due-time, completed-occurrence, retry timing, retry count, exponential backoff, and maximum-delay decisions into `Modules/BridgeSchedulePolicy.psm1`.
- Keeps persistence, notifications, SHOW invocation, recurrence advancement, and execution audit logging in the bridge orchestrator.
- Adds direct policy tests and retains the full schedule store/executor, pause, notification, recurrence, restart, retry, conflict, and review-flow coverage.

## 4.2.39 — 2026-08-22

- Extracts per-layer Cinegy reconciliation decisions into `Modules/BridgeCinegyState.psm1` while retaining network polling, logging, persistence, notifications, and timer cleanup in the bridge orchestrator.
- Preserves tracked scenes on unavailable status, adopts Cinegy's actual id while on air, removes only after confirmed hidden state, and discovers untracked scenes as `cinegy` source only during explicit full comparison.
- Avoids mutating caller-owned tracked records inside the policy module.
- Adds direct policy tests plus existing reconciliation, startup, external-scene, identity persistence, and on-air smoke coverage.

## 4.2.38 — 2026-08-22

- Extracts interactive pending-flow storage, timeout evaluation, and removal into `Modules/BridgeFlowState.psm1`.
- Preserves bridge-owned cleanup effects for layer locks, staged imports, and persisted drafts while reducing direct `$script:PendingState` manipulation.
- Supports numeric chat keys restored through different PowerShell/JSON key types.
- Adds direct flow-state tests and bridge compatibility coverage for cancellation, expiry, periodic cleanup, draft persistence, and lock release.
- Completes the planned first separation of Telegram transport, authorization policy, and interactive flow state.

## 4.2.37 — 2026-08-22

- Extracts private-chat, operator, administrator, disabled-user, and legacy private-chat fallback decisions into `Modules/BridgeAuthorization.psm1`.
- Keeps user administration, approval messages, and interactive flows in the bridge while routing their authorization decisions through an explicit-input policy module.
- Adds direct policy tests for allowed and disabled users, sender/chat separation, administrator identity, group rejection, and legacy positive-id compatibility.
- Includes the authorization module and tests in all parser, required-file, Pester, packaging, signing, and manifest gates.

## 4.2.36 — 2026-08-22

- Extracts bounded Telegram HTTP transport and retry behavior into the independent `Modules/BridgeTelegram.psm1` module.
- Routes message, photo, and document uploads through the shared transport while preserving one transient retry, existing timeouts, logging, and photo fallback behavior.
- Adds direct transport tests for retry success, structured terminal failure, and single-attempt behavior, and keeps bridge-level compatibility tests.
- Includes the Telegram module and its tests in parser, required-file, Pester, release-package, signing, and manifest gates.

## 4.2.35 — 2026-08-22

- Extracts validated JSON primary/backup writes and recovery reads into the independent `Modules/BridgeStorage.psm1` module.
- Adds direct storage tests for atomic primary/backup creation, invalid-input protection, backup recovery, and missing-state handling.
- Organizes reusable modules under `Modules/`, administration and installation tools under `scripts/`, and historical reviews under `docs/archive/`.
- Updates runtime imports, test imports, release packaging, recursive signing/manifest generation, documentation, and required-file/parser gates for the organized layout.
- Removes only regenerated build/test outputs and example-config backups; live `config.json`, its backups, `templates.json`, and `logs/` remain untouched.

## 4.2.34 — 2026-08-22

- Extracts setting lookup, integer normalization, and schema initialization into the pure `BridgeSettings.psm1` module.
- Keeps the existing `Get-Setting`, `Get-SettingInt`, and `Initialize-Settings` bridge interfaces so callers and administrator workflows retain identical behavior.
- Adds direct module tests covering configured values, defaults, numeric boundaries, non-destructive initialization, and complete-schema no-op behavior.
- Includes the new module and tests in parser, required-file, Pester, and allow-listed release-package gates.

## 4.2.33 — 2026-08-22

- Keeps safe rollback disabled by default through `EnableSafeRollback`; administrators can opt in without changing the existing operating workflow.

- Added short-lived, in-memory rollback candidates for correlated bot scenes
  replaced by SHOW or removed manually with HIDE/EXIT.
- Kept editorial field values out of every disk file and expired candidates
  after the configurable `RollbackWindowSeconds` interval.
- Required operator ownership or administrator access, a review step, a layer
  lock, and an immediate Cinegy comparison before restoration.
- Required exact `ActiveId` continuity after replacement or a still-empty layer
  after HIDE/EXIT; external changes, timeout, uncertainty, and expiry cancel and
  invalidate rollback without sending a playout command.
- Added regression tests for correlation, replacement, hide restoration,
  review, expiry, external changes, and Cinegy uncertainty.

## 4.2.32 — 2026-08-22

- Added an administrator-configured template test layer, disabled by default.
- Required the test layer to be absent from every production template
  definition and verified directly with Cinegy that it is empty before SHOW.
- Added administrator review and confirmation, synthetic `TEST` field values,
  explicit `BotTest` tracking, and a mandatory 3-300 second automatic hide.
- Added field-level before/after comparison to template-definition review and
  stated that the existing timestamped backup is created before saving.
- Added regression tests for production-layer rejection, occupied/uncertain
  layer blocking, isolated SHOW targeting, automatic hide, and comparison.

## 4.2.31 — 2026-08-22

- Added administrator template-registry export through Telegram.
- Added opt-in full-management import of JSON documents up to 1 MiB, with
  bounded download, safe Telegram file-path validation, and temporary staging.
- Validated every template key, absolute `.cintitle` path, positive layer, and
  field name before showing added/changed/removed/unchanged counts.
- Required an explicit confirmation before atomic replacement and created a
  timestamped backup of the previous registry.
- Blocked imports that change or remove a template currently on air or used by
  an upcoming schedule, and removed staged files on cancellation or expiry.

## 4.2.30 — 2026-08-22

- Added private per-user template search across key, description, and optional
  category without changing stable callback indexes.
- Added category browsing and documented optional `category` values in the
  example registry and administrator definition editor.
- Added a non-mutating template detail preview before SHOW selection.
- Persisted and displayed the local time of each template's last successful
  use while remaining compatible with legacy integer usage counters.

## 4.2.29 — 2026-08-22

- Added opt-in CurrentUser DPAPI storage for the bot token and live-stream
  source/destination URLs; the existing plaintext configuration remains the
  disabled-by-default behavior.
- Added the protected administrator setting `EnableDpapiSecrets` and an
  explicit `Protect-BridgeSecrets.ps1` migration command.
- Persisted only `dpapi:` references while enabled and prevented later config
  saves from leaking decrypted values back to JSON.
- Protected the encrypted store and the pre-migration backup with the same
  strict Windows ACL, and excluded both from Git and release packages.
- Added DPAPI round-trip, no-plaintext, migration, activation, deactivation,
  and default-off regression tests.

## 4.2.28 — 2026-08-22

- Added Windows ACL enforcement for `config.json`, its fallback copy, and all
  versioned configuration backups at startup and after save or restore.
- Restricted access to the bridge runtime identity, Local System, and the
  built-in Administrators group using stable Windows security identifiers.
- Added an integration test that verifies protected inheritance and the exact
  allow-list on temporary files and directories.

## 4.2.27 — 2026-08-22

- Extended the administrator-confirmed runtime cleanup to all direct `.log`
  files in `logs`, including bridge rotations and relay stdout/stderr logs.
- Kept `audit.jsonl`, `onair.json`, schedules, templates, backups, and
  diagnostic subdirectories outside runtime-log cleanup.
- Retained a fresh audit record after either runtime or audit cleanup.

## 4.2.26 — 2026-08-22

- Added Windows/PowerShell 7 GitHub Actions for parser, PSScriptAnalyzer, and
  Pester verification on pushes and pull requests.
- Persisted NUnit-format Pester results and uploaded verification artifacts.
- Added an allow-listed release builder that excludes live configuration,
  templates, logs, backups, snapshots, audit history, and on-air state.
- Added a release manifest, ZIP SHA-256 sidecar, and optional Authenticode
  signing using a Windows certificate thumbprint.
- Documented checksum/signature verification, side-by-side upgrade, and safe
  rollback preserving validated runtime state.
- Added automated package allow-list, manifest, and checksum tests.

## 4.2.25 — 2026-08-22

- Added `TelegramRequestTimeoutSeconds` with a 15-second default for
  `sendMessage`, `sendPhoto`, and `sendDocument`.
- Retained one bounded retry for transient Telegram send failures.
- Kept long polling on its independent poll timeout.
- Added send retry and timeout propagation tests for text and multipart
  uploads, completing the existing HTTP, Cinegy timeout, disk-failure, and
  corrupt-JSON coverage.

## 4.2.24 — 2026-08-22

- Counted field limits using Unicode text elements rather than UTF-16 code
  units, so emoji and Arabic combining marks match what operators see.
- Added Unicode-safe Telegram chunk boundaries that never split surrogate
  pairs or ordinary combining sequences.
- Preserved the existing 3500-code-unit Telegram safety ceiling.
- Added emoji, Arabic diacritic, invalid-surrogate, long-line, XML-special,
  and message-loss regression coverage.

## 4.2.23 — 2026-08-22

- Added `SchedulePaused` to keep all events pending without executing or
  deleting them.
- Added optional `SchedulePreNotifyMinutes` notifications, disabled by default,
  with one notification per occurrence.
- Added reviewed recurrence end dates for daily and weekly events, including
  validation that the end does not precede the first occurrence.
- Completed recurring events after their next local occurrence exceeds the
  configured end date.
- Preserved recurrence end dates during copy and edit operations.
- Added pause, notification de-duplication, recurrence completion, and UI flow
  coverage.

## 4.2.22 — 2026-08-22

- Added copy and edit-time buttons for each pending schedule event.
- Required a new local time followed by the existing schedule review before
  either mutation is saved.
- Kept the original event unchanged when copying and generated a fresh event
  id for the copy.
- Preserved the stable event id when editing time and reset stale execution or
  retry state only after an atomic save succeeds.
- Added full timezone ids to event summaries in addition to numeric offsets.
- Restricted copy/edit to the event owner or an administrator.
- Added copy isolation, edit identity, and timezone summary tests.

## 4.2.21 — 2026-08-22

- Required template paths to be absolute and use the `.cintitle` extension;
  existence is intentionally not checked because the path may belong to the
  remote Cinegy server.
- Excluded invalid template definitions from SHOW controls and exposed their
  keys and reasons through existing health warnings.
- Reported layers shared by multiple templates as allowed informational data,
  preserving the operational rule that one layer may host many templates.
- Retained scheduled-event proximity warnings as the actual layer conflict
  detector.
- Added invalid-path and shared-layer regression tests.

## 4.2.20 — 2026-08-22

- Added a private, in-memory recent operation history keyed by Telegram user
  id and capped at 20 records per user.
- Excluded editorial field values from operation history.
- Added the `🧾 عملياتي` screen and `/myoperations` (`/myops`) command with
  success, blocked, and failure guidance.
- Added a safe retry button that restores the same user's last SHOW values
  into the confirmation screen without sending directly to Cinegy.
- Added user-isolation, content-minimization, and no-direct-send retry tests.

## 4.2.19 — 2026-08-22

- Added shared validated JSON state read/write helpers with atomic temporary
  files and a last-known-good `.bak` copy.
- Applied automatic corruption recovery to `onair.json` and `schedule.json`.
- Repaired the primary state file from its validated backup during import.
- Preserved current in-memory state when neither primary nor backup can be
  parsed instead of replacing it with an empty collection.
- Added corruption and recovery tests alongside existing Cinegy/Telegram
  outage, recovery-notification, and interrupted-schedule coverage.

## 4.2.18 — 2026-08-22

- Added `SensitiveTemplateKeys` and `SensitiveTemplateAutoHideSeconds`.
- Enforced an automatic-hide timer for configured sensitive templates through
  ordinary, preset, and scheduled SHOW execution.
- Treated the administrator duration as the maximum on-air lifetime while
  retaining a shorter operator-selected timer.
- Replaced an existing timer on the same layer instead of stacking timers that
  could hide a later scene.
- Added mandatory timer and duration-bound regression tests.

## 4.2.17 — 2026-08-22

- Added an administrator-only redacted diagnostic ZIP via the diagnostics
  screen and `/diagbundle`.
- Limited bundle contents to `summary.json` and the latest 200 sanitized
  runtime lines; configuration, on-air state, schedules, templates, and audit
  history are never packaged.
- Removed bot credentials and stable user/chat/from/admin identifiers from
  exported text.
- Deleted the generated ZIP locally after the Telegram send attempt.
- Corrected runtime-path isolation so diagnostics and cleanup always target
  the selected runtime directory.
- Added bundle allow-list, redaction, and administrator delivery tests.

## 4.2.16 — 2026-08-22

- Added administrator-only buttons to clear runtime logs or the structured
  audit history independently.
- Required a fresh, expiring, per-administrator confirmation before deletion.
- Kept `onair.json`, schedules, templates, and backups outside cleanup scope.
- Recreated an audit record after cleanup identifying the administrator and
  selected history type.
- Added configurable low-disk, runtime-storage, and backup-storage warnings to
  administrator diagnostics.
- Added stricter diagnostic text redaction for user/chat/from/admin identifiers.
- Added confirmation, cleanup isolation, audit recreation, warning, and
  redaction regression tests.

## 4.2.15 — 2026-08-22

- Added the independent permanent `logs/audit.jsonl` audit trail.
- Recorded UTC timestamp, correlation id, event, result, actor, action, layer,
  target, duration, and redacted message as one machine-readable JSON object
  per line.
- Reused each air-control operation id in its structured audit record while
  retaining the human-readable `AIR_OP` runtime entry in `bridge.log`.
- Persisted general in-chat audit activity independently from the bounded
  in-memory list.
- Included the audit file size in administrator diagnostics.
- Added correlation, persistence, single-line, and secret-redaction tests.
- Suppressed helper return values such as `True` at the runtime orchestration
  boundary while preserving actual operational log lines in the terminal.

## 4.2.14 — 2026-08-22

- Added bridge build-file time and process uptime to administrator diagnostics.
- Added processor architecture, working-set memory, private memory, and free
  disk capacity.
- Added byte-derived sizes for configuration, templates, on-air state,
  schedule state, schedule execution history, and bridge log files.
- Added in-memory success, failure, and blocked counters for air-control
  operations without reparsing logs.
- Kept diagnostics administrator-only and passed all output through existing
  secret redaction.
- Added diagnostic content and counter regression coverage.

## 4.2.13 — 2026-08-22

- Changed opted-in scheduled retry delays from fixed to exponential backoff.
- Added `ScheduleRetryBackoffFactor` with a default factor of 2.
- Added `ScheduleRetryMaxDelaySeconds` with a default cap of 300 seconds.
- Normalized invalid timing values to a positive bounded delay.
- Added deterministic growth and cap regression coverage.

## 4.2.12 — 2026-08-22

- Added independent `logs/schedule-execution.jsonl` execution history.
- Recorded timestamp, event and execution ids, template, layer, scheduled time,
  attempt number, result, duration, and sanitized error for every scheduled
  SHOW attempt.
- Excluded template field names and editorial values from the execution log.
- Logged retry attempts independently while preserving `schedule.json` as the
  current schedule state.
- Added persistence and privacy regression coverage for the JSONL record.

## 4.2.11 — 2026-08-22

- Added `ScheduleMaxRetries` and `ScheduleRetryDelaySeconds` settings.
- Kept retries disabled by default (`ScheduleMaxRetries = 0`) to avoid
  surprising delayed graphics on air.
- Persisted attempt count, next-attempt time, and last failure reason with each
  scheduled event.
- Deferred retries until their configured time and cleared retry state after a
  successful recurring occurrence.
- Preserved the existing no-replay rule for occurrences interrupted while
  marked `running` across a bridge restart.
- Added deterministic retry-delay regression coverage.

## 4.2.10 — 2026-08-22

- Added same-layer scheduled-event conflict detection.
- Added configurable `ScheduleConflictWindowMinutes`, defaulting to two
  minutes.
- Stored the resolved layer with new schedule events while retaining fallback
  lookup compatibility for older persisted events.
- Displayed each conflicting template and time in schedule review before the
  operator confirms the event.
- Added coverage for same-layer conflicts, different-layer non-conflicts, and
  the Telegram review warning.

## 4.2.9 — 2026-08-22

- Added administrator-editable `ReservedLayers` and `DisabledTemplateKeys`
  settings.
- Blocked SHOW preparation and final execution for reserved layers and
  temporarily disabled templates before any Cinegy query or mutation.
- Kept HIDE and EXIT available for reserved layers so operators can still take
  content off air safely.
- Applied the central execution policy to scheduled and interactive SHOW paths.
- Added regression coverage proving blocked policies send no Cinegy command.

## 4.2.8 — 2026-08-22

- Added a unique `air-*` correlation id to every SHOW, HIDE, EXIT, and live
  UPDATE attempt.
- Added a single structured `AIR_OP` bridge-log result containing action,
  result, duration, operator, chat, layer, target, and sanitized failure reason.
- Recorded blocked maintenance and uncertain-Cinegy attempts as well as
  successful and failed commands.
- Kept historical operation records in `bridge.log`; `onair.json` remains live
  state only.
- Added regression coverage for successful and blocked operation records.

## 4.2.7 — 2026-08-22

- Added a direct target-layer verification immediately before every SHOW.
- Blocked SHOW when Cinegy cannot confirm the target layer instead of assuming
  an unreachable layer is safe.
- Reconciled the verified layer with `onair.json` before any replacement or
  pre-show hide action.
- Added regression coverage proving that an uncertain layer sends no SHOW
  command and does not run reconciliation on an invalid response.

## 4.2.6 — 2026-08-22

- Added the currently tracked scene to SHOW review before a layer replacement.
- Identified whether that scene came from Cinegy Air, the bot, or a named
  operator.
- Added an explicit warning when the layer state sample is not fresh enough to
  trust during review.
- Added regression coverage for replacement and uncertain-state warnings.

## 4.2.5 — 2026-08-22

- Added explicit Cinegy state freshness classification: connected, stale,
  unavailable, or unknown.
- Added `CinegyStateStaleSeconds` with a 45-second default threshold.
- Displayed the classification in both the operator status and administrator
  full-status views.
- Added deterministic tests for all four classifications.

## 4.2.4 — 2026-08-22

- Added an administrator-controlled maintenance mode in Settings.
- Maintenance mode blocks SHOW, ordinary HIDE, EXIT, live-value updates, and
  creation or execution of scheduled SHOW operations without altering current
  on-air scenes.
- Due scheduled events remain pending and resume after maintenance is disabled.
- The administrator-only emergency hide-all action remains available during
  maintenance.
- Added regression coverage proving that blocked controls do not send Cinegy
  commands and that the emergency override is restricted to the hide-all path.

## 4.2.3 — 2026-08-22

- Added `logs/user-profiles.json` with approval time, approving administrator,
  and last authorized activity time; message content is never stored.
- Existing users receive unknown historical metadata until their first new
  activity instead of invented timestamps.
- User management now displays last activity, and revocation removes the
  runtime profile while preserving the audit event.
- Activity persistence is atomically flushed at most once per minute to keep
  disk writes out of the high-frequency interaction path.
- Added regression coverage for approval metadata, authorized-only activity,
  and profile cleanup on revocation.

## 4.2.2 — 2026-08-22

- Added an administrator user-management screen with Alias, role, and active
  or disabled state for every authorized private-chat user.
- Added persistent temporary disable/enable state in
  `logs/disabled-users.json` without deleting whitelist entries.
- Added confirmed permission revocation across chat/user and operator/admin
  lists, with audit logging.
- Prevented revoking or disabling the final active administrator.
- Added regression coverage for disabled authorization, unique user listing,
  confirmation, revocation, and final-administrator protection.

## 4.2.1 — 2026-08-22

- Added `HealthFailureAlertThreshold` (default 3) for consecutive Cinegy and
  Telegram failures before notifying administrators.
- Outages now produce one alert and one recovery message without repeats.
- Full health status includes the consecutive failure count and outage start.
- On-air status distinguishes `🟣 Cinegy Air` external or scheduled scenes
  from `🔵 Bot · operator` scenes created through Telegram.
- Successful HIDE and EXIT commands read the affected Cinegy layer and
  reconcile `onair.json` from the confirmed result.
- Added regression coverage for thresholding, deduplication, recovery, and
  clearing outage state.

## 4.2.0 — 2026-08-22

- Added independently editable per-user favourites in `logs/favorites.json`.
- Kept usage-ranked templates as fallback and added the disabled
  `SharedFavoritesEnabled` placeholder for a future unified list.
- Added persistent operator aliases in `logs/user-aliases.json`, managed by
  `/alias USER_ID name` and displayed in status and audit text.
- Added regression coverage for user isolation, removal, invalid templates,
  alias normalization, and alias removal.
- Expanded the development backlog to retain all previously proposed items.

## 4.1.2 — 2026-08-22

- Fixed the Pester harness writing synthetic template records into the live
  `logs/onair.json` file while `Run-Checks.ps1` was running.
- Added an explicit `RuntimePath` parameter so tests isolate `onair.json`,
  schedules, drafts, recent values, usage data, relay state, snapshots, and
  `bridge.log` inside Pester's `TestDrive`.
- Added a release-gate regression test that fails if test persistence points
  at the live `logs` directory.
- Verified the live `onair.json` hash and timestamp remain unchanged across the
  complete 146-test suite.

## 4.1.1 — 2026-08-22

- Added a full Cinegy layer comparison during bridge startup before operator
  commands are accepted.
- Startup now discovers scenes launched directly from Cinegy and restores them
  to `onair.json` as current, hideable state.
- Unreachable startup layers preserve their previous local records and produce
  an explicit warning in `bridge.log` rather than being treated as hidden.
- Added a startup reconciliation summary to `bridge.log` with checked, added,
  and removed counts.
- Added regression coverage for full startup discovery and uncertain-layer
  preservation. Existing restart tests continue to verify scheduled
  occurrences are not replayed after a service restart.

## 4.1.0 — 2026-08-22

- Added an operator-triggered live reconciliation against every Cinegy GFX
  layer referenced by `templates.json` from both Status and the Layers panel.
- Scenes started directly in Cinegy are discovered, recorded in `onair.json`
  with `Source: cinegy`, and exposed to operators for HIDE/EXIT actions.
- Kept `onair.json` as current operational state only; historical actions and
  detailed add/remove reasons are written to `bridge.log`.
- Unreachable or uncertain Cinegy reads never add or remove an on-air record.
- Persisted each record's source (`bridge` or `cinegy`) across restarts.
- Added the last successful Cinegy comparison time to the status screen and a
  comparison summary to the Layers panel.
- Added regression coverage for discovery, uncertain reads, comparison from
  the Layers panel, and detailed removal logging.

## 4.0.1 — 2026-08-22

- Fixed the release gate so the five on-air persistence smoke tests are
  discovered and executed by Pester.
- Prevented `onair.json` from being rewritten on every Cinegy sync when the
  tracked `ActiveId` has not changed, with a regression test for the behavior.
- Restricted the bridge to private Telegram chats; group, supergroup, and
  channel updates are ignored before authorization or command dispatch.
- Added regression coverage proving a successful EXIT removes and persists the
  on-air template record, while a failed Cinegy EXIT keeps it for recovery.
- Cleaned actionable PSScriptAnalyzer warnings and documented the intentional
  UTF-8-without-BOM and private helper naming exclusions.
- Added `DEVELOPMENT-PLAN.md` with the 4.0.1–5.0 roadmap and release gate.

## 4.0.0

- Fixed onair.json losing live templates: a layer that is genuinely on air is
  no longer dropped just because Cinegy reports a different ActiveId (Cinegy
  generates its own id and ignores the bot's EventId). The record is kept and
  Cinegy's real id is adopted.
- Restored the external-change alert (regression in 3.1.1 on-air fix): a layer
  removed because Cinegy reports it hidden/off-air now produces a `Changes`
  entry, so admins are notified again.
- Added on-air dirty tracking so an updated ActiveId is persisted on the next
  sync instead of only on removal.
- Raised `CinegyMonitorTimeoutSeconds` default from 1 to 3 seconds so Air Pro
  status reads stop timing out and dropping tracked layers.
- Restructured the simple and full status reports with sections, an overall
  status line, and a local timestamp.
- Added `Tests/Smoke-OnAir.Pester.ps1` — a no-server, no-disk smoke test for
  on-air persistence (runs as part of Run-Checks.ps1).

## 3.1.1

- Replaced free-text `LayerNames` editing with an administrator-only layer
  picker: select a layer, enter one label, or use a dedicated clear button.
- Layer labels are validated before saving to prevent malformed configuration.

## 3.0.0 — 2026-08-20

- Added mandatory review before every interactive, preset, or typed `SHOW`,
  required fields, layer preparation locks, draft navigation, previews, and
  two-step confirmation for hide-all.
- Added persistent per-user drafts, repeat-with-edit, recent field choices that
  exclude sensitive values, and a live quick-action layer panel.
- Added administrator preset creation, value editing, rename, and deletion with
  review, atomic writes, and timestamped template-registry backups.
- Added reliable one-time, daily, and weekly scheduling with clock/time-zone
  validation, upcoming-event cancellation, atomic persistence, and crash-safe
  occurrence keys that prevent automatic replay.
- Added `/health`, Telegram/Cinegy latency and health history, startup and
  recovery notifications, and a redacted administrator diagnostics report.
- Split status by role: every authorized user gets a lightweight Air summary,
  while administrators get a combined full layer and service-health report.
- Clarified the layer panel states and its quick-hide behavior in the Arabic help.
- Added an administrator-selected `HideAllLayers` scope, so the emergency
  hide-all action affects only the layers selected from Settings.
- Enriched external Cinegy-change alerts with the tracked template, old/new
  item details, Air endpoint, and available Cinegy client identity.
- Added Arabic unit/description labels to administrator numeric settings.
- Added administrator-configurable operational layer names throughout layer
  status, actions, emergency confirmation, and external-change alerts.
- Added a read-only administrator template catalogue and protected, disabled-
  by-default full template definition management with atomic backups.
- Added configuration backup comparison/restoration and migration coverage.
- Added a real Cinegy dashboard for every GFX layer referenced by the template
  registry, distinguishing bridge-owned, external, hidden, and unknown state.
- Added layer metadata from Cinegy `/status`: active item name, output state,
  license state, and connected client identity.
- Added one-minute Cinegy `/metrics` health aggregation for output, dropped
  frames, missing input, read errors, read time, and heartbeat.
- Added deduplicated admin alerts for external scene changes, unhealthy or
  unreachable telemetry, and health recovery.
- Bounded automatic monitoring requests with a dedicated short timeout.
- Expanded the release gate to validate required files, JSON, PowerShell
  syntax, PSScriptAnalyzer, and the complete Pester suite.
- Kept the existing regular-user/admin permission model and automatic migration
  of 2.x configuration settings.

## 2.8.4

- Improved role-aware Arabic help with short on-air workflows.
# 4.3.0

- أضيف زر دائم **إدارة شريط الأخبار** مع مسودة واحدة مقفلة، إدخال يدوي، تعديل، حذف وترتيب، واستيراد TXT UTF-8.
- أصبح نشر `news.txt` ذريًا مع فحص تعارض SHA-256 ونسخة احتياطية قبل الاستبدال؛ لا يغيّر التحرير أو الاستيراد الملف الحي قبل التأكيد.
- أضيفت صلاحيات مستقلة للمشغل للحذف والاستعادة ومسح الكل، مع بقاء الافتراضي للمشرف فقط، وفاصل قابل للضبط افتراضيه `|`.
# 4.3.1

- أضيف اسم عربي واضح لمسار ملف الأخبار في إعدادات المشرف، مع إبقاء المسار الحالي افتراضيًا ورفض أي مسار غير مطلق أو لا ينتهي بـ `.txt`.
