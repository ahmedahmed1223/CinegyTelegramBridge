# خطة تطوير تجربة المستخدم — مهامّ

آخر تحديث: 2026-09-09 — مقابل الإصدار 8.17.0

> هذا الملف **مقترح**، لا سجلّ منجَز. `DEVELOPMENT-PLAN.md` هو خطة المستودع
> الرسمية؛ هذا امتداد لها يخصّ تجربة المشغّل. كل مهمة هنا لم تُنفَّذ بعد ما لم
> يُذكر غير ذلك.

## الفكرة الحاكمة

الفجوة في هذا النظام ليست نقص ميزات، بل **بين ما يعرفه الجسر وما يصل المشغّل**.

الجسر يقيس حجم كل شاشة، ويعدّ كل فشل، ويعرف زمن كل عملية، وينظر إلى الصورة
الخارجة، ويسجّل كل ضغطة — ومعظم ذلك يعيش في ملف سجلّ لا يفتحه أحد أثناء
المناوبة. الدليل: سطر تحذير عن شاشة تجاوزت حدّها **بقي يوماً كاملاً بلا قارئ**،
واكتُشف بالمصادفة في مراجعة.

**مبدآن يحكمان كل مهمة أدناه:**

1. **ما يعرفه الجسر يظهر حيث يُتصرَّف به** — لا في ملف.
2. **كل تحذير معه زرّ.** النظام اليوم ينصح ولا يفعل.

---

## المرحلة ٠ — سدّ ما هو معروف

بنود موثّقة ومؤجَّلة بقرار سابق، لا اكتشافات جديدة.


### T-02 · اسم المستخدم يسقط إلى رقمه
- **الحالة:** كامن · **الجهد:** صغير
- **المشكلة:** `Get-UserDisplayName` ترجع الرقم لمن لا اسم مستعار له. من يُضاف
  يدوياً (لا عبر طلب وصول) يظهر رقماً في إشعار تعارض الطبقة — وهو بالضبط ما
  كُتبت تلك الرسالة لتجنّبه.
- **العمل:** إمّا اشتقاق اسم من ملف المستخدم، أو إلزام تسمية عند الإضافة اليدوية.
- **القبول:** لا يظهر رقم مجرّد في أي رسالة موجَّهة لإنسان.
- **اليوم:** ١٠/١٠ من المصرَّح لهم لديهم أسماء، فالأثر صفر حتى أول توظيف جديد.


## المرحلة ١ — قريبة الأثر

### T-40 · «قالب بقي على الهواء ولم يُرفع» 🔴 الأولوية القصوى
- **الحالة:** ⭐ **مسنودة بجواب المحطة** (2026-09-09) · **الجهد:** صغير-متوسط
- **الدليل:** سُئلت المحطة «ما آخر خطأ وقع على الهواء فعلًا؟» فكان الجواب:
  **«قالب بقي على الهواء ولم يُرفع.»** هذا أثمن سطر في هذا الملف كلّه — وهو
  يُسقط ترتيبي الأصلي: لا T-15 ولا T-30 تعالج هذه الحالة.
- **ما يفعله النظام اليوم، وأين يفشل** (`Update-StaleOnAirWatchdog`):

  | اليوم | الفجوة |
  |---|---|
  | ينبّه **مرة واحدة** لكل طبقة | **من يفوته التنبيه يفوته إلى الأبد** — ولا شيء يتكرّر |
  | ينبّه **المشرفين** | **لا يُبلَّغ من نشر القالب** — وهو الأقدر على رفعه |
  | التنبيه **نصّ بلا أزرار** | يجب فتح القائمة والبحث عن الطبقة يدويًا |
  | يعتمد على **سجل الجسر** | ما فقد الجسر تتبّعه لا يُنبَّه عنه إطلاقًا |
  | العتبة عندكم **ساعة** | معقولة — الفجوة في التصعيد لا في الرقم |

- **العمل، مرتّبًا بالقيمة:**
  1. **تصعيد بدل مرة واحدة**: تنبيه، ثم تكرار بفواصل متزايدة ما دام القالب
     على الهواء. الصمت بعد تنبيه واحد هو العطل نفسه.
  2. **أبلغ من نشره**، لا المشرفين وحدهم.
  3. **زرّ `[🙈 أخفِها الآن]` داخل التنبيه** — تطبيق مباشر لقاعدة T-12، وأوضح
     موضع لها في النظام كلّه.
  4. **استثناء `longRunning`**: الشعار والشريط يُفترض بقاؤهما — يجب ألّا
     يُنبَّه عنهما (يُتحقَّق قبل البناء).
- **القبول:** قالب يتجاوز العتبة ولا يُرفع → تنبيهات متكرّرة تصل ناشره ومعها
  زرّ إخفاء، حتى يزول من الهواء.

### T-10 · المراجعة بالصورة لا بالنصّ
- **الحالة:** ❌ **مرفوضة بنسخة Air واحدة** — انظر T-11. تُعاد فتحها فقط إن
  نُشرت نسخة Air ثانية غير مذاعة. **البديل العملي: T-15.**
- **الجهد:** متوسط (لو أُعيدت)
- **الفكرة:** زرّ «👁 أرني كيف سيبدو» في شاشة المراجعة: يعرض القالب خارج الهواء،
  يلتقط لقطة، يرسلها صورةً، ثم يخفيه.
- **لماذا:** المشغّل اليوم يقرأ نصّاً ويتخيّل النتيجة. خطأ إملائي في خبر عاجل
  يُرى قبل أن يراه المشاهد.
- **يبني على قدرات قائمة:** التقاط اللقطات، وإرسال الصور، وطبقة التجربة.
- **⛔ الافتراض المكسور:** وصفتُها أولاً بأنها «خارج الهواء» وهذا **غير مؤكَّد**.
  طبقة التجربة تُشترط «غير مستخدمة» لا «خارج البث»، و`AGENTS.md` تشترط نافذة
  تغيير معتمدة حتى معها، واحتياطات الكود (إخفاء تلقائي، `HIDE` مضمون) هي
  احتياطات شيءٍ **يظهر**.
- **القبول:** لا تُكتب سطر كود قبل إغلاق T-11.

### T-11 · حسم: هل توجد قناة معاينة؟ ✅ مُغلق
- **الحالة:** **مُغلق سلبًا** بمراجعة توثيق Cinegy الرسمي (2026-09-09)
  — [Controlling Layers & Devices، Air 26.2](https://open.cinegy.com/products/air/26.2/cinegy-air-http-api/controlling-layers-devices/)
- **الجواب:** **لا توجد معاينة في الواجهة البرمجية.** التوثيق:
  - **لا يذكر أي قناة معاينة ولا وضع بروفة ولا رسمًا خارج المخرج**؛
  - **ولا يوفّر تحكّمًا بظهور الطبقة** — لا إخفاء ولا شفافية ولا تعطيل لطبقة
    بعينها. الأجهزة المعلَنة هي `gfx_0`…`gfx_8` و`gfx_logo` (ومثلها `cg_*`)،
    وكلّها تُركَّب في المخرج.
  - والمنفذ `5521 + رقم النسخة`، فـ«قناة ثانية» تعني **نسخة Air ثانية** — قرار
    ترخيص وعتاد ونشر، لا خيار في الإعدادات.
- **الأثر:** الافتراض الذي بُنيت عليه T-10 **خاطئ**، ولا يُصلحه ضبطُ إعداد.

*(ما تبقى من هذا البند للأرشيف:)*
- **السؤال لمهندس Cinegy:** هل توجد قناة ثانية (معاينة/بروفة) غير مذاعة يمكن
  الالتقاط من مخرجها؟ (`AirChannelNumber` قابل للتوجيه، والمنفذ `5521 + رقم القناة`.)
- **نعم** → T-10 سليمة بالكامل.
- **لا** → «المعاينة قبل النشر» غير ممكنة كما وُصفت؛ ينتقل الثقل إلى T-15.
- **استطلاع أُجري (قراءة فقط، 2026-09-09):**

  | الطبقة | موجودة | على الهواء | ملاحظة |
  |---|---|---|---|
  | ١، ٢، ٦ | ✅ | ❌ | مرشّحة لطبقة التجربة |
  | ٣، ٤ | ✅ | ✅ | **على الهواء وليست في `templates.json`** — اعرف ما هما أولاً |
  | ٥، ٧، ٨، ٩ | ✅ | — | الموجز، العاجل، الشريط، الشعار |
  | ١٠، ١١ | ❌ | — | غير موجودة |

- **إن جُرِّبت طبقة التجربة:** في نافذة تغيير معتمدة فقط، بـ
  `TemplateTestAutoHideSeconds = 3`، عبر `🧪 فحص المسار الحي` القائم، **ومراقبة
  المخرج بالعين أثناءها**. ظهور أي شيء = الطبقة على الهواء.

### T-12 · لا تحذير بلا زرّ ⭐
- **الحالة:** جاهزة للتنفيذ · **الجهد:** صغير-متوسط · **الخطر:** منخفض
- **المشكلة:** تحذير حقيقي وصل مشغّلاً في 2026-09-09:
  > «المدة ٢ أضعاف طول اللوب (٧٥٠ إطار) — سيُعرض كل خبر مرتين. اجعلها ٧٥٠
  > إطاراً أو فعّل مزامنة الظهور.»

  النظام **يعرف الجواب** ويطلب تنفيذه يدوياً — بحساب إطارات، والمشغّل يفكّر
  بالثواني.
- **العمل:** كل تحذير إرشادي يحمل أزرار حلّه: `[اجعلها ٧٥٠ إطاراً]`
  `[فعّل المزامنة]` `[اتركها]`.
- **القاعدة:** إن قدر النظام على صياغة النصيحة، فهو قادر على تنفيذها.
- **⚖️ تصحيح بالقياس (2026-09-09):** وصفتُها أولًا بأنها «مبدأ يشمل كل تحذير»،
  وعمّمتُ من **حالة واحدة رأيتها**. العدّ الفعلي: **ثماني رسائل إرشادية**، ثلاثٌ
  منها نصّ مساعدة لا تحذير وقت تشغيل. فهي إصلاح **٤–٥ مواضع** لا قاعدة عامة —
  ويبقى مفيدًا بهذا الحجم لا بالحجم الذي ادّعيتُه.
- **القبول:** لا يبقى تحذير إرشادي واحد بلا مسار تنفيذ من داخل الرسالة.

### T-42 · الفحص الإملائي قبل الهواء ⭐ *(طلب المحطة)*
- **الحالة:** ⭐ **مطلوبة صراحةً** (2026-09-09) · **الجهد:** متوسط
- **الموضع:** **شاشة المراجعة** — وهي المكان الوحيد الذي يقرأ فيه إنسانٌ النصّ
  قبل خروجه، فهي حيث ينفع التنبيه.
- **⚠️ قيدٌ يحكم التصميم:** النصّ **لا يغادر الشبكة**. خدمة تدقيق خارجية تعني
  إرسال عناوين لم تُبثّ بعدُ إلى طرف ثالث — وهو ما رُفض من أجله «الخبر العاجل
  بالصوت» (T-14). **فالحلّ محليّ أو لا يكون.**
- **ثلاثة مستويات، من الأرخص إلى الأدقّ:**
  1. **أخطاء شكلية بلا قاموس** *(رخيص، يبدأ اليوم)*: حرف مكرّر ثلاثًا،
     مسافة مفقودة حول علامة، أرقام لاتينية وسط نصّ عربي، مسافتان، علامة
     ترقيم معلّقة، تشكيل شاذّ. تمسك أكثر ما يقع فعلًا في الكتابة على الهاتف.
  2. **«كلمة لم تكتبها المحطة من قبل»** *(الأذكى لكلفته)*: عند الجسر أرشيف
     الأخبار والقيم الأخيرة — أي **معجم المحطة نفسها**. كلمةٌ لم ترد فيه ولا
     مرة إمّا خطأ مطبعي أو اسم جديد، **وكلاهما يستحق نظرة ثانية**. بلا قاموس
     ولا اعتماديات ولا شبكة.
  3. **قاموس عربي محلّي** (Hunspell أو ما يعادله) *(الأدقّ، وأثقل)*: يحتاج ملف
     قاموس وتبعية جديدة، وأخطاؤه الكاذبة في العربية كثيرة — فلا يُبنى قبل أن
     يُثبت المستويان الأولان أنهما لا يكفيان.
- **قاعدة لا تُخالَف:** **إرشاديّ لا مانع.** يُظهر تنبيهًا ولا يمنع نشرًا أبدًا —
  خبرٌ عاجل يُحبس بسبب اسم علمٍ لا يعرفه القاموس أسوأ من خطأ إملائي.
- **القبول:** كلمة مشبوهة تُعلَّم في شاشة المراجعة مع زرّ «تجاهل»، والنشر يمضي
  في الحالتين.

### T-13 · شاشة تسليم المناوبة ⭐
- **الحالة:** ✅ **مؤكَّدة بجواب المحطة** · **الجهد:** متوسط
- **الجواب (2026-09-09):** «توجد مناوبات، لكن في معظم الأحيان **التبديل شفوي**.»
  فالحاجة حقيقية لا مفترَضة — والتبديل الشفوي هو بالضبط ما يضيع منه ما لا
  يُقال.
- **ما تحمله الشاشة (احتمالات للاختيار منها):**
  - ما على الهواء الآن، ومنذ متى، ومن نشره ← **ويشمل ما تجاوز عتبة T-40**
  - المواعيد في الساعات القادمة، والفقرة الجارية والتالية (من T-30)
  - الفشل غير المعالَج، والمسودات المفتوحة بأسماء أصحابها
  - قفل شريط الأخبار: بيد من هو الآن
  - سطر ملاحظة حرّ يكتبه المُسلِّم
  - **إقرار استلام** من المُستلِم يُسجَّل — فيصير للتبديل الشفوي أثر
- **العمل:** «🤝 تسليم» تجمع في شاشة واحدة: ما على الهواء، والمواعيد في الست ساعات
  القادمة، والفشل غير المعالَج، والمسودات المفتوحة بأسماء أصحابها، وسطر ملاحظة
  يكتبه المُسلِّم.
- **لماذا:** يحوّل أخطر لحظة في اليوم — تبديل من يمسك الهواء — من كلامٍ شفهي إلى
  سجلّ.
- **يبني على:** «ماذا فاتني»، والجدولة، وحالة الهواء، والمسودات.

---

## المرحلة ١-ج — مادة الفيديو: ما على الهواء وجدول المواد 📺

> **بندٌ مستقل بطلب المحطة (2026-09-09).** يخصّ **الفيديو والمواد المبثوثة**، لا
> الغرافيك ولا الـCG. يحلّ محلّ ~~T-30~~ و~~T-33~~ ويجمعهما.

**الجسر اليوم لا يعرف شيئًا عن المادة المبثوثة.** يعرف ما وضعه هو من غرافيك،
ولا يعرف أي برنامج يجري تحته — والمشغّل يفتح Cinegy ليعرف.

**كل ما يلزم متاح بنداءَي `GET` على المنفذ الذي يكلّمه الجسر أصلًا، وقُيس فعليًا
(2026-09-09):**

| النقطة | ما تعطيه | القياس |
|---|---|---|
| `GET /video/status` | `Active` و`Cued` بمعرّفَين حقيقيَّين | ✅ يُترجمان إلى أسماء |
| `GET /video/list` | جدول المواد كاملًا | **٢٤ مادة · ٤٥٤٤ بايت · تغطي ٢٤ ساعة** |

**حقول كل مادة:** `Id, Name, ScheduledAt, Duration, LoopStart, ProxyProgress`

**عيّنة حيّة:**
```
14:00  تغطية خاصة صباحي الساعة الأولى 08-09-2026   (01:00:00)
15:00  تغطية خاصة صباحي الساعة الثانية 08-09-2026  (01:00:00)
16:00  حلقة اليوم 8-9-2026                          (01:00:00)
```

### T-50 · «الجاري والتالي» ⭐ ابدأ هنا
- **الحالة:** ✅ مقيس · **الجهد:** صغير · **الخطر:** صفر (قراءة فقط)
- **العمل:** سطر في `ℹ️ الحالة` وفي القائمة الرئيسية: **الجاري** و**التالي**
  باسم المادة ووقتها والمتبقّي منها.
- **يُقرأ من:** `Active`/`Cued` في `/video/status`، تُطابَق أسماؤهما من
  `/video/list`.
- **القبول:** المشغّل يعرف ما يُبثّ دون فتح Cinegy.

### T-51 · شاشة جدول المواد
- **الحالة:** مقترحة · **الجهد:** صغير-متوسط
- **العمل:** شاشة `🎞 جدول المواد` تعرض اليوم: الوقت، والاسم، والمدّة، مع تمييز
  الجاري.
- **الحجم مقيس:** ٢٤ مادة تمرّ تحت حدّ الأربعين صفًّا وحدّ الحمولة — ومع ذلك
  **تمرّ بـ`Select-RichTableRows`** كأي جدول، فعدد المواد يوم عطلة قد يختلف.

### T-52 · جدولة الغرافيك نسبةً إلى المادة
- **الحالة:** مقترحة · **الجهد:** متوسط · **الأعلى قيمة على المدى**
- **العمل:** «اعرض العاجل **بعد ٣٠ ثانية من بدء الفقرة**» بدل ساعة حائط.
- **لماذا:** هذه طريقة تفكير المخرج فعلًا. والجدولة بساعة الحائط تنكسر كلما
  تأخّرت المادة أو قُدّمت.
- **يبني على:** `ScheduledAt` و`Duration` من الجدول، والجدولة القائمة.

### T-53 · تنبيه على مادة غير جاهزة
- **الحالة:** فكرة · **الجهد:** صغير
- **الملاحظة:** كل مادة تحمل `ProxyProgress`. مادةٌ مجدولة بعد ساعة ونسختها
  الوسيطة غير مكتملة تستحق تنبيهًا **قبل موعدها** لا عنده.
- **يُتحقَّق أولًا:** ماذا تعني القيمة على نسختكم، وهل تتغيّر فعلًا.

---

## المرحلة ٢ — تغيير التجربة


### T-15 · «قبل / بعد» مع التراجع ⭐
- **الحالة:** **جاهزة للتنفيذ — لا تعتمد على T-11** · **الجهد:** متوسط
- **العمل:** لقطة قبل النشر وأخرى بعده، معروضتان مع زرّ التراجع.
- **لماذا:** «تراجع» اليوم يطلب من المشغّل أن يتذكّر ما كان. أرِه إياه.
- **قيمتها الإضافية:** لا تمنع الخطأ لكنها تقصّر عمره إلى ثوانٍ — **وهي البديل
  العملي لـ T-10 إن لم توجد قناة معاينة**، لأنها تُبنى على قدرتين قائمتين ولا
  تقترب من الهواء.

### T-16 · زمن الوصول إلى الهواء + المسودات المهجورة
- **الحالة:** مقترحة · **الجهد:** متوسط
- **العمل:** قياس الزمن من أول ضغطة إلى النشر، وعرض أبطأ القوالب وأكثر الحقول
  توقّفاً. **وكل مسودة تنتهي مهلتها** تُحتسب: ما يُبدأ ولا يُنشر.
- **لماذا:** ثلاث مسودات مهجورة على قالب واحد لا تعني كسل المشغّل، بل أن القالب
  صعب. يحوّل تحسين التجربة من ذوق إلى قياس.


## المرحلة ٢-ب — قدرات معلنة في Cinegy لا يستعملها الجسر

اكتُشفت بمراجعة التوثيق الرسمي ثم **قِيست على نسختكم فعليًا** (2026-09-09،
استعلامات `GET` فقط). الجسر اليوم يستعمل **نقطتين فقط**: `POST /DEVICE/command`
و`GET /DEVICE/status/active`.

> ⚠️ **فارق إصدار**: التوثيق المراجَع لـ **Air 26.2**، والمثبَّت عندكم
> **`Cinegy Air PRO 22.12.0.4921.15212`** (من `Client Identity` في
> `/video/status`). فما يصفه التوثيق ليس بالضرورة موجودًا هنا — ولهذا قِيس كلّ
> بند أدناه على الجهاز بدل الاكتفاء بالتوثيق. وقد نفع القياس: بندٌ سقط وآخر
> قفز إلى المقدّمة.

**نتيجة القياس على `127.0.0.1:5521`:**

| النقطة | الحالة | ما أعادته |
|---|---|---|
| `GET /gfx_N/status` | ✅ تعمل | `Active`, `Cued`, `License`, **`Output State`**, `Client` |
| `GET /gfx_N/status/cued` | ✅ تعمل | معرّف فارغ على الطبقات المفحوصة |
| `GET /gfx_N/list` | ⚠️ تعمل و**ترجع `<List/>` فارغة** | لا تُعدّد المشاهد |
| `GET /video/list` | ✅ **ترجع الرَّنداون كاملًا** | عناصر بأسماء ومواعيد ومدد |

### T-18 · التحقق من سجل القوالب مقابل ما لدى Cinegy فعلًا
- **الحالة:** ❌ **مرفوضة بالقياس** — `GET /gfx_3/list` تُجيب `<List/>` **فارغة**
  على نسختكم. جهاز الغرافيك **لا يُعدّد مشاهده**، فلا سبيل لمقابلة سجل القوالب
  بهذه الطريقة.
- **ما يبقى ممكنًا:** التحقق من وجود **الملف** على القرص (`templates.json` يحمل
  مسارات `.cintitle`) — فحصٌ محلّي لا يحتاج Cinegy، ويكشف المشهد المحذوف أو
  المنقول. أقلّ يقينًا لكنه يمسك الحالة الشائعة.
- **الأصل (للأرشيف):** `GET /DEVICE/list` و`GET /DEVICE/list/{ITEM-UUID}`.
- **المشكلة اليوم:** `templates.json` يحمل مسارات مشاهد، ولا أحد يعرف أن مشهدًا
  حُذف أو نُقل **حتى يضغط أحدهم «عرض» في السادسة مساءً**.
- **العمل:** فحص دوري (ومن `🩺 مركز الصحة`) يقابل كل قالب بما تعلنه القائمة،
  ويبلّغ عن المفقود **قبل** أن يُطلب على الهواء.
- **القبول:** حذف مشهد من Cinegy يُنتج تنبيهًا خلال دورة الفحص، لا عند أول عرض.

### T-19 · تشخيص **أين** انقطع البثّ، لا أنه انقطع ⭐
- **⚖️ أُعيدت صياغتها بجواب المحطة (2026-09-09):** طلبتُ تمييز «الأسود المتعمَّد»
  وحده، والمحطة تقول إن المطلوب أوسع: **«المراقبة ليست لخارج النظام فقط، بل
  سيرفر البثّ المباشر وانقطاعه — فالأفضل التأكد من المصدر والسيرفر، وللحالتين.»**
- **العمل:** حين تسوَدّ الصورة أو ينقطع الالتقاط، يقول التنبيه **أيّ حلقة
  سقطت**: المصدر؟ أم سيرفر البثّ المباشر؟ أم مخرج القناة؟ بدل «تعذّر الالتقاط».
- **المتاح للبناء عليه:** `Output State` من `/DEVICE/status` (مقيسة ✅)، وحالة
  عملية الترحيل ومصدرها الاحتياطي (قائمة في النظام)، ونتيجة الالتقاط نفسها.
- **القبول:** كل تنبيه انقطاع يسمّي الحلقة المشتبه بها ويقترح الفحص التالي.

*(الصياغة الأصلية — تمييز الأسود المتعمَّد — تبقى جزءًا من هذا:)*
- **الحالة:** ✅ **مؤكَّدة بالقياس — جاهزة للتنفيذ** · **الجهد:** صغير
- **الدليل:** `GET /gfx_N/status` و`GET /video/status` يعيدان
  `<Output State="Normal"/>` على نسختكم الآن. القيمة متاحة، والجسر لا يقرؤها.
- **غير مستعمل:** `<SetOutput State="Normal|Black|Bypass|Clean"/>` وحالة المخرج.
- **المشكلة:** مراقب السواد ينظر إلى الصورة ويقول «أسود». **لا يعرف الفرق** بين
  إشارة مفقودة وبين مخرجٍ ضُبط على `Black` عمدًا في فاصل.
- **العمل:** قراءة حالة المخرج قبل التنبيه، وقول السبب في الرسالة.
- **⚠️ للقراءة فقط:** `SetOutput` أمرٌ **يغيّر الهواء**، ولا يُرسل من الجسر إطلاقًا.


## المرحلة ٣ — رهانات



### T-31 · «ماذا لاحظ الجسر هذا الأسبوع»
- **الجهد:** متوسط
- **العمل:** تقرير أسبوعي يقرأ ما لا يقرأه أحد: الشاشات القريبة من حدّها،
  والقوالب غير المستعملة منذ شهر، والفشل المتكرر بنفس السبب، والإعدادات المغيَّرة.
- **الدليل على حاجته:** لو كان موجوداً لما بقي تحذير الدليل يوماً بلا قارئ.

### T-32 · «هذا ثالث فشل بنفس السبب»
- **الجهد:** صغير-متوسط
- **العمل:** اربط الفشل بسوابقه واذكر العدد. **التكرار هو المعلومة، لا الحدث.**

---

## 🗑 مهامّ حُذفت بالتقييم — والسبب

حُذفت في مراجعة محايدة (2026-09-09) بدل أن تبقى دَينًا وهميًا. محفوظة بأسبابها
كي لا تعود بعد شهر بلا ذاكرة.

- **~~T-01~~ · قراءة غير متزامنة في المدير** — أُجِّلت مرتين بحجة صحيحة
  (السجلّات محليّة، والعلاج إعادة هيكلة). إبقاؤها «مفتوحة» يوهم بدَين لا يُنوى
  سداده. **تُعاد فقط إن انتقلت السجلّات إلى مشاركة شبكية.**
- **~~T-03~~ · قياس أرقام فترة الهدوء** — **دائرية**: تطلب تفعيل ميزة لم تُرَد،
  لقياس رقمٍ لا يهمّ إلا لمن يستعملها. السؤال الصحيح ليس «كم رسالة تُحتجَز» بل
  «لماذا الفترة معطّلة».
- **~~T-14~~ · الخبر العاجل بالصوت** — أعلى مخاطرة وأقلّ دليل. يحتاج خدمة تفريغ
  خارجية، أي أن **صوت غرفة الأخبار يغادر الشبكة** — ويناقض موقف المستودع من
  الأسرار وبيانات التشغيل. والاختناق الذي يعالجه (الكتابة على الهاتف)
  **مُفترَض لا مقيس**.
- **~~T-17~~ · مناوبة الاتصال** — كُتب فيها حرفيًا «أظنّ ولم أتحقق». **ليست
  مهمة بل سؤال**: لماذا فترة الهدوء معطّلة؟ يُبنى بعد الجواب لا قبله.
- **~~T-20~~ · شاشة المحمَّل والجاهز** — القياس أظهر `status/cued` فارغة على كل
  طبقات الغرافيك. ما يبقى منها مُدمج في T-30.

> **~~T-10~~ و~~T-18~~** مرفوضتان بالدليل أيضًا، وتفصيلهما في موضعهما أعلاه.

---

## ⛔ ما لا يُوصى به

رفض الأفكار الجذّابة جزء من الخطة:

- **ذكاء اصطناعي يكتب العناوين** — النظام مبنيّ على «لا شيء يصل الهواء بلا
  مراجعة إنسان»، ونصٌّ يولّده نموذج يغري بالثقة. T-14 مختلفة: تنقل ما قاله
  إنسان، لا تخترع.
- **لوحة ويب** — القوة أن المشغّل لا يتعلّم شيئاً جديداً. لوحة ويب تفتح باب
  صيانة ومصادقة وأمان مقابل ما يفعله تيليجرام أصلاً.
- **نشر بضغطة واحدة بلا مراجعة** — سيطلبها أحدهم. شاشة المراجعة هي سبب عدم وقوع
  كارثة حتى الآن.
- **قوالب بلا حدود صلاحيات** — تبسيطٌ ثمنه أن يضع متدرّب شيئاً على طبقة الشعار.

---

## ترتيب البدء الموصى به

*(مُحدَّث بأجوبة المحطة، 2026-09-09 — وهي أسقطت ترتيبي السابق)*

**رتّبتُ أولًا حسب ما قِسته في الكود. ثم سألتُ المحطة عن آخر خطأ وقع فعلًا،
فكان الجواب «قالب بقي على الهواء ولم يُرفع» — ولا واحد من مرشَّحيّ الثلاثة
الأوائل يعالج تلك الحالة.** هذا ما يفعله سؤال واحد لمستخدم حقيقي بقائمة بُنيت
على استنتاج.

1. **T-40** — «بقي على الهواء ولم يُرفع». **العطل المُبلَّغ عنه فعلًا**، والعلاج
   تصعيدٌ وإبلاغُ الناشر وزرُّ إخفاء: لا شيء منها بحثٌ مفتوح.
2. **T-13** (التسليم) — مؤكَّدة: المناوبات موجودة والتبديل شفوي.
3. **T-42** (الفحص الإملائي) — **طلب صريح من المحطة**. يُبدأ بالمستويين ١ و٢،
   وكلاهما محلّي بلا تبعيات.
4. **T-19** (تشخيص أين انقطع) — بصياغتها الموسَّعة التي طلبتها المحطة.
5. **T-50** (الجاري والتالي) — مقيسة ورخيصة وصفر خطر، لكنها **لا تعالج العطل
   المُبلَّغ عنه**، فتنزل من الأول إلى الخامس. وهي مدخل بند مادة الفيديو كلّه.
6. ثم **T-51** و**T-32** و**T-15**.

> **مشطوبة بالقياس:** ~~T-10~~ (لا معاينة في الواجهة البرمجية) و~~T-18~~
> (`gfx/list` فارغة). **ومرقّاة:** T-30 من رهانٍ كبير إلى أوّل ما يُنفَّذ.
>
> والدرس: أربعة بنود بُنيت على التوثيق، وغيّر القياسُ ثلاثةً منها — وفارق
> الإصدار بين التوثيق (26.2) والمثبَّت (22.12) وحده كان يكفي لتبرير القياس.

> ~~T-11~~ أُغلق: لا معاينة في الواجهة البرمجية. ولا يُعاد فتح T-10 إلا بنسخة
> Air ثانية.
