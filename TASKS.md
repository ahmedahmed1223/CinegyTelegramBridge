# سجل تنفيذ المهام — CinegyTelegramBridge

توثيق لما نُفّذ من مراجعة `REVIEW.md`، مع مكان التنفيذ في الكود وكيفية
التحقق منه عمليًا. الترقيم مطابق لأرقام بنود المراجعة.

**الحالة العامة:** ٢٠/٢٠ بندًا منفّذة + إعدادات تحكم كاملة للمسؤول.

> ⚠️ **مطلوب منك يدويًا:** البند رقم ٣ (تجديد توكن البوت) — لا يمكن تنفيذه
> برمجيًا، انظر تفاصيله أدناه.

---

## 🔴 الأولوية العالية

### ✅ ١. حلقة المعالجة لم تعد تتجمّد

**ما كان:** زر الصورة يوقف البوت ١٥ ثانية لكل المستخدمين.

**ما نُفّذ:**
- `Start-SnapshotJob` تشغّل ffmpeg وتعود **فورًا**، وتسجّل المهمة في
  `$script:SnapshotJobs`.
- `Update-SnapshotJobs` (تُستدعى من `Invoke-BridgeTick` بين كل استطلاع) هي
  التي تسلّم الصورة أو تبلّغ بالفشل.
- `Get-EffectivePollTimeout` تقلّص مهلة الاستطلاع الطويل من ٣٠ ثانية إلى
  **ثانية واحدة** طالما هناك عمل غير متزامن معلّق، فتصل الصورة خلال ثانية
  دون تعطيل بقية الأوامر.
- `Start-LiveRelay` لم تعد تستخدم `Start-Sleep`؛ التحقق من نجاح الإقلاع
  مؤجَّل إلى `Update-RelayWatchdog` عبر `RelayState.VerifyAt`.

**كيف تتحقق:** اضغط 📸 من حساب، وفورًا اضغط 🚨 إخفاء الكل من حساب آخر —
الإخفاء ينفّذ مباشرة دون انتظار.

---

### ✅ ٢. الصلاحيات صارت على مستوى المستخدم

**ما نُفّذ:**
- `Test-Authorized` و`Test-Admin` تأخذان `-UserId` وتفحصان
  `AllowedUserIds` / `AdminUserIds` الجديدتين.
- الإعداد `RequireUserLevelAuth` (مفعّل افتراضيًا): في المجموعات لا تُمنح
  الصلاحية إلا لمن هو مُدرج صراحة في قوائم المستخدمين — إضافة مجموعة لم تعد
  تمنح كل أعضائها التحكم بالهواء.
- في المحادثة الفردية `chat.id == user.id`، فإعدادك الحالي يعمل دون تغيير.
- كل السجلات وسجل التدقيق تسجّل الآن `user <id>` لا رقم المحادثة فقط.

**كيف تتحقق:** `ℹ️ الحالة` يعرض عدد المحادثات وعدد المستخدمين المصرح لهم.

---

### ⚠️ ٣. توكن البوت — **يتطلب إجراءً منك**

لا يمكن تجديد التوكن برمجيًا. الخطوات:

1. راسل **@BotFather** → `/revoke` → اختر البوت (يُبطل التوكن المكشوف).
2. `/token` للحصول على توكن جديد.
3. ضعه في `config.json` مكان `BotToken`.
4. أعد تشغيل الخدمة.

**ما نُفّذ من جانبي:** أُضيف `.gitignore` يستثني `config.json` و`logs/`
والثنائيات المحلية، فلا يتسرّب التوكن إلى Git مستقبلًا.

---

### ✅ ٤. مراقبة البث وإعادة تشغيله تلقائيًا

**ما نُفّذ:** `Update-RelayWatchdog` تفحص كل `RelayWatchdogSeconds` (٢٠ ثانية
افتراضيًا). العلم `RelayState.ShouldRun` يفرّق بين "أوقفه المستخدم" و"مات
وحده"؛ في الحالة الثانية يُعاد التشغيل تلقائيًا حتى `RelayMaxRestarts` مع
إشعار المشرفين (`NotifyAdminsOnRelayFailure`). عند استنفاد المحاولات يُبلّغ
المشرفون ويتوقف.

**تحكّم:** `RelayAutoRestart`, `RelayMaxRestarts`, `RelayWatchdogSeconds`,
`NotifyAdminsOnRelayFailure`.

---

### ✅ ٥. انتهاء صلاحية حالة الإدخال المعلّقة

**ما نُفّذ:** كل حالة تحمل `StartedAt` عبر `Set-PendingState`. الفحص يتم في
موضعين: `Get-PendingState` عند القراءة (فلا تُستهلك حالة قديمة أبدًا)،
و`Update-PendingExpiry` دوريًا التي تحذفها وترسل "⌛ انتهت مهلة الإدخال".

**تحكّم:** `PendingStateTimeoutMinutes` (٥ دقائق افتراضيًا).

---

## 🟠 الأولوية المتوسطة

### ✅ ٦. حماية من خصائص JSON الناقصة

`Get-TemplateStore` تتحقق من كل قالب: `path` و`layer` إلزاميان، وأي قالب
ناقص **يُتخطّى مع رسالة تحذير** بدل انهيار البوت. باقي الحقول (`fields`,
`description`, `order`, `presets`) اختيارية بقيم افتراضية آمنة. التحذيرات
تظهر في `ℹ️ الحالة` و📋 القوالب وفي `bridge.log`.

### ✅ ٧. حدّ 64 بايت على `callback_data`

لم تعد الأسماء تُضمَّن في بيانات الأزرار إطلاقًا — تُستخدم **فهارس رقمية**
(`tpl:3`, `updf:3:1`, `preset:2:0`) مقابل ترتيب ثابت من `Get-TemplateStore`.
أسماء القوالب والحقول العربية مهما طالت لن تكسر لوحة الأزرار.

### ✅ ٨. `Invoke-Command` لم يعد يحجب الأمر المدمج

أُعيدت التسمية إلى **`Invoke-BridgeCommand`**.

### ✅ ٩. تدوير ملف السجل

`Invoke-LogRotation`: عند تجاوز `LogMaxSizeMB` يُعاد التسمية إلى
`bridge.1.log` … مع الاحتفاظ بـ `LogKeepFiles` أجيال.

**تحكّم:** `LogMaxSizeMB` (١٠)، `LogKeepFiles` (٥).

### ✅ ١٠. سقف وانتهاء صلاحية لطلبات الوصول

`Request-Approval` ترفض ما يزيد على `MaxPendingApprovals`، وتُسجّل ذلك بسطر
واحد فقط بدل إغراق المشرفين. `Update-PendingExpiry` تحذف الطلبات الأقدم من
`PendingApprovalExpiryHours`. يمكن تعطيل الاستقبال كليًا عبر
`EnableSelfServiceRequests`.

### ✅ ١١. تهدئة لطلب الصور

خلال `SnapshotCooldownSeconds` تُعاد **آخر لقطة محفوظة** بدل تشغيل ffmpeg من
جديد — يحمي معالج جهاز التشغيل من الضغط المتكرر.

### ✅ ١٢. منع تشغيل نسختين

Mutex عام `Global\CinegyTelegramBridge` عند الإقلاع؛ النسخة الثانية تخرج
برسالة واضحة في اللوق بدل التسبب في أخطاء 409 المتقطعة. للتجاوز عمدًا:
`-AllowMultipleInstances`.

### ✅ ١٣. `Save-Config` لم تعد تمسح تعديلاتك

**تأكيد عملي:** حدثت هذه المشكلة فعلًا أثناء العمل — عند الموافقة على
المستخدم `131017043` أعادت النسخة القديمة كتابة `config.json` من الذاكرة
و**حذفت كتلة `LiveStream`** بالكامل. أُعيدت الكتلة، والدالة الجديدة تقرأ
الملف من القرص أولًا وتدمج الحقول التي يديرها البوت فقط
(`AllowedChatIds`, `AdminChatIds`, `AllowedUserIds`, `AdminUserIds`,
`Settings`, `LiveStream`)، وتتبنّى أي تعديل يدوي آخر في الذاكرة.

### ✅ ١٤. تقسيم الرسائل الطويلة

`Split-TelegramText` تقسّم عند ٣٥٠٠ حرف على حدود الأسطر (لوحة الأزرار تُرفق
بالجزء الأخير فقط)، فلا تفشل قائمة قوالب طويلة صامتة.

### ✅ ١٥. أسماء صور فريدة

كل لقطة `snapshot-<timestamp>.jpg`، ولا يُحتفظ إلا بالأحدث للتهدئة — لا
تضارب بين طلبين متزامنين.

### ✅ ١٦. ترتيب ثابت للقوالب

الترتيب حسب حقل `order` الاختياري ثم الاسم — مواضع الأزرار لا تتغيّر بين
عرض وآخر (ذاكرة العضلات للمشغّل). أُضيف `order` لكل قالب في
`templates.json`.

---

## 🟡 التحسينات والتطوير

### ✅ ١٧. تخفيف الحمل على جهاز التشغيل

`CopyCodec` موثّق ومتاح في `LiveStream`؛ عند `true` يمرّر ffmpeg بـ `-c copy`
دون إعادة ترميز. أُضيف `-loglevel error` لالتقاط الصور.

### ✅ ١٨. الأزرار الجديدة

| الزر | الوظيفة |
|---|---|
| 🚨 **إخفاء الكل** | زر طوارئ يخفي كل الطبقات المعروفة دفعة واحدة ويلغي كل مؤقتات الإخفاء |
| ⏱ **عرض مؤقّت** | إظهار قالب مع إخفاء تلقائي بعد `AutoHideDefaultSeconds` |
| ⭐ **المفضّلة** | أكثر القوالب استخدامًا في صف مستقل بالقائمة الرئيسية (عدّادات محفوظة في `logs/usage.json`) |
| ⚡ **نصوص جاهزة** | قيم محفوظة مسبقًا لكل قالب عبر `presets` في `templates.json` — إظهار بضغطة واحدة |
| 🔁 **إعادة الأخير** | تكرار آخر إظهار قمت به بنفس القيم |
| 🏠 **زر القائمة الثابت** | شريط دائم أسفل الشاشة (🏠 القائمة / 🆘 مساعدة) + قائمة أوامر ☰ أصلية — انظر البند التالي |

### ✅ ١٨-ب. مخرج دائم للمستخدم التائه (محادثة جديدة أو مشكلة)

**المشكلة:** لوحة الأزرار المضمّنة (inline) تبقى مرتبطة برسالة قديمة؛ إن فتح
المستخدم محادثة جديدة، أو مسح المحادثة، أو علق في منتصف تعبئة حقول، لم يكن
أمامه سوى تذكّر كتابة `/بدء`.

**ما نُفّذ — ثلاث طرق مستقلة للرجوع:**

1. **قائمة الأوامر الأصلية (☰):** `Register-BotCommands` تستدعي
   `setMyCommands` و`setChatMenuButton` عند كل إقلاع، فيظهر زر ☰ بجانب مربع
   الكتابة يعرض عشرة أوامر بوصف عربي لكل منها.
   > قيد من تليجرام: أسماء الأوامر المسجَّلة يجب أن تكون
   > **حروفًا لاتينية صغيرة وأرقامًا و`_` فقط** — فلا يمكن تسجيل `/بدء` في
   > هذه القائمة. لذلك المسجَّل هو المرادفات الإنجليزية (`start`, `menu`,
   > `cancel` …) مع **وصف عربي** هو ما يقرأه المشغّل فعليًا. الأوامر العربية
   > تبقى تعمل بالكتابة كالمعتاد.
2. **شريط أزرار ثابت:** `Get-PersistentReplyKeyboard` يثبّت
   **🏠 القائمة** و**🆘 مساعدة** أسفل الشاشة دائمًا. الضغط عليهما يُعالَج
   **قبل** أي حالة إدخال معلّقة، فيعملان حتى لو كان المستخدم في منتصف تعبئة
   قالب — وهو بالضبط سيناريو "علقت وأريد الخروج".
3. **أوامر جديدة:** `/قائمة` (`/menu`) و`/الغاء` (`/cancel`) إضافة إلى
   `/بدء`. جميعها تمرّ عبر `Show-MainMenu` التي **تمسح أي عملية معلّقة**
   ثم تعيد تثبيت الشريط وتعرض القائمة.

**تحكّم:** `EnablePersistentMenuButton` (مفعّل افتراضيًا) لإخفاء الشريط
الثابت دون التأثير على قائمة ☰ أو الأوامر.

### ✅ ١٩. التدقيق والمتابعة

- 📜 **السجل**: زر للمشرف يعرض آخر ٢٠ عملية (من فعل ماذا ومتى) من داخل
  تليجرام — `AuditTrailSize`.
- 💚 **نبضة يومية**: رسالة تأكيد للمشرفين — `HeartbeatEnabled`,
  `HeartbeatHour`.
- `ℹ️ الحالة` وسّعت لتشمل حالة البث، الصور المعلّقة، مؤقتات الإخفاء، أعداد
  المصرح لهم، والطلبات المعلّقة، وتحذيرات القوالب.

### ✅ ٢٠. جودة الكود

- أفعال معتمدة: `Approve-User`→`Grant-UserAccess`، `Reject-User`→
  `Deny-UserAccess`، `Continue-ShowFlow`→`Resume-ShowFlow`،
  `Invoke-Command`→`Invoke-BridgeCommand`.
- `Get-TemplateStore` تخزّن مؤقتًا حسب `LastWriteTimeUtc` — تعديل
  `templates.json` يسري فورًا بلا إعادة تشغيل، ودون إعادة تحليل الملف عشرات
  المرات لكل ضغطة.
- `Get-JsonProp` تدعم الـ hashtables والكائنات معًا.
- `$ConfigPath` يُحوَّل لمسار مطلق نسبةً للسكربت — يعمل كخدمة بأي مجلد عمل.
- `.gitignore` أُضيف.
- تنظيف: حُذفت دالة غير مستخدمة، ولا توجد دوال مكرّرة (٨٧ دالة، فحص آلي).

---

## ⚙️ إعدادات المسؤول (جديد كليًا)

زر **⚙️ الإعدادات** في القائمة الرئيسية (للمشرفين فقط) يعرض كل الخيارات:
المنطقية بزرّ تبديل ✅/❌ فوري، والرقمية بزرّ 🔢 يطلب القيمة الجديدة. كل
تغيير يُحفظ في `config.json` مباشرة ويُسجَّل في سجل التدقيق. زر
**♻️ استعادة الافتراضي** يعيد كل شيء لقيمه الأصلية.

| الإعداد | الافتراضي | الوظيفة |
|---|---|---|
| `RequireUserLevelAuth` | `true` | فحص الصلاحية على المستخدم لا المحادثة |
| `EnableSelfServiceRequests` | `true` | السماح للغرباء بطلب الوصول |
| `EnableRawCommand` | `true` | تفعيل `/أمر` الخام |
| `EnableSnapshot` | `true` | زر الصورة |
| `EnableLiveRelay` | `true` | أزرار البث المباشر |
| `EnableTimedShow` | `true` | زر العرض المؤقّت |
| `EnableHideAll` | `true` | زر إخفاء الكل |
| `EnableFavorites` | `true` | صف المفضّلة |
| `EnablePersistentMenuButton` | `true` | شريط 🏠 القائمة / 🆘 مساعدة الثابت |
| `PendingStateTimeoutMinutes` | `5` | مهلة الإدخال المعلّق |
| `SnapshotCooldownSeconds` | `10` | تهدئة الصور |
| `SnapshotTimeoutSeconds` | `8` | مهلة ffmpeg للصورة |
| `SnapshotRetentionMinutes` | `30` | عمر ملفات اللقطات قبل الكنس التلقائي |
| `DropPendingUpdatesOnStart` | `true` | تجاهل الأوامر المتراكمة قبل الإقلاع |
| `AirCommandTimeoutSeconds` | `3` | مهلة كل أمر يُرسل إلى Air Pro |
| `MaxFieldLength` | `200` | الحد الأقصى لطول النص الذاهب إلى الهواء |
| `AutoHideDefaultSeconds` | `10` | مدة العرض المؤقّت |
| `RelayAutoRestart` | `true` | إعادة تشغيل البث تلقائيًا |
| `RelayMaxRestarts` | `20` | سقف المحاولات |
| `RelayWatchdogSeconds` | `20` | دورية فحص البث |
| `MaxPendingApprovals` | `20` | سقف الطلبات المعلّقة |
| `PendingApprovalExpiryHours` | `24` | انتهاء صلاحية الطلب |
| `FavoritesCount` | `3` | عدد أزرار المفضّلة |
| `LogMaxSizeMB` | `10` | حجم السجل قبل التدوير |
| `LogKeepFiles` | `5` | عدد أجيال السجل |
| `AuditTrailSize` | `50` | حجم سجل التدقيق |
| `HeartbeatEnabled` | `false` | النبضة اليومية |
| `HeartbeatHour` | `9` | ساعة النبضة (0-23) |
| `NotifyAdminsOnRelayFailure` | `true` | إشعار انقطاع البث |

---

---

## 🐛 إصلاح عاجل: رسائل `System.Object[]`

**العَرَض الذي أبلغت عنه:** كل أمر يرد بنص `System.Object[]` بدل الرسالة.

**السبب الجذري:** في `Split-TelegramText` كان مسار الرسائل القصيرة:

```powershell
if ($Text.Length -le $limit) { return , @($Text) }   # ❌
```

معامل الفاصلة الأحادي `,` يلفّ المصفوفة داخل مصفوفة أخرى، فتُخرج الدالة
**كائنًا واحدًا هو مصفوفة**. عند المستدعي أصبح `$chunks[0]` مصفوفة لا نصًا،
ووُضعت كما هي في جسم الطلب:

```powershell
$body = @{ chat_id = $ChatId; text = $chunks[$i] }   # text = [object[]]
```

وتليجرام يستقبلها بعد التحويل النصي كـ `System.Object[]`. ولأن كل رسالة
تقريبًا أقصر من ٣٥٠٠ حرف، فقد مرّت **كل** الرسائل بهذا المسار — وهو ما يفسّر
ظهور المشكلة مع أي أمر.

> لماذا لم يظهر الخطأ عند حدود الدالة؟ لأن معامل `-Text` معرّف
> `[string]$Text`، فـ PowerShell يحوّل أي شيء إلى نص عند الربط. التسريب حدث
> **داخل** الدالة في جدول التجزئة غير المُنمَّط.

**الإصلاح:**
- أُعيدت كتابة `Split-TelegramText` لتُرجع دائمًا `[string[]]` عبر
  `List[string].ToArray()`، ولم يعد يُستخدم أسلوب `, @()` إطلاقًا (مع تعليق
  تحذيري في الكود يمنع إعادته مستقبلًا).
- تنميط صريح عند المستدعي: `[string[]]$chunks` و`text = [string]$chunks[$i]`
  كطبقة حماية ثانية.
- أُعيدت هيكلة خوارزمية التقسيم لتستخدم `StringBuilder`، وتتعامل مع السطر
  الأطول من الحد بتقسيمه قسرًا دون فقدان أي حرف.

**التحقق:** نُقلت الخوارزمية الجديدة حرفيًا إلى Python واختُبرت على ٣٠٨
حالات (فارغة، سطر واحد طويل، أسطر متعددة، نصوص عربية، ٣٠٠ حالة عشوائية):
**لا مقطع يتجاوز الحد، ولا حرف واحد مفقود**.

كما أُزيل `continue` من داخل كتلة `try` في الحلقة الرئيسية واستُبدل ببنية
`if/else` صريحة — تجنّبًا لأي التباس في تدفّق التحكم.

---

## 🐛 إصلاح عاجل ٢: `The property 'Count' cannot be found on this object`

**العَرَض:** كل أمر يفشل بـ "حدث خطأ داخلي"، ويسجّل `bridge.log`:
`The property 'Count' cannot be found on this object.`

**السبب الجذري:** نفس عائلة الخطأ السابق — سلوك PowerShell عند إرجاع
المصفوفات. في `Get-MainMenuKeyboard`:

```powershell
$favs = Get-FavoriteTemplateKeys   # ❌
if ($favs.Count -gt 0) { ... }
```

`Get-FavoriteTemplateKeys` تُرجع `@()` عندما لا يوجد سجل استخدام. ودالة
PowerShell التي تُرجع **مصفوفة فارغة تُخرج صفر كائنات**، فيصبح `$favs`
قيمته `$null`. ومع `Set-StrictMode -Version Latest` فإن `$null.Count`
يرمي هذا الاستثناء بالضبط.

**لماذا ظهر مع كل أمر؟** لأن `logs/usage.json` غير موجود في التثبيت الجديد
(تأكدتُ من ذلك) ⇒ لا توجد مفضّلات ⇒ `$favs = $null` ⇒ والقائمة الرئيسية
تُبنى في نهاية **كل** عملية تقريبًا.

**الإصلاح:**
- `$favs = @(Get-FavoriteTemplateKeys)` — الالتفاف بـ `@()` عند نقطة
  الاستدعاء هو الحل الصحيح: صفر كائنات ⇒ مصفوفة فارغة، لا `$null`.
- طُبّقت القاعدة نفسها على كل الدوال التي تُرجع مصفوفات
  (`Get-KnownLayers`, `Get-FfmpegInputArguments`, `Build-RelayArguments`)،
  مع تعليق في الكود يشرح السبب.
- **فحص شامل آلي** لكل استخدامات `.Count` في الملف (٢٦ موضعًا) وتتبّع مصدر
  كل متغيّر: جميعها الآن مصفوفات أو مجموعات .NET مضمونة.

**إصلاح إضافي في نفس العائلة:** `Fields = @(Get-JsonProp $entry 'fields')` —
إن كان القالب بلا حقل `fields` إطلاقًا تُخرج `Get-JsonProp` قيمة `$null`
واحدة، فتصبح `@($null)` **مصفوفة من عنصر واحد قيمته null** ⇒ البوت يطلب
"حقلًا وهميًا". أُضيف `Where-Object { $null -ne $_ }` للتصفية.

**وتقوية:** `$CallbackQuery.message` قد يكون غائبًا إذا كانت الرسالة الأصلية
قديمة جدًا؛ لم يعد يُقرأ مباشرة بل عبر `Get-JsonProp` مع تجاهل آمن.

> **الدرس المستفاد** (موثّق في الكود): في PowerShell، أي استدعاء لدالة قد
> تُرجع مصفوفة يجب أن يُلفّ بـ `@()` عند نقطة الاستدعاء. وهذان الخطآن معًا
> — `return , @()` و`$x = Func` — وجهان لنفس العملة: كيفية تعامل PowerShell
> مع المصفوفات عبر حدود الدوال.

---

## 🐛 إصلاح عاجل ٣: فشل التقاط الصورة (`exit -22`) + التنظيف التلقائي

### أ. سبب `Snapshot ffmpeg failed (exit -22)`

الدليل من `logs\snapshot-stderr.log`:

```
Unable to choose an output format for 'D:\cingy'; use a standard extension...
Error opening output file D:\cingy.
```

**السبب:** `Start-Process -ArgumentList` عند تمرير **مصفوفة** يدمج عناصرها
بمسافات **دون وضع علامات اقتباس** حول ما يحتوي منها على مسافة. ومسار مشروعك
`D:\cingy cg\...` فيه مسافة، فرأى ffmpeg اسم الملف `D:\cingy` فقط ورفضه
(`-22` = `EINVAL` أي وسيط غير صالح).

**الإصلاح:** أُضيفت `ConvertTo-ProcessArgumentLine` التي تطبّق قواعد اقتباس
سطر أوامر ويندوز القياسية (مع التعامل الصحيح مع الشرطات المائلة قبل علامات
الاقتباس وفي نهاية المسار)، وتمرّ عبرها **كل** عمليات تشغيل ffmpeg — الصورة
والبث معًا. البث كان معرّضًا لنفس المشكلة لو احتوى رابط RTMP أو المصدر على
مسافة.

**التحقق:** نُقلت دالة الاقتباس ومحلّل `CommandLineToArgvW` إلى Python
واختُبرا على مسارك الحقيقي: الأمر الجديد يُحلَّل ليعطي المسار كاملًا
`D:\cingy cg\...\snapshot-....jpg`، بينما القديم كان يعطي `cg\...` — مطابق
تمامًا لما ظهر في سجلّك. واجتازت حالات حدّية (مسافات، شرطة مائلة في النهاية،
علامات اقتباس مضمّنة، قيمة فارغة).

**تحسين مرافق:** رسالة الخطأ في تليجرام صارت تتضمّن **آخر سطر من stderr**
لـ ffmpeg بدل رقم الكود فقط — لو كانت موجودة من البداية لأظهرت سبب المشكلة
فورًا. طُبِّق ذلك على الصورة والبث معًا. وصار لكل عملية التقاط **ملف stderr
خاص بها** بدل ملف مشترك، تجنّبًا لتضارب الرسائل بين طلبين متزامنين.

### ب. التنظيف التلقائي للصور

**الوضع السابق:** مسار النجاح يحذف اللقطة السابقة فقط؛ أما اللقطات الفاشلة
(ملفات صفرية الحجم) أو المتروكة بسبب إيقاف قسري للجسر فتتراكم إلى ما لا
نهاية.

**ما نُفّذ — تنظيف على أربعة مستويات:**

| المستوى | متى |
|---|---|
| حذف فوري | عند نجاح الالتقاط تُحذف اللقطة المخزّنة السابقة |
| حذف فوري | عند فشل الالتقاط أو انتهاء مهلته يُحذف الملف الجزئي/الفارغ |
| حذف فوري | ملف stderr الخاص بكل عملية يُحذف بعد قراءته |
| كنس دوري | `Update-SnapshotCleanup` كل ٥ دقائق تحذف كل `snapshot-*.jpg` و`snapshot-*.err` الأقدم من `SnapshotRetentionMinutes` |
| كنس عند الإقلاع | تُستدعى بـ `-Force` عند بدء التشغيل لإزالة بقايا أي تشغيل سابق |

الكنس لا يمسّ أي التقاط قيد التنفيذ (يقارن بقائمة المهام النشطة)، ولا يمسّ
اللقطة الحديثة المستخدمة للتهدئة. كما يحذف الملف المشترك القديم
`snapshot-stderr.log` من النسخ السابقة.

**إعداد جديد:** `SnapshotRetentionMinutes` (٣٠ دقيقة افتراضيًا)، قابل
للتعديل من شاشة ⚙️ الإعدادات.

---

## 🔍 جولة مراجعة ثانية: الاستقرار وتجربة المستخدم

انطلقت هذه الجولة من **قراءة `bridge.log` الفعلي** لا من التخمين. ثلاثة أخطاء
ظهرت فيه، أحدها ما زال قائمًا وقت المراجعة.

### 🔴 ١. عطل قائم: الضغط على ⏭ تخطي يُسقط الأمر

**من السجل (تكرّر ٣ مرات):**
`Cannot bind argument to parameter 'Value' because it is an empty string.`

**السبب:** في `CinegyAirTitler.psm1`:

```powershell
function Escape-XmlValue {
    param([Parameter(Mandatory)][string]$Value)   # ❌
```

معامل `[string]` **إلزامي** في PowerShell **يرفض النص الفارغ** برسالة مطابقة
تمامًا لما في السجل. فأي إظهار قالب بحقل فارغ — أي كل ضغطة على **⏭ تخطي** —
كان يفشل.

**الإصلاح:** أُضيف `[AllowEmptyString()]`. وإضافة إلى ذلك غُيّرت دلالة زر
التخطي نفسه: بدل إرسال نص فارغ يمحو الحقل، صار **يحذف الحقل من الطلب
تمامًا** فيحتفظ المشهد بالنص المصمَّم عليه أصلًا — وهو المتوقع فعليًا من
"تخطي".

### ✅ ٢. `The property 'message' cannot be found` (من السجل، مُصلَح سابقًا)

كان `$update.message` يُقرأ مباشرة، فتنهار الحلقة عند وصول تحديث من نوع آخر
(`edited_message`, `my_chat_member`…). مُعالَج في إعادة الكتابة عبر
`Get-JsonProp`. وأُضيف الآن حصن مماثل لاستجابة `getUpdates` نفسها: إن ردّ
تليجرام بـ `ok:false` (مثل خطأ 409 عند وجود نسخة ثانية، أو بعد تجديد
التوكن) تُرمى رسالة واضحة بدل انهيار غامض على خاصية `result` المفقودة.

### ✅ ٣. موافقة مكرّرة (من السجل)

السجل يُظهر `approved new user 131017043` **مرتين** خلال ثانية — أزرار
الموافقة تبقى قابلة للضغط، فضغطة مزدوجة (أو مشرفان) تعيد تنفيذ العملية
وتُرسل للمستخدم إشعارًا مكرّرًا. صارت العملية **idempotent**: إن كان المستخدم
مصرّحًا له بالفعل يُرد "مصرّح له بالفعل" دون تكرار شيء.

### ✅ ٤. تقويات استقرار إضافية

| المشكلة | الإصلاح |
|---|---|
| `config.json` مقفل (محرر مفتوح، مضاد فيروسات) كان يُسقط عملية الموافقة/الإعداد | `Save-Config` محاطة بـ try/catch، والتغيير يبقى ساريًا لهذه الجلسة، ويُضاف تنبيه "⚠️ تعذّر الحفظ - التغيير مؤقّت" إلى رد البوت حتى لا يُقال للمستخدم "تم الحفظ" وهو لم يُحفظ |
| ويندوز يعيد استخدام أرقام العمليات (PID)، فقد يشير `relay.pid` القديم إلى **عملية ffmpeg أخرى** — وأرجحها لقطة من نفس البوت | صار الملف يخزّن `pid|وقت البدء`، ويُتحقق من الاثنين معًا |
| فشل شبكي لحظي كان يبتلع رسالة تأكيد أو إشعار موافقة بصمت | إعادة محاولة واحدة بعد ٤٠٠ ملّي ثانية قبل تسجيل الفشل |

> ملاحظة عن `Save-Config`: جُعلت تسجّل النتيجة في راية بدل إرجاع قيمة، لأن
> إرجاع `$true/$false` من دالة تُستدعى في خمسة مواضع كان سيسرّب قيمة منطقية
> إلى مخرجات كل مستدعٍ — نفس فخّ المصفوفات الذي سبق أن عضّنا مرتين.

### ✨ ٥. تحسينات تجربة المستخدم

| التحسين | لماذا |
|---|---|
| **🙈 إخفاء هذا (طبقة N)** بجانب رسالة "تم الإظهار" | إنزال جرافيك خاطئ كان يتطلب: القائمة ← اخفاء طبقة ← اختيار الرقم. الآن ضغطة واحدة في المكان الذي ينظر إليه المشغّل |
| **صف 🔴 على الهواء** في القائمة الرئيسية | زر لكل طبقة يعتقد البوت أنها ظاهرة، باسم القالب — رؤية فورية وإخفاء بضغطة |
| **`🔴 على الهواء` في شاشة الحالة** | مع اسم القالب ومدة بقائه على الهواء |
| **أسماء حقول مفهومة** | `fields` صار يقبل `{ "name": "Ajel.center", "label": "نص الخبر العاجل" }` بدل عرض اسم متغيّر المشهد الخام للمشغّل. الصيغة القديمة (نص مجرّد) ما زالت تعمل |
| **🚨 إخفاء الكل صار أشمل** | يكنس أيضًا أي طبقة يعتقد البوت أنها على الهواء حتى لو لم تعد مذكورة في `templates.json` |

> **حدود تتبّع "على الهواء":** Cinegy Air لا يوفّر استعلامًا عن المحتوى
> الظاهر حاليًا، فالتتبّع يعكس **ما فعله هذا البوت فقط**. جرافيك شُغّل من
> واجهة Air Pro مباشرة لن يظهر في القائمة.

---

## 🛠 جولة ثالثة: تنفيذ بنود `REVIEW-2.md` (الإصدار 2.3.0)

نُفِّذت بنود «خطيرة» و«متوسطة» و«صغيرة» والمقترحات. **أُجِّلت** المراحل
الثانية والثالثة والرابعة (تقليص المزايا، تقسيم الملف، النضج التشغيلي) بطلبك.

### 🔴 الخطيرة

| # | المشكلة | ما نُفِّذ |
|---|---|---|
| ١ | إعادة تنفيذ أوامر قديمة عند الإقلاع | `Clear-PendingTelegramUpdates` تُقرّ كل التحديثات المتراكمة عند البدء فلا يُنفَّذ ضغط زر وصل أثناء توقّف الجسر. يُتحكّم به عبر `DropPendingUpdatesOnStart` |
| ٢ | تسريب مفتاح البث | `Protect-SensitiveText` تُنقّح `rtmp://`, `srt://`, `passphrase=`, وتوكن البوت. **مطبّقة على كل سطر يُكتب في السجل** وعلى كل نص خطأ يُرسَل للشات |
| ٣ | حجب الحلقة بمهلة ١٠ ثوانٍ لكل أمر Air | `AirCommandTimeoutSeconds` (٣ ثوانٍ افتراضيًا) يمرّ الآن إلى **كل** استدعاءات Air الخمسة؛ أُضيف `-TimeoutSec` إلى دوال الموديول |
| ٤ | كتابة `config.json` غير ذرّية | كتابة إلى ملف مؤقت ثم `Move-Item`، مع `config.json.bak`، **و** استرجاع تلقائي من النسخة الاحتياطية عند الإقلاع لو كان الملف تالفًا |

> **تحفّظ صريح على البند ٣:** خفضتُ المهلة ومرّرتها لكل الاستدعاءات، لكن
> **لم أجعل إخفاء الكل متوازيًا**. التوازي في PowerShell (`ForEach-Object
> -Parallel`) يعمل في runspaces منفصلة ويحتاج إعادة تحميل الموديول داخلها —
> تغيير لا أستطيع اختباره هنا، وإدخاله دون اختبار في زر الطوارئ تحديدًا
> مخاطرة أكبر من الفائدة. الأثر الآن: طبقتان = ٦ ثوانٍ في أسوأ حالة بدل ٢٠.
> التوازي مرشّح للمرحلة المؤجَّلة.

### 🟠 المتوسطة

| # | المشكلة | ما نُفِّذ |
|---|---|---|
| ٥ | لا حدّ لطول النص | حدّ عالمي `MaxFieldLength` (٢٠٠) **+ تحكّم لكل قالب ولكل حقل** — انظر القسم التالي |
| ٦ | إعداد أمني يُعطَّل بضغطة | `RequireUserLevelAuth`, `EnableSelfServiceRequests`, `EnableRawCommand` معلَّمة بـ 🔒 وتتطلب تأكيدًا صريحًا. **التعطيل فقط** يتطلب تأكيدًا؛ إعادة التفعيل تبقى بضغطة |
| ٧ | فقدان حالة "على الهواء" عند إعادة التشغيل | تُحفظ في `logs/onair.json` عند كل تغيير وتُستعاد عند الإقلاع |
| ٨ | إغراق السجل عند تلف `templates.json` | نتيجة الفشل تُخزَّن مؤقتًا بنفس آلية `LastWriteTime` |
| ٩ | كتابة قرص على مسار الهواء | `Add-UsageCount` تعدّ في الذاكرة فقط؛ الكتابة مؤجَّلة كل ٦٠ ثانية وعند الإغلاق |

### 🟡 الصغيرة

- إعادة محاولة واحدة لإرسال الصور (كانت للنصوص فقط).
- **مؤشّر إصدار** `$BridgeVersion` يظهر في `ℹ️ الحالة` وفي سطر بدء التشغيل.

### ⚠️ مهم: التعديل لا يسري إلا بعد إعادة التشغيل

PowerShell يحمّل السكربت في الذاكرة **مرة واحدة عند الإقلاع**. أي تعديل على
`TelegramBridge.ps1` لا يؤثر إطلاقًا على عملية تعمل بالفعل.

**كيف تتأكد أي نسخة تعمل الآن:** اضغط **ℹ️ الحالة** — السطر الأول يعرض رقم
الإصدار. أو انظر أول سطر في `bridge.log` بعد الإقلاع:
`Bridge v2.4.0 starting…`

**ملاحظة على ملفَي الإعدادات والقوالب:** `templates.json` يُعاد تحميله
تلقائيًا عند تغيّره (لا يحتاج إعادة تشغيل)، أما `config.json` فيُقرأ عند
الإقلاع فقط — باستثناء ما يُغيَّر من شاشة ⚙️ الإعدادات فيسري فورًا.

**تحذير:** لا تستخدم أزرار الموافقة على مستخدم أو تغيير إعداد **بينما تنوي
تحديث الكود**؛ العملية القديمة قد تعيد كتابة كتلة `Settings` من نسختها في
الذاكرة فتفقد المفاتيح الجديدة.

### ✍️ التحكم بعدد الحروف (بطلبك)

ثلاثة مستويات، الأخصّ يغلب الأعمّ:

| المستوى | أين يُضبط | مثال |
|---|---|---|
| **لكل حقل** | `fields[].maxLength` في `templates.json` | `{ "name": "Ajel.center", "label": "نص الخبر العاجل", "maxLength": 80 }` |
| **لكل قالب** | `maxLength` في القالب | `"maxLength": 120` — يسري على كل حقوله |
| **عام** | `MaxFieldLength` في ⚙️ **الإعدادات** | يُعدَّل من داخل تليجرام مباشرة بزرّ 🔢 |

لماذا ثلاثة مستويات؟ لأن رقمًا عامًا واحدًا لا يكفي: شريط «عاجل» يتّسع
لثمانين حرفًا بينما شريط متحرك قد يتّسع لثلاثمئة. الحدّ العام أرضية، والقالب
والحقل يضبطان الواقع.

**في التجربة:**
- الحدّ المطبَّق **يظهر في نص الطلب قبل الكتابة**: «أرسل نص الحقل (1/1)
  (الحد: 80 حرفًا)».
- تجاوز الحد يُرفض برسالة تبيّن الطول الفعلي والحد، **مع بقاء مسار الإدخال
  مفتوحًا** فيكفي إعادة الكتابة دون البدء من جديد.
- يُطبَّق على المسارين: الأزرار والأوامر المكتوبة (`/عرض`) وتحديث النص.

طُبِّق عمليًا على قالبك: حقل `Ajel.center` حدُّه ٨٠ حرفًا.

### ✅ المرحلة صفر: الاختبار (أهم بند في الخطة)

أُضيف ملفان:

**`Tests\Bridge.Tests.ps1`** — اختبارات Pester تغطي تحديدًا الدوال التي
أنتجت الأعطال الثلاثة التي وصلتك:

| الاختبار | العطل الذي يمنعه |
|---|---|
| `Split-TelegramText` يُرجع نصًا لا مصفوفة متداخلة | `System.Object[]` |
| `Get-FavoriteTemplateKeys` قابلة للعدّ مع سجل فارغ | `$null.Count` |
| `Escape-XmlValue` تقبل النص الفارغ | فشل ⏭ تخطي |
| `ConvertTo-ProcessArgumentLine` تقتبس المسارات ذات المسافات | `exit -22` |
| `Protect-SensitiveText` تُخفي المفاتيح | تسريب مفتاح البث |
| تحليل القوالب: الصيغتان، تخطي الناقص، الترتيب، الحقول الغائبة | أعطال التحليل |

**`Run-Checks.ps1`** — أمر واحد يشغّل: فحص الصياغة بمحلّل PowerShell نفسه ←
`PSScriptAnalyzer` ← `Pester`. لا يتصل بتليجرام ولا بـ Air ولا يلمس
`config.json`، فآمن تشغيله على جهاز البث والجسر يعمل.

```powershell
cd 'D:\cingy cg\CinegyTelegramBridge'
.\Run-Checks.ps1
```

الوحدتان اختياريتان؛ إن لم تكونا مثبتتين يطبع الأمر تعليمات التثبيت ويكمل:

```powershell
Install-Module PSScriptAnalyzer -Scope CurrentUser
Install-Module Pester -MinimumVersion 5.0 -Scope CurrentUser -Force
```

لتمكين الاختبار أُضيف مفتاح `-LoadOnly` إلى `TelegramBridge.ps1`: يُعرّف كل
الدوال دون الاتصال بتليجرام ولا أخذ قفل النسخة الواحدة ولا دخول حلقة
الاستطلاع. كما صُدِّرت `Escape-XmlValue` من الموديول لتصبح قابلة للاختبار
مباشرة.

> **ملاحظة صادقة:** كتبتُ هذه الاختبارات لكنني **لم أستطع تشغيلها** — لا
> يوجد `pwsh` في بيئتي. قد تحتاج تعديلًا طفيفًا عند أول تشغيل عندك (غالبًا
> في نطاق المتغيرات بعد dot-sourcing). أخبرني بأي فشل وأصلحه فورًا. حتى مع
> هذا التحفّظ، وجودها يعني أن أي تعديل قادم يُختبر قبل أن يصل إلى الهواء.

---

## التحقق المنفَّذ

فحوصات آلية على السكربت المعاد كتابته:

- توازن الأقواس: `()` `{}` `[]` كلها متوازنة (١٩٩٤ سطرًا).
- لا توجد دوال مكرّرة الأسماء (٨٧ دالة).
- لا توجد استدعاءات لدوال غير معرّفة.
- **كل** قيمة `callback_data` تُنتجها الأزرار لها معالج مطابق في
  `Invoke-CallbackQuery`، ولا يوجد معالج يتيم بلا زر (فحص آلي مقارن).
- كل إزاحات `Substring` للبادئات مطابقة لأطوالها بالضبط
  (`tpl:`→4، `tplT:`→5، `updtpl:`→7، `approve:`→8 …).
- صحة JSON لكل من `config.json`, `config.example.json`, `templates.json`,
  `templates.example.json`.

- خوارزمية تقسيم الرسائل مُختبَرة على ٣٠٨ حالات (انظر قسم الإصلاح العاجل).
- تتبّع آلي لمصدر كل استخدام لـ `.Count` (٢٦ موضعًا): لا يمكن لأي منها أن
  يقع على `$null`.
- لا يوجد أي استدعاء لدالة تُرجع مصفوفة دون التفاف `@()`.
- أسماء الأوامر المسجَّلة في قائمة ☰ مطابقة لقيد تليجرام
  (`[a-z0-9_]{1,32}`)، وكلها لها معالج في `Invoke-BridgeCommand`.

**لم يُنفَّذ:** تشغيل فعلي للسكربت — `pwsh` غير متاح في بيئة المراجعة
(محاولات تثبيته محجوبة على مستوى الشبكة). يبقى **اختبار تشغيلي واحد عندك**
ضروريًا قبل الاعتماد عليه على الهواء:

```powershell
cd 'D:\cingy cg\CinegyTelegramBridge'
pwsh -File .\TelegramBridge.ps1 -ConfigPath .\config.json
```

جرّب بالترتيب: `/بدء` → 📋 القوالب → قالب بحقول → ⚡ نص جاهز →
⏱ عرض مؤقّت → 🚨 إخفاء الكل → 📸 صورة → ⚙️ الإعدادات (بدّل خيارًا) →
📜 السجل.

> ملاحظة: **أوقف الخدمة القديمة قبل التشغيل** (`Stop-ScheduledTask` أو
> `Stop-Service`)، وإلا سيرفض Mutex النسخة الجديدة العمل — وهو السلوك
> المقصود من البند ١٢.
