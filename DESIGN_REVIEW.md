# تدقيق برنامج التحكم والبوت — 2026-09-24

النطاق: مراجعة الكود وتجربة الاستخدام والاعتمادية والصلاحيات وإتاحة الاستخدام في Windows Forms ومسارات البوت المرتبطة. ليست شهادة أمان شاملة، ولم تُنفّذ عمليات على الهواء. لم يُعدّل كود التطبيق أو إعداد التشغيل.

لا يوجد DESIGN_BRIEF في الجذر؛ معيار المراجعة هو احتياج مشغّل غرفة الأخبار: حالة صادقة، تحكم سريع، وعدم تنفيذ فعل مختلف عمّا وافق عليه. أكد المستخدم شمول المدير والبوت.

## الصور وحدود الفحص البصري

لم تُلتقط صور: أدوات هذه الجلسة لا تتيح التحكم الأصلي في تطبيق Windows. مراجعة الشكل الفعلي، قصّ النص العربي، ترتيب Tab، قارئ الشاشة، وDPI ما زالت تحتاج اختبارًا مرئيًا. ملاحظات الأبعاد أدناه مستنتجة من الكود، وليست مشاهدات من شاشة تشغيل.

لاستكمال ذلك: صور الرئيسية والإعدادات والتقارير وشاشة الهواء في الوضعين الفاتح والداكن، مع نافذة صغيرة وتكبير Windows ‏150%؛ وصور قائمة البوت ومراجعة النشر وتأكيد الإخفاء دون تنفيذ فعل هواء.

## أولوية عالية P1

### 1. تأكيد إزالة القالب لا يرتبط بالقالب الذي راجعه المستخدم

- الدليل: `Parts/Bridge.Keyboards.ps1:532` يبني `hidego:<layer>` أو `exitgo:<layer>` فقط؛ `Parts/Bridge.Callbacks.ps1:2404` وما بعده يعيد قراءة القالب الحالي ويفحص الصلاحيات ثم ينفذ على الطبقة.
- السيناريو: يطلب المشغّل إخفاء A على الطبقة 9، ثم يعرض مستخدم آخر B على الطبقة نفسها، ثم يضغط الأول تأكيد الرسالة القديمة. التأكيد يزيل B إذا كانت صلاحياته تسمح، رغم أن مراجعة التأكيد كانت لـ A. فحص الصلاحيات لا يعالج تغير الهدف.
- الإصلاح: حفظ مرجع تأكيد قصير يرتبط بالمستخدم والطبقة وهوية تشغيل المشهد ومدة صلاحية، واستهلاكه مرة واحدة. رفض التأكيد عند اختلاف الحالة وإعادة عرض الحالة الحالية.
- التحقق المطلوب: اختبار بلا شبكة يبدّل A إلى B بين المراجعة والتأكيد، ويثبت عدم استدعاء الإزالة. يشمل إعادة تشغيل القالب نفسه بنص جديد وإعادة ضغط التأكيد.

### 2. حفظ قائمة المديرين قد ينقل الملكية الضمنية

- الدليل: `Manager/BridgeManager/SettingsForm.cs:140` يستخدم SortedDictionary؛ `:535` يسلسل الأرقام بهذا الترتيب. `Modules/BridgeAuthorization.psm1:101` يعتمد أول مدير مالكًا عندما لا يوجد OwnerUserIds صريح.
- السيناريو: AdminUserIds بالترتيب `[900,100]`، بلا مالك صريح. المالك 900. إضافة مدير 500 من نافذة المدير تجعل القائمة `[100,500,900]` عند حفظها، فيصبح المالك 100 بعد تحميل الإعدادات.
- تحقق محلي: تشغيل Get-BridgeOwnerIds ببيانات وهمية قبل الفرز وبعده أعطى 900 ثم 100. لم تُستخدم إعدادات المحطة.
- الإصلاح: الحفاظ على الترتيب الدلالي للمديرين عند التسلسل، وإظهار المالك الفعلي في الواجهة. أي نقل ملكية يجب أن يكون فعلًا صريحًا. لا يكفي فرز العرض إذا تسرب ترتيبه إلى التخزين.
- التحقق المطلوب: تعديل قائمة غير مرتبة مع غياب OwnerUserIds لا يغير المالك الفعلي.

### 3. فشل قراءة سجل الهواء يُعرض كأنه لا شيء على الهواء

- الدليل: `Manager/BridgeManager/OnAirForm.cs:120` يقرأ ثم يمسح الصفوف؛ `:193` يعيد صفوفًا فارغة عند غياب الملف أو JSON معطوب. الشاشة تقول «لا شيء على الهواء — الشاشة نظيفة» عند عدم وجود صفوف.
- الأثر: فشل القراءة يتحول إلى طمأنة كاذبة، بدل حالة غير معروفة. كما أن الملف القديم لا يحمل في هذه الشاشة تحذيرًا عن قدم الحالة.
- الدليل الاختباري الموجود: SelfTest في `Program.cs:345` وما بعدها يثبّت إرجاع صفر صفوف للملف المفقود والمعطوب؛ الاختبار يحمي الاستقرار لكنه لا يحمي صدق الرسالة.
- الإصلاح: نتيجة قراءة تفصل النجاح عن الفشل، والاحتفاظ بآخر حالة جيدة مع وقت تحديث وتحذير واضح. تُعرض الحالة الفارغة فقط بعد قراءة سليمة، بصياغة تنسبها إلى سجل الجسر.

## أولوية متوسطة P2

### 4. الحفظ يسمح بنتيجة يعرف التطبيق أنها قد تضيع

- الدليل: `Manager/BridgeManager/SettingsForm.cs:661` يسمح بخيار «لا = حفظ الآن على أي حال» عند تعديل قوائم يديرها الجسر الجاري. `Parts/Bridge.Core.ps1` يعيد حفظ القوائم من الذاكرة.
- الأثر: قد يظن المشغّل أنه سحب صلاحية، بينما تظل النسخة الفعالة مختلفة ويعيد الجسر كتابة القائمة القديمة. التحذير موجود، لذا المشكلة ليست غياب التحذير بل إتاحة نتيجة غير مستقرة تحت زر الحفظ.
- الإصلاح: لهذا النوع من التعديل، مساران واضحان: حفظ وتطبيق بعد توقف متحقق، أو العودة للتحرير. ويمكن بدلًا منهما تصميم طلب تعديل حي يعالجه الجسر نفسه.

### 5. تقرير الاستخدام يرسم صفوفًا أكثر من المساحة المتاحة

- الدليل: `ReportsForm.cs:285` يختار حتى 8 أعمدة؛ `BarChart.cs:21` يجعل ارتفاع الصف 56 بكسل و`:65` يبدأ عند 14. آخر شريط ينتهي قرب y=442. نافذة التقارير ارتفاعها الافتراضي 560، وبعد الرأس 62 والتذييل 60 وسطر التبديل والعناوين لا تتبقى هذه المساحة للرسم؛ صفحة الاستخدام بلا تمرير.
- الأثر المتوقع من الحساب: صفوف سفلية مقصوصة، رغم أن التقرير يحسبها ضمن المعروض. يزداد ذلك عند تصغير النافذة. يحتاج تأكيدًا بصريًا.
- الإصلاح: حاوية تمرير بمحتوى ذي ارتفاع محسوب، أو تصفيح بحسب الارتفاع الفعلي، مع بيان العدد الظاهر فعلًا.

### 6. النص الأبيض على أزرار الوضع الداكن ضعيف التباين

- الدليل: `Theme.cs:137` يفرض الأبيض على الأزرار الأساسية. الحساب من RGB يعطي: تشغيل 2.97:1، Accent ‏3.02:1، إيقاف 3.88:1. الخط 9.75 نقطة.
- كمعيار تصميم، حد النص العادي 4.5:1 وفق [W3C](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum). هذه مقارنة عددية لألوان الكود، وليست ادعاء تدقيق امتثال شامل لتطبيق Windows.
- الإصلاح: فصل لون خلفية الزر عن لون حالة التشغيل، أو اختيار لون نص مناسب لكل خلفية، والتحقق من حالات hover وpressed أيضًا.

### 7. الرسم لا يتيح قيمه لقارئ الشاشة

- الدليل: `BarChart.cs:38` يصف فقط «N أعمدة»؛ أسماء القوالب والقيم تُرسم عبر DrawString بلا عناصر وصول مخصصة أو بديل جدولي.
- الأثر: وصف عدد الأعمدة لا ينقل نتيجة التقرير إلى مستخدم قارئ الشاشة.
- الإصلاح: جدول نصي مكافئ قابل للوصول أو AccessibleObject يعرض كل اسم وقيمة. يحتاج التحقق بقارئ شاشة فعلي بعد الإصلاح.

### 8. زر تحديث التقارير لا يحدّث أخطاء الساعة الأخيرة

- الدليل: `ReportsForm.cs:29` يحتفظ بـ `_errorsLastHour` كعدد readonly من لحظة فتح النافذة؛ Reload يعرض العدد نفسه عند كل ضغط تحديث (`:182`).
- الأثر: بقية بيانات التقرير يعاد تحميلها بينما يبقى هذا المؤشر ثابتًا، حتى مع أخطاء جديدة أو خروج أخطاء قديمة من نافذة الساعة.
- الإصلاح: مزود بيانات يعيد احتساب العدد عند التحديث، أو وصف المؤشر صراحة بأنه لقطة وقت فتح التقرير مع طابع زمني.

## تحسينات تستحق اختبارًا مرئيًا

- `SettingsForm.cs:326`: أربعة أزرار في صف لا يلتف، مجموع عروضها 550 بكسل قبل الهوامش والحشو، مقابل حد أدنى للنافذة 620. افحص الوصول لآخر زر عند أصغر حجم وDPI مرتفع، واجعل الصف يتكيف.
- `MainForm.cs:2156` و`SettingsForm.cs:748`: انتظار توقف العملية وانتظار قفل الإعدادات متزامنان على خيط الواجهة، حتى 10 ثوانٍ لكل منهما. انقل الانتظار إلى مسار غير حاجب مع تعطيل الأزرار وتقدم مرئي.
- إظهار حالة المالك الفعلي، وحالة «محفوظ ولم يُطبّق»، ووقت آخر تحديث بجوار الحالة يجعل قرارات المشغّل أوضح من الاعتماد على رسائل منفصلة.

## ما يستحق الحفاظ عليه

- أولوية أدوات الهواء في قائمة البوت، ووضع اليد الواحدة مع استثناء شبكات الوقت والتاريخ.
- إعادة فحص الصلاحيات عند التنفيذ، وليس الاكتفاء بإخفاء الأزرار.
- نص مراجعة قبل النشر وتراجع آمن ضمن مسارات مخصصة.
- ألوان مركزية، وأسماء وصول للأزرار، وعلامة نصية للحالة المفعلة في ToggleChip.
- تنبيه التعديلات غير المحفوظة، وحجب التوكن افتراضيًا وحماية مراجع DPAPI.
- كتابة إعدادات ذرية وحماية ACL، وسجل واجهة محدود العدد، ومراقبة نبضة مستقلة عن صمت السجل.

## التحقق

- نجح بناء نسخة معزولة في `artifacts/uiux-audit-build` دون نشر إلى dist: صفر أخطاء وصفر تحذيرات. المحاولة الأولى داخل البيئة المقيدة تعذرت بسبب إذن قراءة SDK؛ نجحت إعادة البناء بصلاحية الأداة المعتمدة.
- نجح `BridgeManager.exe --selftest`: جميع الفحوص الـ181.
- نجحت اختبارات Pester كلها: 2179 ناجحًا، صفر فاشل، صفر متخطى. اجتاز فحص التركيب وPSScriptAnalyzer أيضًا.
- بوابة PowerShell الكاملة أعلنت FAILED بسبب منع البيئة المقيدة قراءة `config.json`. سطر البوابة يصنف ذلك «invalid JSON»، لكن السبب الفعلي Access denied، ولم يثبت فساد الملف. لذلك لا يُدّعى مرور البوابة كاملة. السجل `artifacts/uiux-audit-checks.log`.
- لم تُنفّذ SHOW أو HIDE أو EXIT_SCENE_LOOP، ولم يُشغّل الجسر أو يُوقف مدير المحطة.
- لا تتضمن المراجعة تفتيشًا شاملًا لتاريخ Git أو اختبار اختراق أو اختبار Telegram/Cinegy حيًا. نجاح الاختبارات الحالية لا ينفي مشكلات السيناريوهات المذكورة.

الترتيب المقترح للإصلاح: تأكيدات الإزالة والملكية وصدق حالة الهواء أولًا؛ ثم الحفظ والتقارير والتباين؛ ثم فحص مرئي للعربية وDPI ولوحة المفاتيح.
